// GENERATED FILE — DO NOT EDIT.
// Source of truth: mpesa/web/content/docs/** (assembled by mpesa/web/lib/docs/bundle.ts).
// Regenerate:      cd ../mpesa && node scripts/sync-agent-docs.mjs
// Verify:          node scripts/check-docs-bundle.mjs   (also runs on prepublishOnly)
//
// The prose lives on the website. If it is wrong here, it is wrong there — fix it there.
// payload-sha256: 3ba4982c0bfc02525549345165494e1c22bbdd85ba637b2872200ea92367f19b

export interface DocTopic {
  readonly id: string;
  readonly title: string;
  readonly keywords: readonly string[];
  readonly aliases: readonly string[];
  /** The docs markdown itself — rendered from web/content/docs/**. */
  readonly content: string;
}

export const DOC_TOPICS: readonly DocTopic[] = [
  {
    id: "integration",
    title: "Integrate M-Pesa with paylod, end to end",
    keywords: ["integrate","integration","setup","set","up","start","started","getting","quickstart","begin","onboard","how","guide","mpesa","m-pesa","daraja","install"],
    aliases: ["quickstart","getting-started"],
    content: "# paylod: M-Pesa payments without a backend\n\npaylod is M-Pesa (Safaricom Daraja) without the backend: one HTTP call sends the STK Push, we host the callback, sign your webhooks and decode every result code.\n\n## What paylod is\n\npaylod is a hosted layer over Safaricom's **Daraja** API. You keep your own till or paybill and your own Daraja credentials — M-Pesa still settles **straight to you**, and paylod never touches the money or takes a cut.\n\nWhat you skip is the part everyone hates: hosting a public HTTPS callback, refreshing OAuth tokens, retrying failed pushes, deciphering `resultCode: 1037`, and building a webhook signing scheme.\n\n```ts title=\"The whole integration\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n\nconst outcome = await paylod.collectAndWait({ amount: 100, phone: \"0712345678\" });\n\nif (outcome.paid) console.log(\"Paid:\", outcome.receipt);\nelse console.log(outcome.message);   // already decoded, safe to show a customer\n```\n\nThat is it. `npm install @paylod/node`, one API key, and paylod does the rest. Not on Node? The same call works over plain HTTP; see the [API reference](/docs/api).\n\n## How it works\n\n1. You create an **application** (a till or a paybill) and paste your Daraja consumer key, secret, shortcode and passkey into it. They are encrypted at rest and never leave the server.\n2. paylod gives you a **hosted callback URL**. You paste that once into the Safaricom Daraja portal. That is the only Safaricom paperwork you do.\n3. Your backend calls `collect()` with an amount and a phone number, using a paylod **API key**. The customer's handset lights up with an M-Pesa PIN prompt.\n4. M-Pesa posts the result to paylod's callback. paylod normalises it, decodes the result code, records the payment, and either **pushes you a signed webhook** or lets you **poll** for the result. Your choice.\n\n> The one thing an integration must never do is trust the client with the amount. Your server owns the amount; see [Secure integration](/docs/guides/security).\n\n## Where to start\n\n| If you want to… | Go to |\n| --- | --- |\n| Send your first STK Push right now | [Quickstart](/docs/quickstart) |\n| Use the Node/TypeScript client | [Node SDK](/docs/sdk) |\n| Send a payment from your terminal | [CLI](/docs/cli) |\n| Understand keys, environments and idempotency | [Authentication](/docs/authentication) |\n| Charge a customer properly, in production | [Accept a payment](/docs/guides/accept-a-payment) |\n| Know when the money actually landed | [Handle the result](/docs/guides/handle-the-result) |\n| Look up an endpoint's parameters | [API reference](/docs/api) |\n| Work out what `1032` means | [Error codes](/docs/errors) |\n| Let an AI agent do all of the above | [MCP server](/docs/mcp) |\n\n## What you can build\n\n- **Checkout** — collect a payment for an order, fulfil it when the webhook confirms the settled amount.\n- **Payouts** — send money out to customers, riders or staff (B2C), and reverse a transaction when you need to refund.\n- **Offline payments** — register C2B so counter payments to your till reach your systems, or hand the customer a dynamic QR code.\n- **Operations** — query a historical transaction by receipt, or read your shortcode's float.\n\nEverything above is one SDK call (or one HTTP request), one API key, and no callback hosting on your side.\n\n---\n\n# Quickstart: your first M-Pesa STK Push\n\nYour first M-Pesa STK Push in five minutes — create an application, add your Daraja credentials, mint a key, and collect a payment.\n\nYou need a Safaricom Daraja account (the [sandbox](https://developer.safaricom.co.ke) is free and instant) and about five minutes. One `npm install`, and nothing to deploy — no callback server, no tunnel.\n\n## 1. Create an application\n\nIn the paylod dashboard, create an **application** — one per till or paybill. Give it a name; you can add both a Sandbox and a Production environment to the same application.\n\n## 2. Add your Daraja credentials\n\nOn the application's **Credentials** tab, paste the four values Daraja gives you:\n\n| Field | Where it comes from |\n| --- | --- |\n| Consumer key | Daraja portal → your app |\n| Consumer secret | Daraja portal → your app |\n| Shortcode | Your till / paybill number (`174379` in sandbox) |\n| Passkey | Daraja portal → Lipa na M-Pesa Online |\n\nThey are encrypted at rest and **write-only** — no API, tool, or dashboard screen ever reads them back.\n\n## 3. Paste the callback URL into Daraja\n\npaylod shows you a hosted callback URL on the application's **Endpoints & Webhooks** tab. Copy it into the Daraja portal as your Lipa na M-Pesa Online callback URL.\n\nThat is the entire \"host a public HTTPS endpoint\" problem, solved. You never write callback-handling code.\n\n## 4. Mint an API key\n\nOn the **API Keys** tab, create a key. Keys are environment-scoped:\n\n- `mp_test_…` → Sandbox\n- `mp_live_…` → Production\n\nThe key is shown **once**. It identifies which application and environment a request runs against, so you never send a shortcode or a Daraja credential over the wire.\n\n> [!warn] The key is a server-side secret. Never ship it in a browser, a mobile app, or anything a payer can open.\n\n## 5. Install the SDK\n\n```bash title=\"Install\"\nnpm install @paylod/node\n```\n\nPut the key in your environment. The client picks it up on its own:\n\n```bash title=\".env\"\nPAYLOD_API_KEY=mp_test_YOUR_API_KEY\n```\n\n## 6. Send your first STK Push\n\n```ts title=\"collect.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n\nconst outcome = await paylod.collectAndWait({\n  amount: 10,\n  phone: \"0712345678\",\n  idempotencyKey: attempt.id,    // one key per payment attempt. A double-click cannot charge twice.\n  accountReference: \"INV-2041\",\n  description: \"Order #2041\",\n});\n\nif (outcome.paid) {\n  console.log(\"Paid:\", outcome.receipt);   // \"UG1F3A1U7J\"\n} else {\n  console.log(outcome.message);            // already decoded, safe to show a customer\n}\n```\n\nThat is the whole integration. One argument to the constructor, one call.\n\n`collectAndWait()` sends the STK Push, then polls until the payment settles. The customer's phone rings, they enter their PIN, and you get an answer back. No callback server, no OAuth token to refresh, no result-code table to write.\n\n> [!warn] **Pass `idempotencyKey`, and mint one per payment attempt.** Duplicates of that one attempt — a double-clicked Pay button, a refreshed tab, a retried request — collapse into **one** prompt and **one** charge, even if they arrive at the same instant. Leave it out and every call is a new charge: two clicks, two prompts, two debits. Do **not** key on the order or product id — that replays an *old* payment instead of making a new one, and a retry after a wrong PIN is a genuinely new charge that needs a **new** key. Omit it and the SDK warns you. [Full rules](/docs/sdk#idempotency).\n\nThe answer is a `PaymentOutcome`: a `message` you can show a customer, already decoded, and a `retryable` flag that tells you whether charging again is safe. A wrong PIN is a business outcome, not a crash, so it comes back as data rather than a thrown error.\n\nPhone numbers are normalised for you, so `0712345678`, `+254712345678` and `254712345678` all work.\n\n> [!warn] A **timeout throws** `PaylodTimeoutError` rather than reporting `status: \"failed\"`. An unanswered prompt is not a failed payment: the customer may still be typing their PIN. Leave the order open. See [Handle the result](/docs/guides/handle-the-result).\n\nYou do not need a webhook for this to work. Polling is a first-class path, and `collectAndWait()` does it for you. Add a signed webhook when you want your server told about payments it is not actively waiting on.\n\n## Not on Node?\n\nThe SDK is a wrapper over a plain HTTP API — every language can call it directly:\n\n```bash title=\"The same call, over HTTP\"\ncurl -X POST https://paylod.dev/functions/v1/collect \\\n  -H \"Authorization: Bearer mp_test_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Idempotency-Key: attempt_9f3c1a7e\" \\\n  -d '{ \"amount\": 10, \"phone\": \"254712345678\" }'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"paymentId\": \"e69e5c00-8a01-44ed-b003-48e2e86a7c9e\",\n  \"status\": \"pending\",\n  \"checkoutRequestId\": \"ws_CO_010720261905029287161380\"\n}\n```\n\nThen poll `GET /functions/v1/status/{paymentId}` until it settles. Going this route means doing yourself what the SDK does for you: the polling loop, the idempotency key, and decoding the result code. The full surface is in the [API reference](/docs/api).\n\n## Next\n\n- Every method, option and error class: [Node SDK](/docs/sdk).\n- Prefer the terminal? The [CLI](/docs/cli) sends the same STK Push in one command.\n- Prefer push over poll? [Handle the result](/docs/guides/handle-the-result) covers signed webhooks.\n- Exercise the failure paths first: [Test in sandbox](/docs/guides/test-in-sandbox).\n- Ready for real money? [Go live](/docs/guides/go-live).",
  },
  {
    id: "sdk",
    title: "@paylod/node — the SDK your application code should use",
    keywords: ["sdk","node","npm","typescript","javascript","client","library","paylod/node","collectandwait","outcome","paymentoutcome","retryable","code"],
    aliases: [],
    content: "# Node SDK for M-Pesa: @paylod/node\n\n@paylod/node is the official Node/TypeScript client: STK Push, status polling with backoff, signed webhook verification and offline error decoding, with zero runtime dependencies.\n\n`@paylod/node` is the official Node/TypeScript client. It wraps the same HTTP API documented in the [API reference](/docs/api) and adds the parts every integration ends up writing anyway: phone-number normalisation, idempotency keys, a polling loop with backoff, webhook signature verification, and an offline result-code decoder.\n\nIt has **zero runtime dependencies** and works on Node 18+, Bun, Deno, Vercel, Cloudflare Workers and any other runtime with a global `fetch`.\n\n```bash title=\"Install\"\nnpm install @paylod/node\n```\n\n## Server-side only\n\nThe API key can move money. `@paylod/node` is a server library — call it from your backend, a serverless function, or an edge worker. Never from a browser or a mobile app.\n\n> [!warn] A key shipped in client-side JavaScript is fully compromised: every visitor downloads it. If you need a browser checkout, have the browser call *your* server, and let your server call paylod. See [Secure integration](/docs/guides/security).\n\n## Set up the client\n\nOne argument: your API key.\n\n```ts title=\"paylod.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nexport const paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n```\n\nThat is the whole configuration. There is no base URL to pass, no OAuth token to fetch and refresh, and no callback URL to host. If you leave the argument off, the client reads `PAYLOD_API_KEY` from the environment itself:\n\n```ts title=\"Also fine\"\nconst paylod = new Paylod();\n```\n\nEither way it throws a `PaylodConfigError` immediately if no key is found, rather than handing you a client that fails on its first call.\n\nTimeouts, retries and the rest have sensible defaults. You can override them, but you will rarely want to: see [Options](#options) at the bottom of this page.\n\n## Collect a payment\n\n```ts title=\"collect.ts\"\nconst outcome = await paylod.collectAndWait({\n  amount: 100,\n  phone: \"0712345678\",\n  idempotencyKey: attempt.id,      // one key per payment attempt — a double-click cannot charge twice\n});\n\nif (outcome.paid) {\n  await fulfil(outcome.receipt);   // \"UG1F3A1U7J\"\n} else {\n  console.log(outcome.message);    // \"That M-Pesa PIN was incorrect. Please try again …\"\n}\n```\n\n`collectAndWait()` sends the STK Push and polls until the payment settles. You get back a `PaymentOutcome`: a flat object with a customer-facing `message` already decoded and a `retryable` flag you can hang a button off.\n\n> [!warn] **Pass `idempotencyKey`, and mint one per payment attempt.** Duplicates of that attempt — a double-clicked Pay button, a refreshed tab, a redelivered job — collapse into **one** prompt and **one** charge. Omit it and every call is a new charge. Do **not** key on the order or product id: that replays an old payment instead of making a new one. A retry after a wrong PIN is a new charge and needs a **new** key. See [Idempotency](#idempotency).\n\nA wrong PIN is a business outcome, not a crash, so it comes back as data rather than a thrown error.\n\nPhone numbers are normalised for you. `0712345678`, `+254712345678`, `254712345678` and `712345678` all work.\n\n## The outcome\n\nEvery method that settles a payment returns the same shape.\n\n```ts title=\"PaymentOutcome\"\n{\n  status: \"succeeded\" | \"pending\" | \"cancelled\" | \"failed\";\n  message: string;        // customer-facing, already decoded. Render this.\n  retryable: boolean;     // SAFE TO CHARGE AGAIN. Gate your retry button on this.\n  paid: boolean;          // the one branch a backend needs\n  receipt: string | null; // M-Pesa confirmation code; non-null exactly when `paid`\n  code: string | null;    // raw result code, for your logs\n  detail: DecodedError | null;\n  payment: Payment;\n}\n```\n\nYour entire UI:\n\n```tsx title=\"Checkout.tsx\"\n<p>{outcome.message}</p>\n{outcome.retryable && <button onClick={retry}>Try again</button>}\n```\n\nNo `switch` on result codes. No error table in your app. If you ever write `if (outcome.code === \"1032\")` to decide what to *show* a human, use `message` instead. `code` and `detail` are for logs and support tooling.\n\n> [!warn] `retryable` means **safe to charge again**, not \"the user is allowed to press a button\". A `pending` payment is never retryable: the STK prompt is still live on the handset, and re-collecting fires a second prompt that can double-charge the customer. Gate the retry button on `retryable` and this cannot happen to you.\n\n## Methods\n\n### `collect(params)`\n\nSends the STK Push and returns as soon as the prompt is on the handset. The payment is `pending`.\n\n```ts title=\"collect\"\nconst ack = await paylod.collect({\n  amount: 100,                      // whole KES, 1–150000. M-Pesa rejects decimals.\n  phone: \"0712345678\",\n  idempotencyKey: attempt.id,       // PASS THIS — one per payment attempt (see below)\n  accountReference: \"INV-2041\",     // optional, ≤ 12 chars — YOUR correlation id (see below)\n  description: \"Order #2041\",       // optional, ≤ 64 chars — shown on the STK prompt\n  metadata: { orderId: \"2041\" },    // optional, opaque; stored with the payment\n});\n\nack.paymentId;          // \"e69e5c00-…\"\nack.checkoutRequestId;  // \"ws_CO_…\"\nack.idempotencyKey;     // the key that was sent — yours, or a random one if you passed none\n```\n\n`idempotencyKey` is what collapses duplicate deliveries of one payment attempt into a single charge. Mint one per attempt and persist it; see [Idempotency](#idempotency) for exactly what it guarantees, what it does **not** guarantee, and what happens if you leave it out.\n\n`amount`, `phone`, `accountReference` and `description` are validated locally before the request leaves your process, so a bad amount fails instantly in your own stack trace instead of coming back as a `422` a round-trip later.\n\n`accountReference` is a **correlation id you choose**, not a message to the customer. It comes back as `accountRef` on `status()` and on the webhook, so it is how you tie a payment to your order. Safaricom only shows it to the payer on a **Paybill** (`CustomerPayBillOnline`), where it is the account number; on a **Till / Buy Goods** shortcode it is not displayed at all — the payer sees your business name and the amount. `description` is the field that reaches the STK prompt. If you omit `accountReference`, it defaults to a short prefix of the `paymentId`, so it is still unique and still traceable back to the payment.\n\nNote that `accountReference` is a **label, not a lock**: putting your order id there does not deduplicate anything. `idempotencyKey` is the field that collapses duplicates of one attempt. Put your order id in `accountReference`, and a per-attempt key in `idempotencyKey` — they are different jobs.\n\n### `status(paymentId)`\n\nReads a payment. If it is still pending, paylod runs a live M-Pesa STK Query and settles it on the spot before answering.\n\n```ts title=\"status\"\nconst payment = await paylod.status(ack.paymentId);\n\npayment.status;        // \"pending\" | \"success\" | \"failed\"\npayment.mpesaReceipt;  // \"UG1F3A1U7J\" on success, otherwise null\npayment.resultCode;    // 0 | 1032 | 2001 | …\npayment.resultDesc;\n```\n\n### `check(paymentId)`\n\n`status()`, already decoded. Returns a `PaymentOutcome` you can render.\n\n```ts title=\"check\"\nconst outcome = await paylod.check(ack.paymentId);\n\noutcome.status;     // \"pending\" | \"succeeded\" | \"cancelled\" | \"failed\"\noutcome.message;    // \"Check your phone and enter your M-Pesa PIN to complete this payment.\"\noutcome.retryable;  // false while pending\n```\n\nThis is what a polling endpoint in your own app should return to your frontend.\n\n### `wait(paymentId, options)`\n\nPolls an existing payment until it settles, on a jittered backoff ramp (1s → 5s). Returns a `PaymentOutcome`.\n\n```ts title=\"wait\"\nconst outcome = await paylod.wait(ack.paymentId, {\n  timeoutMs: 120_000,   // default. STK prompts expire around 60s; PIN entry takes 20–30s more.\n  onPoll: (p) => console.log(\"still\", p.status),\n  signal: controller.signal,\n});\n```\n\n`wait()` decides \"has it settled?\" from the result **code**, not the raw status field. Daraja reports `4999` on a payment it also marks `failed`, but that code means the prompt is still live. `wait()` keeps polling instead of reporting a failure for a payment that is about to succeed.\n\nIf the payment is **still pending** at the deadline, `wait()` throws a `PaylodTimeoutError`. That is not a failure, and it is deliberately not folded into `status: \"failed\"`. The customer may still be typing their PIN. Leave the order open.\n\n### `collectAndWait(params, options)`\n\n`collect()` followed by `wait()`. The one-liner most integrations want. Takes the same `params` as `collect()` — including `idempotencyKey`, which you should pass, one per payment attempt.\n\n### `pendingOutcome(paymentId)`\n\nThe renderable form of a freshly sent prompt. `collect()` hands back an ack, but a prompt sitting on a handset is a pending payment, and your UI wants to render it like any other state.\n\n```ts title=\"pendingOutcome\"\nimport { pendingOutcome } from \"@paylod/node\";\n\nconst ack = await paylod.collect({ amount: 100, phone });\nreturn pendingOutcome(ack.paymentId);   // same shape as check() and wait()\n```\n\n### `decodeError(resultCode, rawDesc?)`\n\nDecodes an M-Pesa result code offline. No network call, no API key needed at call time.\n\n```ts title=\"decodeError\"\nconst err = paylod.decodeError(1032);\n\nerr.title;            // \"Payment cancelled by the customer\"\nerr.cause;            // \"The customer received the STK prompt but pressed Cancel …\"\nerr.fix;              // \"Nothing is wrong with your setup — offer a clear retry …\"\nerr.category;         // \"customer\"\nerr.retryable;        // true\nerr.customerMessage;  // \"Payment cancelled — you can try again whenever you're ready.\"\n```\n\nThese are byte-for-byte the strings paylod puts in the `decoded` object on a `payment.failed` webhook, so what you show a customer is the same either way. The whole catalogue is browsable in the [error reference](/docs/errors).\n\n`decodeError` is also exported as a standalone function, along with the raw `ERROR_CATALOG`:\n\n```ts title=\"Standalone\"\nimport { decodeError, ERROR_CATALOG } from \"@paylod/node\";\n```\n\n### `4999` and `500.001.1001` are pending, not failed\n\nDaraja's STK Query returns `4999` (\"the transaction is still under processing\") while the customer is still looking at the PIN prompt. paylod classifies it — and `500.001.1001` — as **pending**, so `status()` keeps reporting `pending` and `wait()` keeps polling.\n\nIf you ever decode one of those codes directly, `category` is `\"pending\"` and `retryable` is `false` — not because a retry is pointless, but because the first prompt is still live and a second push can double-charge the customer. Do not show a customer a failure on these codes, and do not re-collect. Keep polling.\n\n## Test your checkout without a phone\n\nYour failure paths are where payment bugs live, and until now testing them meant finding a handset and typing a wrong PIN. `paylod.simulate` removes the handset. It removes nothing else: a real payment row, the real Daraja result codes, the real settlement path, and a real signed webhook delivered to your endpoint. Only the phone is fiction.\n\nIt requires an `mp_test_…` key. A `mp_live_…` key is refused **locally**, before a request is sent — a simulator that could touch production is not a feature.\n\n```ts title=\"checkout.test.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_TEST_KEY!);   // mp_test_…\n\nconst outcome = await paylod.simulate.pay({ outcome: \"wrong_pin\" });\n\noutcome.status;     // \"failed\"\noutcome.message;    // \"That M-Pesa PIN was incorrect. Please try again and enter the right PIN.\"\noutcome.retryable;  // true — no money moved, so a fresh charge is safe\n```\n\nThat is an ordinary `PaymentOutcome` — the identical object `check()` and `wait()` return. There is no \"simulated\" type and no special branch, so the code you are testing is the code that runs in production.\n\n### The five outcomes\n\n`outcome` is a typed union, so a typo is a compile error rather than a `422` you find in CI.\n\n| `outcome` | `status` | Result code | `retryable` |\n| --- | --- | --- | --- |\n| `approve` | `succeeded` | `0` | `false` (it worked) |\n| `wrong_pin` | `failed` | `2001` | `true` |\n| `insufficient_funds` | `failed` | `1` | `true` |\n| `user_cancelled` | `cancelled` | `1032` | `true` |\n| `timeout` | `failed` | `1037` | `true` |\n\n`SIM_OUTCOMES` (or `paylod.simulate.outcomes`) is the whole list, so you can loop:\n\n```ts title=\"Drive every path\"\nfor (const outcome of paylod.simulate.outcomes) {\n  const result = await paylod.simulate.pay({ outcome });\n  expect(render(result)).toMatchSnapshot();\n}\n```\n\n> [!note] `timeout` here is Daraja's `1037` — \"we could not reach the handset\" — which is a **settled** failure. It is not `PaylodTimeoutError`, which is what `wait()` throws when a payment is still `pending` at your deadline. An indeterminate payment is not a failed payment.\n\n### `simulate.collect()` and `simulate.outcome()`\n\nSplit it in two and put *your* code in the middle. The payment id is a real one, so your poller, your webhook route and your UI all run unchanged:\n\n```ts title=\"Testing your own code\"\nconst sim = await paylod.simulate.collect({ amount: 250 });\n\nawait readCheckout(sim.paymentId);   // your code — sees a genuinely pending payment\nawait paylod.simulate.outcome(sim.paymentId, \"insufficient_funds\");\nconst view = await readCheckout(sim.paymentId);   // your code — sees the settled failure\n\nexpect(view.message).toMatch(/balance is too low/);\n```\n\nA simulated payment stays `pending` until you force an outcome, which is exactly what a live prompt does while a customer stares at it. You can only settle it once — a second call gets a `409`, same as a real handset.\n\n### Testing your charge path: `{ simulate: true }`\n\nThe one thing the above still does not exercise is your own `collect()` call. Construct the client with `simulate: true` and `collect()` / `collectAndWait()` create a simulated payment instead of ringing a phone — so your `/api/pay` handler runs **completely unchanged**:\n\n```ts title=\"Your handler, under test\"\nconst paylod = new Paylod(process.env.PAYLOD_TEST_KEY!, { simulate: true });\n\nconst view = await startCheckout(order.id, \"0712345678\", attemptId);   // your handler, verbatim\nawait paylod.simulate.outcome(view.paymentId!, \"user_cancelled\");      // no handset involved\nexpect((await readCheckout(view.paymentId!)).status).toBe(\"cancelled\");\n```\n\nThe constructor throws `PaylodSandboxOnlyError` if the key is not `mp_test_…`, so this flag can never point at production even by accident.\n\n> [!note] The simulator honours `Idempotency-Key` with the **same semantics as production**: the same key returns the same `paymentId` and creates no second payment, concurrent duplicates collapse into one, and a different key creates a new payment. So a test that asserts \"a double-click cannot charge twice\" is testing the real behaviour, not a stub of it.\n\n## Idempotency\n\nAn idempotency key names **one payment attempt**. Duplicate deliveries of that one attempt — a double-click, a refreshed tab, a job-queue redelivery, an internal network retry — collapse into a single charge. That is the guarantee, and it is worth understanding precisely, because it is narrower than \"reusing a key is always a safe retry\".\n\n```ts title=\"One key per payment attempt\"\nconst attempt = await db.attempts.create({ orderId: order.id });   // a row per press of Pay\nawait paylod.collectAndWait({ amount, phone, idempotencyKey: attempt.id });\n```\n\n### Pass a key per attempt — not per order, and not per product\n\nThe key must be **stable across duplicates of one attempt** and **fresh for a genuinely new charge**. An order id is stable, but it is not fresh — and a product id is neither:\n\n| Key you pass | What happens |\n| --- | --- |\n| An id minted per **payment attempt** | Correct. Duplicates of that attempt collapse; a new attempt is a new charge. |\n| Your **order id** | The customer mistypes their PIN, you retry the same order — and paylod replays the **failed** first attempt instead of charging them. The order never gets paid. |\n| A **product id** (or any value reused across purchases) | Catastrophic. Every customer after the first replays the **first-ever** payment for that product. Nobody after customer one is charged at all. |\n| `crypto.randomUUID()` **per call** | Equivalent to no key: a double-click is two keys, two prompts, two charges. |\n\nThe rule that resolves all four: **mint the key when a payment attempt begins, persist it on that attempt, and never reuse it for a different charge.** A retry after a wrong PIN, a cancelled prompt or a timeout is a *new attempt* — new row, new key.\n\nBecause a missing key is a money bug, the SDK emits a one-time `console.warn` when `collect()` or `collectAndWait()` is called without one.\n\n> [!warn] Do **not** silence that warning with `idempotencyKey: crypto.randomUUID()` or `Date.now()` **at the call site**. A key that changes on every call is exactly equivalent to having no key at all — it just hides the warning that was telling you the customer is exposed. A random UUID is a perfectly good key; it just has to be minted **once per attempt** and stored, not generated inside the call.\n\n### A concurrent double-click cannot double-charge\n\nThis part is unconditional. Fire ten simultaneous requests with the same `Idempotency-Key` and you get **one** payment and **one** STK push: the key is reserved before the provider is called, so exactly one request wins and the others are made to wait for — and replay — its answer. All ten calls come back with the same `paymentId`. No handset rings twice.\n\n### The one case where the same key is *not* a safe retry\n\nIf a request dies mid-flight against Daraja — after paylod handed the call to the provider, before an answer came back — that key is **spent**. A retry under it does not silently re-dispatch. It returns `409` with an **indeterminate** message:\n\n```text\nA previous request with this Idempotency-Key was interrupted while the provider call was\nin flight, so it may or may not have completed. We will not repeat it — that could charge\nor pay twice. Check the payment/disbursement status; if nothing happened, retry with a NEW key.\n```\n\nA timeout is not evidence that the money did not move; it is the absence of evidence. So paylod refuses to guess. **For money, at-most-once beats at-least-once** — we would rather make you check than charge someone twice.\n\n> [!warn] **If you get the indeterminate `409`, do not retry with the same key.** Read the payment status first — `paylod.check(paymentId)`, or `GET /status/:id`, or your webhook — and only then decide. If the payment did settle, you are done. If nothing happened, start a **new attempt with a new key**. Retrying under the spent key will only return the same `409`, forever.\n\n### The rules\n\n- **Same key + same body, already settled** → the original payment is replayed. Same `paymentId`, same `checkoutRequestId`. No second prompt, no second debit.\n- **Same key + same body, first request still in flight** → `409` with a `Retry-After`, or — for a plain double-click — the SDK simply waits for the winner and hands you its response.\n- **Same key + different body** → `409`, surfaced as `PaylodApiError` with `.isIdempotencyConflict === true`. Always a bug on your side: two different charges collided on one key (you changed the amount but kept the key).\n- **Same key, previous attempt interrupted against the provider** → `409` **indeterminate**. Check status; retry with a **new** key. Never re-dispatched.\n- **Internal retries** (network blip, `5xx`, `429`) reuse the same key automatically — which is what makes retrying a `POST` safe in the first place. If the interruption happened against Daraja, that retry surfaces the indeterminate `409` rather than charging again.\n\nIf you did not pass a key and you intend to retry the *same attempt* later, persist the one you got back:\n\n```ts title=\"Persist the key with the attempt\"\nconst ack = await paylod.collect({ amount, phone });\nawait db.attempts.update(attempt.id, { idempotencyKey: ack.idempotencyKey });\n// Retrying THAT attempt with THAT key collapses into the original payment.\n// A genuinely new attempt gets a new key — reusing this one would replay the old result.\n```\n\nCalling the HTTP API directly? It is the `Idempotency-Key` request header, and it behaves identically.\n\n## Webhooks (optional)\n\nYou do not need a webhook to take a payment. `collectAndWait()` polls for you, and `check()` lets your own frontend poll. That is a complete, supported integration, and it needs nothing but `PAYLOD_API_KEY`.\n\nAdd a webhook when you want your **server** told about a payment it is not currently waiting on: the customer closed the tab right after entering their PIN, your request timed out but the money landed anyway, or your process restarted. Polling tells the browser; a webhook tells your backend. For anything with an order to fulfil, you will eventually want both.\n\nSet the signing secret when you do:\n\n```bash title=\".env\"\nPAYLOD_WEBHOOK_SECRET=whsec_...   # only if you consume webhooks\n```\n\n### Web `Request`/`Response` — Next.js, Hono, Remix, Workers, Bun, Deno\n\n```ts title=\"app/api/webhooks/paylod/route.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod();\n\nexport const POST = paylod.webhookHandler(async (event) => {\n  if (event.type === \"payment.success\") {\n    await fulfil(event.data.paymentId, event.data.mpesaReceipt);\n  } else if (event.data.decoded) {\n    await notify(event.data.decoded.customerMessage);\n  }\n});\n```\n\n`event.data.decoded` is `DecodedError | null` — populated on `payment.failed`, `null` on `payment.success`. Narrowing on `event.type` does not narrow it, so check it before reading it.\n\nThe signature is verified before your handler runs. A bad signature returns `400` and your handler is never called. If your handler throws, the route returns `500` so paylod retries the delivery — better a duplicate than a lost payment, so make your handler idempotent.\n\n### Express\n\n```ts title=\"server.ts\"\napp.post(\"/webhooks/paylod\", paylod.webhook(async (event) => {\n  if (event.type === \"payment.success\") await fulfil(event.data.paymentId);\n}));\n```\n\n> [!warn] Mount this route **before** any global `express.json()`, or give it `express.raw({ type: \"application/json\" })`. Once a JSON parser has turned the body into an object the raw bytes are gone and the signature cannot be verified. The middleware detects this and returns a loud `400` rather than skipping verification.\n\n### Verify it yourself\n\n```ts title=\"verifyWebhook\"\nconst event = paylod.verifyWebhook({\n  payload: rawBody,                            // string | Buffer | Uint8Array — the RAW bytes\n  signature: headers[\"paylod-signature\"],\n  toleranceSec: 300,                           // default\n});\n```\n\nThrows `PaylodSignatureVerificationError` if the signature does not check out. The signature scheme is documented under [Webhooks](/docs/api/webhooks).\n\n## Errors\n\nEvery error thrown by the client extends `PaylodError`.\n\n| Class | When |\n| --- | --- |\n| `PaylodConfigError` | No API key, or no global `fetch`. Thrown from the constructor. |\n| `PaylodSandboxOnlyError` | A `simulate` call (or `{ simulate: true }`) with a key that is not `mp_test_…`. Thrown locally; no request was sent. Extends `PaylodConfigError`. |\n| `PaylodInvalidRequestError` | Local validation failed — bad amount, unparseable phone. No request was sent. |\n| `PaylodApiError` | paylod returned a non-2xx. Carries `.status` and the parsed body. |\n| `PaylodConnectionError` | The request never got an answer (DNS, TLS, socket). Retried automatically. |\n| `PaylodTimeoutError` | `wait()` hit its deadline with the payment still `pending`. Not a failure. |\n| `PaylodSignatureVerificationError` | A webhook signature did not verify. |\n\nTransient failures — network blips, `429`, `5xx` — are retried automatically with backoff. `400`, `401`, `404`, `409` and `422` are real answers and are thrown immediately.\n\n## A complete example\n\nA browser checkout, done safely: the page calls your server, your server calls paylod. The key never leaves the server.\n\n```ts title=\"server.ts\"\nimport { Paylod, pendingOutcome } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n\n// POST /api/pay → ring the phone.\n// Takes an orderId, never an amount: the price is looked up here, so a payer\n// with a proxy cannot discount their own order.\nasync function startPayment(orderId: string, phone: string) {\n  const order = await getOrder(orderId);\n\n  // One row per press of Pay — that row's id IS the idempotency key.\n  // Double-click Pay and the second call replays the first payment instead of\n  // sending a second STK prompt. Retry after a wrong PIN and you get a NEW\n  // attempt, a NEW key, and a real second charge — which is what a retry means.\n  // Keying on `order.id` would replay the FAILED attempt forever; keying on a\n  // product id would replay the first customer's payment to everyone after them.\n  const attempt = await db.attempts.create({ orderId: order.id });\n\n  const ack = await paylod.collect({\n    amount: order.amount,\n    phone,\n    idempotencyKey: attempt.id,\n  });\n  return pendingOutcome(ack.paymentId);\n}\n\n// GET /api/pay/:id → has it settled? Already decoded, ready to render.\nasync function readPayment(paymentId: string) {\n  return paylod.check(paymentId);\n}\n```\n\nBoth functions return the same `PaymentOutcome`, so your frontend has one shape to render and no result codes to interpret.\n\nThat is the whole integration. The [paylod demo](https://github.com/paylod/demo) is a working version of exactly this, including the retry path and a verified webhook.\n\n## Options\n\nYou will not normally need any of these. The defaults are the right answer.\n\n```ts title=\"Escape hatches\"\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!, {\n  timeoutMs: 30_000,\n  maxRetries: 2,\n});\n```\n\n| Option | Default | What it is for |\n| --- | --- | --- |\n| `apiKey` | `process.env.PAYLOD_API_KEY` | Authentication. The only required value. |\n| `timeoutMs` | `30_000` | Per HTTP request. |\n| `maxRetries` | `2` | Transient failures only (network, `429`, `5xx`). |\n| `webhookSecret` | `process.env.PAYLOD_WEBHOOK_SECRET` | Only if you consume webhooks. |\n| `baseUrl` | `https://paylod.dev/functions/v1` | Baked in. Override only to self-host or to point at a stub in tests. |\n| `fetch` | global `fetch` | Inject an instrumented or proxied fetch. |\n| `simulate` | `false` | Tests only. `collect()` creates a simulated payment instead of ringing a phone. Requires an `mp_test_…` key — the constructor throws otherwise. See [Test your checkout without a phone](#test-your-checkout-without-a-phone). |\n\n| Environment variable | Used for |\n| --- | --- |\n| `PAYLOD_API_KEY` | Authentication. Required. |\n| `PAYLOD_WEBHOOK_SECRET` | `verifyWebhook()`, `webhook()`, `webhookHandler()`. Optional. |\n| `PAYLOD_BASE_URL` | Overriding the API base. Rarely needed. |\n\n## Next\n\n- Prefer the terminal? The [CLI](/docs/cli) sends the same STK Push in one command.\n- The HTTP endpoints underneath: [API reference](/docs/api).\n- Every result code, decoded: [error reference](/docs/errors).",
  },
  {
    id: "auth",
    title: "Authentication, environments, idempotency and rate limits",
    keywords: ["auth","authentication","key","apikey","bearer","token","base","url","idempotency","idempotent","mp_live","mp_test","429","rate","limit","environment","sandbox"],
    aliases: [],
    content: "# Authenticating with the paylod M-Pesa API\n\nAPI keys, environments, the base URL, idempotency and rate limits — everything every paylod request has in common.\n\n## Base URL\n\nEvery endpoint lives under `/functions/v1`.\n\n```text title=\"Base URL\"\nhttps://paylod.dev/functions/v1\n```\n\n## API keys\n\nAuthenticate every request with a paylod API key as a bearer token.\n\n```http title=\"Header\"\nAuthorization: Bearer mp_live_YOUR_API_KEY\n```\n\nCreate keys on an application's **API Keys** tab. A key is shown once, and it encodes two things:\n\n- **Which application** the request runs against — so you never send a shortcode.\n- **Which environment** it runs in — so you never send a `?sandbox=true` flag.\n\n| Prefix | Environment | Money |\n| --- | --- | --- |\n| `mp_test_…` | Sandbox | Simulated. Nothing settles. |\n| `mp_live_…` | Production | Real. Settles to your till. |\n\nYour Daraja consumer key, secret and passkey stay in encrypted storage server-side. They never travel over the wire, and no endpoint reads them back.\n\n> [!warn] Keys are server-side secrets. A key embedded in a browser or a mobile app is fully compromised — treat it as public and rotate it. See [Secure integration](/docs/guides/security).\n\n## Idempotency\n\n`POST /collect` accepts an `Idempotency-Key` header (or an `idempotencyKey` field in the body). The key names **one payment attempt**. Send it, and duplicate deliveries of that attempt — a double-click, a refreshed tab, a redelivered job, an internal network retry — collapse into a single payment and a single STK Push.\n\n```bash title=\"Idempotent collect\"\ncurl -X POST https://paylod.dev/functions/v1/collect \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Idempotency-Key: att_9f2c1b40\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{ \"amount\": 10, \"phone\": \"254712345678\" }'\n```\n\n**Mint one key per attempt, not per order and not per product.** paylod reserves the key *before* calling Daraja, so ten simultaneous requests carrying the same key produce exactly one payment and one push — every one of them comes back with the same `paymentId`. But that also means a key is spent once it has been used: keying on an order id replays the failed first attempt when you retry the order, and keying on a product id would replay the first-ever payment for that product to every subsequent customer. A retry after a wrong PIN is a **new charge** and needs a **new key**.\n\nThree ways a reused key is rejected, all `409`:\n\n| Reuse | Response |\n| --- | --- |\n| Same key, **different** body | `409` — two different charges collided on one key. A bug on your side. |\n| Same key, first request still in flight | `409` + `Retry-After`. The winner is talking to Daraja; wait and read the result. |\n| Same key, a previous attempt was **interrupted mid-flight against Daraja** | `409` **indeterminate** — the money may or may not have moved, and we will not repeat the call to find out. |\n\n> [!warn] **The indeterminate `409` is not a retryable error.** A timeout is not evidence that the money did not move, so paylod refuses to silently re-dispatch — for money, at-most-once beats at-least-once. **Do not retry with the same key**: read the payment status (`GET /status/:id` or your webhook), and only then decide. If it settled, you are done; if nothing happened, start a new attempt with a **new** key.\n\n## Rate limits\n\n`POST /collect` is rate-limited per API key and per phone number. Expect `429 Too Many Requests` under bursts, and back off. A `429` is refused *before* anything reaches Daraja, so retrying it with the same `Idempotency-Key` after a short delay is safe — the key is not spent by a rate-limited request.\n\n## Errors\n\npaylod returns conventional HTTP status codes.\n\n| Status | Meaning |\n| --- | --- |\n| `400` | Malformed JSON, or Daraja credentials are not configured for this application and environment. |\n| `401` | Missing or invalid API key. |\n| `404` | The resource does not exist, or belongs to another application. |\n| `409` | Idempotency key reused with a different body, still in flight, or spent by an interrupted provider call (indeterminate — check status, retry with a new key). |\n| `422` | Body failed validation (e.g. `amount` ≤ 0, unparseable phone). |\n| `429` | Rate limited. Back off and retry. |\n| `502` | Daraja rejected the request. The upstream error is passed through. |\n\nBeyond HTTP status, M-Pesa has its own result codes — `1032` (customer cancelled), `1037` (timeout), `2001` (wrong PIN). Every one of them is decoded in the [error reference](/docs/errors).",
  },
  {
    id: "payments",
    title: "Take a payment (STK Push)",
    keywords: ["collect","stk","push","payment","payments","pay","charge","amount","phone","checkout","receipt","accept","money"],
    aliases: [],
    content: "# Accept an M-Pesa payment with an STK Push\n\nCollect money from a customer with an M-Pesa STK Push: what to send, what comes back, and what the customer sees.\n\nCharge a customer. Your server decides the amount, the customer's handset rings, they enter their M-Pesa PIN, and the funds settle to your till.\n\n## Send the push\n\n```ts title=\"charge.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n\nconst outcome = await paylod.collectAndWait({\n  amount: 1500,                     // whole KES, from YOUR order record\n  phone: \"0712345678\",\n  idempotencyKey: attempt.id,       // one key per payment attempt. A double-click cannot charge twice.\n  accountReference: \"INV-2041\",     // YOUR correlation id — comes back on the result\n  description: \"Order #2041\",\n  metadata: { orderId: \"2041\" },    // opaque, stored with the payment\n});\n\nif (outcome.paid) {\n  await fulfil(\"2041\", outcome.receipt);\n} else {\n  await notify(outcome.message);\n}\n```\n\n`collectAndWait()` sends the push, then polls until the payment settles. `outcome.paid` is the only branch you need: on the other side, `outcome.message` is a decoded sentence you can show the customer.\n\n> [!warn] **Pass `idempotencyKey`, and mint one per payment attempt.** Duplicates of that attempt — a double-clicked Pay button, a refreshed tab, a retried job — collapse into **one** prompt and **one** charge. Leave it out and every call is a new charge: two clicks, two prompts, two debits. Do **not** key on the order or product id — that replays an old payment instead of making a new one. See [Idempotency](#idempotency-one-key-per-payment-attempt).\n\nNeed the `paymentId` straight away instead of waiting? Use `collect()`, which returns as soon as the prompt is on the handset. See [Handle the result](/docs/guides/handle-the-result).\n\n## What M-Pesa insists on\n\n- **Whole shillings.** `amount` is an integer, 1 to 150,000. Decimals are rejected.\n- **A Kenyan number.** `0712…`, `254712…` and `+254712…` all work; the SDK normalises them.\n- **One prompt at a time.** A customer already looking at a prompt cannot be sent another.\n\n`accountReference` is capped at 12 characters, `description` at 64. Both are validated locally, before the request leaves your process.\n\n## What `accountReference` actually is\n\nIt is a **correlation id you choose** — an invoice or order number. paylod stores it on the payment and hands it back to you as `accountRef` on `GET /status/:id` and on the webhook, so it is how you tie an M-Pesa payment to the order in your own database.\n\nWhat it is *not* is a message to the customer. Safaricom only surfaces it on a **Paybill** (`CustomerPayBillOnline`), where it is the account number the payer is paying to. On a **Till / Buy Goods** shortcode (`CustomerBuyGoodsOnline`) Safaricom ignores it for display: the payer sees your business name and the amount, and nothing else — exactly what you see paying at any Kenyan shop. Most small merchants are on a Till, so assume the customer will never read it.\n\nIf you omit it, it defaults to a short prefix of the `paymentId` — still unique, still traceable back to the payment from your M-Pesa statement.\n\n> [!warn] Never put anything the customer needs to see into `accountReference`. `description` is what shows up on the STK prompt. And never put anything *secret* in it either — on a Paybill it is visible to the payer.\n\n`accountReference` is a **label, not a lock**. Putting your order id in it does not stop a second charge — it just tags the payment. [`idempotencyKey`](#idempotency-one-key-per-payment-attempt) is the field that collapses duplicates. They are different jobs: order id in `accountReference`, a per-attempt key in `idempotencyKey`.\n\n## Idempotency: one key per payment attempt\n\nA customer double-clicks **Pay**. Your handler runs twice. Without an idempotency key, that is two STK prompts and two debits — and the customer, quite reasonably, wants a refund and an explanation.\n\nMint a key when the attempt begins, and it cannot happen:\n\n```ts title=\"charge.ts\"\nconst attempt = await db.attempts.create({ orderId: order.id });   // one row per press of Pay\n\nawait paylod.collectAndWait({\n  amount: order.amount,\n  phone,\n  idempotencyKey: attempt.id,   // ← the whole fix\n});\n```\n\nBoth requests carry the same key. paylod reserves it *before* it calls Daraja, so exactly one of them reaches the provider and the other replays its answer — same `paymentId`, same `checkoutRequestId`. The handset only ever rings once. This holds even when the two requests land in the same millisecond: ten concurrent calls with one key produce one payment and one push.\n\n| What the customer does | With a per-attempt `idempotencyKey` | Without |\n| --- | --- | --- |\n| Double-clicks **Pay** | 1 prompt, 1 charge | **2 prompts, 2 charges** |\n| Refreshes the tab and re-submits | 1 prompt, 1 charge | **2 prompts, 2 charges** |\n| Your job queue retries the handler | 1 prompt, 1 charge | **2 prompts, 2 charges** |\n\nOmit it and the SDK warns you once, because this is a money bug.\n\n### Per attempt — not per order, not per product\n\nThe key has to be **stable across duplicates of one attempt** and **fresh for a new charge**. That rules out the two shortcuts people reach for:\n\n- **The order id** is stable but never fresh. The customer mistypes their PIN; you retry the order under the same key; paylod replays the **failed** first attempt instead of charging them. The order is never paid.\n- **A product id** — or anything reused across purchases — is worse. Customer two buys the same product, hits the key customer one already spent, and replays customer one's payment. **Nobody after the first customer is charged at all.**\n\nA retry after a wrong PIN, a cancelled prompt or a timeout is a **new attempt**: new row, new key.\n\n> [!warn] Do **not** silence the SDK's warning with `idempotencyKey: crypto.randomUUID()` **at the call site**. A key generated fresh on every call is exactly the same as having no key — it just hides the warning telling you the customer is exposed. A random UUID is a fine key; it just has to be minted **once per attempt** and stored, not re-rolled inside the call.\n\n### When the same key is *not* a safe retry\n\nReusing a key with a *different* body (say, a changed amount) returns `409`. That is always a bug on your side: two different charges have collided on one key.\n\nAnd there is one case that looks like a network error but is not retryable. If a request dies mid-flight against Daraja — after paylod dispatched the call, before an answer came back — that key is **spent**. Retrying it returns `409` with an *indeterminate* message rather than pushing again. A timeout is not evidence that the money did not move, so paylod will not guess: **for money, at-most-once beats at-least-once.**\n\n> [!warn] **On the indeterminate `409`, do not retry with the same key.** Read the payment status first — `paylod.check(paymentId)` or `GET /status/:id` — and only then decide. If it settled, fulfil the order. If nothing happened, open a **new attempt with a new key**. Retrying under the spent key just returns the same `409`.\n\nIf you cannot supply a key up front, persist the generated one against the attempt before you retry it:\n\n```ts title=\"retry.ts\"\nconst ack = await paylod.collect({ amount: 1500, phone: \"0712345678\" });\nawait db.attempts.update(attempt.id, { idempotencyKey: ack.idempotencyKey });\n\n// Retrying THAT attempt: same key, same body → the original payment, no second prompt.\nawait paylod.collect({\n  amount: 1500,\n  phone: \"0712345678\",\n  idempotencyKey: ack.idempotencyKey,\n});\n// A genuinely new attempt gets a NEW key. Reusing this one would replay the old result.\n```\n\n## Never take the amount from the client\n\n> [!warn] The amount must come from your own order record, on your own server. If a browser or mobile app holds both the API key and the amount, a payer can intercept the request and pay less. That is the one way to be undercut, and it is entirely avoidable. See [Secure integration](/docs/guides/security).\n\n## Next\n\n[Handle the result](/docs/guides/handle-the-result) covers webhooks, and what to check before you fulfil.\n\n---\n\n# Payments API: STK Push and payment status\n\nPOST /collect to trigger an STK Push and GET /status/:id to read the outcome — the two endpoints that move and read money in.\n\n## POST /collect\n\nInitiate an STK Push. The customer gets an M-Pesa prompt on their handset; when they enter their PIN, funds settle directly to your till. Returns immediately with a `paymentId` — the final result arrives via the hosted callback, which you read back with `GET /status/:id` or a [webhook](/docs/api/webhooks).\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/collect\n```\n\n### Headers\n\n| Name | Type | Required | Description |\n| --- | --- | --- | --- |\n| `Authorization` | string | Required | Bearer API key (`mp_live_…` / `mp_test_…`). |\n| `Content-Type` | string | Required | `application/json`. |\n| `Idempotency-Key` | string | **Send it** | The double-charge guard. Names **one payment attempt** — mint it per attempt, not per order and not per product. A repeat with the same key **and** body returns the **original payment** instead of sending a second STK prompt, even under concurrency. Same key + a different body → `409`. Same key after a provider call was interrupted → `409` *indeterminate*: check status, retry with a **new** key. Omit it and every call is a new charge — a double-clicked Pay button becomes two prompts and two debits. |\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `amount` | integer | Required | Whole KES, 1–150000. M-Pesa rejects decimals. |\n| `phone` | string | Required | Kenyan MSISDN in any common form (`0712…`, `254712…`, `+254712…`). Normalised for you. |\n| `accountReference` | string | Optional | Your correlation id (invoice/order number) — returned as `accountRef` on `/status/:id` and on the webhook. Safaricom shows it to the payer only on a **Paybill** (`CustomerPayBillOnline`), where it is the account number; a **Till / Buy Goods** shortcode does not display it at all. 1–12 chars. Defaults to a short prefix of the `paymentId`, so an omitted reference is still unique and still traceable. A **label, not a lock**: it does not deduplicate anything — `Idempotency-Key` is what prevents a second charge. |\n| `description` | string | Optional | Free-text label, 1–64 chars. Defaults to `Payment`. |\n| `metadata` | object | Optional | Opaque key/values stored with the payment. Never inspected, and not returned on `/status` or the webhook — key your records on `paymentId`. |\n\n### Example\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/collect \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Idempotency-Key: att_9f2c1b40\" \\\n  -d '{\n    \"amount\": 10,\n    \"phone\": \"254712345678\",\n    \"accountReference\": \"INV-2041\",\n    \"description\": \"Order #2041\",\n    \"metadata\": { \"orderId\": \"2041\" }\n  }'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"paymentId\": \"e69e5c00-8a01-44ed-b003-48e2e86a7c9e\",\n  \"status\": \"pending\",\n  \"checkoutRequestId\": \"ws_CO_010720261905029287161380\"\n}\n```\n\n> [!warn] **Send `Idempotency-Key`, and mint one per payment attempt.** Repeat the same key and you get the same `paymentId` and `checkoutRequestId` back — the handset is not rung a second time, even if the duplicates arrive concurrently. Omit it and a double-clicked Pay button, a refreshed tab or a retried request each fire a fresh STK prompt and charge the customer again. Do **not** key on an order or product id: the key is spent once used, so a reused one replays an old payment instead of making a new one. A retry after a wrong PIN is a **new charge** and needs a **new key**.\n\n> [!warn] **`409` *indeterminate* means stop, not retry.** If a previous request under this key was interrupted while the Daraja call was in flight, paylod will not repeat it — the money may already have moved, and a timeout is not evidence that it did not. For money, at-most-once beats at-least-once. Read `GET /status/:id` first; if nothing happened, retry with a **new** key. The same key will only return this `409` again.\n\n### Errors\n\n| Status | When |\n| --- | --- |\n| `401` | Missing or invalid API key. |\n| `400` | Malformed JSON, or Daraja credentials not configured for this app / environment. |\n| `409` | Idempotency key reused with a different body; or still in flight (`Retry-After`); or spent by an interrupted provider call — *indeterminate*, check status and retry with a new key. |\n| `422` | Body failed validation (e.g. `amount` ≤ 0, bad phone). |\n| `429` | Rate limited — per key and per phone number. |\n| `502` | Daraja rejected the STK Push (upstream error passed through). |\n\n## GET /status/:id\n\nRead a payment's current state — the zero-infrastructure way to get the result, with no webhook receiver needed. If the payment is still `pending`, paylod runs a live M-Pesa STK Query and settles it on the spot before responding. Scoped to the key's application: another application's payment returns `404`.\n\n```text title=\"Endpoint\"\nGET https://paylod.dev/functions/v1/status/:id\n```\n\n### Path\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `id` | string | Required | The `paymentId` returned by `POST /collect`. |\n\n### Example\n\n```bash title=\"Request\"\ncurl https://paylod.dev/functions/v1/status/e69e5c00-8a01-44ed-b003-48e2e86a7c9e \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\"\n```\n\n```json title=\"Response · 200 OK\"\n{\n  \"id\": \"e69e5c00-8a01-44ed-b003-48e2e86a7c9e\",\n  \"status\": \"success\",\n  \"mpesaReceipt\": \"UG1F3A1U7J\",\n  \"resultCode\": 0,\n  \"resultDesc\": \"The service request is processed successfully.\"\n}\n```\n\n### Status values\n\n| Value | Meaning |\n| --- | --- |\n| `pending` | The prompt is out; the customer has not resolved it yet. |\n| `success` | Settled. `mpesaReceipt` is the M-Pesa receipt number. |\n| `failed` | `resultCode` / `resultDesc` carry the decoded M-Pesa reason. |\n\nOn failure, `resultCode` is the raw Safaricom code — `1032` cancelled, `1037` timeout, `2001` wrong PIN. All of them are decoded in the [error reference](/docs/errors).",
  },
  {
    id: "results",
    title: "Read the result — poll, wait, or take a signed webhook",
    keywords: ["result","results","status","poll","polling","wait","settle","settled","pending","4999","timeout","outcome","fulfil","fulfill"],
    aliases: ["status"],
    content: "# Handle the M-Pesa payment result\n\nTwo ways to learn a payment settled — poll GET /status, or receive a signed webhook. How to choose, and how to verify.\n\n`collect()` returns before the customer has typed their PIN. There are two ways to learn what happened, and both read the same settled record.\n\n| | Polling | Webhooks |\n| --- | --- | --- |\n| You host | Nothing | An HTTPS endpoint |\n| Good for | Scripts, a checkout with a spinner | Servers, volume, back-office fulfilment |\n\nStart with polling. Move to webhooks when you have a server that wants to be told.\n\n## Option A: poll\n\n```ts title=\"poll.ts\"\nconst outcome = await paylod.wait(ack.paymentId, { timeoutMs: 120_000 });\n\nif (outcome.paid) await fulfil(outcome.receipt);\nelse              await notify(outcome.message);\n```\n\n`wait()` polls on a jittered backoff ramp until the payment settles. If the payment is still pending, paylod runs a live M-Pesa STK Query and settles it on the spot, so a poll never returns a stale answer.\n\n`paylod.status(paymentId)` reads the record once, without polling.\n\n> A timeout **throws** `PaylodTimeoutError` rather than returning `ok: false`. An unanswered prompt is not a failed payment: the customer may still be typing their PIN, and it can still succeed. Do not mark the order failed. Let the webhook tell you.\n\n## Option B: receive a signed webhook\n\nRegister an endpoint on the application's **Endpoints & Webhooks** tab. The SDK verifies the signature for you.\n\n```ts title=\"app/api/webhooks/paylod/route.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod();\n\nexport const POST = paylod.webhookHandler(async (event) => {\n  if (event.type === \"payment.success\") {\n    await fulfil(event.data.paymentId, event.data.mpesaReceipt);\n  } else if (event.data.decoded) {\n    await notify(event.data.decoded.customerMessage);\n  }\n});\n```\n\n`decoded` is `DecodedError | null`: it is populated on `payment.failed` and `null` on `payment.success`. Check it before you read it.\n\nThis works anywhere the Web `Request`/`Response` API does: Next.js, Hono, Remix, Workers, Bun, Deno. On Express, use `paylod.webhook(handler)` instead.\n\nThe signature is checked **before** your handler runs. A bad signature returns `400` and your handler never sees the event. If your handler throws, the route returns `500` and paylod retries the delivery, so make the handler idempotent.\n\n> [!warn] On Express, mount this route **before** any global `express.json()`. Once a JSON parser has consumed the body the raw bytes are gone and no signature can be verified. The middleware detects this and fails loudly rather than skipping the check.\n\n## Check the amount before you fulfil\n\nThe webhook carries the **true settled amount**. Fulfilling on `status` alone is how merchants get underpaid.\n\n```ts title=\"fulfil.ts\"\nexport const POST = paylod.webhookHandler(async (event) => {\n  if (event.type !== \"payment.success\") return;\n\n  // Look the order up by paymentId — the id you stored when you called collect().\n  const order = await db.orders.findByPaymentId(event.data.paymentId);\n  if (!order) return;\n\n  if (event.data.amount === order.amountKes) {\n    await fulfil(order);\n  } else {\n    await flagForReview(order, event.data);   // underpaid\n  }\n});\n```\n\nStore `ack.paymentId` against your order when you send the push, and key the lookup on it. The event does not echo back the `metadata` you passed to `collect()`.\n\n## Failures are results, not outages\n\n`payment.failed` is a normal outcome. `event.data.decoded` is already decoded for you:\n\n- `1032` the customer pressed Cancel. Offer a retry.\n- `1037` the prompt never reached the handset, or was ignored.\n- `2001` wrong M-Pesa PIN.\n- `1` insufficient balance.\n\n`4999` is **not** a failure. It means the customer has not entered their PIN yet. Keep polling, and never re-collect: a second push fires a second prompt and can double-charge. Every code is in the [error reference](/docs/errors).\n\n## Retrying a failed payment\n\nWhen a payment genuinely failed — cancelled, wrong PIN, insufficient balance — charging again is a **new payment attempt**, and it needs a **new idempotency key**. Retrying under the key of the failed attempt does not re-charge; it replays the failure you already have.\n\n```ts title=\"retry.ts\"\nif (!outcome.paid && outcome.retryable) {\n  const retry = await db.attempts.create({ orderId: order.id });   // new attempt → new key\n  await paylod.collectAndWait({ amount, phone, idempotencyKey: retry.id });\n}\n```\n\n> [!warn] **One `409` is not a retry signal: the *indeterminate* one.** If an earlier request under that key died while the call to Daraja was in flight, paylod refuses to re-dispatch it — the money may already have moved, and a timeout is not evidence that it did not. Read the payment status before you do anything else (`paylod.check(paymentId)`, `GET /status/:id`, or wait for the webhook). If it settled, fulfil the order. If nothing happened, open a new attempt with a **new** key. Never retry the spent key. Full rules in [Idempotency](/docs/sdk#idempotency).\n\n## Verify it yourself\n\nIf you are not using the SDK, the signature is an HMAC-SHA256 over `t + \".\" + rawBody`, keyed by the endpoint's signing secret, with a 5-minute freshness window. Compute it over the raw bytes, never a re-serialised object. The scheme is specified in the [webhooks reference](/docs/api/webhooks).\n\n## Next\n\n[Test in sandbox](/docs/guides/test-in-sandbox) forces each of these outcomes on demand.",
  },
  {
    id: "webhooks",
    title: "Webhooks — signed delivery of the settled result",
    keywords: ["webhook","webhooks","signature","hmac","signing","secret","event","delivery","verify","endpoint"],
    aliases: [],
    content: "# M-Pesa payment webhooks: events and signatures\n\nThe event envelope, the delivery headers, the HMAC signature scheme, and every event type paylod can send you.\n\nWebhooks are **optional**. Register an endpoint on an application's **Endpoints & Webhooks** tab and paylod POSTs a signed event the moment something settles. If you would rather not run a receiver, poll [`GET /status/:id`](/docs/api/payments) instead — every payment is recorded either way.\n\n## Delivery headers\n\n| Header | Description |\n| --- | --- |\n| `x-webhook-event` | The event type, e.g. `payment.success`. |\n| `x-webhook-id` | Unique delivery id. Use it to dedupe retries. |\n| `x-webhook-signature` | HMAC signature, format `t=<unix>,v1=<hex>`. |\n\n## Event types\n\n| Event | Fired when |\n| --- | --- |\n| `payment.success` | A collection settled (STK Push or offline C2B). |\n| `payment.failed` | A collection failed — cancelled, timed out, wrong PIN, insufficient funds. |\n| `payout.result` | A [`/payout`](/docs/api/payouts) reached a terminal state. |\n| `refund.result` | A [`/reversal`](/docs/api/payouts) reached a terminal state. |\n| `admin.transaction_status` | A [`/transaction-status`](/docs/api/other) query resolved. |\n| `admin.account_balance` | An [`/account-balance`](/docs/api/other) query resolved. |\n\nEvery event uses the same envelope and the same signature scheme.\n\n## The envelope\n\n```http title=\"Example delivery\"\nPOST https://your-app.com/webhooks/mpesa\nx-webhook-event: payment.success\nx-webhook-id: 8f3c…\nx-webhook-signature: t=1751394302,v1=5a2f…c9\n\n{\n  \"type\": \"payment.success\",\n  \"created\": 1751394302,\n  \"data\": {\n    \"paymentId\": \"e69e5c00-…\",\n    \"applicationId\": \"…\",\n    \"env\": \"production\",\n    \"status\": \"success\",\n    \"amount\": 10,\n    \"phone\": \"254712345678\",\n    \"accountRef\": \"INV-2041\",\n    \"mpesaReceipt\": \"UG1F3A1U7J\",\n    \"checkoutRequestId\": \"ws_CO_…\",\n    \"resultCode\": 0,\n    \"resultDesc\": \"The service request is processed successfully.\",\n    \"metadata\": { \"orderId\": \"2041\" }\n  }\n}\n```\n\n## Verifying the signature\n\nThe signature is an HMAC-SHA256 over the string `t + \".\" + rawBody`, keyed by the endpoint's signing secret (shown once, when you add the endpoint).\n\n```js title=\"Verify (Node)\"\nimport crypto from \"node:crypto\";\n\n// Express raw-body handler\nexport function verify(req, secret) {\n  const header = req.headers[\"x-webhook-signature\"]; // \"t=…,v1=…\"\n  const parts = Object.fromEntries(header.split(\",\").map((p) => p.split(\"=\")));\n\n  const signed = `${parts.t}.${req.rawBody}`;      // timestamp + raw JSON\n  const expected = crypto\n    .createHmac(\"sha256\", secret)\n    .update(signed)\n    .digest(\"hex\");\n\n  const ok = crypto.timingSafeEqual(\n    Buffer.from(expected),\n    Buffer.from(parts.v1),\n  );\n\n  // Reject if !ok, or if Date.now()/1000 - parts.t > 300 (replay window).\n  return ok;\n}\n```\n\nThree rules, in order:\n\n1. Compute the HMAC over the **raw** request body — not a re-serialised object.\n2. Compare in **constant time** against `v1`.\n3. Reject deliveries whose `t` is older than about five minutes, to blunt replay.\n\n## Responding\n\nRespond `2xx` to acknowledge. A non-2xx response is retried with backoff, and the attempts are visible in the dashboard. Deliveries are at-least-once: dedupe on `x-webhook-id`.\n\n> [!warn] Verify the signature, then compare `data.amount` against your own order total, **before** you fulfil. The webhook always carries the true settled amount. See [Secure integration](/docs/guides/security).",
  },
  {
    id: "errors",
    title: "M-Pesa result codes and paylod HTTP errors",
    keywords: ["error","errors","resultcode","failed","failure","decode","1032","2001","1037","1001","4999","insufficient","cancelled","pin","debug"],
    aliases: [],
    content: "# M-Pesa and Daraja error codes, decoded\n\nEvery Safaricom M-Pesa result code and Daraja API error decoded — cause, fix, and a customer-facing message. Searchable.\n\nM-Pesa tells you *what* went wrong in a number and almost nothing else. `resultCode: 1037` is a real answer to a real question, and it means nothing until someone decodes it.\n\nThis page decodes all of them: what the code means, whose fault it is, whether retrying can possibly help, and what to say to the customer.\n\n## How to read a failure\n\nA failed payment gives you two fields, and they come from two different places:\n\n| Field | Where it comes from |\n| --- | --- |\n| HTTP status | paylod — `401` bad key, `422` bad body, `502` Daraja refused the request. See [API errors](/docs/api). |\n| `resultCode` / `resultDesc` | M-Pesa — the customer cancelled, the PIN was wrong, the phone was off. Decoded below. |\n\nA `202` followed by `resultCode: 1032` is not a bug. It means the push worked and the customer pressed Cancel.\n\n## You do not have to implement this table\n\nThe [Node SDK](/docs/sdk) decodes result codes for you, offline — no network call, no API key needed at call time:\n\n```ts title=\"decode.ts\"\nimport { decodeError } from \"@paylod/node\";\n\nconst err = decodeError(1032);\n\nerr.title;            // \"Payment cancelled by the customer\"\nerr.category;         // \"customer\"\nerr.retryable;        // true\nerr.customerMessage;  // \"Payment cancelled — you can try again whenever you're ready.\"\n```\n\nOn a failed payment, `collectAndWait()` hands you the same object as `outcome.detail`, and a `payment.failed` webhook carries it as `event.data.decoded`. They are byte-for-byte identical, so what you show a customer is the same however you integrate. The whole catalogue is also exported as `ERROR_CATALOG`.\n\nMost of the time you will not touch any of this: `outcome.message` is the customer-facing sentence, already decoded, and `outcome.retryable` tells you whether charging again is safe. The catalogue below is for your logs, your dashboards, and mid-incident debugging.\n\nThe [CLI](/docs/cli) decodes the same catalogue from your terminal — `paylod errors 1032` — which is the fastest way to answer \"what does this code mean\" mid-incident.\n\n> [!warn] **`4999` and `500.001.1001` are pending, not failed.** Daraja returns them while the customer is still looking at the PIN prompt. They decode with `category: \"pending\"` and `retryable: false` — not because a retry is pointless, but because the first prompt is still live and a second push can **double-charge** the customer. Do not show a failure on these codes and do not re-collect. Keep polling; the SDK already does.\n\n## The ones you will actually see\n\n| Code | Meaning | What to do |\n| --- | --- | --- |\n| `0` | Success. | Fulfil — after checking the settled `amount`. |\n| `1032` | The customer pressed **Cancel** on the prompt. | Offer a friendly retry. Not an error on your side. |\n| `1037` | Timeout — the prompt never reached the handset, or was ignored. | Retry. Phone off, out of coverage, or the customer walked away. |\n| `2001` | Wrong M-Pesa PIN. | Ask them to try again. |\n| `1` | Insufficient balance. | Tell the customer their M-Pesa balance is too low. |\n| `4999` | **Not a failure.** The customer has not entered their PIN yet. | Keep polling. Never re-collect — the prompt is still live and a second push can double-charge. |\n\nEverything else — every Daraja API error, every C2B/B2C result code — is in the searchable catalogue below.\n\n## Full catalogue\n\nThe full catalogue is a **tool**, not a table to copy: call `decode_mpesa_error` with any result code and it returns `{ code, title, cause, fix, category, retryable, customerMessage }` offline. In application code, the SDK does it for you — `outcome.message` is already the customer-facing sentence. The browsable reference is at https://paylod.dev/docs/errors.",
  },
  {
    id: "security",
    title: "Secure integration — where the API key may and may not go",
    keywords: ["security","secure","safe","tamper","proxy","undercut","browser","client","frontend","leak","best","practice"],
    aliases: [],
    content: "# Secure your M-Pesa integration\n\nSix rules that make it impossible for a payer with an intercepting proxy to undercut you, and why the amount must stay on your server.\n\n## The question everyone asks\n\n*Could a customer open my checkout in an intercepting proxy — Burp, mitmproxy — and lower the amount before it reaches M-Pesa?*\n\nUnder a correct integration, **no**.\n\nThe API key that calls `/collect` authenticates **you**, the merchant — not the payer. Only `amount` and `phone` come from the request body; your shortcode, passkey and Daraja credentials are pulled from encrypted storage server-side and never travel over the wire. Settlement is bound to the amount your server initiated, and every webhook is HMAC-signed and reports the **true settled amount**.\n\nThere is exactly one way to be undercut: hand the payer control of **both** the key and the amount — i.e. call `/collect` straight from a browser or mobile app with a client-supplied amount. Then a payer can edit the request and pay less. The platform will faithfully report the true, lower amount, but it cannot rescue a merchant who trusted the client with it.\n\nKeep the amount on your server and the attack disappears.\n\n## The short version\n\n| Do | Don't |\n| --- | --- |\n| Keep `mp_live_…` / `mp_test_…` server-side only | Embed the API key in a browser, mobile app, or any client |\n| Derive `amount` from your own order record | Forward a client-supplied amount verbatim to `/collect` |\n| Verify `x-webhook-signature` before reading the body | Trust an unverified webhook |\n| Confirm `amount` matches the order **and** `status === \"success\"` before fulfilling | Fulfil on `status` alone |\n| Send one `Idempotency-Key` per **payment attempt** | Key on an order or product id, or re-roll the key on every call |\n| Expect `429`s and back off | Hammer on rate limits |\n\n## 1. Keys are server-only secrets\n\nNever expose `mp_live_…` or `mp_test_…` in a browser, mobile app, or any client. Keys live only on your server. A client-embedded key is fully compromised — treat it as public. Anyone holding it can call your account.\n\n## 2. Your server owns the amount\n\nYour server is the sole authority on the amount. Derive it from your own order or price record; never pass a client-supplied amount straight through to `/collect`. If the client both holds the key and sets the amount, a payer can lower it — that is the one and only way to be undercut.\n\n## 3. Verify, then check the amount, before fulfilling\n\nOn the webhook: verify the signature **first**, then confirm the amount and the status **before** fulfilling. `paylod.webhookHandler()` does the verification for you, so your handler only runs on an authentic event.\n\n```ts title=\"fulfil.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n\nexport const POST = paylod.webhookHandler(async (event) => {\n  if (event.type !== \"payment.success\") return;\n\n  // Look the order up by paymentId — the id you stored when you called collect().\n  const order = await db.orders.findByPaymentId(event.data.paymentId);\n  if (!order) return;\n\n  // Never fulfil on status alone. Check the TRUE settled amount.\n  if (event.data.amount === order.amountKes) {\n    await fulfil(order);                    // paid in full\n  } else {\n    await flagForReview(order, event.data); // underpaid / mismatch\n  }\n});\n```\n\nThe webhook always carries the true settled amount, so any partial or underpayment is visible the moment you check it.\n\nStore `ack.paymentId` against your order when you call `collect()`, and key the lookup on it. The event carries `paymentId`, `accountRef` and the M-Pesa fields. It does **not** echo back the `metadata` you sent to `collect()`, so do not build your fulfilment around that.\n\nMake the handler **idempotent**. Deliveries are retried until you return `2xx`, and a repeat of an event you have already handled is normal rather than an error.\n\n## 4. Treat callback and mint tokens as secrets\n\nThe callback URL and the mint token are secrets. Don't log them, don't expose them client-side — they belong on your server, alongside the API key.\n\n## 5. One idempotency key per payment attempt\n\nMint an `Idempotency-Key` per **payment attempt** — not per order, and never per product. Duplicates of one attempt (a double-click, a redelivered job) then collapse into a single charge, even under concurrency. Reusing a key with a **different** body is rejected with `409`.\n\nA key is **spent** once used, which is why an order or product id is the wrong choice: a reused key replays the *original* payment rather than making a new one. A retry after a wrong PIN is a new charge and needs a new key.\n\n> [!warn] If a request is interrupted while the provider call is in flight, that key returns `409` *indeterminate* and is never silently re-dispatched — a timeout is not evidence that the money did not move. **Do not retry the same key.** Read the payment status, then retry with a **new** key if nothing happened. For money, at-most-once beats at-least-once. See [Idempotency](/docs/sdk#idempotency).\n\n## 6. Expect rate limits\n\n`/collect` is rate-limited per API key and per phone number. Expect `429 Too Many Requests` under bursts and back off. A `429` is refused before anything reaches Daraja, so it does not spend the key — retrying with the same `Idempotency-Key` after a short delay is safe.\n\n---\n\nFollow these six rules and a payer wielding an intercepting proxy cannot undercut you, over-charge you, redirect funds, or trick you into fulfilling an order that was not paid in full. The platform is safe by design — it just needs the amount to stay on your server.",
  },
  {
    id: "sandbox",
    title: "Test in the sandbox, including every failure path",
    keywords: ["test","testing","simulate","simulation","fake","sandbox","shortcode","174379"],
    aliases: [],
    content: "# Test M-Pesa payments in the Daraja sandbox\n\nBuild the whole flow against Safaricom's sandbox with test credentials — including the failure paths — before a shilling moves.\n\n## Test every failure path without a phone\n\nStart here. You do **not** need a handset to test cancellation, a wrong PIN, insufficient funds or a timeout — and you should not try to produce them by hand. paylod ships a simulator that drives the **real** settlement path: the same payment row, the same result codes, the same signed webhook your production code will receive. Nothing is mocked except the handset.\n\nTwo calls:\n\n1. **Create a simulated payment** — `paylod.simulate.collect()` in the SDK, `simulate_test_payment` (MCP tool), or `POST /simulate/collect` over REST with your `mp_test_…` key. No STK prompt is sent. It returns `{ paymentId, status: \"pending\", outcomes }`, where `outcomes` is an array of `{ id, label, status }`.\n2. **Force how it resolves** — `paylod.simulate.outcome()`, `simulate_outcome` (MCP tool), or `POST /simulate/outcome`, with that `paymentId` and one `outcome` id.\n\n| `outcome` | Settles as | Result code |\n| --- | --- | --- |\n| `approve` | `success` | `0` |\n| `user_cancelled` | `failed` | `1032` |\n| `timeout` | `failed` | `1037` |\n| `wrong_pin` | `failed` | `2001` |\n| `insufficient_funds` | `failed` | `1` |\n\n### From the SDK\n\nIf you are on Node, this is the path to use. It is one call, and it hands you back the same `PaymentOutcome` your production code already renders — so the code under test is the real code, not a special one.\n\n```ts title=\"checkout.test.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_TEST_KEY!);   // mp_test_…\n\nfor (const outcome of paylod.simulate.outcomes) {\n  const result = await paylod.simulate.pay({ outcome, amount: 10 });\n  console.log(outcome, \"→\", result.status, result.retryable, result.message);\n}\n// approve            → succeeded false Payment received — thank you!\n// wrong_pin          → failed    true  That M-Pesa PIN was incorrect. …\n// insufficient_funds → failed    true  Your M-Pesa balance is too low. …\n// user_cancelled     → cancelled true  Payment cancelled — you can try again …\n// timeout            → failed    true  The M-Pesa prompt expired before it was answered. …\n```\n\nTo test *your* handler rather than the SDK's, build the client with `{ simulate: true }` — `collect()` then creates a simulated payment instead of ringing a phone, and your `/api/pay` route runs unchanged. A `mp_live_…` key is refused locally, before any request is sent. Full details in the [SDK reference](/docs/sdk#test-your-checkout-without-a-phone).\n\n### Over REST\n\n```bash title=\"Simulate a cancelled payment over REST\"\n# 1. create the pending simulated payment\ncurl -sX POST https://paylod.dev/functions/v1/simulate/collect \\\n  -H \"Authorization: Bearer $PAYLOD_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"applicationId\":\"<your-app-id>\",\"phone\":\"254708374149\",\"amount\":10}'\n# → { \"paymentId\": \"…\", \"status\": \"pending\", \"outcomes\": [ { \"id\": \"approve\", … } ] }\n\n# 2. force the outcome — your webhook fires, your poller settles, exactly as in production\ncurl -sX POST https://paylod.dev/functions/v1/simulate/outcome \\\n  -H \"Authorization: Bearer $PAYLOD_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"paymentId\":\"<paymentId>\",\"outcome\":\"user_cancelled\"}'\n```\n\nAn AI agent with the `paylod:payments.simulate` scope can do the same thing with `simulate_test_payment` + `simulate_outcome` and never be able to touch real money. See the [MCP server](/docs/mcp).\n\n> [!note] The simulator is sandbox-only. It is the fastest way to prove your code renders `outcome.message`, offers a retry only when `retryable` is true, and never fulfils an order on a `1032`.\n\n## Get sandbox credentials\n\nCreate an app on the [Safaricom Daraja portal](https://developer.safaricom.co.ke) and open its **Sandbox** credentials. You want four values:\n\n| Field | Sandbox value |\n| --- | --- |\n| Consumer key | From the Daraja portal |\n| Consumer secret | From the Daraja portal |\n| Shortcode | `174379` (Safaricom's shared test paybill) |\n| Passkey | From \"Lipa na M-Pesa Online\" in the portal |\n\nPaste them into your application's **Sandbox** environment in paylod, then mint an `mp_test_…` key.\n\n## What sandbox actually does\n\nSandbox is Safaricom's own environment, not a paylod mock. The STK Push is real: it goes to Daraja, Daraja answers, and the callback comes back to paylod's hosted receiver exactly as it would in production. What is *not* real is the money — nothing settles.\n\nThat means the shape of everything you build — the `202`, the `paymentId`, the webhook envelope, the signature, the result codes — is identical in production. There is no second integration to write.\n\n## Send a test payment\n\nSet `PAYLOD_API_KEY` to the `mp_test_…` key. Nothing else changes.\n\n```ts title=\"sandbox.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);   // mp_test_… key\n\nconst outcome = await paylod.collectAndWait({\n  amount: 10,\n  phone: \"254708374149\",     // Safaricom's published test MSISDN\n  accountReference: \"TEST-1\",\n});\n\nconsole.log(outcome.paid ? outcome.receipt : outcome.message);\n```\n\nUse a real number you control and you will get a real prompt, with no real money attached.\n\n## Exercise the failure paths\n\nMost production incidents are failure paths nobody tried. Before you go live, make sure your code does something sensible for each of these. Use the **simulator** (above) — it is deterministic, needs no handset, runs in your test suite, and `1037` alone would otherwise cost you a minute of staring at a phone. The handset column is there only if you want to see it happen for real once.\n\n| Result code | Simulator `outcome` | On a real handset | What your code should do |\n| --- | --- | --- | --- |\n| `1032` | `user_cancelled` | Press **Cancel** on the prompt | Show a friendly retry, do not fulfil |\n| `1037` | `timeout` | Ignore the prompt until it expires | Retry, or fall back to polling |\n| `2001` | `wrong_pin` | Enter the wrong M-Pesa PIN | Ask them to try again |\n| `1` | `insufficient_funds` | Push an amount above the test balance | Tell the customer the balance is low |\n\nEvery code, with its cause and fix, is in the [error reference](/docs/errors).\n\n## Test your webhook locally\n\nNo tunnel needed. The [CLI](/docs/cli) streams your live webhook events to `localhost` with the signature intact:\n\n```bash title=\"Terminal\"\nnpx @paylod/cli listen --forward http://localhost:3000/webhook\n```\n\nYour handler verifies a real signature against a real payload, on your laptop. See [Handle the result](/docs/guides/handle-the-result).\n\n## Next\n\n[Go live](/docs/guides/go-live) — swap the credentials, swap the key, keep the code.",
  },
  {
    id: "go-live",
    title: "Go live — the callback URL, production credentials, the checklist",
    keywords: ["live","production","go-live","launch","ship","callback","callbackurl","resulturl","validationurl","confirmationurl","portal","safaricom"],
    aliases: ["callback-url","production"],
    content: "# Go live on production Daraja credentials\n\nMove from sandbox to production: production Daraja credentials, the callback URL you paste into the Daraja portal, and the go-live checklist.\n\n## What changes\n\nAlmost nothing. Your code is the same; three things move.\n\n1. Your application's **Production** environment gets your *production* Daraja credentials and your real shortcode.\n2. You paste the **production callback URL** into the Daraja portal.\n3. Your backend swaps `mp_test_…` for `mp_live_…`.\n\nThe endpoints, the request bodies, the webhook envelope and the result codes are identical. That is the whole point of building against [sandbox](/docs/guides/test-in-sandbox) first.\n\n## 1. Get production Daraja credentials\n\nProduction credentials are issued by Safaricom against your **real** till or paybill, on the [Daraja portal](https://developer.safaricom.co.ke) under \"Go Live\". You will need:\n\n- Your consumer key and secret for the production app.\n- Your shortcode — the till or paybill number customers already pay.\n- Your Lipa na M-Pesa Online passkey.\n- For payouts, reversals, and the admin queries: an **Initiator Name** and **Initiator Password** (a.k.a. the security credential), issued separately.\n\nPaste them into the application's **Production** environment. They are encrypted at rest and never read back.\n\n## 2. Set the callback URL in the Daraja portal\n\npaylod hosts the callback receiver, so what you paste into Daraja is the URL paylod shows you on the application's **Endpoints & Webhooks** tab.\n\n> This is the one step nobody can do for you — Safaricom has no API for registering a callback URL. Everything else in a paylod integration is automatable.\n\nTreat the callback URL as a secret. It carries a per-application token.\n\n## 3. Swap the key\n\nMint an `mp_live_…` key on the **API Keys** tab and put it in your server's secret store. Your code does not change at all:\n\n```bash title=\".env\"\nPAYLOD_API_KEY=mp_live_YOUR_API_KEY\n```\n\nThe key alone decides that this is production. There is no environment flag to forget.\n\n## Go-live checklist\n\n- The API key lives on your **server**, never in a browser or mobile app.\n- The `amount` comes from your own order record, never from the client.\n- Your webhook handler **verifies the HMAC signature** before parsing the body. `paylod.webhookHandler()` does this for you.\n- You compare `data.amount` against the order total **before** fulfilling.\n- One `Idempotency-Key` per **payment attempt** — not per order or product. A retry after a failure is a new attempt and gets a new key.\n- Your code handles the `409` *indeterminate* reply by **reading the payment status**, not by retrying the same key.\n- `429` is handled with a backoff, not a crash.\n- Your failure paths (`1032`, `1037`, `2001`) have been exercised at least once.\n- If you use payouts or admin queries, the Initiator Name and Password are set on the **Production** environment.\n\nThe reasoning behind each of these is in [Secure integration](/docs/guides/security).\n\n## Then watch it\n\nPayments, webhook deliveries and their retries are all visible in the dashboard. If a delivery is failing, you will see the attempts and the response your endpoint returned.",
  },
  {
    id: "mcp",
    title: "The MCP server — the tools, and the order to call them in",
    keywords: ["mcp","tool","tools","agent","agents","scope","scopes","oauth","create_app","set_credentials","mint_key","consent"],
    aliases: [],
    content: "# MCP server: M-Pesa for AI agents\n\nLet an AI agent manage and use your M-Pesa integration — a remote, OAuth-authenticated MCP endpoint. Nothing to install.\n\n## Nothing to install\n\npaylod ships a **remote** MCP server. There is no npm package to add, no process to run, and no API key to paste into a config file. You give your AI client one URL; it discovers the authorization server, opens your browser once, and you approve exactly the capabilities you want. The agent gets a scoped token — never your Daraja credentials, never your paylod API key.\n\n```text title=\"Server URL\"\nhttps://mcp.paylod.dev/mcp\n```\n\nTransport: `streamable-http`. Auth: OAuth 2.1 (authorization code + PKCE, with Dynamic Client Registration) against `https://paylod.dev/oauth`. Any MCP client that speaks remote HTTP works — and stdio-only clients can bridge.\n\n## Add it to your client\n\nPick your client. Each one runs the OAuth flow for you on first connect: a browser tab opens, you sign in to paylod, you consent to scopes, done.\n\n### Claude Code\n\nOne command. Use `--scope user` to make it available across all your projects (`local` is the default; `project` writes a shared `.mcp.json`).\n\n```bash title=\"Terminal\"\nclaude mcp add --transport http --scope user paylod https://mcp.paylod.dev/mcp\n```\n\nThen run `/mcp` inside Claude Code and pick **paylod** to authenticate. After that, `/mcp` shows the server connected with its tools listed.\n\nPrefer to commit the config? Put this in `.mcp.json` at the repo root. The `type` field is required — an entry with a `url` but no `type` is treated as stdio and skipped.\n\n```json title=\".mcp.json\"\n{\n  \"mcpServers\": {\n    \"paylod\": {\n      \"type\": \"http\",\n      \"url\": \"https://mcp.paylod.dev/mcp\"\n    }\n  }\n}\n```\n\n### Claude Desktop\n\nRemote servers are added in the UI, not in `claude_desktop_config.json` (that file is for local stdio servers). Go to **Settings → Connectors → Add custom connector**, paste the server URL, and click **Connect** to run the OAuth flow. Leave the advanced client-ID/secret fields blank — paylod supports Dynamic Client Registration, so your client registers itself.\n\n### Cursor\n\nAdd to `~/.cursor/mcp.json` (global) or `.cursor/mcp.json` (per-project). Cursor keys remote servers off `url`. Open **Settings → MCP** and click the login prompt to authorize.\n\n```json title=\"~/.cursor/mcp.json\"\n{\n  \"mcpServers\": {\n    \"paylod\": {\n      \"url\": \"https://mcp.paylod.dev/mcp\"\n    }\n  }\n}\n```\n\n### VS Code\n\nAdd to `.vscode/mcp.json`. Note the top-level key is `servers`, not `mcpServers`. VS Code implements the MCP authorization spec including DCR, so it prompts you to sign in on first use.\n\n```json title=\".vscode/mcp.json\"\n{\n  \"servers\": {\n    \"paylod\": {\n      \"type\": \"http\",\n      \"url\": \"https://mcp.paylod.dev/mcp\"\n    }\n  }\n}\n```\n\n### Windsurf\n\nAdd to `~/.codeium/windsurf/mcp_config.json`. Windsurf uses `serverUrl` for remote servers and supports OAuth on Streamable HTTP.\n\n```json title=\"~/.codeium/windsurf/mcp_config.json\"\n{\n  \"mcpServers\": {\n    \"paylod\": {\n      \"serverUrl\": \"https://mcp.paylod.dev/mcp\"\n    }\n  }\n}\n```\n\n### Codex CLI\n\nCodex supports remote Streamable-HTTP MCP servers with OAuth. Add the block below to `~/.codex/config.toml`, then run `codex mcp login paylod`.\n\n```toml title=\"~/.codex/config.toml\"\n[mcp_servers.paylod]\nurl = \"https://mcp.paylod.dev/mcp\"\n```\n\n### Fallback for stdio-only clients\n\nIf your client only speaks stdio, bridge it with `mcp-remote` — the standard stdio-to-HTTP proxy. It handles the OAuth flow (opening your browser, caching the token under `~/.mcp-auth`) and needs no flags against paylod.\n\n```json title=\"mcp-remote bridge\"\n{\n  \"mcpServers\": {\n    \"paylod\": {\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"mcp-remote\", \"https://mcp.paylod.dev/mcp\"]\n    }\n  }\n}\n```\n\nYou almost certainly do not need this — Claude Code, Claude Desktop, Cursor, VS Code, Windsurf and Codex all speak remote HTTP natively.\n\n## First thing to try\n\n`decode_mpesa_error` is pure and offline — it needs no scopes, no credentials, and touches no network. That makes it the ideal way to confirm the server is wired up before you grant anything. Ask your agent:\n\n```text title=\"Prompt\"\nWhat does M-Pesa error 2001 mean?\n```\n\nA good answer comes back with the cause (wrong PIN), the fix, and the message to show the customer — that is the tool, not the model guessing. Anyone who has stared at a bare `2001` in a Daraja response knows why this exists.\n\n## How the OAuth flow works\n\nYour client hits the server URL with no token and gets a `401` carrying a `WWW-Authenticate` header that points at the protected-resource metadata. From there it discovers the authorization server at `https://paylod.dev/oauth`, registers itself dynamically, and opens your browser. You sign in to paylod and land on a consent screen listing exactly which capabilities the agent is asking for — one scope per capability, each with its own checkbox.\n\n```http title=\"The 401 that starts it all\"\ncurl -i -X POST https://mcp.paylod.dev/mcp \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"tools/list\"}'\n\nHTTP/1.1 401 Unauthorized\nWWW-Authenticate: Bearer resource_metadata=\"https://mcp.paylod.dev/.well-known/oauth-protected-resource\"\n```\n\nThe three high-risk scopes — `payments.payout` (send money out), `credentials.write` (write your Daraja keys) and `keys.mint` (mint API keys) — are **unchecked by default**. You have to deliberately turn them on. Leave them off and the agent cannot call `payout`, `reversal`, `set_credentials` or `mint_key` at all: the server rejects the call on the scope, so it is not a prompt you have to trust the model to respect. Grant only `payments.simulate` and the agent can build against the sandbox without ever being able to touch real money.\n\nThe resulting access token is bound to this server as its audience, held by your client, and never pasted into the conversation. Your Daraja consumer key and secret are write-only — no tool reads them back, so an agent can store them for you but can never exfiltrate them.\n\n## Tools\n\nEvery call is authorized by the OAuth access token and gated on the scope shown next to it — a token without the scope cannot invoke the tool.\n\n### Management — set the integration up\n\n| Tool | Scope | What it does |\n| --- | --- | --- |\n| `create_app` | `paylod:apps.write` | First-run onboarding: create your organization and its first application (paybill or till). Returns the applicationId, your callback URL and a one-time API key. |\n| `create_application` | `paylod:apps.write` | Add another application to an organization you already have. |\n| `get_callback_url` | `paylod:apps.write` | Get the hosted M-Pesa callback URL for an application — the URL you paste into the Daraja portal. Treat it as a secret. |\n| `set_credentials` | `paylod:credentials.write` | Store or rotate the Daraja consumer key, secret, shortcode and passkey. Write-only — never read back. |\n| `mint_key` | `paylod:keys.mint` | Mint a paylod API key (`mp_test_…` / `mp_live_…`) for your own backend. Returned once. |\n| `configure_webhook` | `paylod:webhooks.write` | Create or update a webhook endpoint so results are pushed to your server, signed. |\n| `list_webhooks` | `paylod:webhooks.write` | List the webhook endpoints configured for an application. |\n| `list_applications` | `paylod:team.read` | List the applications you can access, with their environments and configuration state. |\n| `authenticate` | — (no scope) | Show who the token belongs to and which scopes were actually granted. |\n\n### Runtime — move and read money\n\n| Tool | Scope | What it does |\n| --- | --- | --- |\n| `request_stk_push` | `paylod:payments.collect` | Send an STK Push to a customer's handset. Accepts an idempotency key — one per payment attempt — so duplicate deliveries of that attempt collapse into a single charge. An interrupted call spends the key and returns `409` *indeterminate*: read `get_payment_status`, then retry with a **new** key. |\n| `get_payment_status` | `paylod:payments.read` | Look up one payment by id. If still pending, runs a live STK query and settles it on the spot. |\n| `generate_qr` | `paylod:payments.collect` | Generate an M-Pesa QR code (base64 PNG). Stateless — no money moves. |\n| `register_c2b` | `paylod:payments.collect` | Register C2B validation/confirmation URLs so paybill payments made outside your app reach you. |\n| `get_account_balance` | `paylod:payments.read` | Query the M-Pesa account balance. Asynchronous — returns a queryId; the result arrives on your callback. |\n| `get_transaction_status` | `paylod:payments.read` | Query the status of an M-Pesa transaction by receipt. Asynchronous, same callback pattern. |\n| `payout` | `paylod:payments.payout` | Send money out (B2C). Irreversible on a live application. Requires the opt-in money-out scope. |\n| `reversal` | `paylod:payments.payout` | Reverse or refund a transaction. Requires the opt-in money-out scope. |\n\n### Sandbox & offline\n\n| Tool | Scope | What it does |\n| --- | --- | --- |\n| `simulate_test_payment` | `paylod:payments.simulate` | Create a simulated collection in sandbox — no handset, no real money. |\n| `simulate_outcome` | `paylod:payments.simulate` | Force a simulated payment to succeed or fail, so the agent can exercise your failure paths. |\n| `decode_mpesa_error` | — (no scope) | Decode any Safaricom result code or Daraja error into cause, fix, and a customer-facing message. Pure and offline. |\n\nThe same codes `decode_mpesa_error` returns are browsable in the [error reference](/docs/errors).\n\n## Set an integration up with the tools, in order\n\nThe tools are for **setting the integration up and inspecting it**. They are not what your application calls at runtime — that is the [Node SDK](/docs/sdk). Call them in this order.\n\n1. **Do I already have an app?** → `list_applications`. Anything back means you are onboarded.\n2. **Create the application.** A brand-new user calls `create_app` (it creates the organization *and* its first application). Someone who already has an organization calls `create_application` instead — `create_app` returns a `409 already onboarded` for them. Picking the wrong one is the single most common first-call mistake.\n3. **Store the Daraja credentials** → `set_credentials`. It needs `product` — the *kind of shortcode*, `paybill` or `till`, not the Daraja product name. For a **till** you must also pass `partyB` (the till number that receives the funds), and it has to differ from `shortcode`, or Daraja rejects the push with `2002`. Credentials are encrypted at rest and write-only: no tool reads them back.\n4. **Get the callback URL** → `get_callback_url`, then **paste it into the Safaricom Daraja portal** as the CallbackURL / ResultURL / ValidationURL / ConfirmationURL. This is the one step no agent can do for you — Safaricom has no API for it — and skipping it means payments never resolve. Treat the URL as a secret: the random `cbk_…` token in the path is what authenticates Safaricom's callback.\n5. **Mint an API key** → `mint_key` (`mp_test_…` sandbox, `mp_live_…` production). Shown once. This is the key your *server* puts in `PAYLOD_API_KEY` — the MCP server itself never uses one.\n6. **Optionally register a webhook** → `configure_webhook`. Skip it and poll instead; every payment is recorded either way.\n7. **Then write the application code with the SDK** — `npm install @paylod/node`, `new Paylod(process.env.PAYLOD_API_KEY!)`, `await paylod.collectAndWait({ amount, phone })`. Render `outcome.message`; offer a retry only when `outcome.retryable`. Do not write a fetch wrapper, a polling loop, or a callback handler: see [Quickstart](/docs/quickstart) and the [Node SDK](/docs/sdk).\n8. **Exercise the failure paths** → `simulate_test_payment` + `simulate_outcome` drive success, cancellation, a wrong PIN or a timeout through the real settlement path, without a handset.\n\n`request_stk_push` and `get_payment_status` exist so an agent can *test* the loop end to end (and so a human can say \"charge this number 10 bob\"). Shipping them as your runtime integration means re-implementing the SDK badly.\n\n> [!warn] `4999` is a **pending** payment, not a failed one — the customer has not entered their PIN yet. It is not retryable: a second `request_stk_push` sends a second prompt and can double-charge. Keep polling `get_payment_status`.\n\n## What an agent can actually do\n\nThe point of the management tools is that an agent can take you from nothing to a working, live M-Pesa integration without you opening a dashboard. Connect the server, then ask:\n\n```text title=\"Prompt\"\nSet up M-Pesa for my shop \"Acme Ltd\". Here are my Daraja sandbox\ncredentials: consumer key …, secret …, shortcode 174379, passkey ….\nCreate the app, store them, mint me a test API key, give me my\ncallback URL, then send a 10 KES STK push to 0712345678 and tell me\nwhen it settles.\n```\n\nThe agent calls `create_app` → `set_credentials` → `mint_key` → `get_callback_url` → `request_stk_push`, then polls `get_payment_status` until the payment settles. If it fails, it calls `decode_mpesa_error` and tells you in plain English that the customer cancelled the prompt (`1032`) or the handset timed out (`1037`). You hand it a phone number; it hands you back an API key and a receipt.\n\nThe one step the agent cannot do for you is paste that callback URL into the Safaricom Daraja portal — Safaricom has no API for it. Everything else, including the webhook that pushes results to your own server (`configure_webhook`), it can do.\n\n> Everything the agent does over MCP is the same platform your backend talks to over HTTP at `/functions/v1` — see the [API reference](/docs/api). MCP is a second front door, not a separate system.",
  },
  {
    id: "cli",
    title: "The paylod CLI (installs as @paylod/cli)",
    keywords: ["cli","terminal","command","shell","npx","forward","tunnel","ngrok"],
    aliases: [],
    content: "# paylod CLI: M-Pesa from your terminal\n\nSend a real M-Pesa STK Push from your terminal, decode any Daraja result code offline, and forward live webhooks to localhost without ngrok.\n\nThe `paylod` CLI sends a real M-Pesa STK Push from your terminal, decodes Daraja result codes offline, and forwards live webhooks to `localhost` without ngrok. It talks to the same API documented in the [API reference](/docs/api).\n\n```bash title=\"Install\"\nnpm install -g @paylod/cli\n```\n\nThe package is `@paylod/cli`; the command it installs is `paylod`.\n\nOr run it without installing:\n\n```bash title=\"npx\"\nnpx @paylod/cli errors 1032\n```\n\nNode 20 or newer.\n\n## Set up\n\n```bash title=\"Terminal\"\npaylod init\n```\n\n`init` signs you in (it opens your browser), lets you pick an application, and mints an API key for this machine. Credentials go into your OS keychain, or a `0600` file under `~/.config/paylod` if there is no keychain available.\n\nIf you only want to sign in:\n\n```bash title=\"Terminal\"\npaylod login\npaylod whoami       # who am I, and where is the token stored?\n```\n\nFor CI, skip the login entirely and set `PAYLOD_API_KEY` in the environment. Every data-plane command (`collect`, `status`, `listen`) will use it.\n\n## Take a payment\n\n```bash title=\"Terminal\"\npaylod collect --phone 254712345678 --amount 100\n```\n\nThe customer's phone rings. The CLI polls until the payment settles and prints the outcome — an M-Pesa receipt on success, or a decoded reason on failure.\n\n| Flag | What it does |\n| --- | --- |\n| `-p, --phone <msisdn>` | Customer's number. `0712…`, `254712…`, `+254712…` all work. |\n| `-a, --amount <kes>` | Whole shillings. M-Pesa rejects decimals. |\n| `-r, --ref <reference>` | Your own correlation id (invoice/order number), returned on the result. ≤ 12 chars. Shown to the payer only on a Paybill; a Till never displays it. |\n| `-d, --description <text>` | Payment description on the STK prompt. ≤ 64 chars. |\n| `--idempotency-key <key>` | Names one payment attempt. Duplicates of that attempt collapse into a single charge. A new charge needs a new key. |\n| `--no-wait` | Fire the push and exit immediately, without polling. |\n| `--timeout <secs>` | How long to wait for settlement. Default 120. |\n| `--env <sandbox\\|production>` | Which M-Pesa environment to charge on. |\n| `--api-key <key>` | Use this key instead of `PAYLOD_API_KEY` or your stored profile. |\n\nLook a payment up later, or watch it settle:\n\n```bash title=\"Terminal\"\npaylod status e69e5c00-8a01-44ed-b003-48e2e86a7c9e\npaylod status e69e5c00-8a01-44ed-b003-48e2e86a7c9e --watch\n```\n\n`status` decodes the failure reason for you — you never have to look `2001` up in a PDF.\n\n## Decode a result code\n\nOffline. No login, no network, no API key.\n\n```bash title=\"Terminal\"\npaylod errors 1032          # why did that payment fail?\npaylod errors --list        # the whole catalogue\npaylod errors 2001 --json   # machine-readable, for your error handler\n```\n\nThis is the fastest way to answer \"what does `2001` mean\" mid-incident. The same catalogue is browsable in the [error reference](/docs/errors).\n\n> `4999` and `500.001.1001` decode as **pending**, not failed — they mean the customer has not entered their PIN yet. The payment is still live and still payable. Do not show a customer a failure on those codes.\n\n## Webhooks on localhost, without ngrok\n\n```bash title=\"Terminal\"\npaylod listen --forward http://localhost:3000/webhook\n```\n\n`listen` streams your application's live webhook events and re-POSTs each one to your local server, with the signature intact — so the code you are writing verifies a real signature against a real payload. No tunnel, no public URL, no ngrok.\n\n| Flag | What it does |\n| --- | --- |\n| `-f, --forward <url>` | POST each event to this local URL. |\n| `-e, --events <types...>` | Only forward these types (`payment.success`, `payment.failed`). |\n| `--print-json` | Print the full JSON payload of every event. |\n| `--secret <whsec>` | Signing secret. Otherwise taken from your profile, or fetched. |\n| `--tolerance <secs>` | Signature freshness window. Default 300; `0` disables the check. |\n| `--skip-verify` | Do not verify the HMAC. Debugging only. |\n| `--port <port>` | Local receiver port. Default 4242. |\n\nRoll (or fetch) the signing secret with:\n\n```bash title=\"Terminal\"\npaylod webhooks secret\n```\n\n## Test without spending money\n\n```bash title=\"Terminal\"\npaylod simulate --outcome user_cancelled\npaylod simulate --interactive\n```\n\n`simulate` creates a payment in the paylod sandbox and forces an outcome — no Daraja call, no handset, no money. It is how you exercise your failure paths in CI.\n\n| Outcome | Result code |\n| --- | --- |\n| `approve` | `0` |\n| `wrong_pin` | `2001` |\n| `insufficient_funds` | `1` |\n| `user_cancelled` | `1032` |\n| `timeout` | `1037` |\n\n## Manage the integration\n\nThese commands do everything the dashboard does.\n\n| Command | What it manages |\n| --- | --- |\n| `paylod apps list \\| create \\| use \\| rename \\| delete` | Applications — one per till or paybill. |\n| `paylod creds` | Your Daraja consumer key, secret, shortcode and passkey. Write-only. |\n| `paylod keys mint \\| list \\| revoke \\| use` | paylod API keys (`mp_test_…` / `mp_live_…`). |\n| `paylod webhooks list \\| add \\| toggle \\| delete \\| secret` | Webhook endpoints. |\n| `paylod payments` | Browse your M-Pesa payments. |\n| `paylod orgs` | Organizations. |\n\n## Scripting\n\nEvery command takes a global `--json` flag and prints machine-readable output.\n\n```bash title=\"Terminal\"\npaylod collect -p 254712345678 -a 100 --json | jq -r '.mpesaReceipt'\n```\n\n`paylod collect` exits `5` if the payment is still pending when the timeout expires — which is not a failure, just an unanswered prompt. A non-zero exit on a settled failure carries the decoded reason in the JSON.\n\n## Environment variables\n\n| Variable | What it does |\n| --- | --- |\n| `PAYLOD_API_KEY` | Merchant API key. Skips the config file entirely — this is how you run paylod in CI. |\n| `PAYLOD_WEBHOOK_SECRET` | Signing secret for `paylod listen`. |\n| `PAYLOD_API_BASE` | Override the API base. Defaults to `https://paylod.dev/functions/v1`. |\n| `PAYLOD_CONFIG_DIR` | Where the CLI stores config. Default `~/.config/paylod`. |\n| `PAYLOD_NO_KEYCHAIN=1` | Never use the OS keychain; use the `0600` file instead. Useful on headless Linux and in containers. |\n| `NO_COLOR` | Disable colour. |\n\n> [!warn] `mp_live_…` keys move real money. Keep them out of shell history and out of committed `.env` files.\n\n## Next\n\n- Building an app rather than a script? Use the [Node SDK](/docs/sdk).\n- Wiring an AI agent? The [MCP server](/docs/mcp) exposes the same capabilities as tools.\n- The HTTP endpoints underneath: [API reference](/docs/api).",
  },
  {
    id: "endpoints",
    title: "The full REST surface — payouts, reversals, QR, C2B, queries",
    keywords: ["endpoint","endpoints","rest","http","api","reference","payout","b2c","reversal","refund","qr","c2b","balance","transaction"],
    aliases: ["api","payouts"],
    content: "# M-Pesa API overview: base URL, auth, conventions\n\nThe base URL, the auth header, the request/response conventions, and an index of every paylod endpoint.\n\n## Base URL\n\n```text title=\"Base URL\"\nhttps://paylod.dev/functions/v1\n```\n\n## Authentication\n\nEvery endpoint takes a bearer API key. The key determines the application and the environment, so no request ever carries a shortcode or a Daraja credential.\n\n```http title=\"Header\"\nAuthorization: Bearer mp_live_YOUR_API_KEY\n```\n\nSee [Authentication](/docs/authentication) for keys, environments, idempotency and rate limits.\n\n## Endpoints\n\n| Method | Path | What it does |\n| --- | --- | --- |\n| `POST` | [`/collect`](/docs/api/payments) | Send an STK Push to a customer's handset |\n| `GET` | [`/status/:id`](/docs/api/payments) | Read a payment's current state |\n| `POST` | [`/payout`](/docs/api/payouts) | Send money out to a customer (B2C) |\n| `POST` | [`/reversal`](/docs/api/payouts) | Reverse or refund a completed transaction |\n| `POST` | [`/qr-generate`](/docs/api/other) | Generate a dynamic Lipa na M-Pesa QR code |\n| `POST` | [`/c2b-register`](/docs/api/other) | Register offline C2B confirmation + validation URLs |\n| `POST` | [`/transaction-status`](/docs/api/other) | Query a historical transaction by receipt |\n| `POST` | [`/account-balance`](/docs/api/other) | Query your shortcode's M-Pesa float |\n\n## Synchronous vs asynchronous\n\nM-Pesa is an asynchronous network, and paylod does not pretend otherwise.\n\n- **`200 OK`** — the answer is in the response body. Only `GET /status/:id` and `POST /qr-generate` and `POST /c2b-register` work this way.\n- **`202 Accepted`** — the request was accepted and handed to M-Pesa. The real outcome arrives later, on your [webhook](/docs/api/webhooks) (or, for `/collect`, by polling `GET /status/:id`). The body carries an id you can correlate on.\n\nAsynchronous endpoints that move money or read the ledger (`/payout`, `/reversal`, `/transaction-status`, `/account-balance`) also require the application's **Initiator Name** and **Initiator Password** to be configured. See [Go live](/docs/guides/go-live).\n\n## Errors\n\n| Status | Meaning |\n| --- | --- |\n| `400` | Malformed JSON, or Daraja credentials not configured for this app and environment |\n| `401` | Missing or invalid API key |\n| `404` | Not found, or belongs to another application |\n| `409` | Idempotency key reused with a different body, still in flight, or spent by an interrupted provider call |\n| `422` | Body failed validation |\n| `429` | Rate limited |\n| `502` | Daraja rejected the request — the upstream error is passed through |\n\nM-Pesa's own result codes (`1032`, `1037`, `2001`, …) are decoded in the [error reference](/docs/errors).\n\n---\n\n# Payouts API: M-Pesa B2C payouts and reversals\n\nPOST /payout to send money out (B2C) and POST /reversal to refund a completed transaction.\n\nBoth endpoints move money **out** of your shortcode. Both are asynchronous: the HTTP call acknowledges with `202`, and the terminal result is delivered to your [webhook](/docs/api/webhooks). Both require the application's **Initiator Name** and **Initiator Password** to be configured.\n\n> [!warn] On a production application these are irreversible. Guard them behind your own authorisation, and never expose them to a client.\n\n## POST /payout\n\nB2C payout — send money from your shortcode to a customer.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/payout\n```\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `amount` | integer | Required | Whole KES to send, 1–150000. |\n| `phone` | string | Required | Recipient MSISDN (`0712…`, `254712…`, `+254712…`). Normalised for you. |\n| `commandId` | string | Optional | `SalaryPayment`, `BusinessPayment`, or `PromotionPayment`. Defaults to `BusinessPayment`. |\n| `remarks` | string | Optional | Free-text remarks. Up to 100 chars. |\n| `occasion` | string | Optional | Optional occasion label. Up to 100 chars. |\n\n### Example\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/payout \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"amount\": 2500,\n    \"phone\": \"254712345678\",\n    \"commandId\": \"BusinessPayment\",\n    \"remarks\": \"Payout for July\"\n  }'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"disbursementId\": \"d3f1a2b3-…\",\n  \"conversationId\": \"AG_20260707_0000c1d2e3f4\",\n  \"status\": \"pending\"\n}\n```\n\nThe terminal result is pushed to your webhook as an HMAC-signed `payout.result` event. Correlate it via `disbursementId` or `conversationId`.\n\n## POST /reversal\n\nReverse or refund a completed transaction back to the payer.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/reversal\n```\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `transactionId` | string | Required | The M-Pesa receipt to reverse. |\n| `amount` | integer | Required | Whole KES, 1–150000. Must match the original transaction amount. |\n| `remarks` | string | Optional | Free-text remarks. Up to 100 chars. |\n| `occasion` | string | Optional | Optional occasion label. Up to 100 chars. |\n\n### Example\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/reversal \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"transactionId\": \"UG1F3A1U7J\",\n    \"amount\": 2500,\n    \"remarks\": \"Customer refund\"\n  }'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"disbursementId\": \"f7e6d5c4-…\",\n  \"conversationId\": \"AG_20260707_0000a9b8c7d6\",\n  \"status\": \"pending\"\n}\n```\n\nThe result is pushed to your webhook as an HMAC-signed `refund.result` event.\n\n---\n\n# QR codes, C2B registration and M-Pesa queries\n\nGenerate an M-Pesa QR code, register offline C2B URLs, and query transaction status or account balance.\n\nThe rest of the Daraja surface, on the same hosted, credential-abstracted model: the same base URL, the same bearer key, no shortcode on the wire.\n\n## POST /qr-generate\n\nGenerate a dynamic Lipa na M-Pesa QR code for a specific amount. **Synchronous** — returns a base64-encoded PNG you render for the customer to scan. Stateless: no ledger row is created here. When the customer pays, the payment arrives later via your C2B confirmation URL (source `c2b`) and a signed webhook, exactly like any other offline payment.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/qr-generate\n```\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `amount` | number | Required | Amount to encode in the QR. Must be greater than 0. |\n| `refNo` | string | Optional | Reference shown to the customer. Up to 64 chars. |\n| `merchantName` | string | Optional | Merchant / display name. Up to 64 chars. |\n| `trxCode` | string | Optional | `BG` Buy Goods (till), `PB` Pay Bill, `WA` Withdraw at Agent, `SB` Send to Business, `SM` Send Money. Defaults to `BG`. |\n| `cpi` | string | Optional | Credit Party Identifier (till / paybill). Defaults to the tenant shortcode. |\n| `size` | string | Optional | Requested QR image size in pixels. |\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/qr-generate \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"amount\": 500,\n    \"refNo\": \"INV-2041\",\n    \"merchantName\": \"Acme Ltd\",\n    \"trxCode\": \"BG\"\n  }'\n```\n\n```json title=\"Response · 200 OK\"\n{\n  \"qrBase64\": \"iVBORw0KGgoAAAANSUhEUgAAAP…\"\n}\n```\n\n## POST /c2b-register\n\nRegister the offline C2B (Lipa na M-Pesa paybill / till) confirmation and validation URLs for your shortcode. **Idempotent** — safe to re-run. No body is required: the shortcode and callback URL are derived server-side from your API key.\n\nAfter registering, offline customer payments to the till or paybill are delivered to paylod as C2B confirmations, recorded as payments (source `c2b`), and a signed webhook fires — the **same envelope** as STK payments.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/c2b-register\n```\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/c2b-register \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{}'\n```\n\n```json title=\"Response · 200 OK\"\n{\n  \"registered\": true,\n  \"shortcode\": \"600123\",\n  \"confirmationUrl\": \"https://paylod.dev/functions/v1/c2b-hook?app=…\",\n  \"validationUrl\": \"https://paylod.dev/functions/v1/c2b-hook?app=…&validate=1\",\n  \"responseCode\": \"0\",\n  \"responseDescription\": \"Success\"\n}\n```\n\n`responseCode` and `responseDescription` echo Daraja's registration acknowledgement, and may be `null` if the shortcode was already registered.\n\n## POST /transaction-status\n\nQuery the status of any historical M-Pesa transaction by its receipt. **Asynchronous** — the HTTP call acknowledges with `202`; the real answer arrives on a signed webhook of type `admin.transaction_status`. Requires the application's Initiator Name and Initiator Password.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/transaction-status\n```\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `transactionId` | string | Required | The M-Pesa receipt / TransactionID to look up. |\n| `identifierType` | string | Optional | `1` MSISDN, `2` till, `4` shortcode. Defaults to `4`. |\n| `remarks` | string | Optional | Free-text remarks for the query. |\n| `occasion` | string | Optional | Optional occasion label. |\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/transaction-status \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"transactionId\": \"UG1F3A1U7J\",\n    \"identifierType\": \"4\"\n  }'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"queryId\": \"b1e2c3d4-…\",\n  \"conversationId\": \"AG_20260707_00001a2b3c4d\"\n}\n```\n\nCorrelate the webhook via `queryId` or `conversationId`.\n\n## POST /account-balance\n\nQuery your shortcode's M-Pesa float. **Asynchronous** — acknowledges with `202`; the answer arrives on a signed webhook of type `admin.account_balance`. Requires the application's Initiator Name and Initiator Password.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/account-balance\n```\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `identifierType` | string | Optional | `1` MSISDN, `2` till, `4` shortcode. Defaults to `4`. |\n| `remarks` | string | Optional | Free-text remarks for the query. |\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/account-balance \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{}'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"queryId\": \"9a8b7c6d-…\",\n  \"conversationId\": \"AG_20260707_00005e6f7a8b\"\n}\n```",
  },
];

/** Every page, concatenated — the same document served at https://paylod.dev/llms-full.txt. */
export const DOCS_FULL: string = "# paylod — complete documentation for AI agents (llms-full.txt)\n\n> paylod is M-Pesa (Safaricom Daraja) without the backend. Your server calls one SDK method;\n> paylod holds the Daraja credentials, hosts the callback, decodes every result code and signs\n> your webhooks. Money settles straight to your own till or paybill — paylod never takes custody\n> and never takes a cut.\n\nThis file is the whole of https://paylod.dev/docs in one request. It is GENERATED from the docs\nmarkdown at build time, so it cannot disagree with the website. The index is at\nhttps://paylod.dev/llms.txt; each page is also served as raw markdown at `<page>.md`.\n\n## Read this first (the nine things agents get wrong)\n\n1. **Use the SDK. Do not hand-roll HTTP.** `npm install @paylod/node`, then\n   `new Paylod(process.env.PAYLOD_API_KEY!)` and `await paylod.collectAndWait({ amount, phone })`.\n   Do not write a fetch wrapper, a polling loop, a callback endpoint, a Daraja OAuth token fetch,\n   or a base64 `shortcode+passkey+timestamp` password. All of that already exists. Only reach for\n   the raw HTTP API if you are not on a JavaScript runtime.\n2. **Render `result.message`.** Every settling call returns a `PaymentOutcome`:\n   `{ status, message, retryable, paid, receipt, code, detail, payment }`. `message` is a\n   customer-facing sentence, already decoded. Show it. Offer a retry **only** when\n   `retryable` is true. An integrator should never branch on a Daraja result code to decide what\n   a human sees.\n3. **`4999` is PENDING, not failed.** It means the STK prompt is still on the handset and the\n   customer has not entered their PIN. It is `retryable: false` — re-collecting fires a SECOND STK\n   prompt and can DOUBLE-CHARGE a real customer. Keep polling. (`500.001.1001` is the same thing.)\n4. **A timeout throws.** `wait()` / `collectAndWait()` throw `PaylodTimeoutError` rather than\n   returning `status: \"failed\"`, because an indeterminate payment is not a failed payment — the\n   customer may still be typing their PIN. Leave the order open; reconcile with `check()`.\n5. **The API key is server-side only.** It can move money (`/payout`, `/reversal`) and the amount\n   is caller-supplied, so a key in client-side JavaScript lets a payer set their own price. Never\n   put it in a browser, a mobile app, or a `NEXT_PUBLIC_*` variable. The browser calls YOUR server;\n   your server calls paylod.\n6. **Webhooks are optional.** Polling is a first-class, complete integration. Add a signed webhook\n   when your *server* needs to hear about a payment it is not actively waiting on.\n7. **`1032` (cancelled), `1037` (timeout) and `2001` (wrong PIN) are normal outcomes, not bugs.**\n   `2001`'s raw Daraja text says \"The initiator information is invalid\", which is misleading: it is\n   a customer-entered wrong PIN, not a credentials problem.\n8. **`accountReference` is a correlation id, not a message to the customer.** paylod returns it\n   to you as `accountRef` on the status read and on the webhook, so use it to tie a payment to\n   your order. Safaricom shows it to the payer only on a **Paybill** (`CustomerPayBillOnline`),\n   where it is the account number; on a **Till / Buy Goods** shortcode it is not displayed at all —\n   the payer sees the business name and the amount. `description` is what reaches the STK prompt.\n9. **Packages:** the SDK is `@paylod/node` (v0.2.0). The CLI installs as **`@paylod/cli`** — npm\n   rejected the unscoped name as too similar to `payload` — while the command it installs is still\n   `paylod`. The MCP server is remote: `https://mcp.paylod.dev/mcp`, OAuth 2.1, nothing to install.\n\n---\n\n## paylod: M-Pesa payments without a backend\n\nSource: https://paylod.dev/docs\n\npaylod is M-Pesa (Safaricom Daraja) without the backend: one HTTP call sends the STK Push, we host the callback, sign your webhooks and decode every result code.\n\n## What paylod is\n\npaylod is a hosted layer over Safaricom's **Daraja** API. You keep your own till or paybill and your own Daraja credentials — M-Pesa still settles **straight to you**, and paylod never touches the money or takes a cut.\n\nWhat you skip is the part everyone hates: hosting a public HTTPS callback, refreshing OAuth tokens, retrying failed pushes, deciphering `resultCode: 1037`, and building a webhook signing scheme.\n\n```ts title=\"The whole integration\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n\nconst outcome = await paylod.collectAndWait({ amount: 100, phone: \"0712345678\" });\n\nif (outcome.paid) console.log(\"Paid:\", outcome.receipt);\nelse console.log(outcome.message);   // already decoded, safe to show a customer\n```\n\nThat is it. `npm install @paylod/node`, one API key, and paylod does the rest. Not on Node? The same call works over plain HTTP; see the [API reference](/docs/api).\n\n## How it works\n\n1. You create an **application** (a till or a paybill) and paste your Daraja consumer key, secret, shortcode and passkey into it. They are encrypted at rest and never leave the server.\n2. paylod gives you a **hosted callback URL**. You paste that once into the Safaricom Daraja portal. That is the only Safaricom paperwork you do.\n3. Your backend calls `collect()` with an amount and a phone number, using a paylod **API key**. The customer's handset lights up with an M-Pesa PIN prompt.\n4. M-Pesa posts the result to paylod's callback. paylod normalises it, decodes the result code, records the payment, and either **pushes you a signed webhook** or lets you **poll** for the result. Your choice.\n\n> The one thing an integration must never do is trust the client with the amount. Your server owns the amount; see [Secure integration](/docs/guides/security).\n\n## Where to start\n\n| If you want to… | Go to |\n| --- | --- |\n| Send your first STK Push right now | [Quickstart](/docs/quickstart) |\n| Use the Node/TypeScript client | [Node SDK](/docs/sdk) |\n| Send a payment from your terminal | [CLI](/docs/cli) |\n| Understand keys, environments and idempotency | [Authentication](/docs/authentication) |\n| Charge a customer properly, in production | [Accept a payment](/docs/guides/accept-a-payment) |\n| Know when the money actually landed | [Handle the result](/docs/guides/handle-the-result) |\n| Look up an endpoint's parameters | [API reference](/docs/api) |\n| Work out what `1032` means | [Error codes](/docs/errors) |\n| Let an AI agent do all of the above | [MCP server](/docs/mcp) |\n\n## What you can build\n\n- **Checkout** — collect a payment for an order, fulfil it when the webhook confirms the settled amount.\n- **Payouts** — send money out to customers, riders or staff (B2C), and reverse a transaction when you need to refund.\n- **Offline payments** — register C2B so counter payments to your till reach your systems, or hand the customer a dynamic QR code.\n- **Operations** — query a historical transaction by receipt, or read your shortcode's float.\n\nEverything above is one SDK call (or one HTTP request), one API key, and no callback hosting on your side.\n\n---\n\n## Quickstart: your first M-Pesa STK Push\n\nSource: https://paylod.dev/docs/quickstart\n\nYour first M-Pesa STK Push in five minutes — create an application, add your Daraja credentials, mint a key, and collect a payment.\n\nYou need a Safaricom Daraja account (the [sandbox](https://developer.safaricom.co.ke) is free and instant) and about five minutes. One `npm install`, and nothing to deploy — no callback server, no tunnel.\n\n## 1. Create an application\n\nIn the paylod dashboard, create an **application** — one per till or paybill. Give it a name; you can add both a Sandbox and a Production environment to the same application.\n\n## 2. Add your Daraja credentials\n\nOn the application's **Credentials** tab, paste the four values Daraja gives you:\n\n| Field | Where it comes from |\n| --- | --- |\n| Consumer key | Daraja portal → your app |\n| Consumer secret | Daraja portal → your app |\n| Shortcode | Your till / paybill number (`174379` in sandbox) |\n| Passkey | Daraja portal → Lipa na M-Pesa Online |\n\nThey are encrypted at rest and **write-only** — no API, tool, or dashboard screen ever reads them back.\n\n## 3. Paste the callback URL into Daraja\n\npaylod shows you a hosted callback URL on the application's **Endpoints & Webhooks** tab. Copy it into the Daraja portal as your Lipa na M-Pesa Online callback URL.\n\nThat is the entire \"host a public HTTPS endpoint\" problem, solved. You never write callback-handling code.\n\n## 4. Mint an API key\n\nOn the **API Keys** tab, create a key. Keys are environment-scoped:\n\n- `mp_test_…` → Sandbox\n- `mp_live_…` → Production\n\nThe key is shown **once**. It identifies which application and environment a request runs against, so you never send a shortcode or a Daraja credential over the wire.\n\n> [!warn] The key is a server-side secret. Never ship it in a browser, a mobile app, or anything a payer can open.\n\n## 5. Install the SDK\n\n```bash title=\"Install\"\nnpm install @paylod/node\n```\n\nPut the key in your environment. The client picks it up on its own:\n\n```bash title=\".env\"\nPAYLOD_API_KEY=mp_test_YOUR_API_KEY\n```\n\n## 6. Send your first STK Push\n\n```ts title=\"collect.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n\nconst outcome = await paylod.collectAndWait({\n  amount: 10,\n  phone: \"0712345678\",\n  idempotencyKey: attempt.id,    // one key per payment attempt. A double-click cannot charge twice.\n  accountReference: \"INV-2041\",\n  description: \"Order #2041\",\n});\n\nif (outcome.paid) {\n  console.log(\"Paid:\", outcome.receipt);   // \"UG1F3A1U7J\"\n} else {\n  console.log(outcome.message);            // already decoded, safe to show a customer\n}\n```\n\nThat is the whole integration. One argument to the constructor, one call.\n\n`collectAndWait()` sends the STK Push, then polls until the payment settles. The customer's phone rings, they enter their PIN, and you get an answer back. No callback server, no OAuth token to refresh, no result-code table to write.\n\n> [!warn] **Pass `idempotencyKey`, and mint one per payment attempt.** Duplicates of that one attempt — a double-clicked Pay button, a refreshed tab, a retried request — collapse into **one** prompt and **one** charge, even if they arrive at the same instant. Leave it out and every call is a new charge: two clicks, two prompts, two debits. Do **not** key on the order or product id — that replays an *old* payment instead of making a new one, and a retry after a wrong PIN is a genuinely new charge that needs a **new** key. Omit it and the SDK warns you. [Full rules](/docs/sdk#idempotency).\n\nThe answer is a `PaymentOutcome`: a `message` you can show a customer, already decoded, and a `retryable` flag that tells you whether charging again is safe. A wrong PIN is a business outcome, not a crash, so it comes back as data rather than a thrown error.\n\nPhone numbers are normalised for you, so `0712345678`, `+254712345678` and `254712345678` all work.\n\n> [!warn] A **timeout throws** `PaylodTimeoutError` rather than reporting `status: \"failed\"`. An unanswered prompt is not a failed payment: the customer may still be typing their PIN. Leave the order open. See [Handle the result](/docs/guides/handle-the-result).\n\nYou do not need a webhook for this to work. Polling is a first-class path, and `collectAndWait()` does it for you. Add a signed webhook when you want your server told about payments it is not actively waiting on.\n\n## Not on Node?\n\nThe SDK is a wrapper over a plain HTTP API — every language can call it directly:\n\n```bash title=\"The same call, over HTTP\"\ncurl -X POST https://paylod.dev/functions/v1/collect \\\n  -H \"Authorization: Bearer mp_test_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Idempotency-Key: attempt_9f3c1a7e\" \\\n  -d '{ \"amount\": 10, \"phone\": \"254712345678\" }'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"paymentId\": \"e69e5c00-8a01-44ed-b003-48e2e86a7c9e\",\n  \"status\": \"pending\",\n  \"checkoutRequestId\": \"ws_CO_010720261905029287161380\"\n}\n```\n\nThen poll `GET /functions/v1/status/{paymentId}` until it settles. Going this route means doing yourself what the SDK does for you: the polling loop, the idempotency key, and decoding the result code. The full surface is in the [API reference](/docs/api).\n\n## Next\n\n- Every method, option and error class: [Node SDK](/docs/sdk).\n- Prefer the terminal? The [CLI](/docs/cli) sends the same STK Push in one command.\n- Prefer push over poll? [Handle the result](/docs/guides/handle-the-result) covers signed webhooks.\n- Exercise the failure paths first: [Test in sandbox](/docs/guides/test-in-sandbox).\n- Ready for real money? [Go live](/docs/guides/go-live).\n\n---\n\n## Authenticating with the paylod M-Pesa API\n\nSource: https://paylod.dev/docs/authentication\n\nAPI keys, environments, the base URL, idempotency and rate limits — everything every paylod request has in common.\n\n## Base URL\n\nEvery endpoint lives under `/functions/v1`.\n\n```text title=\"Base URL\"\nhttps://paylod.dev/functions/v1\n```\n\n## API keys\n\nAuthenticate every request with a paylod API key as a bearer token.\n\n```http title=\"Header\"\nAuthorization: Bearer mp_live_YOUR_API_KEY\n```\n\nCreate keys on an application's **API Keys** tab. A key is shown once, and it encodes two things:\n\n- **Which application** the request runs against — so you never send a shortcode.\n- **Which environment** it runs in — so you never send a `?sandbox=true` flag.\n\n| Prefix | Environment | Money |\n| --- | --- | --- |\n| `mp_test_…` | Sandbox | Simulated. Nothing settles. |\n| `mp_live_…` | Production | Real. Settles to your till. |\n\nYour Daraja consumer key, secret and passkey stay in encrypted storage server-side. They never travel over the wire, and no endpoint reads them back.\n\n> [!warn] Keys are server-side secrets. A key embedded in a browser or a mobile app is fully compromised — treat it as public and rotate it. See [Secure integration](/docs/guides/security).\n\n## Idempotency\n\n`POST /collect` accepts an `Idempotency-Key` header (or an `idempotencyKey` field in the body). The key names **one payment attempt**. Send it, and duplicate deliveries of that attempt — a double-click, a refreshed tab, a redelivered job, an internal network retry — collapse into a single payment and a single STK Push.\n\n```bash title=\"Idempotent collect\"\ncurl -X POST https://paylod.dev/functions/v1/collect \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Idempotency-Key: att_9f2c1b40\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{ \"amount\": 10, \"phone\": \"254712345678\" }'\n```\n\n**Mint one key per attempt, not per order and not per product.** paylod reserves the key *before* calling Daraja, so ten simultaneous requests carrying the same key produce exactly one payment and one push — every one of them comes back with the same `paymentId`. But that also means a key is spent once it has been used: keying on an order id replays the failed first attempt when you retry the order, and keying on a product id would replay the first-ever payment for that product to every subsequent customer. A retry after a wrong PIN is a **new charge** and needs a **new key**.\n\nThree ways a reused key is rejected, all `409`:\n\n| Reuse | Response |\n| --- | --- |\n| Same key, **different** body | `409` — two different charges collided on one key. A bug on your side. |\n| Same key, first request still in flight | `409` + `Retry-After`. The winner is talking to Daraja; wait and read the result. |\n| Same key, a previous attempt was **interrupted mid-flight against Daraja** | `409` **indeterminate** — the money may or may not have moved, and we will not repeat the call to find out. |\n\n> [!warn] **The indeterminate `409` is not a retryable error.** A timeout is not evidence that the money did not move, so paylod refuses to silently re-dispatch — for money, at-most-once beats at-least-once. **Do not retry with the same key**: read the payment status (`GET /status/:id` or your webhook), and only then decide. If it settled, you are done; if nothing happened, start a new attempt with a **new** key.\n\n## Rate limits\n\n`POST /collect` is rate-limited per API key and per phone number. Expect `429 Too Many Requests` under bursts, and back off. A `429` is refused *before* anything reaches Daraja, so retrying it with the same `Idempotency-Key` after a short delay is safe — the key is not spent by a rate-limited request.\n\n## Errors\n\npaylod returns conventional HTTP status codes.\n\n| Status | Meaning |\n| --- | --- |\n| `400` | Malformed JSON, or Daraja credentials are not configured for this application and environment. |\n| `401` | Missing or invalid API key. |\n| `404` | The resource does not exist, or belongs to another application. |\n| `409` | Idempotency key reused with a different body, still in flight, or spent by an interrupted provider call (indeterminate — check status, retry with a new key). |\n| `422` | Body failed validation (e.g. `amount` ≤ 0, unparseable phone). |\n| `429` | Rate limited. Back off and retry. |\n| `502` | Daraja rejected the request. The upstream error is passed through. |\n\nBeyond HTTP status, M-Pesa has its own result codes — `1032` (customer cancelled), `1037` (timeout), `2001` (wrong PIN). Every one of them is decoded in the [error reference](/docs/errors).\n\n---\n\n## Accept an M-Pesa payment with an STK Push\n\nSource: https://paylod.dev/docs/guides/accept-a-payment\n\nCollect money from a customer with an M-Pesa STK Push: what to send, what comes back, and what the customer sees.\n\nCharge a customer. Your server decides the amount, the customer's handset rings, they enter their M-Pesa PIN, and the funds settle to your till.\n\n## Send the push\n\n```ts title=\"charge.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n\nconst outcome = await paylod.collectAndWait({\n  amount: 1500,                     // whole KES, from YOUR order record\n  phone: \"0712345678\",\n  idempotencyKey: attempt.id,       // one key per payment attempt. A double-click cannot charge twice.\n  accountReference: \"INV-2041\",     // YOUR correlation id — comes back on the result\n  description: \"Order #2041\",\n  metadata: { orderId: \"2041\" },    // opaque, stored with the payment\n});\n\nif (outcome.paid) {\n  await fulfil(\"2041\", outcome.receipt);\n} else {\n  await notify(outcome.message);\n}\n```\n\n`collectAndWait()` sends the push, then polls until the payment settles. `outcome.paid` is the only branch you need: on the other side, `outcome.message` is a decoded sentence you can show the customer.\n\n> [!warn] **Pass `idempotencyKey`, and mint one per payment attempt.** Duplicates of that attempt — a double-clicked Pay button, a refreshed tab, a retried job — collapse into **one** prompt and **one** charge. Leave it out and every call is a new charge: two clicks, two prompts, two debits. Do **not** key on the order or product id — that replays an old payment instead of making a new one. See [Idempotency](#idempotency-one-key-per-payment-attempt).\n\nNeed the `paymentId` straight away instead of waiting? Use `collect()`, which returns as soon as the prompt is on the handset. See [Handle the result](/docs/guides/handle-the-result).\n\n## What M-Pesa insists on\n\n- **Whole shillings.** `amount` is an integer, 1 to 150,000. Decimals are rejected.\n- **A Kenyan number.** `0712…`, `254712…` and `+254712…` all work; the SDK normalises them.\n- **One prompt at a time.** A customer already looking at a prompt cannot be sent another.\n\n`accountReference` is capped at 12 characters, `description` at 64. Both are validated locally, before the request leaves your process.\n\n## What `accountReference` actually is\n\nIt is a **correlation id you choose** — an invoice or order number. paylod stores it on the payment and hands it back to you as `accountRef` on `GET /status/:id` and on the webhook, so it is how you tie an M-Pesa payment to the order in your own database.\n\nWhat it is *not* is a message to the customer. Safaricom only surfaces it on a **Paybill** (`CustomerPayBillOnline`), where it is the account number the payer is paying to. On a **Till / Buy Goods** shortcode (`CustomerBuyGoodsOnline`) Safaricom ignores it for display: the payer sees your business name and the amount, and nothing else — exactly what you see paying at any Kenyan shop. Most small merchants are on a Till, so assume the customer will never read it.\n\nIf you omit it, it defaults to a short prefix of the `paymentId` — still unique, still traceable back to the payment from your M-Pesa statement.\n\n> [!warn] Never put anything the customer needs to see into `accountReference`. `description` is what shows up on the STK prompt. And never put anything *secret* in it either — on a Paybill it is visible to the payer.\n\n`accountReference` is a **label, not a lock**. Putting your order id in it does not stop a second charge — it just tags the payment. [`idempotencyKey`](#idempotency-one-key-per-payment-attempt) is the field that collapses duplicates. They are different jobs: order id in `accountReference`, a per-attempt key in `idempotencyKey`.\n\n## Idempotency: one key per payment attempt\n\nA customer double-clicks **Pay**. Your handler runs twice. Without an idempotency key, that is two STK prompts and two debits — and the customer, quite reasonably, wants a refund and an explanation.\n\nMint a key when the attempt begins, and it cannot happen:\n\n```ts title=\"charge.ts\"\nconst attempt = await db.attempts.create({ orderId: order.id });   // one row per press of Pay\n\nawait paylod.collectAndWait({\n  amount: order.amount,\n  phone,\n  idempotencyKey: attempt.id,   // ← the whole fix\n});\n```\n\nBoth requests carry the same key. paylod reserves it *before* it calls Daraja, so exactly one of them reaches the provider and the other replays its answer — same `paymentId`, same `checkoutRequestId`. The handset only ever rings once. This holds even when the two requests land in the same millisecond: ten concurrent calls with one key produce one payment and one push.\n\n| What the customer does | With a per-attempt `idempotencyKey` | Without |\n| --- | --- | --- |\n| Double-clicks **Pay** | 1 prompt, 1 charge | **2 prompts, 2 charges** |\n| Refreshes the tab and re-submits | 1 prompt, 1 charge | **2 prompts, 2 charges** |\n| Your job queue retries the handler | 1 prompt, 1 charge | **2 prompts, 2 charges** |\n\nOmit it and the SDK warns you once, because this is a money bug.\n\n### Per attempt — not per order, not per product\n\nThe key has to be **stable across duplicates of one attempt** and **fresh for a new charge**. That rules out the two shortcuts people reach for:\n\n- **The order id** is stable but never fresh. The customer mistypes their PIN; you retry the order under the same key; paylod replays the **failed** first attempt instead of charging them. The order is never paid.\n- **A product id** — or anything reused across purchases — is worse. Customer two buys the same product, hits the key customer one already spent, and replays customer one's payment. **Nobody after the first customer is charged at all.**\n\nA retry after a wrong PIN, a cancelled prompt or a timeout is a **new attempt**: new row, new key.\n\n> [!warn] Do **not** silence the SDK's warning with `idempotencyKey: crypto.randomUUID()` **at the call site**. A key generated fresh on every call is exactly the same as having no key — it just hides the warning telling you the customer is exposed. A random UUID is a fine key; it just has to be minted **once per attempt** and stored, not re-rolled inside the call.\n\n### When the same key is *not* a safe retry\n\nReusing a key with a *different* body (say, a changed amount) returns `409`. That is always a bug on your side: two different charges have collided on one key.\n\nAnd there is one case that looks like a network error but is not retryable. If a request dies mid-flight against Daraja — after paylod dispatched the call, before an answer came back — that key is **spent**. Retrying it returns `409` with an *indeterminate* message rather than pushing again. A timeout is not evidence that the money did not move, so paylod will not guess: **for money, at-most-once beats at-least-once.**\n\n> [!warn] **On the indeterminate `409`, do not retry with the same key.** Read the payment status first — `paylod.check(paymentId)` or `GET /status/:id` — and only then decide. If it settled, fulfil the order. If nothing happened, open a **new attempt with a new key**. Retrying under the spent key just returns the same `409`.\n\nIf you cannot supply a key up front, persist the generated one against the attempt before you retry it:\n\n```ts title=\"retry.ts\"\nconst ack = await paylod.collect({ amount: 1500, phone: \"0712345678\" });\nawait db.attempts.update(attempt.id, { idempotencyKey: ack.idempotencyKey });\n\n// Retrying THAT attempt: same key, same body → the original payment, no second prompt.\nawait paylod.collect({\n  amount: 1500,\n  phone: \"0712345678\",\n  idempotencyKey: ack.idempotencyKey,\n});\n// A genuinely new attempt gets a NEW key. Reusing this one would replay the old result.\n```\n\n## Never take the amount from the client\n\n> [!warn] The amount must come from your own order record, on your own server. If a browser or mobile app holds both the API key and the amount, a payer can intercept the request and pay less. That is the one way to be undercut, and it is entirely avoidable. See [Secure integration](/docs/guides/security).\n\n## Next\n\n[Handle the result](/docs/guides/handle-the-result) covers webhooks, and what to check before you fulfil.\n\n---\n\n## Handle the M-Pesa payment result\n\nSource: https://paylod.dev/docs/guides/handle-the-result\n\nTwo ways to learn a payment settled — poll GET /status, or receive a signed webhook. How to choose, and how to verify.\n\n`collect()` returns before the customer has typed their PIN. There are two ways to learn what happened, and both read the same settled record.\n\n| | Polling | Webhooks |\n| --- | --- | --- |\n| You host | Nothing | An HTTPS endpoint |\n| Good for | Scripts, a checkout with a spinner | Servers, volume, back-office fulfilment |\n\nStart with polling. Move to webhooks when you have a server that wants to be told.\n\n## Option A: poll\n\n```ts title=\"poll.ts\"\nconst outcome = await paylod.wait(ack.paymentId, { timeoutMs: 120_000 });\n\nif (outcome.paid) await fulfil(outcome.receipt);\nelse              await notify(outcome.message);\n```\n\n`wait()` polls on a jittered backoff ramp until the payment settles. If the payment is still pending, paylod runs a live M-Pesa STK Query and settles it on the spot, so a poll never returns a stale answer.\n\n`paylod.status(paymentId)` reads the record once, without polling.\n\n> A timeout **throws** `PaylodTimeoutError` rather than returning `ok: false`. An unanswered prompt is not a failed payment: the customer may still be typing their PIN, and it can still succeed. Do not mark the order failed. Let the webhook tell you.\n\n## Option B: receive a signed webhook\n\nRegister an endpoint on the application's **Endpoints & Webhooks** tab. The SDK verifies the signature for you.\n\n```ts title=\"app/api/webhooks/paylod/route.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod();\n\nexport const POST = paylod.webhookHandler(async (event) => {\n  if (event.type === \"payment.success\") {\n    await fulfil(event.data.paymentId, event.data.mpesaReceipt);\n  } else if (event.data.decoded) {\n    await notify(event.data.decoded.customerMessage);\n  }\n});\n```\n\n`decoded` is `DecodedError | null`: it is populated on `payment.failed` and `null` on `payment.success`. Check it before you read it.\n\nThis works anywhere the Web `Request`/`Response` API does: Next.js, Hono, Remix, Workers, Bun, Deno. On Express, use `paylod.webhook(handler)` instead.\n\nThe signature is checked **before** your handler runs. A bad signature returns `400` and your handler never sees the event. If your handler throws, the route returns `500` and paylod retries the delivery, so make the handler idempotent.\n\n> [!warn] On Express, mount this route **before** any global `express.json()`. Once a JSON parser has consumed the body the raw bytes are gone and no signature can be verified. The middleware detects this and fails loudly rather than skipping the check.\n\n## Check the amount before you fulfil\n\nThe webhook carries the **true settled amount**. Fulfilling on `status` alone is how merchants get underpaid.\n\n```ts title=\"fulfil.ts\"\nexport const POST = paylod.webhookHandler(async (event) => {\n  if (event.type !== \"payment.success\") return;\n\n  // Look the order up by paymentId — the id you stored when you called collect().\n  const order = await db.orders.findByPaymentId(event.data.paymentId);\n  if (!order) return;\n\n  if (event.data.amount === order.amountKes) {\n    await fulfil(order);\n  } else {\n    await flagForReview(order, event.data);   // underpaid\n  }\n});\n```\n\nStore `ack.paymentId` against your order when you send the push, and key the lookup on it. The event does not echo back the `metadata` you passed to `collect()`.\n\n## Failures are results, not outages\n\n`payment.failed` is a normal outcome. `event.data.decoded` is already decoded for you:\n\n- `1032` the customer pressed Cancel. Offer a retry.\n- `1037` the prompt never reached the handset, or was ignored.\n- `2001` wrong M-Pesa PIN.\n- `1` insufficient balance.\n\n`4999` is **not** a failure. It means the customer has not entered their PIN yet. Keep polling, and never re-collect: a second push fires a second prompt and can double-charge. Every code is in the [error reference](/docs/errors).\n\n## Retrying a failed payment\n\nWhen a payment genuinely failed — cancelled, wrong PIN, insufficient balance — charging again is a **new payment attempt**, and it needs a **new idempotency key**. Retrying under the key of the failed attempt does not re-charge; it replays the failure you already have.\n\n```ts title=\"retry.ts\"\nif (!outcome.paid && outcome.retryable) {\n  const retry = await db.attempts.create({ orderId: order.id });   // new attempt → new key\n  await paylod.collectAndWait({ amount, phone, idempotencyKey: retry.id });\n}\n```\n\n> [!warn] **One `409` is not a retry signal: the *indeterminate* one.** If an earlier request under that key died while the call to Daraja was in flight, paylod refuses to re-dispatch it — the money may already have moved, and a timeout is not evidence that it did not. Read the payment status before you do anything else (`paylod.check(paymentId)`, `GET /status/:id`, or wait for the webhook). If it settled, fulfil the order. If nothing happened, open a new attempt with a **new** key. Never retry the spent key. Full rules in [Idempotency](/docs/sdk#idempotency).\n\n## Verify it yourself\n\nIf you are not using the SDK, the signature is an HMAC-SHA256 over `t + \".\" + rawBody`, keyed by the endpoint's signing secret, with a 5-minute freshness window. Compute it over the raw bytes, never a re-serialised object. The scheme is specified in the [webhooks reference](/docs/api/webhooks).\n\n## Next\n\n[Test in sandbox](/docs/guides/test-in-sandbox) forces each of these outcomes on demand.\n\n---\n\n## Test M-Pesa payments in the Daraja sandbox\n\nSource: https://paylod.dev/docs/guides/test-in-sandbox\n\nBuild the whole flow against Safaricom's sandbox with test credentials — including the failure paths — before a shilling moves.\n\n## Test every failure path without a phone\n\nStart here. You do **not** need a handset to test cancellation, a wrong PIN, insufficient funds or a timeout — and you should not try to produce them by hand. paylod ships a simulator that drives the **real** settlement path: the same payment row, the same result codes, the same signed webhook your production code will receive. Nothing is mocked except the handset.\n\nTwo calls:\n\n1. **Create a simulated payment** — `paylod.simulate.collect()` in the SDK, `simulate_test_payment` (MCP tool), or `POST /simulate/collect` over REST with your `mp_test_…` key. No STK prompt is sent. It returns `{ paymentId, status: \"pending\", outcomes }`, where `outcomes` is an array of `{ id, label, status }`.\n2. **Force how it resolves** — `paylod.simulate.outcome()`, `simulate_outcome` (MCP tool), or `POST /simulate/outcome`, with that `paymentId` and one `outcome` id.\n\n| `outcome` | Settles as | Result code |\n| --- | --- | --- |\n| `approve` | `success` | `0` |\n| `user_cancelled` | `failed` | `1032` |\n| `timeout` | `failed` | `1037` |\n| `wrong_pin` | `failed` | `2001` |\n| `insufficient_funds` | `failed` | `1` |\n\n### From the SDK\n\nIf you are on Node, this is the path to use. It is one call, and it hands you back the same `PaymentOutcome` your production code already renders — so the code under test is the real code, not a special one.\n\n```ts title=\"checkout.test.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_TEST_KEY!);   // mp_test_…\n\nfor (const outcome of paylod.simulate.outcomes) {\n  const result = await paylod.simulate.pay({ outcome, amount: 10 });\n  console.log(outcome, \"→\", result.status, result.retryable, result.message);\n}\n// approve            → succeeded false Payment received — thank you!\n// wrong_pin          → failed    true  That M-Pesa PIN was incorrect. …\n// insufficient_funds → failed    true  Your M-Pesa balance is too low. …\n// user_cancelled     → cancelled true  Payment cancelled — you can try again …\n// timeout            → failed    true  The M-Pesa prompt expired before it was answered. …\n```\n\nTo test *your* handler rather than the SDK's, build the client with `{ simulate: true }` — `collect()` then creates a simulated payment instead of ringing a phone, and your `/api/pay` route runs unchanged. A `mp_live_…` key is refused locally, before any request is sent. Full details in the [SDK reference](/docs/sdk#test-your-checkout-without-a-phone).\n\n### Over REST\n\n```bash title=\"Simulate a cancelled payment over REST\"\n# 1. create the pending simulated payment\ncurl -sX POST https://paylod.dev/functions/v1/simulate/collect \\\n  -H \"Authorization: Bearer $PAYLOD_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"applicationId\":\"<your-app-id>\",\"phone\":\"254708374149\",\"amount\":10}'\n# → { \"paymentId\": \"…\", \"status\": \"pending\", \"outcomes\": [ { \"id\": \"approve\", … } ] }\n\n# 2. force the outcome — your webhook fires, your poller settles, exactly as in production\ncurl -sX POST https://paylod.dev/functions/v1/simulate/outcome \\\n  -H \"Authorization: Bearer $PAYLOD_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"paymentId\":\"<paymentId>\",\"outcome\":\"user_cancelled\"}'\n```\n\nAn AI agent with the `paylod:payments.simulate` scope can do the same thing with `simulate_test_payment` + `simulate_outcome` and never be able to touch real money. See the [MCP server](/docs/mcp).\n\n> [!note] The simulator is sandbox-only. It is the fastest way to prove your code renders `outcome.message`, offers a retry only when `retryable` is true, and never fulfils an order on a `1032`.\n\n## Get sandbox credentials\n\nCreate an app on the [Safaricom Daraja portal](https://developer.safaricom.co.ke) and open its **Sandbox** credentials. You want four values:\n\n| Field | Sandbox value |\n| --- | --- |\n| Consumer key | From the Daraja portal |\n| Consumer secret | From the Daraja portal |\n| Shortcode | `174379` (Safaricom's shared test paybill) |\n| Passkey | From \"Lipa na M-Pesa Online\" in the portal |\n\nPaste them into your application's **Sandbox** environment in paylod, then mint an `mp_test_…` key.\n\n## What sandbox actually does\n\nSandbox is Safaricom's own environment, not a paylod mock. The STK Push is real: it goes to Daraja, Daraja answers, and the callback comes back to paylod's hosted receiver exactly as it would in production. What is *not* real is the money — nothing settles.\n\nThat means the shape of everything you build — the `202`, the `paymentId`, the webhook envelope, the signature, the result codes — is identical in production. There is no second integration to write.\n\n## Send a test payment\n\nSet `PAYLOD_API_KEY` to the `mp_test_…` key. Nothing else changes.\n\n```ts title=\"sandbox.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);   // mp_test_… key\n\nconst outcome = await paylod.collectAndWait({\n  amount: 10,\n  phone: \"254708374149\",     // Safaricom's published test MSISDN\n  accountReference: \"TEST-1\",\n});\n\nconsole.log(outcome.paid ? outcome.receipt : outcome.message);\n```\n\nUse a real number you control and you will get a real prompt, with no real money attached.\n\n## Exercise the failure paths\n\nMost production incidents are failure paths nobody tried. Before you go live, make sure your code does something sensible for each of these. Use the **simulator** (above) — it is deterministic, needs no handset, runs in your test suite, and `1037` alone would otherwise cost you a minute of staring at a phone. The handset column is there only if you want to see it happen for real once.\n\n| Result code | Simulator `outcome` | On a real handset | What your code should do |\n| --- | --- | --- | --- |\n| `1032` | `user_cancelled` | Press **Cancel** on the prompt | Show a friendly retry, do not fulfil |\n| `1037` | `timeout` | Ignore the prompt until it expires | Retry, or fall back to polling |\n| `2001` | `wrong_pin` | Enter the wrong M-Pesa PIN | Ask them to try again |\n| `1` | `insufficient_funds` | Push an amount above the test balance | Tell the customer the balance is low |\n\nEvery code, with its cause and fix, is in the [error reference](/docs/errors).\n\n## Test your webhook locally\n\nNo tunnel needed. The [CLI](/docs/cli) streams your live webhook events to `localhost` with the signature intact:\n\n```bash title=\"Terminal\"\nnpx @paylod/cli listen --forward http://localhost:3000/webhook\n```\n\nYour handler verifies a real signature against a real payload, on your laptop. See [Handle the result](/docs/guides/handle-the-result).\n\n## Next\n\n[Go live](/docs/guides/go-live) — swap the credentials, swap the key, keep the code.\n\n---\n\n## Go live on production Daraja credentials\n\nSource: https://paylod.dev/docs/guides/go-live\n\nMove from sandbox to production: production Daraja credentials, the callback URL you paste into the Daraja portal, and the go-live checklist.\n\n## What changes\n\nAlmost nothing. Your code is the same; three things move.\n\n1. Your application's **Production** environment gets your *production* Daraja credentials and your real shortcode.\n2. You paste the **production callback URL** into the Daraja portal.\n3. Your backend swaps `mp_test_…` for `mp_live_…`.\n\nThe endpoints, the request bodies, the webhook envelope and the result codes are identical. That is the whole point of building against [sandbox](/docs/guides/test-in-sandbox) first.\n\n## 1. Get production Daraja credentials\n\nProduction credentials are issued by Safaricom against your **real** till or paybill, on the [Daraja portal](https://developer.safaricom.co.ke) under \"Go Live\". You will need:\n\n- Your consumer key and secret for the production app.\n- Your shortcode — the till or paybill number customers already pay.\n- Your Lipa na M-Pesa Online passkey.\n- For payouts, reversals, and the admin queries: an **Initiator Name** and **Initiator Password** (a.k.a. the security credential), issued separately.\n\nPaste them into the application's **Production** environment. They are encrypted at rest and never read back.\n\n## 2. Set the callback URL in the Daraja portal\n\npaylod hosts the callback receiver, so what you paste into Daraja is the URL paylod shows you on the application's **Endpoints & Webhooks** tab.\n\n> This is the one step nobody can do for you — Safaricom has no API for registering a callback URL. Everything else in a paylod integration is automatable.\n\nTreat the callback URL as a secret. It carries a per-application token.\n\n## 3. Swap the key\n\nMint an `mp_live_…` key on the **API Keys** tab and put it in your server's secret store. Your code does not change at all:\n\n```bash title=\".env\"\nPAYLOD_API_KEY=mp_live_YOUR_API_KEY\n```\n\nThe key alone decides that this is production. There is no environment flag to forget.\n\n## Go-live checklist\n\n- The API key lives on your **server**, never in a browser or mobile app.\n- The `amount` comes from your own order record, never from the client.\n- Your webhook handler **verifies the HMAC signature** before parsing the body. `paylod.webhookHandler()` does this for you.\n- You compare `data.amount` against the order total **before** fulfilling.\n- One `Idempotency-Key` per **payment attempt** — not per order or product. A retry after a failure is a new attempt and gets a new key.\n- Your code handles the `409` *indeterminate* reply by **reading the payment status**, not by retrying the same key.\n- `429` is handled with a backoff, not a crash.\n- Your failure paths (`1032`, `1037`, `2001`) have been exercised at least once.\n- If you use payouts or admin queries, the Initiator Name and Password are set on the **Production** environment.\n\nThe reasoning behind each of these is in [Secure integration](/docs/guides/security).\n\n## Then watch it\n\nPayments, webhook deliveries and their retries are all visible in the dashboard. If a delivery is failing, you will see the attempts and the response your endpoint returned.\n\n---\n\n## Secure your M-Pesa integration\n\nSource: https://paylod.dev/docs/guides/security\n\nSix rules that make it impossible for a payer with an intercepting proxy to undercut you, and why the amount must stay on your server.\n\n## The question everyone asks\n\n*Could a customer open my checkout in an intercepting proxy — Burp, mitmproxy — and lower the amount before it reaches M-Pesa?*\n\nUnder a correct integration, **no**.\n\nThe API key that calls `/collect` authenticates **you**, the merchant — not the payer. Only `amount` and `phone` come from the request body; your shortcode, passkey and Daraja credentials are pulled from encrypted storage server-side and never travel over the wire. Settlement is bound to the amount your server initiated, and every webhook is HMAC-signed and reports the **true settled amount**.\n\nThere is exactly one way to be undercut: hand the payer control of **both** the key and the amount — i.e. call `/collect` straight from a browser or mobile app with a client-supplied amount. Then a payer can edit the request and pay less. The platform will faithfully report the true, lower amount, but it cannot rescue a merchant who trusted the client with it.\n\nKeep the amount on your server and the attack disappears.\n\n## The short version\n\n| Do | Don't |\n| --- | --- |\n| Keep `mp_live_…` / `mp_test_…` server-side only | Embed the API key in a browser, mobile app, or any client |\n| Derive `amount` from your own order record | Forward a client-supplied amount verbatim to `/collect` |\n| Verify `x-webhook-signature` before reading the body | Trust an unverified webhook |\n| Confirm `amount` matches the order **and** `status === \"success\"` before fulfilling | Fulfil on `status` alone |\n| Send one `Idempotency-Key` per **payment attempt** | Key on an order or product id, or re-roll the key on every call |\n| Expect `429`s and back off | Hammer on rate limits |\n\n## 1. Keys are server-only secrets\n\nNever expose `mp_live_…` or `mp_test_…` in a browser, mobile app, or any client. Keys live only on your server. A client-embedded key is fully compromised — treat it as public. Anyone holding it can call your account.\n\n## 2. Your server owns the amount\n\nYour server is the sole authority on the amount. Derive it from your own order or price record; never pass a client-supplied amount straight through to `/collect`. If the client both holds the key and sets the amount, a payer can lower it — that is the one and only way to be undercut.\n\n## 3. Verify, then check the amount, before fulfilling\n\nOn the webhook: verify the signature **first**, then confirm the amount and the status **before** fulfilling. `paylod.webhookHandler()` does the verification for you, so your handler only runs on an authentic event.\n\n```ts title=\"fulfil.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n\nexport const POST = paylod.webhookHandler(async (event) => {\n  if (event.type !== \"payment.success\") return;\n\n  // Look the order up by paymentId — the id you stored when you called collect().\n  const order = await db.orders.findByPaymentId(event.data.paymentId);\n  if (!order) return;\n\n  // Never fulfil on status alone. Check the TRUE settled amount.\n  if (event.data.amount === order.amountKes) {\n    await fulfil(order);                    // paid in full\n  } else {\n    await flagForReview(order, event.data); // underpaid / mismatch\n  }\n});\n```\n\nThe webhook always carries the true settled amount, so any partial or underpayment is visible the moment you check it.\n\nStore `ack.paymentId` against your order when you call `collect()`, and key the lookup on it. The event carries `paymentId`, `accountRef` and the M-Pesa fields. It does **not** echo back the `metadata` you sent to `collect()`, so do not build your fulfilment around that.\n\nMake the handler **idempotent**. Deliveries are retried until you return `2xx`, and a repeat of an event you have already handled is normal rather than an error.\n\n## 4. Treat callback and mint tokens as secrets\n\nThe callback URL and the mint token are secrets. Don't log them, don't expose them client-side — they belong on your server, alongside the API key.\n\n## 5. One idempotency key per payment attempt\n\nMint an `Idempotency-Key` per **payment attempt** — not per order, and never per product. Duplicates of one attempt (a double-click, a redelivered job) then collapse into a single charge, even under concurrency. Reusing a key with a **different** body is rejected with `409`.\n\nA key is **spent** once used, which is why an order or product id is the wrong choice: a reused key replays the *original* payment rather than making a new one. A retry after a wrong PIN is a new charge and needs a new key.\n\n> [!warn] If a request is interrupted while the provider call is in flight, that key returns `409` *indeterminate* and is never silently re-dispatched — a timeout is not evidence that the money did not move. **Do not retry the same key.** Read the payment status, then retry with a **new** key if nothing happened. For money, at-most-once beats at-least-once. See [Idempotency](/docs/sdk#idempotency).\n\n## 6. Expect rate limits\n\n`/collect` is rate-limited per API key and per phone number. Expect `429 Too Many Requests` under bursts and back off. A `429` is refused before anything reaches Daraja, so it does not spend the key — retrying with the same `Idempotency-Key` after a short delay is safe.\n\n---\n\nFollow these six rules and a payer wielding an intercepting proxy cannot undercut you, over-charge you, redirect funds, or trick you into fulfilling an order that was not paid in full. The platform is safe by design — it just needs the amount to stay on your server.\n\n---\n\n## M-Pesa API overview: base URL, auth, conventions\n\nSource: https://paylod.dev/docs/api\n\nThe base URL, the auth header, the request/response conventions, and an index of every paylod endpoint.\n\n## Base URL\n\n```text title=\"Base URL\"\nhttps://paylod.dev/functions/v1\n```\n\n## Authentication\n\nEvery endpoint takes a bearer API key. The key determines the application and the environment, so no request ever carries a shortcode or a Daraja credential.\n\n```http title=\"Header\"\nAuthorization: Bearer mp_live_YOUR_API_KEY\n```\n\nSee [Authentication](/docs/authentication) for keys, environments, idempotency and rate limits.\n\n## Endpoints\n\n| Method | Path | What it does |\n| --- | --- | --- |\n| `POST` | [`/collect`](/docs/api/payments) | Send an STK Push to a customer's handset |\n| `GET` | [`/status/:id`](/docs/api/payments) | Read a payment's current state |\n| `POST` | [`/payout`](/docs/api/payouts) | Send money out to a customer (B2C) |\n| `POST` | [`/reversal`](/docs/api/payouts) | Reverse or refund a completed transaction |\n| `POST` | [`/qr-generate`](/docs/api/other) | Generate a dynamic Lipa na M-Pesa QR code |\n| `POST` | [`/c2b-register`](/docs/api/other) | Register offline C2B confirmation + validation URLs |\n| `POST` | [`/transaction-status`](/docs/api/other) | Query a historical transaction by receipt |\n| `POST` | [`/account-balance`](/docs/api/other) | Query your shortcode's M-Pesa float |\n\n## Synchronous vs asynchronous\n\nM-Pesa is an asynchronous network, and paylod does not pretend otherwise.\n\n- **`200 OK`** — the answer is in the response body. Only `GET /status/:id` and `POST /qr-generate` and `POST /c2b-register` work this way.\n- **`202 Accepted`** — the request was accepted and handed to M-Pesa. The real outcome arrives later, on your [webhook](/docs/api/webhooks) (or, for `/collect`, by polling `GET /status/:id`). The body carries an id you can correlate on.\n\nAsynchronous endpoints that move money or read the ledger (`/payout`, `/reversal`, `/transaction-status`, `/account-balance`) also require the application's **Initiator Name** and **Initiator Password** to be configured. See [Go live](/docs/guides/go-live).\n\n## Errors\n\n| Status | Meaning |\n| --- | --- |\n| `400` | Malformed JSON, or Daraja credentials not configured for this app and environment |\n| `401` | Missing or invalid API key |\n| `404` | Not found, or belongs to another application |\n| `409` | Idempotency key reused with a different body, still in flight, or spent by an interrupted provider call |\n| `422` | Body failed validation |\n| `429` | Rate limited |\n| `502` | Daraja rejected the request — the upstream error is passed through |\n\nM-Pesa's own result codes (`1032`, `1037`, `2001`, …) are decoded in the [error reference](/docs/errors).\n\n---\n\n## Payments API: STK Push and payment status\n\nSource: https://paylod.dev/docs/api/payments\n\nPOST /collect to trigger an STK Push and GET /status/:id to read the outcome — the two endpoints that move and read money in.\n\n## POST /collect\n\nInitiate an STK Push. The customer gets an M-Pesa prompt on their handset; when they enter their PIN, funds settle directly to your till. Returns immediately with a `paymentId` — the final result arrives via the hosted callback, which you read back with `GET /status/:id` or a [webhook](/docs/api/webhooks).\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/collect\n```\n\n### Headers\n\n| Name | Type | Required | Description |\n| --- | --- | --- | --- |\n| `Authorization` | string | Required | Bearer API key (`mp_live_…` / `mp_test_…`). |\n| `Content-Type` | string | Required | `application/json`. |\n| `Idempotency-Key` | string | **Send it** | The double-charge guard. Names **one payment attempt** — mint it per attempt, not per order and not per product. A repeat with the same key **and** body returns the **original payment** instead of sending a second STK prompt, even under concurrency. Same key + a different body → `409`. Same key after a provider call was interrupted → `409` *indeterminate*: check status, retry with a **new** key. Omit it and every call is a new charge — a double-clicked Pay button becomes two prompts and two debits. |\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `amount` | integer | Required | Whole KES, 1–150000. M-Pesa rejects decimals. |\n| `phone` | string | Required | Kenyan MSISDN in any common form (`0712…`, `254712…`, `+254712…`). Normalised for you. |\n| `accountReference` | string | Optional | Your correlation id (invoice/order number) — returned as `accountRef` on `/status/:id` and on the webhook. Safaricom shows it to the payer only on a **Paybill** (`CustomerPayBillOnline`), where it is the account number; a **Till / Buy Goods** shortcode does not display it at all. 1–12 chars. Defaults to a short prefix of the `paymentId`, so an omitted reference is still unique and still traceable. A **label, not a lock**: it does not deduplicate anything — `Idempotency-Key` is what prevents a second charge. |\n| `description` | string | Optional | Free-text label, 1–64 chars. Defaults to `Payment`. |\n| `metadata` | object | Optional | Opaque key/values stored with the payment. Never inspected, and not returned on `/status` or the webhook — key your records on `paymentId`. |\n\n### Example\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/collect \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Idempotency-Key: att_9f2c1b40\" \\\n  -d '{\n    \"amount\": 10,\n    \"phone\": \"254712345678\",\n    \"accountReference\": \"INV-2041\",\n    \"description\": \"Order #2041\",\n    \"metadata\": { \"orderId\": \"2041\" }\n  }'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"paymentId\": \"e69e5c00-8a01-44ed-b003-48e2e86a7c9e\",\n  \"status\": \"pending\",\n  \"checkoutRequestId\": \"ws_CO_010720261905029287161380\"\n}\n```\n\n> [!warn] **Send `Idempotency-Key`, and mint one per payment attempt.** Repeat the same key and you get the same `paymentId` and `checkoutRequestId` back — the handset is not rung a second time, even if the duplicates arrive concurrently. Omit it and a double-clicked Pay button, a refreshed tab or a retried request each fire a fresh STK prompt and charge the customer again. Do **not** key on an order or product id: the key is spent once used, so a reused one replays an old payment instead of making a new one. A retry after a wrong PIN is a **new charge** and needs a **new key**.\n\n> [!warn] **`409` *indeterminate* means stop, not retry.** If a previous request under this key was interrupted while the Daraja call was in flight, paylod will not repeat it — the money may already have moved, and a timeout is not evidence that it did not. For money, at-most-once beats at-least-once. Read `GET /status/:id` first; if nothing happened, retry with a **new** key. The same key will only return this `409` again.\n\n### Errors\n\n| Status | When |\n| --- | --- |\n| `401` | Missing or invalid API key. |\n| `400` | Malformed JSON, or Daraja credentials not configured for this app / environment. |\n| `409` | Idempotency key reused with a different body; or still in flight (`Retry-After`); or spent by an interrupted provider call — *indeterminate*, check status and retry with a new key. |\n| `422` | Body failed validation (e.g. `amount` ≤ 0, bad phone). |\n| `429` | Rate limited — per key and per phone number. |\n| `502` | Daraja rejected the STK Push (upstream error passed through). |\n\n## GET /status/:id\n\nRead a payment's current state — the zero-infrastructure way to get the result, with no webhook receiver needed. If the payment is still `pending`, paylod runs a live M-Pesa STK Query and settles it on the spot before responding. Scoped to the key's application: another application's payment returns `404`.\n\n```text title=\"Endpoint\"\nGET https://paylod.dev/functions/v1/status/:id\n```\n\n### Path\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `id` | string | Required | The `paymentId` returned by `POST /collect`. |\n\n### Example\n\n```bash title=\"Request\"\ncurl https://paylod.dev/functions/v1/status/e69e5c00-8a01-44ed-b003-48e2e86a7c9e \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\"\n```\n\n```json title=\"Response · 200 OK\"\n{\n  \"id\": \"e69e5c00-8a01-44ed-b003-48e2e86a7c9e\",\n  \"status\": \"success\",\n  \"mpesaReceipt\": \"UG1F3A1U7J\",\n  \"resultCode\": 0,\n  \"resultDesc\": \"The service request is processed successfully.\"\n}\n```\n\n### Status values\n\n| Value | Meaning |\n| --- | --- |\n| `pending` | The prompt is out; the customer has not resolved it yet. |\n| `success` | Settled. `mpesaReceipt` is the M-Pesa receipt number. |\n| `failed` | `resultCode` / `resultDesc` carry the decoded M-Pesa reason. |\n\nOn failure, `resultCode` is the raw Safaricom code — `1032` cancelled, `1037` timeout, `2001` wrong PIN. All of them are decoded in the [error reference](/docs/errors).\n\n---\n\n## Payouts API: M-Pesa B2C payouts and reversals\n\nSource: https://paylod.dev/docs/api/payouts\n\nPOST /payout to send money out (B2C) and POST /reversal to refund a completed transaction.\n\nBoth endpoints move money **out** of your shortcode. Both are asynchronous: the HTTP call acknowledges with `202`, and the terminal result is delivered to your [webhook](/docs/api/webhooks). Both require the application's **Initiator Name** and **Initiator Password** to be configured.\n\n> [!warn] On a production application these are irreversible. Guard them behind your own authorisation, and never expose them to a client.\n\n## POST /payout\n\nB2C payout — send money from your shortcode to a customer.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/payout\n```\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `amount` | integer | Required | Whole KES to send, 1–150000. |\n| `phone` | string | Required | Recipient MSISDN (`0712…`, `254712…`, `+254712…`). Normalised for you. |\n| `commandId` | string | Optional | `SalaryPayment`, `BusinessPayment`, or `PromotionPayment`. Defaults to `BusinessPayment`. |\n| `remarks` | string | Optional | Free-text remarks. Up to 100 chars. |\n| `occasion` | string | Optional | Optional occasion label. Up to 100 chars. |\n\n### Example\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/payout \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"amount\": 2500,\n    \"phone\": \"254712345678\",\n    \"commandId\": \"BusinessPayment\",\n    \"remarks\": \"Payout for July\"\n  }'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"disbursementId\": \"d3f1a2b3-…\",\n  \"conversationId\": \"AG_20260707_0000c1d2e3f4\",\n  \"status\": \"pending\"\n}\n```\n\nThe terminal result is pushed to your webhook as an HMAC-signed `payout.result` event. Correlate it via `disbursementId` or `conversationId`.\n\n## POST /reversal\n\nReverse or refund a completed transaction back to the payer.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/reversal\n```\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `transactionId` | string | Required | The M-Pesa receipt to reverse. |\n| `amount` | integer | Required | Whole KES, 1–150000. Must match the original transaction amount. |\n| `remarks` | string | Optional | Free-text remarks. Up to 100 chars. |\n| `occasion` | string | Optional | Optional occasion label. Up to 100 chars. |\n\n### Example\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/reversal \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"transactionId\": \"UG1F3A1U7J\",\n    \"amount\": 2500,\n    \"remarks\": \"Customer refund\"\n  }'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"disbursementId\": \"f7e6d5c4-…\",\n  \"conversationId\": \"AG_20260707_0000a9b8c7d6\",\n  \"status\": \"pending\"\n}\n```\n\nThe result is pushed to your webhook as an HMAC-signed `refund.result` event.\n\n---\n\n## QR codes, C2B registration and M-Pesa queries\n\nSource: https://paylod.dev/docs/api/other\n\nGenerate an M-Pesa QR code, register offline C2B URLs, and query transaction status or account balance.\n\nThe rest of the Daraja surface, on the same hosted, credential-abstracted model: the same base URL, the same bearer key, no shortcode on the wire.\n\n## POST /qr-generate\n\nGenerate a dynamic Lipa na M-Pesa QR code for a specific amount. **Synchronous** — returns a base64-encoded PNG you render for the customer to scan. Stateless: no ledger row is created here. When the customer pays, the payment arrives later via your C2B confirmation URL (source `c2b`) and a signed webhook, exactly like any other offline payment.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/qr-generate\n```\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `amount` | number | Required | Amount to encode in the QR. Must be greater than 0. |\n| `refNo` | string | Optional | Reference shown to the customer. Up to 64 chars. |\n| `merchantName` | string | Optional | Merchant / display name. Up to 64 chars. |\n| `trxCode` | string | Optional | `BG` Buy Goods (till), `PB` Pay Bill, `WA` Withdraw at Agent, `SB` Send to Business, `SM` Send Money. Defaults to `BG`. |\n| `cpi` | string | Optional | Credit Party Identifier (till / paybill). Defaults to the tenant shortcode. |\n| `size` | string | Optional | Requested QR image size in pixels. |\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/qr-generate \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"amount\": 500,\n    \"refNo\": \"INV-2041\",\n    \"merchantName\": \"Acme Ltd\",\n    \"trxCode\": \"BG\"\n  }'\n```\n\n```json title=\"Response · 200 OK\"\n{\n  \"qrBase64\": \"iVBORw0KGgoAAAANSUhEUgAAAP…\"\n}\n```\n\n## POST /c2b-register\n\nRegister the offline C2B (Lipa na M-Pesa paybill / till) confirmation and validation URLs for your shortcode. **Idempotent** — safe to re-run. No body is required: the shortcode and callback URL are derived server-side from your API key.\n\nAfter registering, offline customer payments to the till or paybill are delivered to paylod as C2B confirmations, recorded as payments (source `c2b`), and a signed webhook fires — the **same envelope** as STK payments.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/c2b-register\n```\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/c2b-register \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{}'\n```\n\n```json title=\"Response · 200 OK\"\n{\n  \"registered\": true,\n  \"shortcode\": \"600123\",\n  \"confirmationUrl\": \"https://paylod.dev/functions/v1/c2b-hook?app=…\",\n  \"validationUrl\": \"https://paylod.dev/functions/v1/c2b-hook?app=…&validate=1\",\n  \"responseCode\": \"0\",\n  \"responseDescription\": \"Success\"\n}\n```\n\n`responseCode` and `responseDescription` echo Daraja's registration acknowledgement, and may be `null` if the shortcode was already registered.\n\n## POST /transaction-status\n\nQuery the status of any historical M-Pesa transaction by its receipt. **Asynchronous** — the HTTP call acknowledges with `202`; the real answer arrives on a signed webhook of type `admin.transaction_status`. Requires the application's Initiator Name and Initiator Password.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/transaction-status\n```\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `transactionId` | string | Required | The M-Pesa receipt / TransactionID to look up. |\n| `identifierType` | string | Optional | `1` MSISDN, `2` till, `4` shortcode. Defaults to `4`. |\n| `remarks` | string | Optional | Free-text remarks for the query. |\n| `occasion` | string | Optional | Optional occasion label. |\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/transaction-status \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"transactionId\": \"UG1F3A1U7J\",\n    \"identifierType\": \"4\"\n  }'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"queryId\": \"b1e2c3d4-…\",\n  \"conversationId\": \"AG_20260707_00001a2b3c4d\"\n}\n```\n\nCorrelate the webhook via `queryId` or `conversationId`.\n\n## POST /account-balance\n\nQuery your shortcode's M-Pesa float. **Asynchronous** — acknowledges with `202`; the answer arrives on a signed webhook of type `admin.account_balance`. Requires the application's Initiator Name and Initiator Password.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/account-balance\n```\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `identifierType` | string | Optional | `1` MSISDN, `2` till, `4` shortcode. Defaults to `4`. |\n| `remarks` | string | Optional | Free-text remarks for the query. |\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/account-balance \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{}'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"queryId\": \"9a8b7c6d-…\",\n  \"conversationId\": \"AG_20260707_00005e6f7a8b\"\n}\n```\n\n---\n\n## M-Pesa payment webhooks: events and signatures\n\nSource: https://paylod.dev/docs/api/webhooks\n\nThe event envelope, the delivery headers, the HMAC signature scheme, and every event type paylod can send you.\n\nWebhooks are **optional**. Register an endpoint on an application's **Endpoints & Webhooks** tab and paylod POSTs a signed event the moment something settles. If you would rather not run a receiver, poll [`GET /status/:id`](/docs/api/payments) instead — every payment is recorded either way.\n\n## Delivery headers\n\n| Header | Description |\n| --- | --- |\n| `x-webhook-event` | The event type, e.g. `payment.success`. |\n| `x-webhook-id` | Unique delivery id. Use it to dedupe retries. |\n| `x-webhook-signature` | HMAC signature, format `t=<unix>,v1=<hex>`. |\n\n## Event types\n\n| Event | Fired when |\n| --- | --- |\n| `payment.success` | A collection settled (STK Push or offline C2B). |\n| `payment.failed` | A collection failed — cancelled, timed out, wrong PIN, insufficient funds. |\n| `payout.result` | A [`/payout`](/docs/api/payouts) reached a terminal state. |\n| `refund.result` | A [`/reversal`](/docs/api/payouts) reached a terminal state. |\n| `admin.transaction_status` | A [`/transaction-status`](/docs/api/other) query resolved. |\n| `admin.account_balance` | An [`/account-balance`](/docs/api/other) query resolved. |\n\nEvery event uses the same envelope and the same signature scheme.\n\n## The envelope\n\n```http title=\"Example delivery\"\nPOST https://your-app.com/webhooks/mpesa\nx-webhook-event: payment.success\nx-webhook-id: 8f3c…\nx-webhook-signature: t=1751394302,v1=5a2f…c9\n\n{\n  \"type\": \"payment.success\",\n  \"created\": 1751394302,\n  \"data\": {\n    \"paymentId\": \"e69e5c00-…\",\n    \"applicationId\": \"…\",\n    \"env\": \"production\",\n    \"status\": \"success\",\n    \"amount\": 10,\n    \"phone\": \"254712345678\",\n    \"accountRef\": \"INV-2041\",\n    \"mpesaReceipt\": \"UG1F3A1U7J\",\n    \"checkoutRequestId\": \"ws_CO_…\",\n    \"resultCode\": 0,\n    \"resultDesc\": \"The service request is processed successfully.\",\n    \"metadata\": { \"orderId\": \"2041\" }\n  }\n}\n```\n\n## Verifying the signature\n\nThe signature is an HMAC-SHA256 over the string `t + \".\" + rawBody`, keyed by the endpoint's signing secret (shown once, when you add the endpoint).\n\n```js title=\"Verify (Node)\"\nimport crypto from \"node:crypto\";\n\n// Express raw-body handler\nexport function verify(req, secret) {\n  const header = req.headers[\"x-webhook-signature\"]; // \"t=…,v1=…\"\n  const parts = Object.fromEntries(header.split(\",\").map((p) => p.split(\"=\")));\n\n  const signed = `${parts.t}.${req.rawBody}`;      // timestamp + raw JSON\n  const expected = crypto\n    .createHmac(\"sha256\", secret)\n    .update(signed)\n    .digest(\"hex\");\n\n  const ok = crypto.timingSafeEqual(\n    Buffer.from(expected),\n    Buffer.from(parts.v1),\n  );\n\n  // Reject if !ok, or if Date.now()/1000 - parts.t > 300 (replay window).\n  return ok;\n}\n```\n\nThree rules, in order:\n\n1. Compute the HMAC over the **raw** request body — not a re-serialised object.\n2. Compare in **constant time** against `v1`.\n3. Reject deliveries whose `t` is older than about five minutes, to blunt replay.\n\n## Responding\n\nRespond `2xx` to acknowledge. A non-2xx response is retried with backoff, and the attempts are visible in the dashboard. Deliveries are at-least-once: dedupe on `x-webhook-id`.\n\n> [!warn] Verify the signature, then compare `data.amount` against your own order total, **before** you fulfil. The webhook always carries the true settled amount. See [Secure integration](/docs/guides/security).\n\n---\n\n## M-Pesa and Daraja error codes, decoded\n\nSource: https://paylod.dev/docs/errors\n\nEvery Safaricom M-Pesa result code and Daraja API error decoded — cause, fix, and a customer-facing message. Searchable.\n\nM-Pesa tells you *what* went wrong in a number and almost nothing else. `resultCode: 1037` is a real answer to a real question, and it means nothing until someone decodes it.\n\nThis page decodes all of them: what the code means, whose fault it is, whether retrying can possibly help, and what to say to the customer.\n\n## How to read a failure\n\nA failed payment gives you two fields, and they come from two different places:\n\n| Field | Where it comes from |\n| --- | --- |\n| HTTP status | paylod — `401` bad key, `422` bad body, `502` Daraja refused the request. See [API errors](/docs/api). |\n| `resultCode` / `resultDesc` | M-Pesa — the customer cancelled, the PIN was wrong, the phone was off. Decoded below. |\n\nA `202` followed by `resultCode: 1032` is not a bug. It means the push worked and the customer pressed Cancel.\n\n## You do not have to implement this table\n\nThe [Node SDK](/docs/sdk) decodes result codes for you, offline — no network call, no API key needed at call time:\n\n```ts title=\"decode.ts\"\nimport { decodeError } from \"@paylod/node\";\n\nconst err = decodeError(1032);\n\nerr.title;            // \"Payment cancelled by the customer\"\nerr.category;         // \"customer\"\nerr.retryable;        // true\nerr.customerMessage;  // \"Payment cancelled — you can try again whenever you're ready.\"\n```\n\nOn a failed payment, `collectAndWait()` hands you the same object as `outcome.detail`, and a `payment.failed` webhook carries it as `event.data.decoded`. They are byte-for-byte identical, so what you show a customer is the same however you integrate. The whole catalogue is also exported as `ERROR_CATALOG`.\n\nMost of the time you will not touch any of this: `outcome.message` is the customer-facing sentence, already decoded, and `outcome.retryable` tells you whether charging again is safe. The catalogue below is for your logs, your dashboards, and mid-incident debugging.\n\nThe [CLI](/docs/cli) decodes the same catalogue from your terminal — `paylod errors 1032` — which is the fastest way to answer \"what does this code mean\" mid-incident.\n\n> [!warn] **`4999` and `500.001.1001` are pending, not failed.** Daraja returns them while the customer is still looking at the PIN prompt. They decode with `category: \"pending\"` and `retryable: false` — not because a retry is pointless, but because the first prompt is still live and a second push can **double-charge** the customer. Do not show a failure on these codes and do not re-collect. Keep polling; the SDK already does.\n\n## The ones you will actually see\n\n| Code | Meaning | What to do |\n| --- | --- | --- |\n| `0` | Success. | Fulfil — after checking the settled `amount`. |\n| `1032` | The customer pressed **Cancel** on the prompt. | Offer a friendly retry. Not an error on your side. |\n| `1037` | Timeout — the prompt never reached the handset, or was ignored. | Retry. Phone off, out of coverage, or the customer walked away. |\n| `2001` | Wrong M-Pesa PIN. | Ask them to try again. |\n| `1` | Insufficient balance. | Tell the customer their M-Pesa balance is too low. |\n| `4999` | **Not a failure.** The customer has not entered their PIN yet. | Keep polling. Never re-collect — the prompt is still live and a second push can double-charge. |\n\nEverything else — every Daraja API error, every C2B/B2C result code — is in the searchable catalogue below.\n\n## Full catalogue\n\nThe full catalogue is a **tool**, not a table to copy: call `decode_mpesa_error` with any result code and it returns `{ code, title, cause, fix, category, retryable, customerMessage }` offline. In application code, the SDK does it for you — `outcome.message` is already the customer-facing sentence. The browsable reference is at https://paylod.dev/docs/errors.\n\n---\n\n## Node SDK for M-Pesa: @paylod/node\n\nSource: https://paylod.dev/docs/sdk\n\n@paylod/node is the official Node/TypeScript client: STK Push, status polling with backoff, signed webhook verification and offline error decoding, with zero runtime dependencies.\n\n`@paylod/node` is the official Node/TypeScript client. It wraps the same HTTP API documented in the [API reference](/docs/api) and adds the parts every integration ends up writing anyway: phone-number normalisation, idempotency keys, a polling loop with backoff, webhook signature verification, and an offline result-code decoder.\n\nIt has **zero runtime dependencies** and works on Node 18+, Bun, Deno, Vercel, Cloudflare Workers and any other runtime with a global `fetch`.\n\n```bash title=\"Install\"\nnpm install @paylod/node\n```\n\n## Server-side only\n\nThe API key can move money. `@paylod/node` is a server library — call it from your backend, a serverless function, or an edge worker. Never from a browser or a mobile app.\n\n> [!warn] A key shipped in client-side JavaScript is fully compromised: every visitor downloads it. If you need a browser checkout, have the browser call *your* server, and let your server call paylod. See [Secure integration](/docs/guides/security).\n\n## Set up the client\n\nOne argument: your API key.\n\n```ts title=\"paylod.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nexport const paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n```\n\nThat is the whole configuration. There is no base URL to pass, no OAuth token to fetch and refresh, and no callback URL to host. If you leave the argument off, the client reads `PAYLOD_API_KEY` from the environment itself:\n\n```ts title=\"Also fine\"\nconst paylod = new Paylod();\n```\n\nEither way it throws a `PaylodConfigError` immediately if no key is found, rather than handing you a client that fails on its first call.\n\nTimeouts, retries and the rest have sensible defaults. You can override them, but you will rarely want to: see [Options](#options) at the bottom of this page.\n\n## Collect a payment\n\n```ts title=\"collect.ts\"\nconst outcome = await paylod.collectAndWait({\n  amount: 100,\n  phone: \"0712345678\",\n  idempotencyKey: attempt.id,      // one key per payment attempt — a double-click cannot charge twice\n});\n\nif (outcome.paid) {\n  await fulfil(outcome.receipt);   // \"UG1F3A1U7J\"\n} else {\n  console.log(outcome.message);    // \"That M-Pesa PIN was incorrect. Please try again …\"\n}\n```\n\n`collectAndWait()` sends the STK Push and polls until the payment settles. You get back a `PaymentOutcome`: a flat object with a customer-facing `message` already decoded and a `retryable` flag you can hang a button off.\n\n> [!warn] **Pass `idempotencyKey`, and mint one per payment attempt.** Duplicates of that attempt — a double-clicked Pay button, a refreshed tab, a redelivered job — collapse into **one** prompt and **one** charge. Omit it and every call is a new charge. Do **not** key on the order or product id: that replays an old payment instead of making a new one. A retry after a wrong PIN is a new charge and needs a **new** key. See [Idempotency](#idempotency).\n\nA wrong PIN is a business outcome, not a crash, so it comes back as data rather than a thrown error.\n\nPhone numbers are normalised for you. `0712345678`, `+254712345678`, `254712345678` and `712345678` all work.\n\n## The outcome\n\nEvery method that settles a payment returns the same shape.\n\n```ts title=\"PaymentOutcome\"\n{\n  status: \"succeeded\" | \"pending\" | \"cancelled\" | \"failed\";\n  message: string;        // customer-facing, already decoded. Render this.\n  retryable: boolean;     // SAFE TO CHARGE AGAIN. Gate your retry button on this.\n  paid: boolean;          // the one branch a backend needs\n  receipt: string | null; // M-Pesa confirmation code; non-null exactly when `paid`\n  code: string | null;    // raw result code, for your logs\n  detail: DecodedError | null;\n  payment: Payment;\n}\n```\n\nYour entire UI:\n\n```tsx title=\"Checkout.tsx\"\n<p>{outcome.message}</p>\n{outcome.retryable && <button onClick={retry}>Try again</button>}\n```\n\nNo `switch` on result codes. No error table in your app. If you ever write `if (outcome.code === \"1032\")` to decide what to *show* a human, use `message` instead. `code` and `detail` are for logs and support tooling.\n\n> [!warn] `retryable` means **safe to charge again**, not \"the user is allowed to press a button\". A `pending` payment is never retryable: the STK prompt is still live on the handset, and re-collecting fires a second prompt that can double-charge the customer. Gate the retry button on `retryable` and this cannot happen to you.\n\n## Methods\n\n### `collect(params)`\n\nSends the STK Push and returns as soon as the prompt is on the handset. The payment is `pending`.\n\n```ts title=\"collect\"\nconst ack = await paylod.collect({\n  amount: 100,                      // whole KES, 1–150000. M-Pesa rejects decimals.\n  phone: \"0712345678\",\n  idempotencyKey: attempt.id,       // PASS THIS — one per payment attempt (see below)\n  accountReference: \"INV-2041\",     // optional, ≤ 12 chars — YOUR correlation id (see below)\n  description: \"Order #2041\",       // optional, ≤ 64 chars — shown on the STK prompt\n  metadata: { orderId: \"2041\" },    // optional, opaque; stored with the payment\n});\n\nack.paymentId;          // \"e69e5c00-…\"\nack.checkoutRequestId;  // \"ws_CO_…\"\nack.idempotencyKey;     // the key that was sent — yours, or a random one if you passed none\n```\n\n`idempotencyKey` is what collapses duplicate deliveries of one payment attempt into a single charge. Mint one per attempt and persist it; see [Idempotency](#idempotency) for exactly what it guarantees, what it does **not** guarantee, and what happens if you leave it out.\n\n`amount`, `phone`, `accountReference` and `description` are validated locally before the request leaves your process, so a bad amount fails instantly in your own stack trace instead of coming back as a `422` a round-trip later.\n\n`accountReference` is a **correlation id you choose**, not a message to the customer. It comes back as `accountRef` on `status()` and on the webhook, so it is how you tie a payment to your order. Safaricom only shows it to the payer on a **Paybill** (`CustomerPayBillOnline`), where it is the account number; on a **Till / Buy Goods** shortcode it is not displayed at all — the payer sees your business name and the amount. `description` is the field that reaches the STK prompt. If you omit `accountReference`, it defaults to a short prefix of the `paymentId`, so it is still unique and still traceable back to the payment.\n\nNote that `accountReference` is a **label, not a lock**: putting your order id there does not deduplicate anything. `idempotencyKey` is the field that collapses duplicates of one attempt. Put your order id in `accountReference`, and a per-attempt key in `idempotencyKey` — they are different jobs.\n\n### `status(paymentId)`\n\nReads a payment. If it is still pending, paylod runs a live M-Pesa STK Query and settles it on the spot before answering.\n\n```ts title=\"status\"\nconst payment = await paylod.status(ack.paymentId);\n\npayment.status;        // \"pending\" | \"success\" | \"failed\"\npayment.mpesaReceipt;  // \"UG1F3A1U7J\" on success, otherwise null\npayment.resultCode;    // 0 | 1032 | 2001 | …\npayment.resultDesc;\n```\n\n### `check(paymentId)`\n\n`status()`, already decoded. Returns a `PaymentOutcome` you can render.\n\n```ts title=\"check\"\nconst outcome = await paylod.check(ack.paymentId);\n\noutcome.status;     // \"pending\" | \"succeeded\" | \"cancelled\" | \"failed\"\noutcome.message;    // \"Check your phone and enter your M-Pesa PIN to complete this payment.\"\noutcome.retryable;  // false while pending\n```\n\nThis is what a polling endpoint in your own app should return to your frontend.\n\n### `wait(paymentId, options)`\n\nPolls an existing payment until it settles, on a jittered backoff ramp (1s → 5s). Returns a `PaymentOutcome`.\n\n```ts title=\"wait\"\nconst outcome = await paylod.wait(ack.paymentId, {\n  timeoutMs: 120_000,   // default. STK prompts expire around 60s; PIN entry takes 20–30s more.\n  onPoll: (p) => console.log(\"still\", p.status),\n  signal: controller.signal,\n});\n```\n\n`wait()` decides \"has it settled?\" from the result **code**, not the raw status field. Daraja reports `4999` on a payment it also marks `failed`, but that code means the prompt is still live. `wait()` keeps polling instead of reporting a failure for a payment that is about to succeed.\n\nIf the payment is **still pending** at the deadline, `wait()` throws a `PaylodTimeoutError`. That is not a failure, and it is deliberately not folded into `status: \"failed\"`. The customer may still be typing their PIN. Leave the order open.\n\n### `collectAndWait(params, options)`\n\n`collect()` followed by `wait()`. The one-liner most integrations want. Takes the same `params` as `collect()` — including `idempotencyKey`, which you should pass, one per payment attempt.\n\n### `pendingOutcome(paymentId)`\n\nThe renderable form of a freshly sent prompt. `collect()` hands back an ack, but a prompt sitting on a handset is a pending payment, and your UI wants to render it like any other state.\n\n```ts title=\"pendingOutcome\"\nimport { pendingOutcome } from \"@paylod/node\";\n\nconst ack = await paylod.collect({ amount: 100, phone });\nreturn pendingOutcome(ack.paymentId);   // same shape as check() and wait()\n```\n\n### `decodeError(resultCode, rawDesc?)`\n\nDecodes an M-Pesa result code offline. No network call, no API key needed at call time.\n\n```ts title=\"decodeError\"\nconst err = paylod.decodeError(1032);\n\nerr.title;            // \"Payment cancelled by the customer\"\nerr.cause;            // \"The customer received the STK prompt but pressed Cancel …\"\nerr.fix;              // \"Nothing is wrong with your setup — offer a clear retry …\"\nerr.category;         // \"customer\"\nerr.retryable;        // true\nerr.customerMessage;  // \"Payment cancelled — you can try again whenever you're ready.\"\n```\n\nThese are byte-for-byte the strings paylod puts in the `decoded` object on a `payment.failed` webhook, so what you show a customer is the same either way. The whole catalogue is browsable in the [error reference](/docs/errors).\n\n`decodeError` is also exported as a standalone function, along with the raw `ERROR_CATALOG`:\n\n```ts title=\"Standalone\"\nimport { decodeError, ERROR_CATALOG } from \"@paylod/node\";\n```\n\n### `4999` and `500.001.1001` are pending, not failed\n\nDaraja's STK Query returns `4999` (\"the transaction is still under processing\") while the customer is still looking at the PIN prompt. paylod classifies it — and `500.001.1001` — as **pending**, so `status()` keeps reporting `pending` and `wait()` keeps polling.\n\nIf you ever decode one of those codes directly, `category` is `\"pending\"` and `retryable` is `false` — not because a retry is pointless, but because the first prompt is still live and a second push can double-charge the customer. Do not show a customer a failure on these codes, and do not re-collect. Keep polling.\n\n## Test your checkout without a phone\n\nYour failure paths are where payment bugs live, and until now testing them meant finding a handset and typing a wrong PIN. `paylod.simulate` removes the handset. It removes nothing else: a real payment row, the real Daraja result codes, the real settlement path, and a real signed webhook delivered to your endpoint. Only the phone is fiction.\n\nIt requires an `mp_test_…` key. A `mp_live_…` key is refused **locally**, before a request is sent — a simulator that could touch production is not a feature.\n\n```ts title=\"checkout.test.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_TEST_KEY!);   // mp_test_…\n\nconst outcome = await paylod.simulate.pay({ outcome: \"wrong_pin\" });\n\noutcome.status;     // \"failed\"\noutcome.message;    // \"That M-Pesa PIN was incorrect. Please try again and enter the right PIN.\"\noutcome.retryable;  // true — no money moved, so a fresh charge is safe\n```\n\nThat is an ordinary `PaymentOutcome` — the identical object `check()` and `wait()` return. There is no \"simulated\" type and no special branch, so the code you are testing is the code that runs in production.\n\n### The five outcomes\n\n`outcome` is a typed union, so a typo is a compile error rather than a `422` you find in CI.\n\n| `outcome` | `status` | Result code | `retryable` |\n| --- | --- | --- | --- |\n| `approve` | `succeeded` | `0` | `false` (it worked) |\n| `wrong_pin` | `failed` | `2001` | `true` |\n| `insufficient_funds` | `failed` | `1` | `true` |\n| `user_cancelled` | `cancelled` | `1032` | `true` |\n| `timeout` | `failed` | `1037` | `true` |\n\n`SIM_OUTCOMES` (or `paylod.simulate.outcomes`) is the whole list, so you can loop:\n\n```ts title=\"Drive every path\"\nfor (const outcome of paylod.simulate.outcomes) {\n  const result = await paylod.simulate.pay({ outcome });\n  expect(render(result)).toMatchSnapshot();\n}\n```\n\n> [!note] `timeout` here is Daraja's `1037` — \"we could not reach the handset\" — which is a **settled** failure. It is not `PaylodTimeoutError`, which is what `wait()` throws when a payment is still `pending` at your deadline. An indeterminate payment is not a failed payment.\n\n### `simulate.collect()` and `simulate.outcome()`\n\nSplit it in two and put *your* code in the middle. The payment id is a real one, so your poller, your webhook route and your UI all run unchanged:\n\n```ts title=\"Testing your own code\"\nconst sim = await paylod.simulate.collect({ amount: 250 });\n\nawait readCheckout(sim.paymentId);   // your code — sees a genuinely pending payment\nawait paylod.simulate.outcome(sim.paymentId, \"insufficient_funds\");\nconst view = await readCheckout(sim.paymentId);   // your code — sees the settled failure\n\nexpect(view.message).toMatch(/balance is too low/);\n```\n\nA simulated payment stays `pending` until you force an outcome, which is exactly what a live prompt does while a customer stares at it. You can only settle it once — a second call gets a `409`, same as a real handset.\n\n### Testing your charge path: `{ simulate: true }`\n\nThe one thing the above still does not exercise is your own `collect()` call. Construct the client with `simulate: true` and `collect()` / `collectAndWait()` create a simulated payment instead of ringing a phone — so your `/api/pay` handler runs **completely unchanged**:\n\n```ts title=\"Your handler, under test\"\nconst paylod = new Paylod(process.env.PAYLOD_TEST_KEY!, { simulate: true });\n\nconst view = await startCheckout(order.id, \"0712345678\", attemptId);   // your handler, verbatim\nawait paylod.simulate.outcome(view.paymentId!, \"user_cancelled\");      // no handset involved\nexpect((await readCheckout(view.paymentId!)).status).toBe(\"cancelled\");\n```\n\nThe constructor throws `PaylodSandboxOnlyError` if the key is not `mp_test_…`, so this flag can never point at production even by accident.\n\n> [!note] The simulator honours `Idempotency-Key` with the **same semantics as production**: the same key returns the same `paymentId` and creates no second payment, concurrent duplicates collapse into one, and a different key creates a new payment. So a test that asserts \"a double-click cannot charge twice\" is testing the real behaviour, not a stub of it.\n\n## Idempotency\n\nAn idempotency key names **one payment attempt**. Duplicate deliveries of that one attempt — a double-click, a refreshed tab, a job-queue redelivery, an internal network retry — collapse into a single charge. That is the guarantee, and it is worth understanding precisely, because it is narrower than \"reusing a key is always a safe retry\".\n\n```ts title=\"One key per payment attempt\"\nconst attempt = await db.attempts.create({ orderId: order.id });   // a row per press of Pay\nawait paylod.collectAndWait({ amount, phone, idempotencyKey: attempt.id });\n```\n\n### Pass a key per attempt — not per order, and not per product\n\nThe key must be **stable across duplicates of one attempt** and **fresh for a genuinely new charge**. An order id is stable, but it is not fresh — and a product id is neither:\n\n| Key you pass | What happens |\n| --- | --- |\n| An id minted per **payment attempt** | Correct. Duplicates of that attempt collapse; a new attempt is a new charge. |\n| Your **order id** | The customer mistypes their PIN, you retry the same order — and paylod replays the **failed** first attempt instead of charging them. The order never gets paid. |\n| A **product id** (or any value reused across purchases) | Catastrophic. Every customer after the first replays the **first-ever** payment for that product. Nobody after customer one is charged at all. |\n| `crypto.randomUUID()` **per call** | Equivalent to no key: a double-click is two keys, two prompts, two charges. |\n\nThe rule that resolves all four: **mint the key when a payment attempt begins, persist it on that attempt, and never reuse it for a different charge.** A retry after a wrong PIN, a cancelled prompt or a timeout is a *new attempt* — new row, new key.\n\nBecause a missing key is a money bug, the SDK emits a one-time `console.warn` when `collect()` or `collectAndWait()` is called without one.\n\n> [!warn] Do **not** silence that warning with `idempotencyKey: crypto.randomUUID()` or `Date.now()` **at the call site**. A key that changes on every call is exactly equivalent to having no key at all — it just hides the warning that was telling you the customer is exposed. A random UUID is a perfectly good key; it just has to be minted **once per attempt** and stored, not generated inside the call.\n\n### A concurrent double-click cannot double-charge\n\nThis part is unconditional. Fire ten simultaneous requests with the same `Idempotency-Key` and you get **one** payment and **one** STK push: the key is reserved before the provider is called, so exactly one request wins and the others are made to wait for — and replay — its answer. All ten calls come back with the same `paymentId`. No handset rings twice.\n\n### The one case where the same key is *not* a safe retry\n\nIf a request dies mid-flight against Daraja — after paylod handed the call to the provider, before an answer came back — that key is **spent**. A retry under it does not silently re-dispatch. It returns `409` with an **indeterminate** message:\n\n```text\nA previous request with this Idempotency-Key was interrupted while the provider call was\nin flight, so it may or may not have completed. We will not repeat it — that could charge\nor pay twice. Check the payment/disbursement status; if nothing happened, retry with a NEW key.\n```\n\nA timeout is not evidence that the money did not move; it is the absence of evidence. So paylod refuses to guess. **For money, at-most-once beats at-least-once** — we would rather make you check than charge someone twice.\n\n> [!warn] **If you get the indeterminate `409`, do not retry with the same key.** Read the payment status first — `paylod.check(paymentId)`, or `GET /status/:id`, or your webhook — and only then decide. If the payment did settle, you are done. If nothing happened, start a **new attempt with a new key**. Retrying under the spent key will only return the same `409`, forever.\n\n### The rules\n\n- **Same key + same body, already settled** → the original payment is replayed. Same `paymentId`, same `checkoutRequestId`. No second prompt, no second debit.\n- **Same key + same body, first request still in flight** → `409` with a `Retry-After`, or — for a plain double-click — the SDK simply waits for the winner and hands you its response.\n- **Same key + different body** → `409`, surfaced as `PaylodApiError` with `.isIdempotencyConflict === true`. Always a bug on your side: two different charges collided on one key (you changed the amount but kept the key).\n- **Same key, previous attempt interrupted against the provider** → `409` **indeterminate**. Check status; retry with a **new** key. Never re-dispatched.\n- **Internal retries** (network blip, `5xx`, `429`) reuse the same key automatically — which is what makes retrying a `POST` safe in the first place. If the interruption happened against Daraja, that retry surfaces the indeterminate `409` rather than charging again.\n\nIf you did not pass a key and you intend to retry the *same attempt* later, persist the one you got back:\n\n```ts title=\"Persist the key with the attempt\"\nconst ack = await paylod.collect({ amount, phone });\nawait db.attempts.update(attempt.id, { idempotencyKey: ack.idempotencyKey });\n// Retrying THAT attempt with THAT key collapses into the original payment.\n// A genuinely new attempt gets a new key — reusing this one would replay the old result.\n```\n\nCalling the HTTP API directly? It is the `Idempotency-Key` request header, and it behaves identically.\n\n## Webhooks (optional)\n\nYou do not need a webhook to take a payment. `collectAndWait()` polls for you, and `check()` lets your own frontend poll. That is a complete, supported integration, and it needs nothing but `PAYLOD_API_KEY`.\n\nAdd a webhook when you want your **server** told about a payment it is not currently waiting on: the customer closed the tab right after entering their PIN, your request timed out but the money landed anyway, or your process restarted. Polling tells the browser; a webhook tells your backend. For anything with an order to fulfil, you will eventually want both.\n\nSet the signing secret when you do:\n\n```bash title=\".env\"\nPAYLOD_WEBHOOK_SECRET=whsec_...   # only if you consume webhooks\n```\n\n### Web `Request`/`Response` — Next.js, Hono, Remix, Workers, Bun, Deno\n\n```ts title=\"app/api/webhooks/paylod/route.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod();\n\nexport const POST = paylod.webhookHandler(async (event) => {\n  if (event.type === \"payment.success\") {\n    await fulfil(event.data.paymentId, event.data.mpesaReceipt);\n  } else if (event.data.decoded) {\n    await notify(event.data.decoded.customerMessage);\n  }\n});\n```\n\n`event.data.decoded` is `DecodedError | null` — populated on `payment.failed`, `null` on `payment.success`. Narrowing on `event.type` does not narrow it, so check it before reading it.\n\nThe signature is verified before your handler runs. A bad signature returns `400` and your handler is never called. If your handler throws, the route returns `500` so paylod retries the delivery — better a duplicate than a lost payment, so make your handler idempotent.\n\n### Express\n\n```ts title=\"server.ts\"\napp.post(\"/webhooks/paylod\", paylod.webhook(async (event) => {\n  if (event.type === \"payment.success\") await fulfil(event.data.paymentId);\n}));\n```\n\n> [!warn] Mount this route **before** any global `express.json()`, or give it `express.raw({ type: \"application/json\" })`. Once a JSON parser has turned the body into an object the raw bytes are gone and the signature cannot be verified. The middleware detects this and returns a loud `400` rather than skipping verification.\n\n### Verify it yourself\n\n```ts title=\"verifyWebhook\"\nconst event = paylod.verifyWebhook({\n  payload: rawBody,                            // string | Buffer | Uint8Array — the RAW bytes\n  signature: headers[\"paylod-signature\"],\n  toleranceSec: 300,                           // default\n});\n```\n\nThrows `PaylodSignatureVerificationError` if the signature does not check out. The signature scheme is documented under [Webhooks](/docs/api/webhooks).\n\n## Errors\n\nEvery error thrown by the client extends `PaylodError`.\n\n| Class | When |\n| --- | --- |\n| `PaylodConfigError` | No API key, or no global `fetch`. Thrown from the constructor. |\n| `PaylodSandboxOnlyError` | A `simulate` call (or `{ simulate: true }`) with a key that is not `mp_test_…`. Thrown locally; no request was sent. Extends `PaylodConfigError`. |\n| `PaylodInvalidRequestError` | Local validation failed — bad amount, unparseable phone. No request was sent. |\n| `PaylodApiError` | paylod returned a non-2xx. Carries `.status` and the parsed body. |\n| `PaylodConnectionError` | The request never got an answer (DNS, TLS, socket). Retried automatically. |\n| `PaylodTimeoutError` | `wait()` hit its deadline with the payment still `pending`. Not a failure. |\n| `PaylodSignatureVerificationError` | A webhook signature did not verify. |\n\nTransient failures — network blips, `429`, `5xx` — are retried automatically with backoff. `400`, `401`, `404`, `409` and `422` are real answers and are thrown immediately.\n\n## A complete example\n\nA browser checkout, done safely: the page calls your server, your server calls paylod. The key never leaves the server.\n\n```ts title=\"server.ts\"\nimport { Paylod, pendingOutcome } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n\n// POST /api/pay → ring the phone.\n// Takes an orderId, never an amount: the price is looked up here, so a payer\n// with a proxy cannot discount their own order.\nasync function startPayment(orderId: string, phone: string) {\n  const order = await getOrder(orderId);\n\n  // One row per press of Pay — that row's id IS the idempotency key.\n  // Double-click Pay and the second call replays the first payment instead of\n  // sending a second STK prompt. Retry after a wrong PIN and you get a NEW\n  // attempt, a NEW key, and a real second charge — which is what a retry means.\n  // Keying on `order.id` would replay the FAILED attempt forever; keying on a\n  // product id would replay the first customer's payment to everyone after them.\n  const attempt = await db.attempts.create({ orderId: order.id });\n\n  const ack = await paylod.collect({\n    amount: order.amount,\n    phone,\n    idempotencyKey: attempt.id,\n  });\n  return pendingOutcome(ack.paymentId);\n}\n\n// GET /api/pay/:id → has it settled? Already decoded, ready to render.\nasync function readPayment(paymentId: string) {\n  return paylod.check(paymentId);\n}\n```\n\nBoth functions return the same `PaymentOutcome`, so your frontend has one shape to render and no result codes to interpret.\n\nThat is the whole integration. The [paylod demo](https://github.com/paylod/demo) is a working version of exactly this, including the retry path and a verified webhook.\n\n## Options\n\nYou will not normally need any of these. The defaults are the right answer.\n\n```ts title=\"Escape hatches\"\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!, {\n  timeoutMs: 30_000,\n  maxRetries: 2,\n});\n```\n\n| Option | Default | What it is for |\n| --- | --- | --- |\n| `apiKey` | `process.env.PAYLOD_API_KEY` | Authentication. The only required value. |\n| `timeoutMs` | `30_000` | Per HTTP request. |\n| `maxRetries` | `2` | Transient failures only (network, `429`, `5xx`). |\n| `webhookSecret` | `process.env.PAYLOD_WEBHOOK_SECRET` | Only if you consume webhooks. |\n| `baseUrl` | `https://paylod.dev/functions/v1` | Baked in. Override only to self-host or to point at a stub in tests. |\n| `fetch` | global `fetch` | Inject an instrumented or proxied fetch. |\n| `simulate` | `false` | Tests only. `collect()` creates a simulated payment instead of ringing a phone. Requires an `mp_test_…` key — the constructor throws otherwise. See [Test your checkout without a phone](#test-your-checkout-without-a-phone). |\n\n| Environment variable | Used for |\n| --- | --- |\n| `PAYLOD_API_KEY` | Authentication. Required. |\n| `PAYLOD_WEBHOOK_SECRET` | `verifyWebhook()`, `webhook()`, `webhookHandler()`. Optional. |\n| `PAYLOD_BASE_URL` | Overriding the API base. Rarely needed. |\n\n## Next\n\n- Prefer the terminal? The [CLI](/docs/cli) sends the same STK Push in one command.\n- The HTTP endpoints underneath: [API reference](/docs/api).\n- Every result code, decoded: [error reference](/docs/errors).\n\n---\n\n## paylod CLI: M-Pesa from your terminal\n\nSource: https://paylod.dev/docs/cli\n\nSend a real M-Pesa STK Push from your terminal, decode any Daraja result code offline, and forward live webhooks to localhost without ngrok.\n\nThe `paylod` CLI sends a real M-Pesa STK Push from your terminal, decodes Daraja result codes offline, and forwards live webhooks to `localhost` without ngrok. It talks to the same API documented in the [API reference](/docs/api).\n\n```bash title=\"Install\"\nnpm install -g @paylod/cli\n```\n\nThe package is `@paylod/cli`; the command it installs is `paylod`.\n\nOr run it without installing:\n\n```bash title=\"npx\"\nnpx @paylod/cli errors 1032\n```\n\nNode 20 or newer.\n\n## Set up\n\n```bash title=\"Terminal\"\npaylod init\n```\n\n`init` signs you in (it opens your browser), lets you pick an application, and mints an API key for this machine. Credentials go into your OS keychain, or a `0600` file under `~/.config/paylod` if there is no keychain available.\n\nIf you only want to sign in:\n\n```bash title=\"Terminal\"\npaylod login\npaylod whoami       # who am I, and where is the token stored?\n```\n\nFor CI, skip the login entirely and set `PAYLOD_API_KEY` in the environment. Every data-plane command (`collect`, `status`, `listen`) will use it.\n\n## Take a payment\n\n```bash title=\"Terminal\"\npaylod collect --phone 254712345678 --amount 100\n```\n\nThe customer's phone rings. The CLI polls until the payment settles and prints the outcome — an M-Pesa receipt on success, or a decoded reason on failure.\n\n| Flag | What it does |\n| --- | --- |\n| `-p, --phone <msisdn>` | Customer's number. `0712…`, `254712…`, `+254712…` all work. |\n| `-a, --amount <kes>` | Whole shillings. M-Pesa rejects decimals. |\n| `-r, --ref <reference>` | Your own correlation id (invoice/order number), returned on the result. ≤ 12 chars. Shown to the payer only on a Paybill; a Till never displays it. |\n| `-d, --description <text>` | Payment description on the STK prompt. ≤ 64 chars. |\n| `--idempotency-key <key>` | Names one payment attempt. Duplicates of that attempt collapse into a single charge. A new charge needs a new key. |\n| `--no-wait` | Fire the push and exit immediately, without polling. |\n| `--timeout <secs>` | How long to wait for settlement. Default 120. |\n| `--env <sandbox\\|production>` | Which M-Pesa environment to charge on. |\n| `--api-key <key>` | Use this key instead of `PAYLOD_API_KEY` or your stored profile. |\n\nLook a payment up later, or watch it settle:\n\n```bash title=\"Terminal\"\npaylod status e69e5c00-8a01-44ed-b003-48e2e86a7c9e\npaylod status e69e5c00-8a01-44ed-b003-48e2e86a7c9e --watch\n```\n\n`status` decodes the failure reason for you — you never have to look `2001` up in a PDF.\n\n## Decode a result code\n\nOffline. No login, no network, no API key.\n\n```bash title=\"Terminal\"\npaylod errors 1032          # why did that payment fail?\npaylod errors --list        # the whole catalogue\npaylod errors 2001 --json   # machine-readable, for your error handler\n```\n\nThis is the fastest way to answer \"what does `2001` mean\" mid-incident. The same catalogue is browsable in the [error reference](/docs/errors).\n\n> `4999` and `500.001.1001` decode as **pending**, not failed — they mean the customer has not entered their PIN yet. The payment is still live and still payable. Do not show a customer a failure on those codes.\n\n## Webhooks on localhost, without ngrok\n\n```bash title=\"Terminal\"\npaylod listen --forward http://localhost:3000/webhook\n```\n\n`listen` streams your application's live webhook events and re-POSTs each one to your local server, with the signature intact — so the code you are writing verifies a real signature against a real payload. No tunnel, no public URL, no ngrok.\n\n| Flag | What it does |\n| --- | --- |\n| `-f, --forward <url>` | POST each event to this local URL. |\n| `-e, --events <types...>` | Only forward these types (`payment.success`, `payment.failed`). |\n| `--print-json` | Print the full JSON payload of every event. |\n| `--secret <whsec>` | Signing secret. Otherwise taken from your profile, or fetched. |\n| `--tolerance <secs>` | Signature freshness window. Default 300; `0` disables the check. |\n| `--skip-verify` | Do not verify the HMAC. Debugging only. |\n| `--port <port>` | Local receiver port. Default 4242. |\n\nRoll (or fetch) the signing secret with:\n\n```bash title=\"Terminal\"\npaylod webhooks secret\n```\n\n## Test without spending money\n\n```bash title=\"Terminal\"\npaylod simulate --outcome user_cancelled\npaylod simulate --interactive\n```\n\n`simulate` creates a payment in the paylod sandbox and forces an outcome — no Daraja call, no handset, no money. It is how you exercise your failure paths in CI.\n\n| Outcome | Result code |\n| --- | --- |\n| `approve` | `0` |\n| `wrong_pin` | `2001` |\n| `insufficient_funds` | `1` |\n| `user_cancelled` | `1032` |\n| `timeout` | `1037` |\n\n## Manage the integration\n\nThese commands do everything the dashboard does.\n\n| Command | What it manages |\n| --- | --- |\n| `paylod apps list \\| create \\| use \\| rename \\| delete` | Applications — one per till or paybill. |\n| `paylod creds` | Your Daraja consumer key, secret, shortcode and passkey. Write-only. |\n| `paylod keys mint \\| list \\| revoke \\| use` | paylod API keys (`mp_test_…` / `mp_live_…`). |\n| `paylod webhooks list \\| add \\| toggle \\| delete \\| secret` | Webhook endpoints. |\n| `paylod payments` | Browse your M-Pesa payments. |\n| `paylod orgs` | Organizations. |\n\n## Scripting\n\nEvery command takes a global `--json` flag and prints machine-readable output.\n\n```bash title=\"Terminal\"\npaylod collect -p 254712345678 -a 100 --json | jq -r '.mpesaReceipt'\n```\n\n`paylod collect` exits `5` if the payment is still pending when the timeout expires — which is not a failure, just an unanswered prompt. A non-zero exit on a settled failure carries the decoded reason in the JSON.\n\n## Environment variables\n\n| Variable | What it does |\n| --- | --- |\n| `PAYLOD_API_KEY` | Merchant API key. Skips the config file entirely — this is how you run paylod in CI. |\n| `PAYLOD_WEBHOOK_SECRET` | Signing secret for `paylod listen`. |\n| `PAYLOD_API_BASE` | Override the API base. Defaults to `https://paylod.dev/functions/v1`. |\n| `PAYLOD_CONFIG_DIR` | Where the CLI stores config. Default `~/.config/paylod`. |\n| `PAYLOD_NO_KEYCHAIN=1` | Never use the OS keychain; use the `0600` file instead. Useful on headless Linux and in containers. |\n| `NO_COLOR` | Disable colour. |\n\n> [!warn] `mp_live_…` keys move real money. Keep them out of shell history and out of committed `.env` files.\n\n## Next\n\n- Building an app rather than a script? Use the [Node SDK](/docs/sdk).\n- Wiring an AI agent? The [MCP server](/docs/mcp) exposes the same capabilities as tools.\n- The HTTP endpoints underneath: [API reference](/docs/api).\n\n---\n\n## MCP server: M-Pesa for AI agents\n\nSource: https://paylod.dev/docs/mcp\n\nLet an AI agent manage and use your M-Pesa integration — a remote, OAuth-authenticated MCP endpoint. Nothing to install.\n\n## Nothing to install\n\npaylod ships a **remote** MCP server. There is no npm package to add, no process to run, and no API key to paste into a config file. You give your AI client one URL; it discovers the authorization server, opens your browser once, and you approve exactly the capabilities you want. The agent gets a scoped token — never your Daraja credentials, never your paylod API key.\n\n```text title=\"Server URL\"\nhttps://mcp.paylod.dev/mcp\n```\n\nTransport: `streamable-http`. Auth: OAuth 2.1 (authorization code + PKCE, with Dynamic Client Registration) against `https://paylod.dev/oauth`. Any MCP client that speaks remote HTTP works — and stdio-only clients can bridge.\n\n## Add it to your client\n\nPick your client. Each one runs the OAuth flow for you on first connect: a browser tab opens, you sign in to paylod, you consent to scopes, done.\n\n### Claude Code\n\nOne command. Use `--scope user` to make it available across all your projects (`local` is the default; `project` writes a shared `.mcp.json`).\n\n```bash title=\"Terminal\"\nclaude mcp add --transport http --scope user paylod https://mcp.paylod.dev/mcp\n```\n\nThen run `/mcp` inside Claude Code and pick **paylod** to authenticate. After that, `/mcp` shows the server connected with its tools listed.\n\nPrefer to commit the config? Put this in `.mcp.json` at the repo root. The `type` field is required — an entry with a `url` but no `type` is treated as stdio and skipped.\n\n```json title=\".mcp.json\"\n{\n  \"mcpServers\": {\n    \"paylod\": {\n      \"type\": \"http\",\n      \"url\": \"https://mcp.paylod.dev/mcp\"\n    }\n  }\n}\n```\n\n### Claude Desktop\n\nRemote servers are added in the UI, not in `claude_desktop_config.json` (that file is for local stdio servers). Go to **Settings → Connectors → Add custom connector**, paste the server URL, and click **Connect** to run the OAuth flow. Leave the advanced client-ID/secret fields blank — paylod supports Dynamic Client Registration, so your client registers itself.\n\n### Cursor\n\nAdd to `~/.cursor/mcp.json` (global) or `.cursor/mcp.json` (per-project). Cursor keys remote servers off `url`. Open **Settings → MCP** and click the login prompt to authorize.\n\n```json title=\"~/.cursor/mcp.json\"\n{\n  \"mcpServers\": {\n    \"paylod\": {\n      \"url\": \"https://mcp.paylod.dev/mcp\"\n    }\n  }\n}\n```\n\n### VS Code\n\nAdd to `.vscode/mcp.json`. Note the top-level key is `servers`, not `mcpServers`. VS Code implements the MCP authorization spec including DCR, so it prompts you to sign in on first use.\n\n```json title=\".vscode/mcp.json\"\n{\n  \"servers\": {\n    \"paylod\": {\n      \"type\": \"http\",\n      \"url\": \"https://mcp.paylod.dev/mcp\"\n    }\n  }\n}\n```\n\n### Windsurf\n\nAdd to `~/.codeium/windsurf/mcp_config.json`. Windsurf uses `serverUrl` for remote servers and supports OAuth on Streamable HTTP.\n\n```json title=\"~/.codeium/windsurf/mcp_config.json\"\n{\n  \"mcpServers\": {\n    \"paylod\": {\n      \"serverUrl\": \"https://mcp.paylod.dev/mcp\"\n    }\n  }\n}\n```\n\n### Codex CLI\n\nCodex supports remote Streamable-HTTP MCP servers with OAuth. Add the block below to `~/.codex/config.toml`, then run `codex mcp login paylod`.\n\n```toml title=\"~/.codex/config.toml\"\n[mcp_servers.paylod]\nurl = \"https://mcp.paylod.dev/mcp\"\n```\n\n### Fallback for stdio-only clients\n\nIf your client only speaks stdio, bridge it with `mcp-remote` — the standard stdio-to-HTTP proxy. It handles the OAuth flow (opening your browser, caching the token under `~/.mcp-auth`) and needs no flags against paylod.\n\n```json title=\"mcp-remote bridge\"\n{\n  \"mcpServers\": {\n    \"paylod\": {\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"mcp-remote\", \"https://mcp.paylod.dev/mcp\"]\n    }\n  }\n}\n```\n\nYou almost certainly do not need this — Claude Code, Claude Desktop, Cursor, VS Code, Windsurf and Codex all speak remote HTTP natively.\n\n## First thing to try\n\n`decode_mpesa_error` is pure and offline — it needs no scopes, no credentials, and touches no network. That makes it the ideal way to confirm the server is wired up before you grant anything. Ask your agent:\n\n```text title=\"Prompt\"\nWhat does M-Pesa error 2001 mean?\n```\n\nA good answer comes back with the cause (wrong PIN), the fix, and the message to show the customer — that is the tool, not the model guessing. Anyone who has stared at a bare `2001` in a Daraja response knows why this exists.\n\n## How the OAuth flow works\n\nYour client hits the server URL with no token and gets a `401` carrying a `WWW-Authenticate` header that points at the protected-resource metadata. From there it discovers the authorization server at `https://paylod.dev/oauth`, registers itself dynamically, and opens your browser. You sign in to paylod and land on a consent screen listing exactly which capabilities the agent is asking for — one scope per capability, each with its own checkbox.\n\n```http title=\"The 401 that starts it all\"\ncurl -i -X POST https://mcp.paylod.dev/mcp \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"tools/list\"}'\n\nHTTP/1.1 401 Unauthorized\nWWW-Authenticate: Bearer resource_metadata=\"https://mcp.paylod.dev/.well-known/oauth-protected-resource\"\n```\n\nThe three high-risk scopes — `payments.payout` (send money out), `credentials.write` (write your Daraja keys) and `keys.mint` (mint API keys) — are **unchecked by default**. You have to deliberately turn them on. Leave them off and the agent cannot call `payout`, `reversal`, `set_credentials` or `mint_key` at all: the server rejects the call on the scope, so it is not a prompt you have to trust the model to respect. Grant only `payments.simulate` and the agent can build against the sandbox without ever being able to touch real money.\n\nThe resulting access token is bound to this server as its audience, held by your client, and never pasted into the conversation. Your Daraja consumer key and secret are write-only — no tool reads them back, so an agent can store them for you but can never exfiltrate them.\n\n## Tools\n\nEvery call is authorized by the OAuth access token and gated on the scope shown next to it — a token without the scope cannot invoke the tool.\n\n### Management — set the integration up\n\n| Tool | Scope | What it does |\n| --- | --- | --- |\n| `create_app` | `paylod:apps.write` | First-run onboarding: create your organization and its first application (paybill or till). Returns the applicationId, your callback URL and a one-time API key. |\n| `create_application` | `paylod:apps.write` | Add another application to an organization you already have. |\n| `get_callback_url` | `paylod:apps.write` | Get the hosted M-Pesa callback URL for an application — the URL you paste into the Daraja portal. Treat it as a secret. |\n| `set_credentials` | `paylod:credentials.write` | Store or rotate the Daraja consumer key, secret, shortcode and passkey. Write-only — never read back. |\n| `mint_key` | `paylod:keys.mint` | Mint a paylod API key (`mp_test_…` / `mp_live_…`) for your own backend. Returned once. |\n| `configure_webhook` | `paylod:webhooks.write` | Create or update a webhook endpoint so results are pushed to your server, signed. |\n| `list_webhooks` | `paylod:webhooks.write` | List the webhook endpoints configured for an application. |\n| `list_applications` | `paylod:team.read` | List the applications you can access, with their environments and configuration state. |\n| `authenticate` | — (no scope) | Show who the token belongs to and which scopes were actually granted. |\n\n### Runtime — move and read money\n\n| Tool | Scope | What it does |\n| --- | --- | --- |\n| `request_stk_push` | `paylod:payments.collect` | Send an STK Push to a customer's handset. Accepts an idempotency key — one per payment attempt — so duplicate deliveries of that attempt collapse into a single charge. An interrupted call spends the key and returns `409` *indeterminate*: read `get_payment_status`, then retry with a **new** key. |\n| `get_payment_status` | `paylod:payments.read` | Look up one payment by id. If still pending, runs a live STK query and settles it on the spot. |\n| `generate_qr` | `paylod:payments.collect` | Generate an M-Pesa QR code (base64 PNG). Stateless — no money moves. |\n| `register_c2b` | `paylod:payments.collect` | Register C2B validation/confirmation URLs so paybill payments made outside your app reach you. |\n| `get_account_balance` | `paylod:payments.read` | Query the M-Pesa account balance. Asynchronous — returns a queryId; the result arrives on your callback. |\n| `get_transaction_status` | `paylod:payments.read` | Query the status of an M-Pesa transaction by receipt. Asynchronous, same callback pattern. |\n| `payout` | `paylod:payments.payout` | Send money out (B2C). Irreversible on a live application. Requires the opt-in money-out scope. |\n| `reversal` | `paylod:payments.payout` | Reverse or refund a transaction. Requires the opt-in money-out scope. |\n\n### Sandbox & offline\n\n| Tool | Scope | What it does |\n| --- | --- | --- |\n| `simulate_test_payment` | `paylod:payments.simulate` | Create a simulated collection in sandbox — no handset, no real money. |\n| `simulate_outcome` | `paylod:payments.simulate` | Force a simulated payment to succeed or fail, so the agent can exercise your failure paths. |\n| `decode_mpesa_error` | — (no scope) | Decode any Safaricom result code or Daraja error into cause, fix, and a customer-facing message. Pure and offline. |\n\nThe same codes `decode_mpesa_error` returns are browsable in the [error reference](/docs/errors).\n\n## Set an integration up with the tools, in order\n\nThe tools are for **setting the integration up and inspecting it**. They are not what your application calls at runtime — that is the [Node SDK](/docs/sdk). Call them in this order.\n\n1. **Do I already have an app?** → `list_applications`. Anything back means you are onboarded.\n2. **Create the application.** A brand-new user calls `create_app` (it creates the organization *and* its first application). Someone who already has an organization calls `create_application` instead — `create_app` returns a `409 already onboarded` for them. Picking the wrong one is the single most common first-call mistake.\n3. **Store the Daraja credentials** → `set_credentials`. It needs `product` — the *kind of shortcode*, `paybill` or `till`, not the Daraja product name. For a **till** you must also pass `partyB` (the till number that receives the funds), and it has to differ from `shortcode`, or Daraja rejects the push with `2002`. Credentials are encrypted at rest and write-only: no tool reads them back.\n4. **Get the callback URL** → `get_callback_url`, then **paste it into the Safaricom Daraja portal** as the CallbackURL / ResultURL / ValidationURL / ConfirmationURL. This is the one step no agent can do for you — Safaricom has no API for it — and skipping it means payments never resolve. Treat the URL as a secret: the random `cbk_…` token in the path is what authenticates Safaricom's callback.\n5. **Mint an API key** → `mint_key` (`mp_test_…` sandbox, `mp_live_…` production). Shown once. This is the key your *server* puts in `PAYLOD_API_KEY` — the MCP server itself never uses one.\n6. **Optionally register a webhook** → `configure_webhook`. Skip it and poll instead; every payment is recorded either way.\n7. **Then write the application code with the SDK** — `npm install @paylod/node`, `new Paylod(process.env.PAYLOD_API_KEY!)`, `await paylod.collectAndWait({ amount, phone })`. Render `outcome.message`; offer a retry only when `outcome.retryable`. Do not write a fetch wrapper, a polling loop, or a callback handler: see [Quickstart](/docs/quickstart) and the [Node SDK](/docs/sdk).\n8. **Exercise the failure paths** → `simulate_test_payment` + `simulate_outcome` drive success, cancellation, a wrong PIN or a timeout through the real settlement path, without a handset.\n\n`request_stk_push` and `get_payment_status` exist so an agent can *test* the loop end to end (and so a human can say \"charge this number 10 bob\"). Shipping them as your runtime integration means re-implementing the SDK badly.\n\n> [!warn] `4999` is a **pending** payment, not a failed one — the customer has not entered their PIN yet. It is not retryable: a second `request_stk_push` sends a second prompt and can double-charge. Keep polling `get_payment_status`.\n\n## What an agent can actually do\n\nThe point of the management tools is that an agent can take you from nothing to a working, live M-Pesa integration without you opening a dashboard. Connect the server, then ask:\n\n```text title=\"Prompt\"\nSet up M-Pesa for my shop \"Acme Ltd\". Here are my Daraja sandbox\ncredentials: consumer key …, secret …, shortcode 174379, passkey ….\nCreate the app, store them, mint me a test API key, give me my\ncallback URL, then send a 10 KES STK push to 0712345678 and tell me\nwhen it settles.\n```\n\nThe agent calls `create_app` → `set_credentials` → `mint_key` → `get_callback_url` → `request_stk_push`, then polls `get_payment_status` until the payment settles. If it fails, it calls `decode_mpesa_error` and tells you in plain English that the customer cancelled the prompt (`1032`) or the handset timed out (`1037`). You hand it a phone number; it hands you back an API key and a receipt.\n\nThe one step the agent cannot do for you is paste that callback URL into the Safaricom Daraja portal — Safaricom has no API for it. Everything else, including the webhook that pushes results to your own server (`configure_webhook`), it can do.\n\n> Everything the agent does over MCP is the same platform your backend talks to over HTTP at `/functions/v1` — see the [API reference](/docs/api). MCP is a second front door, not a separate system.\n";
