// GENERATED FILE — DO NOT EDIT.
// Source of truth: mpesa/web/content/docs/** (assembled by mpesa/web/lib/docs/bundle.ts).
// Regenerate:      cd ../mpesa && node scripts/sync-agent-docs.mjs
// Verify:          node scripts/check-docs-bundle.mjs   (also runs on prepublishOnly)
//
// The prose lives on the website. If it is wrong here, it is wrong there — fix it there.
// payload-sha256: 5be16de4fe040173e2ce8d10906a1af5831e9a885914cdaada9f4ce384286fdd

export interface DocTopic {
  readonly id: string;
  readonly title: string;
  readonly keywords: readonly string[];
  readonly aliases: readonly string[];
  /** The docs markdown itself — rendered from web/content/docs/**. */
  readonly content: string;
}

export const DOC_TOPICS: readonly DocTopic[] = [
  {
    id: "integration",
    title: "Integrate M-Pesa with paylod, end to end",
    keywords: ["integrate","integration","setup","set","up","start","started","getting","quickstart","begin","onboard","how","guide","mpesa","m-pesa","daraja","install"],
    aliases: ["quickstart","getting-started"],
    content: "# paylod: M-Pesa payments without a backend\n\npaylod is M-Pesa (Safaricom Daraja) without the backend: one HTTP call sends the STK Push, we host the callback, sign your webhooks and decode every result code.\n\n## What paylod is\n\npaylod is a hosted layer over Safaricom's **Daraja** API. You keep your own till or paybill, and your own Daraja credentials. M-Pesa settles the money **straight to you**. paylod never holds the money and never takes a part of it.\n\npaylod does the difficult work for you. You do not host a public HTTPS callback. You do not refresh access tokens. You do not retry failed STK pushes. You do not decode `resultCode: 1037`. You do not build a webhook signature scheme.\n\n```ts title=\"The whole integration\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n\nconst outcome = await paylod.collectAndWait({ amount: 100, phone: \"0712345678\" });\n\nif (outcome.paid) console.log(\"Paid:\", outcome.receipt);\nelse console.log(outcome.message);   // already decoded, safe to show a customer\n```\n\nThat is the full integration. Install `@paylod/node` and use one API key. paylod does the other work. If you do not use Node, make the same call over plain HTTP. See the [API reference](/docs/api).\n\n## How it works\n\n1. Create an **application** for a till or a paybill. Then put your Daraja consumer key, consumer secret, shortcode and passkey into the application. paylod encrypts these four values at rest, and the values never leave the server.\n2. paylod gives you a **hosted callback URL**. Copy the callback URL into the Safaricom Daraja portal one time. That is the only Safaricom procedure that you do.\n3. Your backend calls `collect()` with an amount and a phone number, and with a paylod **API key**. The customer's handset shows an M-Pesa PIN prompt.\n4. M-Pesa sends the result to paylod's callback. paylod puts the result into a standard format, decodes the result code, and records the payment. Then paylod **sends you a signed webhook**, or you **poll** for the result. You select the method.\n\n> Your integration must never let the client set the amount. Your server owns the amount. See [Secure integration](/docs/guides/security).\n\n## Where to start\n\n| If you want to… | Go to |\n| --- | --- |\n| Send your first STK push now | [Quickstart](/docs/quickstart) |\n| Use the Node/TypeScript client | [Node SDK](/docs/sdk) |\n| Send a payment from your terminal | [CLI](/docs/cli) |\n| Understand keys, environments and idempotency | [Authentication](/docs/authentication) |\n| Charge a customer correctly, in production | [Accept a payment](/docs/guides/accept-a-payment) |\n| Know when the money is in your account | [Handle the result](/docs/guides/handle-the-result) |\n| Find the parameters of an endpoint | [API reference](/docs/api) |\n| Find what `1032` means | [Error codes](/docs/errors) |\n| Let an AI agent do all of the above | [MCP server](/docs/mcp) |\n\n## What you can build\n\n- **Checkout** — collect a payment for an order. Fulfil the order when the webhook confirms the settled amount.\n- **Payouts** — send money to customers, riders or staff (B2C). Reverse a transaction when you must refund a customer.\n- **Offline payments** — register C2B, and counter payments to your till then reach your systems. Or give the customer a dynamic QR code.\n- **Operations** — read a past transaction by receipt, or read the float of your shortcode.\n\nEach item above is one SDK call, or one HTTP request. Each item uses one API key. You host no callback.\n\n---\n\n# Quickstart: your first M-Pesa STK Push\n\nYour first M-Pesa STK Push in five minutes — create an application, add your Daraja credentials, mint a key, and collect a payment.\n\nYou need a Safaricom Daraja account and approximately five minutes. The [sandbox](https://developer.safaricom.co.ke) account is free and immediate. Do one `npm install`, or make one cURL call from any other stack. You deploy nothing: no callback server and no tunnel.\n\n## 1. Create an application\n\nIn the paylod dashboard, create an **application**. Create one application for each till or paybill. Give the application a name. You can add a Sandbox environment and a Production environment to the same application.\n\n## 2. Add your Daraja credentials\n\nOn the application's **Credentials** tab, put in the four values that Daraja gives you:\n\n| Field | Where it comes from |\n| --- | --- |\n| Consumer key | Daraja portal → your app |\n| Consumer secret | Daraja portal → your app |\n| Shortcode | Your till / paybill number (`174379` in sandbox) |\n| Passkey | Daraja portal → Lipa na M-Pesa Online |\n\npaylod encrypts the four values at rest. The four values are **write-only**: no API, no tool and no dashboard screen reads them back.\n\n## 3. Copy the callback URL into Daraja\n\npaylod shows you a hosted callback URL on the application's **Endpoints & Webhooks** tab. Copy the callback URL into the Daraja portal as your Lipa na M-Pesa Online callback URL.\n\nThis step replaces the public HTTPS endpoint that you would otherwise host. You write no callback code.\n\n## 4. Create an API key\n\nOn the **API Keys** tab, create a key. Each key applies to one environment:\n\n- `mp_test_…` → Sandbox\n- `mp_live_…` → Production\n\npaylod shows the key **one time only**. The key tells paylod which application and which environment to use for a request. Thus you never send a shortcode or a Daraja credential in a request.\n\n> [!warn] The API key is a server-side secret. Never put the API key in a browser, in a mobile app, or in other software that a payer opens.\n\n## 5. Install the SDK\n\nOn Node, add the official client. For all other languages, call the HTTP API directly with cURL. The PHP, Python, Java and Kotlin clients are still in development. The samples below on this page show the interface that these clients will have. Use the language selector in the top-left corner to change all the samples on this page together.\n\n```bash tab=\"node\" title=\"npm\"\nnpm install @paylod/node\n```\n```text tab=\"php\" title=\"PHP — not released yet\"\nThe PHP SDK is not on Packagist yet. Follow this quickstart on the cURL tab.\n```\n```text tab=\"python\" title=\"Python — not released yet\"\nThe Python SDK is not on PyPI yet. Follow this quickstart on the cURL tab.\n```\n```text tab=\"java\" title=\"Java — not released yet\"\nThe Java SDK is not on Maven Central yet. Follow this quickstart on the cURL tab.\n```\n```text tab=\"kotlin\" title=\"Kotlin — not released yet\"\nThe Kotlin SDK is not on Maven Central yet. Follow this quickstart on the cURL tab.\n```\n```bash tab=\"curl\" title=\"No dependency\"\n# Nothing to install — any HTTP client works. Check you have cURL:\ncurl --version\n```\n\nPut the API key in your environment. Each client reads the API key automatically:\n\n```bash title=\".env\"\nPAYLOD_API_KEY=mp_test_YOUR_API_KEY\n```\n\n## 6. Send your first STK push\n\n> [!warn] **Always pass `idempotencyKey`. Create one idempotency key for each payment attempt.** A double-clicked Pay button, a refreshed tab and a retried request are duplicates of one attempt. Duplicates of one attempt then become **one** prompt and **one** charge, even when they arrive at the same moment. If you do not pass `idempotencyKey`, each call is a new charge: two clicks give two prompts and two debits. Do **not** use the order id or the product id as the idempotency key. A used key replays the *old* payment, and does not make a new payment. A retry after a wrong M-Pesa PIN is a new charge, and needs a **new** key. If you omit `idempotencyKey`, the SDK gives you a warning. [Full rules](/docs/sdk#idempotency).\n\n```ts tab=\"node\" title=\"collect.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n\nconst outcome = await paylod.collectAndWait({\n  amount: 10,\n  phone: \"0712345678\",\n  idempotencyKey: attempt.id,    // one key per payment attempt. A double-click cannot charge twice.\n  accountReference: \"INV-2041\",\n  description: \"Order #2041\",\n});\n\nif (outcome.paid) {\n  console.log(\"Paid:\", outcome.receipt);   // \"UG1F3A1U7J\"\n} else {\n  console.log(outcome.message);            // already decoded, safe to show a customer\n}\n```\n```php tab=\"php\" title=\"collect.php\"\nuse Paylod\\Paylod;\n\n$paylod = new Paylod(getenv('PAYLOD_API_KEY'));\n\n$outcome = $paylod->collectAndWait([\n    'amount' => 10,\n    'phone' => '0712345678',\n    'idempotencyKey' => $attempt->id,   // one key per payment attempt\n    'accountReference' => 'INV-2041',\n    'description' => 'Order #2041',\n]);\n\nif ($outcome->paid) {\n    echo \"Paid: {$outcome->receipt}\";\n} else {\n    echo $outcome->message;             // already decoded, safe to show a customer\n}\n```\n```python tab=\"python\" title=\"collect.py\"\nimport os\nfrom paylod import Paylod\n\npaylod = Paylod(os.environ[\"PAYLOD_API_KEY\"])\n\noutcome = paylod.collect_and_wait(\n    amount=10,\n    phone=\"0712345678\",\n    idempotency_key=attempt.id,   # one key per payment attempt\n    account_reference=\"INV-2041\",\n    description=\"Order #2041\",\n)\n\nif outcome.paid:\n    print(\"Paid:\", outcome.receipt)\nelse:\n    print(outcome.message)        # already decoded, safe to show a customer\n```\n```java tab=\"java\" title=\"Collect.java\"\nimport dev.paylod.Paylod;\nimport dev.paylod.CollectParams;\nimport dev.paylod.PaymentOutcome;\n\nPaylod paylod = new Paylod(System.getenv(\"PAYLOD_API_KEY\"));\n\nPaymentOutcome outcome = paylod.collectAndWait(\n    CollectParams.builder()\n        .amount(10)\n        .phone(\"0712345678\")\n        .idempotencyKey(attempt.id())   // one key per payment attempt\n        .accountReference(\"INV-2041\")\n        .description(\"Order #2041\")\n        .build());\n\nif (outcome.isPaid()) {\n    System.out.println(\"Paid: \" + outcome.getReceipt());\n} else {\n    System.out.println(outcome.getMessage());\n}\n```\n```kotlin tab=\"kotlin\" title=\"Collect.kt\"\nimport dev.paylod.Paylod\n\nval paylod = Paylod(System.getenv(\"PAYLOD_API_KEY\"))\n\nval outcome = paylod.collectAndWait(\n    amount = 10,\n    phone = \"0712345678\",\n    idempotencyKey = attempt.id,   // one key per payment attempt\n    accountReference = \"INV-2041\",\n    description = \"Order #2041\",\n)\n\nif (outcome.paid) {\n    println(\"Paid: ${outcome.receipt}\")\n} else {\n    println(outcome.message)\n}\n```\n```bash tab=\"curl\" title=\"Raw HTTP\"\n# collectAndWait is SDK sugar: POST /collect, then poll GET /status/:id.\ncurl -X POST https://paylod.dev/functions/v1/collect \\\n  -H \"Authorization: Bearer mp_test_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Idempotency-Key: att_9f2c1b40\" \\\n  -d '{ \"amount\": 10, \"phone\": \"254712345678\", \"accountReference\": \"INV-2041\", \"description\": \"Order #2041\" }'\n# → 202 { \"paymentId\": \"...\", \"status\": \"pending\" }\n# then poll GET /functions/v1/status/{paymentId} until status is success or failed\n```\n\nThat is the whole integration: one argument to the constructor, and one call.\n\n`collectAndWait()` sends the STK push. Then `collectAndWait()` polls until the payment settles. The customer's handset shows the prompt, the customer enters the M-Pesa PIN, and you get an answer. You write no callback server. You refresh no access token. You write no result code table.\n\n> [!warn] A **timeout throws** `PaylodTimeoutError`. A timeout does not report `status: \"failed\"`. An unanswered prompt is not a failed payment, because the customer can still be busy with the M-Pesa PIN. Keep the order open. See [Handle the result](/docs/guides/handle-the-result).\n\nThe answer is a `PaymentOutcome`. The `message` field is already decoded, and you can show the `message` field to a customer. The `retryable` flag tells you if a second charge is safe. A wrong M-Pesa PIN is a business outcome and not a program fault. Thus the SDK returns a wrong M-Pesa PIN as data, and does not throw an error.\n\npaylod puts phone numbers into a standard format for you. Thus `0712345678`, `+254712345678` and `254712345678` all work.\n\nYou do not need a webhook. A poll is a fully supported method, and `collectAndWait()` polls for you. Add a signed webhook when your server does not wait for a payment. paylod then tells your server about that payment.\n\n## If you do not use Node\n\nThe SDK is a wrapper around a plain HTTP API. Every language can call the HTTP API directly:\n\n```bash title=\"The same call, over HTTP\"\ncurl -X POST https://paylod.dev/functions/v1/collect \\\n  -H \"Authorization: Bearer mp_test_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Idempotency-Key: attempt_9f3c1a7e\" \\\n  -d '{ \"amount\": 10, \"phone\": \"254712345678\" }'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"paymentId\": \"e69e5c00-8a01-44ed-b003-48e2e86a7c9e\",\n  \"status\": \"pending\",\n  \"checkoutRequestId\": \"ws_CO_010720261905029287161380\"\n}\n```\n\nThen poll `GET /functions/v1/status/{paymentId}` until the payment settles. If you call the HTTP API directly, you must write the code that the SDK usually gives you: the poll loop, the idempotency key, and the result code decoder. The [API reference](/docs/api) shows the full interface.\n\n## Next\n\n- Every method, option and error class: [Node SDK](/docs/sdk).\n- Do you prefer the terminal? The [CLI](/docs/cli) sends the same STK push in one command.\n- Do you prefer a webhook instead of a poll? [Handle the result](/docs/guides/handle-the-result) describes signed webhooks.\n- Test the failure paths first: [Test in sandbox](/docs/guides/test-in-sandbox).\n- Are you ready for real money? [Go live](/docs/guides/go-live).",
  },
  {
    id: "sdk",
    title: "The paylod SDKs — Node, PHP, Python, Java, Kotlin — and the WooCommerce plugin",
    keywords: ["sdk","sdks","node","npm","typescript","javascript","php","laravel","composer","python","pip","java","maven","gradle","kotlin","woocommerce","wordpress","plugin","client","library","paylod/node","collectandwait","collect_and_wait","verify_webhook","decode_error","outcome","paymentoutcome","retryable","code"],
    aliases: [],
    content: "# M-Pesa SDKs and libraries for paylod\n\nEvery official paylod client — Node, PHP (Laravel-ready), Python, Java and Kotlin — plus the WooCommerce plugin. All mirror the same surface: collect, collectAndWait, check, verifyWebhook, decodeError.\n\npaylod makes an official client for each common runtime of a Kenyan checkout. Each client is a thin wrapper over the HTTP API in the [API reference](/docs/api). Each client has the same methods: `collect`, `collectAndWait`, `check`, `verifyWebhook` and `decodeError`. Therefore the code below has the same shape in each language. Use the language picker in the top-left corner to switch all the samples on this page and in the docs at the same time.\n\npaylod publishes only the Node client today. For each other language, the samples on this page show a client that is still in development. The HTTP API below those samples works now. The cURL tab shows that HTTP API.\n\nYour language may not be on this list. The API is plain HTTP with a bearer token, so you can call the API directly. The [quickstart](/docs/quickstart) and the [API reference](/docs/api) both show the raw cURL.\n\n## Official SDKs\n\n| Language | Status | Install | Repository |\n| --- | --- | --- | --- |\n| Node / TypeScript | Released | `npm install @paylod/node` | [github.com/mosesmrima/paylod-sdk](https://github.com/mosesmrima/paylod-sdk) |\n| PHP (Laravel-ready) | In development | Not published yet — use the [HTTP API](/docs/api) | [github.com/mosesmrima/paylod-php](https://github.com/mosesmrima/paylod-php) |\n| Python | In development | Not published yet — use the [HTTP API](/docs/api) | [github.com/mosesmrima/paylod-python](https://github.com/mosesmrima/paylod-python) |\n| Java | In development | Not published yet — use the [HTTP API](/docs/api) | [github.com/mosesmrima/paylod-jvm](https://github.com/mosesmrima/paylod-jvm) |\n| Kotlin | In development | Not published yet — use the [HTTP API](/docs/api) | [github.com/mosesmrima/paylod-jvm](https://github.com/mosesmrima/paylod-jvm) |\n\n**Node is the only client that paylod publishes today.** The PHP, Python, Java and Kotlin clients are complete and in test. But none of them is on Packagist, PyPI or Maven Central yet. Therefore no install command for them resolves. The samples below show the methods that those clients will have. Until paylod releases those clients, call the HTTP API directly. The API needs only a bearer token and a JSON body. The cURL tab on each sample shows the exact request.\n\nThe Node client has the most complete reference: the [Node SDK](/docs/sdk) page. paylod builds the other clients to match the Node client method for method. Each language keeps its own idiom. Python has the methods `collect_and_wait`, `verify_webhook` and `decode_error`. The other languages keep the `collectAndWait`, `verifyWebhook` and `decodeError` spelling.\n\n> [!note] The PHP client works with any framework. The PHP client also includes a Laravel service provider and facade. On Laravel you will resolve `Paylod` from the container and read the config from `config/paylod.php`. You will not construct the client by hand.\n\nEach SDK repository is public. You can read the source, file an issue, or open a pull request. Java and Kotlin share one repository, [paylod-jvm](https://github.com/mosesmrima/paylod-jvm). A public repository is not a release. The unreleased clients still have no install command that resolves. Read the repository, but do not depend on it.\n\n## Install\n\n```bash tab=\"node\" title=\"npm\"\nnpm install @paylod/node\n```\n```text tab=\"php\" title=\"PHP — not released yet\"\nThe PHP SDK is not on Packagist yet, so there is no composer command to run.\nCall the HTTP API directly in the meantime — switch to the cURL tab.\n```\n```text tab=\"python\" title=\"Python — not released yet\"\nThe Python SDK is not on PyPI yet, so there is no pip command to run.\nCall the HTTP API directly in the meantime — switch to the cURL tab.\n```\n```text tab=\"java\" title=\"Java — not released yet\"\nThe Java SDK is not on Maven Central yet, so there is no coordinate to depend on.\nCall the HTTP API directly in the meantime — switch to the cURL tab.\n```\n```text tab=\"kotlin\" title=\"Kotlin — not released yet\"\nThe Kotlin SDK is not on Maven Central yet, so there is no coordinate to depend on.\nCall the HTTP API directly in the meantime — switch to the cURL tab.\n```\n```bash tab=\"curl\" title=\"No dependency\"\n# Nothing to install — any HTTP client works.\nexport PAYLOD_API_KEY=mp_test_YOUR_API_KEY\n```\n\n## Initialize the client\n\n> [!warn] Never put the API key in a browser, a mobile app, or any other program that a customer can open. The API key can move money. Therefore the API key is a server-side secret. See [Secure integration](/docs/guides/security).\n\nThe client takes one argument: your API key. You set no base URL, and you refresh no OAuth token.\n\n```ts tab=\"node\" title=\"paylod.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nexport const paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n```\n```php tab=\"php\" title=\"paylod.php\"\nuse Paylod\\Paylod;\n\n$paylod = new Paylod(getenv('PAYLOD_API_KEY'));\n```\n```python tab=\"python\" title=\"paylod.py\"\nimport os\nfrom paylod import Paylod\n\npaylod = Paylod(os.environ[\"PAYLOD_API_KEY\"])\n```\n```java tab=\"java\" title=\"Paylod setup\"\nimport dev.paylod.Paylod;\n\nPaylod paylod = new Paylod(System.getenv(\"PAYLOD_API_KEY\"));\n```\n```kotlin tab=\"kotlin\" title=\"Paylod setup\"\nimport dev.paylod.Paylod\n\nval paylod = Paylod(System.getenv(\"PAYLOD_API_KEY\"))\n```\n```bash tab=\"curl\" title=\"Just an env var\"\n# The \"client\" is a bearer token on every request.\nexport PAYLOD_API_KEY=mp_test_YOUR_API_KEY\n```\n\n## Collect a payment\n\n`collectAndWait` sends the STK Push. Then `collectAndWait` polls until the payment settles. Then `collectAndWait` returns a `PaymentOutcome`. The `PaymentOutcome` contains a decoded `message` for the customer and a `retryable` flag. `retryable=true` means that you can safely charge the customer again. `retryable=false` means that paylod cannot prove that no debit occurred, or that the first prompt is still live.\n\n```ts tab=\"node\" title=\"collect.ts\"\nconst outcome = await paylod.collectAndWait({\n  amount: 100,\n  phone: \"0712345678\",\n  idempotencyKey: attempt.id,   // one key per payment attempt — a double-click cannot charge twice\n});\n\nif (outcome.paid) fulfil(outcome.receipt);\nelse console.log(outcome.message);\n```\n```php tab=\"php\" title=\"collect.php\"\n$outcome = $paylod->collectAndWait([\n    'amount' => 100,\n    'phone' => '0712345678',\n    'idempotencyKey' => $attempt->id,   // one key per payment attempt\n]);\n\nif ($outcome->paid) {\n    fulfil($outcome->receipt);\n} else {\n    echo $outcome->message;\n}\n```\n```python tab=\"python\" title=\"collect.py\"\noutcome = paylod.collect_and_wait(\n    amount=100,\n    phone=\"0712345678\",\n    idempotency_key=attempt.id,   # one key per payment attempt\n)\n\nif outcome.paid:\n    fulfil(outcome.receipt)\nelse:\n    print(outcome.message)\n```\n```java tab=\"java\" title=\"Collect.java\"\nPaymentOutcome outcome = paylod.collectAndWait(\n    CollectParams.builder()\n        .amount(100)\n        .phone(\"0712345678\")\n        .idempotencyKey(attempt.id())   // one key per payment attempt\n        .build());\n\nif (outcome.isPaid()) {\n    fulfil(outcome.getReceipt());\n} else {\n    System.out.println(outcome.getMessage());\n}\n```\n```kotlin tab=\"kotlin\" title=\"Collect.kt\"\nval outcome = paylod.collectAndWait(\n    amount = 100,\n    phone = \"0712345678\",\n    idempotencyKey = attempt.id,   // one key per payment attempt\n)\n\nif (outcome.paid) fulfil(outcome.receipt)\nelse println(outcome.message)\n```\n```bash tab=\"curl\" title=\"Raw HTTP\"\ncurl -X POST https://paylod.dev/functions/v1/collect \\\n  -H \"Authorization: Bearer mp_test_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Idempotency-Key: att_9f2c1b40\" \\\n  -d '{ \"amount\": 100, \"phone\": \"254712345678\" }'\n# → 202 { \"paymentId\": \"...\", \"status\": \"pending\" } — then poll GET /status/:id\n```\n\n## Check a payment\n\n`check` reads a payment. `check` then decodes the payment into the same `PaymentOutcome` that you can show to the customer. A polling endpoint in your own application must return this `PaymentOutcome` to its frontend.\n\n```ts tab=\"node\" title=\"check\"\nconst outcome = await paylod.check(paymentId);\noutcome.status;   // \"pending\" | \"succeeded\" | \"cancelled\" | \"failed\"\noutcome.message;  // renderable, already decoded\n```\n```php tab=\"php\" title=\"check\"\n$outcome = $paylod->check($paymentId);\n$outcome->status;   // \"pending\" | \"succeeded\" | \"cancelled\" | \"failed\"\n$outcome->message;  // renderable, already decoded\n```\n```python tab=\"python\" title=\"check\"\noutcome = paylod.check(payment_id)\noutcome.status   # \"pending\" | \"succeeded\" | \"cancelled\" | \"failed\"\noutcome.message  # renderable, already decoded\n```\n```java tab=\"java\" title=\"check\"\nPaymentOutcome outcome = paylod.check(paymentId);\noutcome.getStatus();   // \"pending\" | \"succeeded\" | \"cancelled\" | \"failed\"\noutcome.getMessage();  // renderable, already decoded\n```\n```kotlin tab=\"kotlin\" title=\"check\"\nval outcome = paylod.check(paymentId)\noutcome.status    // \"pending\" | \"succeeded\" | \"cancelled\" | \"failed\"\noutcome.message   // renderable, already decoded\n```\n```bash tab=\"curl\" title=\"GET /status/:id\"\ncurl https://paylod.dev/functions/v1/status/e69e5c00-8a01-44ed-b003-48e2e86a7c9e \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\"\n# → 200 { \"id\": \"...\", \"status\": \"success\", \"mpesaReceipt\": \"UG1F3A1U7J\", ... }\n```\n\n## Verify a webhook\n\nPass the **raw** request bytes. Do not pass an object that you serialised again. Each SDK verifies the `x-webhook-signature` header and returns a typed event. The header is an HMAC-SHA256 over `t + \".\" + rawBody`. The [Webhooks](/docs/api/webhooks) page documents this scheme.\n\n```ts tab=\"node\" title=\"verifyWebhook\"\nconst event = paylod.verifyWebhook({\n  payload: rawBody,\n  signature: headers[\"x-webhook-signature\"],\n  secret: process.env.PAYLOD_WEBHOOK_SECRET,\n});\n```\n```php tab=\"php\" title=\"verifyWebhook\"\n$event = $paylod->verifyWebhook(\n    payload: $rawBody,\n    signature: $_SERVER['HTTP_X_WEBHOOK_SIGNATURE'],\n    secret: getenv('PAYLOD_WEBHOOK_SECRET'),\n);\n```\n```python tab=\"python\" title=\"verify_webhook\"\nevent = paylod.verify_webhook(\n    payload=raw_body,\n    signature=request.headers[\"x-webhook-signature\"],\n    secret=os.environ[\"PAYLOD_WEBHOOK_SECRET\"],\n)\n```\n```java tab=\"java\" title=\"verifyWebhook\"\nWebhookEvent event = paylod.verifyWebhook(\n    rawBody,\n    request.getHeader(\"x-webhook-signature\"),\n    System.getenv(\"PAYLOD_WEBHOOK_SECRET\"));\n```\n```kotlin tab=\"kotlin\" title=\"verifyWebhook\"\nval event = paylod.verifyWebhook(\n    payload = rawBody,\n    signature = request.header(\"x-webhook-signature\"),\n    secret = System.getenv(\"PAYLOD_WEBHOOK_SECRET\"),\n)\n```\n\n## Decode an error\n\n`decodeError` turns a raw M-Pesa result code into a decoded error that you can show. `decodeError` works offline and makes no network call. You rarely need `decodeError` directly, because `check` and `collectAndWait` already return a decoded `message`. Use `decodeError` for records, dashboards and support tools. The [error reference](/docs/errors) shows the full catalogue.\n\n```ts tab=\"node\" title=\"decodeError\"\nconst err = paylod.decodeError(1032);\n// err.title, err.cause, err.fix, err.category, err.retryable, err.customerMessage\n```\n```php tab=\"php\" title=\"decodeError\"\n$err = $paylod->decodeError(1032);\n// $err->title, $err->cause, $err->fix, $err->category, $err->retryable, $err->customerMessage\n```\n```python tab=\"python\" title=\"decode_error\"\nerr = paylod.decode_error(1032)\n# err.title, err.cause, err.fix, err.category, err.retryable, err.customer_message\n```\n```java tab=\"java\" title=\"decodeError\"\nDecodedError err = paylod.decodeError(1032);\n// err.getTitle(), err.getCause(), err.getFix(), err.getCategory(), err.isRetryable(), err.getCustomerMessage()\n```\n```kotlin tab=\"kotlin\" title=\"decodeError\"\nval err = paylod.decodeError(1032)\n// err.title, err.cause, err.fix, err.category, err.retryable, err.customerMessage\n```\n\n## Plugins\n\n| Plugin | Platform | Status | Repository |\n| --- | --- | --- | --- |\n| paylod for WooCommerce | WordPress / WooCommerce | In development — not yet downloadable | [github.com/mosesmrima/paylod-woocommerce](https://github.com/mosesmrima/paylod-woocommerce) |\n\nDo not install the WooCommerce plugin on a live store. paylod did not release the plugin yet. The repository above is public to read, but you cannot download the plugin.\n\nThe WooCommerce plugin adds M-Pesa as a checkout method. You install the plugin and paste an `mp_live_…` key. The plugin then handles the STK Push, the hosted callback and the order-status update. You write no code. Until paylod releases the plugin, a WordPress store can call the [HTTP API](/docs/api). The store can make that call from a small snippet or from any server-side hook.\n\n## Next\n\n- For the most complete per-method reference, see the [Node SDK](/docs/sdk).\n- For the HTTP endpoints below each SDK, see the [API reference](/docs/api).\n- To work in the terminal, use the [CLI](/docs/cli). The CLI sends the same STK Push in one command.\n- For each decoded result code, see the [error reference](/docs/errors).\n\n---\n\n# Node SDK for M-Pesa: @paylod/node\n\n@paylod/node is the official Node/TypeScript client: STK Push, status polling with backoff, signed webhook verification and offline error decoding, with zero runtime dependencies.\n\n`@paylod/node` is the official Node/TypeScript client. It wraps the same HTTP API that the [API reference](/docs/api) documents. The client also adds the parts that almost every integration must write:\n\n- phone-number normalisation\n- idempotency keys\n- a polling loop with backoff\n- webhook signature verification\n- an offline result-code decoder\n\nThe client has **zero runtime dependencies**. It works on Node 18+, Bun, Deno, Vercel, Cloudflare Workers and any other runtime with a global `fetch`.\n\nThis page is the Node reference. Node is the only client that paylod publishes today. The PHP, Python, Java and Kotlin clients are still in development, and so is the WooCommerce plugin. These clients will mirror this same surface; see [SDKs & libraries](/docs/sdks). Until paylod releases them, call the [HTTP API](/docs/api) directly from those languages. Source: [github.com/mosesmrima/paylod-sdk](https://github.com/mosesmrima/paylod-sdk).\n\n```bash tab=\"node\" title=\"npm\"\nnpm install @paylod/node\n```\n```text tab=\"php\" title=\"PHP — not released yet\"\nThe PHP SDK is not on Packagist yet. Call the HTTP API directly — see the cURL tab.\n```\n```text tab=\"python\" title=\"Python — not released yet\"\nThe Python SDK is not on PyPI yet. Call the HTTP API directly — see the cURL tab.\n```\n```text tab=\"java\" title=\"Java — not released yet\"\nThe Java SDK is not on Maven Central yet. Call the HTTP API directly — see the cURL tab.\n```\n```text tab=\"kotlin\" title=\"Kotlin — not released yet\"\nThe Kotlin SDK is not on Maven Central yet. Call the HTTP API directly — see the cURL tab.\n```\n```bash tab=\"curl\" title=\"No dependency\"\n# Nothing to install — any HTTP client works.\nexport PAYLOD_API_KEY=mp_test_YOUR_API_KEY\n```\n\n## Server-side only\n\nThe API key can move money. `@paylod/node` is a server library. Call it from your backend, a serverless function, or an edge worker. Never call it from a browser or a mobile app.\n\n> [!warn] Never put an API key in client-side JavaScript. Every visitor downloads that key, and the key is then fully compromised. If you need a browser checkout, make the browser call *your* server, and let your server call paylod. See [Secure integration](/docs/guides/security).\n\n## Set up the client\n\nOne argument: your API key.\n\n```ts title=\"paylod.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nexport const paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n```\n\nThat is the whole configuration. There is no base URL to pass, no OAuth token to get and refresh, and no callback URL to host. If you omit the argument, the client reads `PAYLOD_API_KEY` from the environment:\n\n```ts title=\"Also fine\"\nconst paylod = new Paylod();\n```\n\nIn both cases the client throws a `PaylodConfigError` immediately when it finds no key. It does not give you a client that fails on its first call.\n\nTimeouts, retries and the other options have sensible defaults. You can override them, but you rarely need to. See [Options](#options) at the end of this page.\n\n## Collect a payment\n\n```ts title=\"collect.ts\"\nconst outcome = await paylod.collectAndWait({\n  amount: 100,\n  phone: \"0712345678\",\n  idempotencyKey: attempt.id,      // one key per payment attempt — a double-click cannot charge twice\n});\n\nif (outcome.paid) {\n  await fulfil(outcome.receipt);   // \"UG1F3A1U7J\"\n} else {\n  console.log(outcome.message);    // \"That M-Pesa PIN was incorrect. Please try again …\"\n}\n```\n\n`collectAndWait()` sends the STK Push and polls until the payment settles. You get back a `PaymentOutcome`. This flat object holds a customer-facing `message` in decoded form, and a `retryable` flag for your retry button.\n\n> [!warn] **Pass `idempotencyKey`, and make one key for each payment attempt.** Duplicates of one attempt collapse into **one** prompt and **one** charge: a double-clicked Pay button, a refreshed tab, or a redelivered job. If you omit the key, every call makes a new charge. Do **not** key on the order id or the product id. Such a key replays an old payment instead of a new one. A retry after a wrong PIN is a new charge and needs a **new** key. See [Idempotency](#idempotency).\n\nA wrong PIN is a business outcome, not a crash. The client returns it as data, and does not throw an error.\n\nThe client normalises phone numbers for you. `0712345678`, `+254712345678`, `254712345678` and `712345678` all work.\n\n## The outcome\n\nEvery method that settles a payment returns the same shape.\n\n```ts title=\"PaymentOutcome\"\n{\n  status: \"succeeded\" | \"pending\" | \"cancelled\" | \"failed\";\n  message: string;        // customer-facing, already decoded. Render this.\n  retryable: boolean;     // SAFE TO CHARGE AGAIN. Gate your retry button on this.\n  paid: boolean;          // the one branch a backend needs\n  receipt: string | null; // M-Pesa confirmation code; non-null exactly when `paid`\n  code: string | null;    // raw result code, for your logs\n  detail: DecodedError | null;\n  payment: Payment;\n}\n```\n\nYour whole UI:\n\n```tsx title=\"Checkout.tsx\"\n<p>{outcome.message}</p>\n{outcome.retryable && <button onClick={retry}>Try again</button>}\n```\n\nDo not `switch` on result codes. Your app needs no error table. To decide what to *show* a person, use `message` and not `code`. The `code` and `detail` fields are for your records and for support tools.\n\n> [!warn] Gate the retry button on `retryable`. `retryable` means **safe to charge again**. It does not mean that the customer is allowed to press a button. A `pending` payment is never retryable, because the STK prompt is still live on the handset. A second collect call sends a second prompt, which can charge the customer twice.\n\n## Methods\n\n### `collect(params)`\n\nThe client sends the STK Push and returns as soon as the prompt reaches the handset. The payment is `pending`.\n\n```ts title=\"collect\"\nconst ack = await paylod.collect({\n  amount: 100,                      // whole KES, 1–150000. M-Pesa rejects decimals.\n  phone: \"0712345678\",\n  idempotencyKey: attempt.id,       // PASS THIS — one per payment attempt (see below)\n  accountReference: \"INV-2041\",     // optional, ≤ 12 chars — YOUR correlation id (see below)\n  description: \"Order #2041\",       // optional, ≤ 64 chars — shown on the STK prompt\n  metadata: { orderId: \"2041\" },    // optional, opaque; stored with the payment\n});\n\nack.paymentId;          // \"e69e5c00-…\"\nack.checkoutRequestId;  // \"ws_CO_…\"\nack.idempotencyKey;     // the key that was sent — yours, or a random one if you passed none\n```\n\n`idempotencyKey` collapses duplicate deliveries of one payment attempt into a single charge. Make one key for each attempt and store it. See [Idempotency](#idempotency) for what the key guarantees, what it does **not** guarantee, and what happens if you omit it.\n\nThe client validates `amount`, `phone`, `accountReference` and `description` locally, before the request leaves your process. A wrong amount fails immediately in your own stack trace, and not as a `422` one round trip later.\n\n`accountReference` is a **correlation id that you choose**. It is not a message to the customer. It comes back as `accountRef` on the webhook, so you use it to tie a payment to your order. The `status()` read returns only `id`, `status`, `mpesaReceipt`, `resultCode` and `resultDesc`, and not `accountRef`. Safaricom shows `accountReference` to the customer only on a **Paybill** (`CustomerPayBillOnline`), where it is the account number. On a **Till / Buy Goods** shortcode Safaricom does not show it at all: the customer sees your business name and the amount.\n\n`description` is the field that reaches the STK prompt. If you omit `accountReference`, it defaults to a short prefix of the `paymentId`. That default is still unique, and you can still trace it back to the payment.\n\n`accountReference` is only a **label**. Your order id in that field does not remove any duplicate. `idempotencyKey` is the field that collapses duplicates of one attempt. Put your order id in `accountReference`, and a per-attempt key in `idempotencyKey`. The two fields do different jobs.\n\n### `status(paymentId)`\n\nThis method reads a payment. If the payment is still pending, paylod runs a live M-Pesa STK Query and settles the payment before it answers.\n\n```ts title=\"status\"\nconst payment = await paylod.status(ack.paymentId);\n\npayment.status;        // \"pending\" | \"success\" | \"failed\"\npayment.mpesaReceipt;  // \"UG1F3A1U7J\" on success, otherwise null\npayment.resultCode;    // 0 | 1032 | 2001 | …\npayment.resultDesc;\n```\n\n### `check(paymentId)`\n\nThis method reads a payment and decodes it. It returns a `PaymentOutcome` that you can show.\n\n```ts title=\"check\"\nconst outcome = await paylod.check(ack.paymentId);\n\noutcome.status;     // \"pending\" | \"succeeded\" | \"cancelled\" | \"failed\"\noutcome.message;    // \"Check your phone and enter your M-Pesa PIN to complete this payment.\"\noutcome.retryable;  // false while pending\n```\n\nA polling endpoint in your own app should return this shape to your frontend.\n\n### `wait(paymentId, options)`\n\nThis method polls an existing payment until the payment settles. It uses a jittered backoff ramp (1s → 5s). It returns a `PaymentOutcome`.\n\n```ts title=\"wait\"\nconst outcome = await paylod.wait(ack.paymentId, {\n  timeoutMs: 120_000,   // default. STK prompts expire around 60s; PIN entry takes 20–30s more.\n  onPoll: (p) => console.log(\"still\", p.status),\n  signal: controller.signal,\n});\n```\n\n`wait()` decides that a payment settles from the result **code**, and not from the raw status field. Daraja reports `4999` on a payment that it also marks `failed`. That code means the prompt is still live. `wait()` continues to poll, and does not report a failure for a payment that can still succeed.\n\nIf the payment is **still pending** at the deadline, `wait()` throws a `PaylodTimeoutError`. That error is not a failure, and paylod deliberately does not fold it into `status: \"failed\"`. The customer can still type the PIN. Leave the order open.\n\n### `collectAndWait(params, options)`\n\nThis method calls `collect()` and then `wait()`. Most integrations want this one call. It takes the same `params` as `collect()`, and this includes `idempotencyKey`. Pass one `idempotencyKey` for each payment attempt.\n\n### `pendingOutcome(paymentId)`\n\nThis function returns the form that your UI can show for a prompt that you just sent. `collect()` gives you an ack, but a prompt on a handset is a pending payment. Your UI shows that payment like any other state.\n\n```ts title=\"pendingOutcome\"\nimport { pendingOutcome } from \"@paylod/node\";\n\nconst ack = await paylod.collect({ amount: 100, phone });\nreturn pendingOutcome(ack.paymentId);   // same shape as check() and wait()\n```\n\n### `decodeError(resultCode, rawDesc?)`\n\nThis method decodes an M-Pesa result code offline. It makes no network call, and it needs no API key at call time.\n\n```ts title=\"decodeError\"\nconst err = paylod.decodeError(1032);\n\nerr.title;            // \"Payment cancelled by the customer\"\nerr.cause;            // \"The customer received the STK prompt but pressed Cancel …\"\nerr.fix;              // \"Nothing is wrong with your setup — offer a clear retry …\"\nerr.category;         // \"customer\"\nerr.retryable;        // true\nerr.customerMessage;  // \"Payment cancelled — you can try again whenever you're ready.\"\n```\n\nThese strings are byte-for-byte the strings that paylod puts in the `decoded` object on a `payment.failed` webhook. What you show a customer is therefore the same in both cases. You can browse the whole catalogue in the [error reference](/docs/errors).\n\npaylod also exports `decodeError` as a standalone function, together with the raw `ERROR_CATALOG`:\n\n```ts title=\"Standalone\"\nimport { decodeError, ERROR_CATALOG } from \"@paylod/node\";\n```\n\n### `4999` and `500.001.1001` are pending, not failed\n\nDaraja's STK Query returns `4999` (\"the transaction is still under processing\") while the customer still looks at the PIN prompt. paylod classifies `4999` and `500.001.1001` as **pending**. Therefore `status()` continues to report `pending`, and `wait()` continues to poll.\n\nIf you decode one of these two codes directly, `category` is `\"pending\"` and `retryable` is `false`. `retryable` is `false` because the first prompt is still live, and a second push can charge the customer twice. It is not `false` because a retry is pointless. Do not show a customer a failure on these codes, and do not collect again. Continue to poll.\n\n## Test your checkout without a phone\n\nPayment bugs live in your failure paths, and until now a test of those paths needed a real handset and a wrong PIN. `paylod.simulate` removes the handset. It removes nothing else. You get a real payment row, the real Daraja result codes, and the real settlement path. paylod also sends a real signed webhook to your webhook endpoint. Only the handset is not real.\n\n`paylod.simulate` requires an `mp_test_…` key. The client refuses a `mp_live_…` key **locally**, before it sends a request. A simulator must never touch production.\n\n```ts title=\"checkout.test.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_TEST_KEY!);   // mp_test_…\n\nconst outcome = await paylod.simulate.pay({ outcome: \"wrong_pin\" });\n\noutcome.status;     // \"failed\"\noutcome.message;    // \"That M-Pesa PIN was incorrect. Please try again and enter the right PIN.\"\noutcome.retryable;  // true — no money moved, so a fresh charge is safe\n```\n\nThat is an ordinary `PaymentOutcome`. It is the same object that `check()` and `wait()` return. There is no \"simulated\" type and no special branch. The code that you test is therefore the code that runs in production.\n\n### The five outcomes\n\n`outcome` is a typed union. A typo is therefore a compile error, and not a `422` that you find in CI.\n\n| `outcome` | `status` | Result code | `retryable` |\n| --- | --- | --- | --- |\n| `approve` | `succeeded` | `0` | `false` (it worked) |\n| `wrong_pin` | `failed` | `2001` | `true` |\n| `insufficient_funds` | `failed` | `1` | `true` |\n| `user_cancelled` | `cancelled` | `1032` | `true` |\n| `timeout` | `failed` | `1037` | `true` |\n\n`SIM_OUTCOMES` (or `paylod.simulate.outcomes`) is the whole list. You can loop over it:\n\n```ts title=\"Drive every path\"\nfor (const outcome of paylod.simulate.outcomes) {\n  const result = await paylod.simulate.pay({ outcome });\n  expect(render(result)).toMatchSnapshot();\n}\n```\n\n> [!note] `timeout` here is Daraja's `1037`, which means that M-Pesa could not reach the handset. That result is a **settled** failure. It is not `PaylodTimeoutError`. `wait()` throws `PaylodTimeoutError` when a payment is still `pending` at your deadline. An indeterminate payment is not a failed payment.\n\n### `simulate.collect()` and `simulate.outcome()`\n\nSplit the simulation in two parts, and put *your* code in the middle. The payment id is a real id, so your poller, your webhook route and your UI all run unchanged:\n\n```ts title=\"Testing your own code\"\nconst sim = await paylod.simulate.collect({ amount: 250 });\n\nawait readCheckout(sim.paymentId);   // your code — sees a genuinely pending payment\nawait paylod.simulate.outcome(sim.paymentId, \"insufficient_funds\");\nconst view = await readCheckout(sim.paymentId);   // your code — sees the settled failure\n\nexpect(view.message).toMatch(/balance is too low/);\n```\n\nA simulated payment stays `pending` until you force an outcome. A live prompt behaves the same way while a customer looks at it. You can settle a simulated payment only once. A second call gets a `409`, the same as a real handset.\n\n### How to test your charge path: `{ simulate: true }`\n\nThe steps above still do not exercise your own `collect()` call. Construct the client with `simulate: true`. Then `collect()` and `collectAndWait()` create a simulated payment instead of a real STK Push to a handset. Your `/api/pay` handler therefore runs **completely unchanged**:\n\n```ts title=\"Your handler, under test\"\nconst paylod = new Paylod(process.env.PAYLOD_TEST_KEY!, { simulate: true });\n\nconst view = await startCheckout(order.id, \"0712345678\", attemptId);   // your handler, verbatim\nawait paylod.simulate.outcome(view.paymentId!, \"user_cancelled\");      // no handset involved\nexpect((await readCheckout(view.paymentId!)).status).toBe(\"cancelled\");\n```\n\nThe constructor throws `PaylodSandboxOnlyError` if the key is not `mp_test_…`. This flag can therefore never point at production, even by accident.\n\n> [!note] The simulator obeys `Idempotency-Key` with the **same semantics as production**. The same key returns the same `paymentId` and creates no second payment. Concurrent duplicates collapse into one payment. A different key creates a new payment. A test that asserts \"a double-click cannot charge twice\" therefore tests the real behaviour, and not a stub of it.\n\n## Idempotency\n\nAn idempotency key names **one payment attempt**. Duplicate deliveries of that one attempt collapse into a single charge: a double-click, a refreshed tab, a job-queue redelivery, or an internal network retry. That is the guarantee. Understand it precisely, because it is narrower than \"a reused key is always a safe retry\".\n\n```ts title=\"One key per payment attempt\"\nconst attempt = await db.attempts.create({ orderId: order.id });   // a row per press of Pay\nawait paylod.collectAndWait({ amount, phone, idempotencyKey: attempt.id });\n```\n\n### Pass a key per attempt — not per order, and not per product\n\nThe key must be **stable across duplicates of one attempt** and **fresh for a genuinely new charge**. An order id is stable, but it is not fresh. A product id is not fresh either, and many customers share it:\n\n| Key you pass | What happens |\n| --- | --- |\n| An id minted per **payment attempt** | Correct. Duplicates of that attempt collapse into one charge. A new attempt is a new charge. |\n| Your **order id** | The customer types a wrong PIN, and you retry the same order. paylod then replays the **failed** first attempt and charges nobody. The order never gets paid. |\n| A **product id** (or any value reused across purchases) | Catastrophic. Every customer after the first one replays the **first-ever** payment for that product. paylod charges nobody after customer one. |\n| `crypto.randomUUID()` **per call** | The same as no key: a double-click makes two keys, two prompts, and two charges. |\n\nOne rule resolves all four rows. **Make the key when a payment attempt starts. Store the key on that attempt. Never reuse it for a different charge.** A retry after a wrong PIN, a cancelled prompt or a timeout is a *new attempt*, with a new row and a new key.\n\nA missing key is a money bug. The SDK therefore writes a one-time `console.warn` when you call `collect()` or `collectAndWait()` without a key.\n\n> [!warn] Do **not** silence that warning with `idempotencyKey: crypto.randomUUID()` or `Date.now()` **at the call site**. A key that changes on every call is exactly the same as no key at all. Such a key only hides the warning that tells you the customer is at risk. A random UUID is a good key, but you must make it **once for each attempt** and store it. Do not generate the key inside the call.\n\n### A concurrent double-click cannot double-charge\n\nThis part is unconditional. Send ten simultaneous requests with the same `Idempotency-Key`, and you get **one** payment and **one** STK push. paylod reserves the key before it calls the provider, so exactly one request wins. The other requests wait for the answer of the winner and then replay that answer. All ten calls come back with the same `paymentId`. The handset receives only one prompt.\n\n### The one case where the same key is *not* a safe retry\n\nA request can die mid-flight against Daraja: after paylod hands the call to the provider, and before an answer comes back. That key is then **spent**. A retry under that key does not silently send the request again. It returns `409` with an **indeterminate** message:\n\n```text\nA previous request with this Idempotency-Key was interrupted while the provider call was\nin flight, so it may or may not have completed. We will not repeat it — that could charge\nor pay twice. Check the payment/disbursement status; if nothing happened, retry with a NEW key.\n```\n\nA timeout is not evidence that the money did not move. It is the absence of evidence. paylod therefore refuses to guess. **For money, at-most-once is better than at-least-once.** paylod makes you check the status, rather than charge someone twice.\n\n> [!warn] **If you get the indeterminate `409`, do not retry with the same key.** Read the payment status first, with `paylod.check(paymentId)`, `GET /status/:id`, or your webhook endpoint. Then decide. If the payment did settle, you are done. If nothing happened, start a **new attempt with a new key**. A retry under the spent key returns the same `409` every time.\n\n### The rules\n\n- **Same key + same body, already settled** → paylod replays the original payment. You get the same `paymentId` and the same `checkoutRequestId`. There is no second prompt and no second debit.\n- **Same key + same body, first request still in flight** → `409` with a `Retry-After`. For a plain double-click, the SDK waits for the winner and gives you its response.\n- **Same key + different body** → `409`, which the SDK shows as `PaylodApiError` with `.isIdempotencyConflict === true`. This is always a bug on your side: two different charges collided on one key. For example, you changed the amount but kept the key.\n- **Same key, previous attempt interrupted against the provider** → `409` **indeterminate**. Read the status, then retry with a **new** key. paylod never sends that request again.\n- **Internal retries** (network blip, `5xx`, `429`) reuse the same key automatically. That reuse is what makes a retry of a `POST` safe. If the interruption happened against Daraja, that retry returns the indeterminate `409` instead of a second charge.\n\nIf you did not pass a key, and you want to retry the *same attempt* later, store the key that you got back:\n\n```ts title=\"Persist the key with the attempt\"\nconst ack = await paylod.collect({ amount, phone });\nawait db.attempts.update(attempt.id, { idempotencyKey: ack.idempotencyKey });\n// Retrying THAT attempt with THAT key collapses into the original payment.\n// A genuinely new attempt gets a new key — reusing this one would replay the old result.\n```\n\nDo you call the HTTP API directly? Use the `Idempotency-Key` request header. It behaves in the same way.\n\n## Webhooks (optional)\n\nYou do not need a webhook to take a payment. `collectAndWait()` polls for you, and `check()` lets your own frontend poll. That is a complete, supported integration, and it needs nothing but `PAYLOD_API_KEY`.\n\nAdd a webhook when you want paylod to tell your **server** about a payment that your server does not wait on. For example, the customer closed the tab right after PIN entry, your request timed out but the money landed, or your process restarted. Polling tells the browser, and a webhook tells your backend. If you have an order to fulfil, you will want both.\n\nSet the signing secret when you add a webhook:\n\n```bash title=\".env\"\nPAYLOD_WEBHOOK_SECRET=whsec_...   # only if you consume webhooks\n```\n\n### Web `Request`/`Response` — Next.js, Hono, Remix, Workers, Bun, Deno\n\n```ts title=\"app/api/webhooks/paylod/route.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod();\n\nexport const POST = paylod.webhookHandler(async (event) => {\n  if (event.type === \"payment.success\") {\n    await fulfil(event.data.paymentId, event.data.mpesaReceipt);\n  } else if (event.data.decoded) {\n    await notify(event.data.decoded.customerMessage);\n  }\n});\n```\n\n`event.data.decoded` is `DecodedError | null`. paylod populates it on `payment.failed`, and sets it to `null` on `payment.success`. A check on `event.type` does not narrow that field, so check the field before you read it.\n\npaylod verifies the signature before your handler runs. A wrong signature returns `400`, and paylod never calls your handler. If your handler throws, the route returns `500` and paylod retries the delivery. A duplicate is better than a lost payment, so make your handler idempotent.\n\n### Express\n\n```ts title=\"server.ts\"\napp.post(\"/webhooks/paylod\", paylod.webhook(async (event) => {\n  if (event.type === \"payment.success\") await fulfil(event.data.paymentId);\n}));\n```\n\n> [!warn] Mount this route **before** any global `express.json()`, or give it `express.raw({ type: \"application/json\" })`. A JSON parser turns the body into an object and destroys the raw bytes. paylod cannot verify the signature without the raw bytes. The middleware detects this problem and returns a `400`, rather than skip the verification.\n\n### Verify it yourself\n\n```ts title=\"verifyWebhook\"\nconst event = paylod.verifyWebhook({\n  payload: rawBody,                            // string | Buffer | Uint8Array — the RAW bytes\n  signature: headers[\"x-webhook-signature\"],\n  toleranceSec: 300,                           // default\n});\n```\n\n`verifyWebhook()` throws `PaylodSignatureVerificationError` if the signature does not verify. [Webhooks](/docs/api/webhooks) documents the signature scheme.\n\n## Errors\n\nEvery error that the client throws extends `PaylodError`.\n\n| Class | When |\n| --- | --- |\n| `PaylodConfigError` | No API key, or no global `fetch`. The constructor throws it. |\n| `PaylodSandboxOnlyError` | A `simulate` call (or `{ simulate: true }`) with a key that is not `mp_test_…`. The client throws it locally and sends no request. It extends `PaylodConfigError`. |\n| `PaylodInvalidRequestError` | Local validation failed: a wrong amount, or a phone number that the client cannot parse. The client sent no request. |\n| `PaylodApiError` | paylod returned a non-2xx status. It carries `.status` and the parsed body. |\n| `PaylodConnectionError` | The request never got an answer (DNS, TLS, socket). The client retries it automatically. |\n| `PaylodTimeoutError` | `wait()` hit its deadline with the payment still `pending`. This is not a failure. |\n| `PaylodSignatureVerificationError` | A webhook signature did not verify. |\n\nThe client retries transient failures automatically, with backoff: network blips, `429` and `5xx`. `400`, `401`, `404`, `409` and `422` are real answers, and the client throws them immediately.\n\n## A complete example\n\nThis is a safe browser checkout. The page calls your server, and your server calls paylod. The key never leaves the server.\n\n```ts title=\"server.ts\"\nimport { Paylod, pendingOutcome } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n\n// POST /api/pay → ring the phone.\n// Takes an orderId, never an amount: the price is looked up here, so a payer\n// with a proxy cannot discount their own order.\nasync function startPayment(orderId: string, phone: string) {\n  const order = await getOrder(orderId);\n\n  // One row per press of Pay — that row's id IS the idempotency key.\n  // Double-click Pay and the second call replays the first payment instead of\n  // sending a second STK prompt. Retry after a wrong PIN and you get a NEW\n  // attempt, a NEW key, and a real second charge — which is what a retry means.\n  // Keying on `order.id` would replay the FAILED attempt forever; keying on a\n  // product id would replay the first customer's payment to everyone after them.\n  const attempt = await db.attempts.create({ orderId: order.id });\n\n  const ack = await paylod.collect({\n    amount: order.amount,\n    phone,\n    idempotencyKey: attempt.id,\n  });\n  return pendingOutcome(ack.paymentId);\n}\n\n// GET /api/pay/:id → has it settled? Already decoded, ready to render.\nasync function readPayment(paymentId: string) {\n  return paylod.check(paymentId);\n}\n```\n\nBoth functions return the same `PaymentOutcome`. Your frontend therefore has one shape to show, and no result codes to interpret.\n\nThat is the whole integration. The [paylod demo](https://github.com/paylod/demo) is a working version of exactly this integration. It includes the retry path and a verified webhook.\n\n## Options\n\nYou do not normally need any of these options. The defaults are the right answer.\n\n```ts title=\"Escape hatches\"\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!, {\n  timeoutMs: 30_000,\n  maxRetries: 2,\n});\n```\n\n| Option | Default | What it is for |\n| --- | --- | --- |\n| `apiKey` | `process.env.PAYLOD_API_KEY` | Authentication. The only required value. |\n| `timeoutMs` | `30_000` | Per HTTP request. |\n| `maxRetries` | `2` | Transient failures only (network, `429`, `5xx`). |\n| `webhookSecret` | `process.env.PAYLOD_WEBHOOK_SECRET` | Only if you consume webhooks. |\n| `baseUrl` | `https://paylod.dev/functions/v1` | paylod sets this value. Override it only when you self-host, or when you point the client at a stub in tests. |\n| `fetch` | global `fetch` | Inject an instrumented or proxied fetch. |\n| `simulate` | `false` | Tests only. `collect()` creates a simulated payment instead of a real STK Push to a handset. It requires an `mp_test_…` key; otherwise the constructor throws. See [Test your checkout without a phone](#test-your-checkout-without-a-phone). |\n\n| Environment variable | Used for |\n| --- | --- |\n| `PAYLOD_API_KEY` | Authentication. Required. |\n| `PAYLOD_WEBHOOK_SECRET` | `verifyWebhook()`, `webhook()`, `webhookHandler()`. Optional. |\n| `PAYLOD_BASE_URL` | An override of the API base URL. Rarely needed. |\n\n## Next\n\n- Do you prefer the terminal? The [CLI](/docs/cli) sends the same STK Push in one command.\n- The HTTP endpoints underneath: [API reference](/docs/api).\n- Every result code, decoded: [error reference](/docs/errors).",
  },
  {
    id: "auth",
    title: "Authentication, environments, idempotency and rate limits",
    keywords: ["auth","authentication","key","apikey","bearer","token","base","url","idempotency","idempotent","mp_live","mp_test","429","rate","limit","environment","sandbox"],
    aliases: [],
    content: "# Authenticating with the paylod M-Pesa API\n\nAPI keys, environments, the base URL, idempotency and rate limits — everything every paylod request has in common.\n\n## Base URL\n\nEvery endpoint is under `/functions/v1`.\n\n```text title=\"Base URL\"\nhttps://paylod.dev/functions/v1\n```\n\n## API keys\n\nAuthenticate every request with a paylod API key as a bearer token.\n\n```http title=\"Header\"\nAuthorization: Bearer mp_live_YOUR_API_KEY\n```\n\nCreate API keys on the application's **API Keys** tab. paylod shows an API key once. The API key encodes two things:\n\n- **The application** that the request runs against. You therefore never send a shortcode.\n- **The environment** that the request runs in. You therefore never send a `?sandbox=true` flag.\n\n| Prefix | Environment | Money |\n| --- | --- | --- |\n| `mp_test_…` | Sandbox | Simulated. Nothing settles. |\n| `mp_live_…` | Production | Real. The money settles to your till. |\n\nYour Daraja consumer key, secret and passkey stay in encrypted storage on the server. They never travel over the network. No endpoint reads them back.\n\n> [!warn] Keep every API key on the server. An API key in a browser or in a mobile app is fully compromised. Treat that API key as public and rotate it. See [Secure integration](/docs/guides/security).\n\n## Idempotency\n\n`POST /collect` accepts an `Idempotency-Key` header. It also accepts an `idempotencyKey` field in the body. The idempotency key names **one payment attempt**. Send the idempotency key, and duplicate deliveries of that attempt collapse into a single payment and a single STK Push. A double-click, a refreshed tab, a redelivered job and an internal network retry are all duplicate deliveries.\n\n```bash tab=\"curl\" title=\"Idempotent collect\"\ncurl -X POST https://paylod.dev/functions/v1/collect \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Idempotency-Key: att_9f2c1b40\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{ \"amount\": 10, \"phone\": \"254712345678\" }'\n```\n```ts tab=\"node\" title=\"Idempotent collect\"\n// The SDK sends an Idempotency-Key on every collect() — pass one per attempt.\nconst ack = await paylod.collect({\n  amount: 10,\n  phone: \"254712345678\",\n  idempotencyKey: attempt.id,\n});\n```\n```php tab=\"php\" title=\"Idempotent collect\"\n// The SDK sends an Idempotency-Key on every collect() — pass one per attempt.\n$ack = $paylod->collect([\n    'amount' => 10,\n    'phone' => '254712345678',\n    'idempotencyKey' => $attempt->id,\n]);\n```\n```python tab=\"python\" title=\"Idempotent collect\"\n# The SDK sends an Idempotency-Key on every collect() — pass one per attempt.\nack = paylod.collect(\n    amount=10,\n    phone=\"254712345678\",\n    idempotency_key=attempt.id,\n)\n```\n```java tab=\"java\" title=\"Idempotent collect\"\n// The SDK sends an Idempotency-Key on every collect() — pass one per attempt.\nCollectAck ack = paylod.collect(\n    CollectParams.builder()\n        .amount(10)\n        .phone(\"254712345678\")\n        .idempotencyKey(attempt.id())\n        .build());\n```\n```kotlin tab=\"kotlin\" title=\"Idempotent collect\"\n// The SDK sends an Idempotency-Key on every collect() — pass one per attempt.\nval ack = paylod.collect(\n    amount = 10,\n    phone = \"254712345678\",\n    idempotencyKey = attempt.id,\n)\n```\n\n**Make one idempotency key for each attempt, not for each order and not for each product.** paylod reserves the idempotency key *before* it calls Daraja. Ten simultaneous requests with the same key therefore produce exactly one payment and one push. All ten requests return the same `paymentId`. A key is also spent once you use it. If you key on an order id, a retry of that order replays the failed first attempt. If you key on a product id, every later customer replays the first payment for that product. A retry after a wrong PIN is a **new charge** and needs a **new key**.\n\npaylod rejects a reused idempotency key in three cases. All three cases return `409`:\n\n| Reuse | Response |\n| --- | --- |\n| Same key, **different** body | `409`. Two different charges collided on one idempotency key. This case is a bug in your code. |\n| Same key, first request still in flight | `409` with `Retry-After`. The first request is still in contact with Daraja. Wait, then read the result. |\n| Same key, a previous attempt was **interrupted mid-flight against Daraja** | `409` **indeterminate**. paylod cannot prove that no debit occurred, and paylod does not repeat the call. |\n\n> [!warn] **Do not retry the indeterminate `409` with the same idempotency key.** A timeout is not evidence that the money did not move. paylod therefore does not send the call again. For money, at-most-once is better than at-least-once. Read the payment status first, with `GET /status/:id` or with your webhook. Then decide. If the payment settled, you are done. If nothing happened, start a new attempt with a **new** key.\n\n## Rate limits\n\npaylod limits the rate of `POST /collect` for each API key and for each phone number. A burst returns `429 Too Many Requests`. Back off when you receive a `429`. paylod refuses a `429` *before* the request reaches Daraja. A rate-limited request does not spend the idempotency key. You can therefore retry with the same `Idempotency-Key` after a short delay.\n\n## Errors\n\npaylod returns conventional HTTP status codes.\n\n| Status | Meaning |\n| --- | --- |\n| `400` | The JSON is malformed, or you did not set Daraja credentials for this application and environment. |\n| `401` | The API key is missing or invalid. |\n| `404` | The resource does not exist, or it belongs to another application. |\n| `409` | You reused the idempotency key with a different body. Or the first request is still in flight. Or an interrupted provider call spent the key. The third case is indeterminate: read the status, then retry with a new key. |\n| `422` | The body failed validation. For example, `amount` ≤ 0, or paylod cannot parse the phone number. |\n| `429` | You reached the rate limit. Back off, then retry. |\n| `502` | Daraja rejected the request. paylod passes the upstream error through. |\n\nM-Pesa also has its own result codes: `1032` customer cancelled, `1037` timeout, `2001` wrong PIN. The [error reference](/docs/errors) decodes every one of these codes.",
  },
  {
    id: "payments",
    title: "Take a payment (STK Push)",
    keywords: ["collect","stk","push","payment","payments","pay","charge","amount","phone","checkout","receipt","accept","money"],
    aliases: [],
    content: "# Accept an M-Pesa payment with an STK Push\n\nCollect money from a customer with an M-Pesa STK Push: what to send, what comes back, and what the customer sees.\n\nCharge a customer. Your server sets the amount. The customer's handset shows a prompt, the customer enters the M-Pesa PIN, and the money settles to your till.\n\n## Send the push\n\n> [!warn] **Always pass `idempotencyKey`. Create one idempotency key for each payment attempt.** A double-clicked Pay button, a refreshed tab and a retried job are duplicates of one attempt. Duplicates of one attempt then become **one** prompt and **one** charge. If you do not pass `idempotencyKey`, each call is a new charge: two clicks give two prompts and two debits. Do **not** use the order id or the product id as the idempotency key. A used key replays an old payment, and does not make a new payment. See [Idempotency](#idempotency-one-key-per-payment-attempt).\n\n```ts title=\"charge.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n\nconst outcome = await paylod.collectAndWait({\n  amount: 1500,                     // whole KES, from YOUR order record\n  phone: \"0712345678\",\n  idempotencyKey: attempt.id,       // one key per payment attempt. A double-click cannot charge twice.\n  accountReference: \"INV-2041\",     // YOUR correlation id — comes back on the result\n  description: \"Order #2041\",\n  metadata: { orderId: \"2041\" },    // opaque, stored with the payment\n});\n\nif (outcome.paid) {\n  await fulfil(\"2041\", outcome.receipt);\n} else {\n  await notify(outcome.message);\n}\n```\n\n`collectAndWait()` sends the STK push. Then `collectAndWait()` polls until the payment settles. You need only one condition: `outcome.paid`. If `outcome.paid` is false, `outcome.message` is a decoded sentence that you can show to the customer.\n\nDo you want the `paymentId` immediately? Use `collect()`. `collect()` returns when the prompt is on the handset. See [Handle the result](/docs/guides/handle-the-result).\n\n## What M-Pesa requires\n\n- **Whole shillings.** `amount` is an integer from 1 to 150,000. M-Pesa rejects decimals.\n- **A Kenyan number.** `0712…`, `254712…` and `+254712…` all work. The SDK puts them into a standard format.\n- **One prompt at a time.** You cannot send a second prompt to a customer who already has a prompt.\n\nThe maximum length of `accountReference` is 12 characters. The maximum length of `description` is 64 characters. The SDK checks both fields locally, before the request leaves your process.\n\n## What `accountReference` is\n\n`accountReference` is a **correlation id that you select**, for example an invoice number or an order number. paylod stores `accountReference` on the payment. paylod then returns the value as `accountRef` on the webhook. Thus you use `accountReference` to connect an M-Pesa payment to the order in your own database. The `GET /status/:id` read returns only `id`, `status`, `mpesaReceipt`, `resultCode` and `resultDesc`.\n\n> [!warn] Never put text that the customer must read into `accountReference`. The `description` field supplies the text that the prompt shows. Also, never put a **secret** into `accountReference`, because a payer on a paybill can see the value.\n\n`accountReference` is *not* a message to the customer. Safaricom shows `accountReference` only on a **paybill** (`CustomerPayBillOnline`). On a paybill, `accountReference` is the account number that the payer pays to. On a **till** shortcode (`CustomerBuyGoodsOnline`), Safaricom does not show `accountReference`. The payer sees only your business name and the amount. Most small merchants use a till. Thus you must assume that the customer never reads `accountReference`.\n\nIf you omit `accountReference`, paylod uses a short prefix of the `paymentId` as the default value. The default value is unique, and it connects the payment to your M-Pesa statement.\n\n`accountReference` is a **label, and not a lock**. If you put your order id into `accountReference`, a second charge can still occur. `accountReference` only marks the payment. [`idempotencyKey`](#idempotency-one-key-per-payment-attempt) is the field that collapses duplicates. The two fields have different functions: put the order id into `accountReference`, and put a key for each attempt into `idempotencyKey`.\n\n## Idempotency: one key per payment attempt\n\nA customer double-clicks **Pay**, and your handler runs two times. Without an idempotency key, the customer gets two STK prompts and two debits. Then the customer asks you for a refund and an explanation.\n\nCreate a key when the attempt starts, and two debits cannot occur:\n\n```ts title=\"charge.ts\"\nconst attempt = await db.attempts.create({ orderId: order.id });   // one row per press of Pay\n\nawait paylod.collectAndWait({\n  amount: order.amount,\n  phone,\n  idempotencyKey: attempt.id,   // ← the whole fix\n});\n```\n\nThe two requests carry the same key. paylod reserves the key *before* paylod calls Daraja. Thus only one request reaches Daraja, and the other request replays the first answer. Both requests get the same `paymentId` and the same `checkoutRequestId`. The handset shows only one prompt. This behaviour also applies when the two requests arrive in the same millisecond: ten concurrent calls with one key make one payment and one STK push.\n\n| What the customer does | With a per-attempt `idempotencyKey` | Without a key |\n| --- | --- | --- |\n| Double-clicks **Pay** | 1 prompt, 1 charge | **2 prompts, 2 charges** |\n| Refreshes the tab and re-submits | 1 prompt, 1 charge | **2 prompts, 2 charges** |\n| Your job queue retries the handler | 1 prompt, 1 charge | **2 prompts, 2 charges** |\n\nIf you omit the key, the SDK gives you a warning one time. A missing key is a money defect.\n\n### Per attempt — not per order, not per product\n\nThe key must be **the same for all duplicates of one attempt**. The key must also be **new for a new charge**. Two usual short methods do not obey these two rules:\n\n- **The order id** is the same for all duplicates, but the order id is never new. The customer enters a wrong M-Pesa PIN. You then retry the order with the same key. paylod replays the **failed** first attempt, and does not charge the customer. Thus the order is never paid.\n- **A product id**, or any other value that you use again for a different purchase, is worse. The second customer buys the same product and uses the key that the first customer already spent. paylod then replays the payment of the first customer. **paylod charges no customer after the first customer.**\n\nA retry after a wrong M-Pesa PIN, a cancelled prompt or a timeout is a **new attempt**. A new attempt needs a new row and a new key.\n\n> [!warn] Do **not** stop the SDK warning with `idempotencyKey: crypto.randomUUID()` **at the call site**. A key that you generate again on each call has the same effect as no key. Such a key only hides the warning that tells you the customer is exposed. A random UUID is a good key. But you must create the UUID **one time for each attempt** and store it. Do not generate a new UUID inside the call.\n\n### When the same key is *not* a safe retry\n\nIf you use a key again with a *different* body, for example a different amount, paylod returns `409`. A `409` of this type is always a defect in your code: two different charges use one key.\n\nOne condition looks like a network error, but you must not retry it. A request can stop after paylod sent the call to Daraja and before an answer came back. That key is then **spent**. If you retry the spent key, paylod returns `409` with an *indeterminate* message, and sends no second STK push. The outcome of such a payment is unknown, and a timeout is not proof that the money did not move. Thus paylod does not guess: **for money, at-most-once is better than at-least-once.**\n\n> [!warn] **After an indeterminate `409`, do not retry with the same key.** First read the payment status with `paylod.check(paymentId)` or `GET /status/:id`. Then decide. If the payment settled, fulfil the order. If no payment occurred, open a **new attempt with a new key**. A retry with the spent key returns the same `409`.\n\nYou cannot always supply a key at the start. Then store the generated key with the attempt before you retry:\n\n```ts title=\"retry.ts\"\nconst ack = await paylod.collect({ amount: 1500, phone: \"0712345678\" });\nawait db.attempts.update(attempt.id, { idempotencyKey: ack.idempotencyKey });\n\n// Retrying THAT attempt: same key, same body → the original payment, no second prompt.\nawait paylod.collect({\n  amount: 1500,\n  phone: \"0712345678\",\n  idempotencyKey: ack.idempotencyKey,\n});\n// A genuinely new attempt gets a NEW key. Reusing this one would replay the old result.\n```\n\n## Never take the amount from the client\n\n> [!warn] Always take the amount from your own order record, on your own server. A browser or a mobile app can hold both the API key and the amount. A payer can then change the request and pay less. This is the only method that lets a payer pay you less, and you can prevent it. See [Secure integration](/docs/guides/security).\n\n## Next\n\n[Handle the result](/docs/guides/handle-the-result) describes webhooks. It also tells you what to check before you fulfil an order.\n\n---\n\n# Payments API: STK Push and payment status\n\nPOST /collect to trigger an STK Push and GET /status/:id to read the outcome — the two endpoints that move and read money in.\n\n## POST /collect\n\nStart an STK Push. The customer receives an M-Pesa prompt on the handset. When the customer enters the PIN, the funds settle directly to your till. The endpoint returns a `paymentId` immediately. The final result arrives on the hosted callback. You read that result with `GET /status/:id` or with a [webhook](/docs/api/webhooks).\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/collect\n```\n\n### Headers\n\n| Name | Type | Required | Description |\n| --- | --- | --- | --- |\n| `Authorization` | string | Required | Bearer API key (`mp_live_…` / `mp_test_…`). |\n| `Content-Type` | string | Required | `application/json`. |\n| `Idempotency-Key` | string | **Send it** | The double-charge guard. The idempotency key names **one payment attempt**. Make a new key for each attempt, not for each order and not for each product. If you repeat the same key **and** the same body, paylod returns the **original payment**. paylod does not send a second STK prompt, even for concurrent duplicates. If you repeat the same key with a different body, paylod returns `409`. If an interrupted provider call spent the key, paylod returns `409` *indeterminate*. In that case, read the payment status, then retry with a **new** key. If you omit the idempotency key, every call is a new charge. A double-clicked Pay button then sends two prompts and makes two debits. |\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `amount` | integer | Required | Whole KES, 1–150000. M-Pesa rejects decimals. |\n| `phone` | string | Required | Kenyan MSISDN in any common form (`0712…`, `254712…`, `+254712…`). paylod normalises it for you. |\n| `accountReference` | string | Optional | Your correlation id, such as an invoice number or an order number. 1–12 chars. The webhook returns this value as `accountRef`. The `GET /status/:id` read carries only `id`, `status`, `mpesaReceipt`, `resultCode` and `resultDesc`. Safaricom shows this value to the customer only on a **Paybill** (`CustomerPayBillOnline`), where it is the account number. A **Till / Buy Goods** shortcode does not show it. The default is a short prefix of the `paymentId`, so an omitted reference stays unique and traceable. This value is a **label, not a lock**. It does not deduplicate anything. Only `Idempotency-Key` prevents a second charge. |\n| `description` | string | Optional | Free-text label, 1–64 chars. Defaults to `Payment`. |\n| `metadata` | object | Optional | Opaque keys and values that paylod stores with the payment. paylod never inspects them. `/status` and the webhook do not return them. Key your records on `paymentId`. |\n\n### Example\n\n```bash tab=\"curl\" title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/collect \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Idempotency-Key: att_9f2c1b40\" \\\n  -d '{\n    \"amount\": 10,\n    \"phone\": \"254712345678\",\n    \"accountReference\": \"INV-2041\",\n    \"description\": \"Order #2041\",\n    \"metadata\": { \"orderId\": \"2041\" }\n  }'\n```\n```ts tab=\"node\" title=\"collect\"\nconst ack = await paylod.collect({\n  amount: 10,\n  phone: \"254712345678\",\n  idempotencyKey: attempt.id,\n  accountReference: \"INV-2041\",\n  description: \"Order #2041\",\n  metadata: { orderId: \"2041\" },\n});\n// ack.paymentId, ack.status (\"pending\"), ack.checkoutRequestId\n```\n```php tab=\"php\" title=\"collect\"\n$ack = $paylod->collect([\n    'amount' => 10,\n    'phone' => '254712345678',\n    'idempotencyKey' => $attempt->id,\n    'accountReference' => 'INV-2041',\n    'description' => 'Order #2041',\n    'metadata' => ['orderId' => '2041'],\n]);\n// $ack->paymentId, $ack->status (\"pending\"), $ack->checkoutRequestId\n```\n```python tab=\"python\" title=\"collect\"\nack = paylod.collect(\n    amount=10,\n    phone=\"254712345678\",\n    idempotency_key=attempt.id,\n    account_reference=\"INV-2041\",\n    description=\"Order #2041\",\n    metadata={\"orderId\": \"2041\"},\n)\n# ack.payment_id, ack.status (\"pending\"), ack.checkout_request_id\n```\n```java tab=\"java\" title=\"collect\"\nCollectAck ack = paylod.collect(\n    CollectParams.builder()\n        .amount(10)\n        .phone(\"254712345678\")\n        .idempotencyKey(attempt.id())\n        .accountReference(\"INV-2041\")\n        .description(\"Order #2041\")\n        .metadata(Map.of(\"orderId\", \"2041\"))\n        .build());\n// ack.getPaymentId(), ack.getStatus() (\"pending\"), ack.getCheckoutRequestId()\n```\n```kotlin tab=\"kotlin\" title=\"collect\"\nval ack = paylod.collect(\n    amount = 10,\n    phone = \"254712345678\",\n    idempotencyKey = attempt.id,\n    accountReference = \"INV-2041\",\n    description = \"Order #2041\",\n    metadata = mapOf(\"orderId\" to \"2041\"),\n)\n// ack.paymentId, ack.status (\"pending\"), ack.checkoutRequestId\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"paymentId\": \"e69e5c00-8a01-44ed-b003-48e2e86a7c9e\",\n  \"status\": \"pending\",\n  \"checkoutRequestId\": \"ws_CO_010720261905029287161380\"\n}\n```\n\n> [!warn] **Send `Idempotency-Key`, and make one key for each payment attempt.** If you repeat the same key, you get the same `paymentId` and `checkoutRequestId` back. paylod does not send a second prompt to the handset, even for concurrent duplicates. If you omit the idempotency key, a double-clicked Pay button, a refreshed tab or a retried request each send a fresh STK prompt. Each one charges the customer again. Do **not** key on an order id or a product id. A key is spent once you use it, so a reused key replays an old payment. A retry after a wrong PIN is a **new charge** and needs a **new key**.\n\n> [!warn] **A `409` *indeterminate* means stop. It does not mean retry.** If an earlier request under this idempotency key stopped while the Daraja call was in flight, paylod does not repeat that call. The money may already have moved. A timeout is not evidence that the money did not move. For money, at-most-once is better than at-least-once. Read `GET /status/:id` first. If nothing happened, retry with a **new** key. The same key returns this `409` again.\n\n### Errors\n\n| Status | When |\n| --- | --- |\n| `401` | The API key is missing or invalid. |\n| `400` | The JSON is malformed, or you did not set Daraja credentials for this application and environment. |\n| `409` | You reused the idempotency key with a different body. Or the first request is still in flight (`Retry-After`). Or an interrupted provider call spent the key. The third case is *indeterminate*: read the status, then retry with a new key. |\n| `422` | The body failed validation. For example, `amount` ≤ 0, or paylod cannot parse the phone number. |\n| `429` | You reached the rate limit. paylod limits each API key and each phone number. |\n| `502` | Daraja rejected the STK Push. paylod passes the upstream error through. |\n\n## GET /status/:id\n\nRead the current state of a payment. This endpoint needs no webhook endpoint and no other infrastructure. If the payment is still `pending`, paylod runs a live M-Pesa STK Query. paylod settles the payment before it responds. The API key limits this read to its own application. A payment of another application returns `404`.\n\n```text title=\"Endpoint\"\nGET https://paylod.dev/functions/v1/status/:id\n```\n\n### Path\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `id` | string | Required | The `paymentId` returned by `POST /collect`. |\n\n### Example\n\n```bash tab=\"curl\" title=\"Request\"\ncurl https://paylod.dev/functions/v1/status/e69e5c00-8a01-44ed-b003-48e2e86a7c9e \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\"\n```\n```ts tab=\"node\" title=\"check\"\n// check() reads the payment and decodes it into a renderable PaymentOutcome.\nconst outcome = await paylod.check(\"e69e5c00-8a01-44ed-b003-48e2e86a7c9e\");\nif (outcome.paid) fulfil(outcome.receipt);\nelse console.log(outcome.message);\n```\n```php tab=\"php\" title=\"check\"\n// check() reads the payment and decodes it into a renderable PaymentOutcome.\n$outcome = $paylod->check('e69e5c00-8a01-44ed-b003-48e2e86a7c9e');\nif ($outcome->paid) {\n    fulfil($outcome->receipt);\n} else {\n    echo $outcome->message;\n}\n```\n```python tab=\"python\" title=\"check\"\n# check() reads the payment and decodes it into a renderable PaymentOutcome.\noutcome = paylod.check(\"e69e5c00-8a01-44ed-b003-48e2e86a7c9e\")\nif outcome.paid:\n    fulfil(outcome.receipt)\nelse:\n    print(outcome.message)\n```\n```java tab=\"java\" title=\"check\"\n// check() reads the payment and decodes it into a renderable PaymentOutcome.\nPaymentOutcome outcome = paylod.check(\"e69e5c00-8a01-44ed-b003-48e2e86a7c9e\");\nif (outcome.isPaid()) {\n    fulfil(outcome.getReceipt());\n} else {\n    System.out.println(outcome.getMessage());\n}\n```\n```kotlin tab=\"kotlin\" title=\"check\"\n// check() reads the payment and decodes it into a renderable PaymentOutcome.\nval outcome = paylod.check(\"e69e5c00-8a01-44ed-b003-48e2e86a7c9e\")\nif (outcome.paid) fulfil(outcome.receipt)\nelse println(outcome.message)\n```\n\n```json title=\"Response · 200 OK\"\n{\n  \"id\": \"e69e5c00-8a01-44ed-b003-48e2e86a7c9e\",\n  \"status\": \"success\",\n  \"mpesaReceipt\": \"UG1F3A1U7J\",\n  \"resultCode\": 0,\n  \"resultDesc\": \"The service request is processed successfully.\"\n}\n```\n\n### Status values\n\n| Value | Meaning |\n| --- | --- |\n| `pending` | paylod sent the prompt. The customer did not answer it yet. This state is not a failure. |\n| `success` | The payment settled. `mpesaReceipt` is the M-Pesa receipt number. |\n| `failed` | The payment did not happen. `resultCode` and `resultDesc` carry the M-Pesa reason. |\n\nOn a failure, `resultCode` is the raw Safaricom code: `1032` cancelled, `1037` timeout, `2001` wrong PIN. The [error reference](/docs/errors) decodes all of these codes.",
  },
  {
    id: "results",
    title: "Read the result — poll, wait, or take a signed webhook",
    keywords: ["result","results","status","poll","polling","wait","settle","settled","pending","4999","timeout","outcome","fulfil","fulfill"],
    aliases: ["status"],
    content: "# Handle the M-Pesa payment result\n\nTwo ways to learn a payment settled — poll GET /status, or receive a signed webhook. How to choose, and how to verify.\n\n`collect()` returns before the customer enters the M-Pesa PIN. Two methods tell you the result, and both methods read the same settled record.\n\n| | Poll | Webhook |\n| --- | --- | --- |\n| You host | Nothing | An HTTPS endpoint |\n| Good for | Scripts, a checkout with a spinner | Servers, high volume, back-office fulfilment |\n\nStart with the poll method. Change to a webhook when you have a server that must receive the result.\n\n## Option A: poll\n\n> A timeout **throws** `PaylodTimeoutError`. A timeout does not return `ok: false`. An unanswered prompt is not a failed payment: the customer can still enter the M-Pesa PIN, and the payment can still succeed. Do not mark the order as failed. Let the webhook tell you the result.\n\n```ts title=\"poll.ts\"\nconst outcome = await paylod.wait(ack.paymentId, { timeoutMs: 120_000 });\n\nif (outcome.paid) await fulfil(outcome.receipt);\nelse              await notify(outcome.message);\n```\n\n`wait()` polls with a random and increasing delay until the payment settles. If the payment is still pending, paylod makes a live M-Pesa STK Query and settles the payment immediately. Thus a poll never returns an old answer.\n\n`paylod.status(paymentId)` reads the record one time, and does not poll.\n\n## Option B: receive a signed webhook\n\nRegister an endpoint on the application's **Endpoints & Webhooks** tab. The SDK verifies the signature for you.\n\n```ts title=\"app/api/webhooks/paylod/route.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod();\n\nexport const POST = paylod.webhookHandler(async (event) => {\n  if (event.type === \"payment.success\") {\n    await fulfil(event.data.paymentId, event.data.mpesaReceipt);\n  } else if (event.data.decoded) {\n    await notify(event.data.decoded.customerMessage);\n  }\n});\n```\n\n`decoded` is `DecodedError | null`. paylod fills `decoded` on `payment.failed`, and sets `decoded` to `null` on `payment.success`. Test `decoded` before you read it.\n\nThis handler operates on each platform that supplies the Web `Request` and `Response` API: Next.js, Hono, Remix, Workers, Bun and Deno. On Express, use `paylod.webhook(handler)`.\n\n> [!warn] On Express, mount this route **before** each global `express.json()`. A JSON parser consumes the body, and the raw bytes are then not available. Without the raw bytes, no software can verify the signature. The middleware finds this condition and throws an error. The middleware does not skip the check.\n\npaylod checks the signature **before** your handler runs. An incorrect signature returns `400`, and your handler does not receive the event. If your handler throws an error, the route returns `500`. paylod then sends the event again. Thus you must make your handler idempotent.\n\n## Check the amount before you fulfil\n\nThe webhook carries the **true settled amount**. If you fulfil an order on the `status` field alone, a customer can pay you less than the order total.\n\n```ts title=\"fulfil.ts\"\nexport const POST = paylod.webhookHandler(async (event) => {\n  if (event.type !== \"payment.success\") return;\n\n  // Look the order up by paymentId — the id you stored when you called collect().\n  const order = await db.orders.findByPaymentId(event.data.paymentId);\n  if (!order) return;\n\n  if (event.data.amount === order.amountKes) {\n    await fulfil(order);\n  } else {\n    await flagForReview(order, event.data);   // underpaid\n  }\n});\n```\n\nStore `ack.paymentId` with your order when you send the STK push. Then use `ack.paymentId` for the lookup. The event does not return the `metadata` that you passed to `collect()`.\n\n## Failures are results, not outages\n\n`payment.failed` is a normal outcome. paylod decodes `event.data.decoded` for you:\n\n- `1032` — the customer pressed Cancel. Offer a retry.\n- `1037` — the prompt did not reach the handset, or the customer ignored the prompt.\n- `2001` — the customer entered a wrong M-Pesa PIN.\n- `1` — the customer has insufficient balance.\n\n`4999` is **not** a failure. `4999` means that the customer did not enter the M-Pesa PIN yet. Continue to poll. Never send a second `collect()` call. A second STK push makes a second prompt, and can charge the customer two times. The [error reference](/docs/errors) lists every code.\n\n## Retry a failed payment\n\nA payment can truly fail: the customer cancelled the prompt, entered a wrong M-Pesa PIN, or has insufficient balance. A second charge is then a **new payment attempt**, and the new attempt needs a **new idempotency key**. A retry with the key of the failed attempt does not charge the customer. Such a retry only replays the failure that you already have.\n\n> [!warn] **One `409` is not a retry signal: the *indeterminate* `409`.** An earlier request under that key can stop while the call to Daraja is in flight. paylod then refuses to send the call again, because the money can already have moved. A timeout is not proof that the money did not move. First read the payment status with `paylod.check(paymentId)` or `GET /status/:id`, or wait for the webhook. If the payment settled, fulfil the order. If no payment occurred, open a new attempt with a **new** key. Never retry the spent key. See [Idempotency](/docs/sdk#idempotency) for the full rules.\n\n```ts title=\"retry.ts\"\nif (!outcome.paid && outcome.retryable) {\n  const retry = await db.attempts.create({ orderId: order.id });   // new attempt → new key\n  await paylod.collectAndWait({ amount, phone, idempotencyKey: retry.id });\n}\n```\n\n## Verify it yourself\n\nIf you do not use the SDK, compute the signature yourself. The signature is an HMAC-SHA256 over `t + \".\" + rawBody`, with the signing secret of the endpoint as the key. The freshness window is 5 minutes. Compute the signature over the raw bytes. Never compute the signature over an object that you serialised again. The [webhooks reference](/docs/api/webhooks) specifies the scheme.\n\n## Next\n\n[Test in sandbox](/docs/guides/test-in-sandbox) makes each of these outcomes on demand.",
  },
  {
    id: "webhooks",
    title: "Webhooks — signed delivery of the settled result",
    keywords: ["webhook","webhooks","signature","hmac","signing","secret","event","delivery","verify","endpoint"],
    aliases: [],
    content: "# M-Pesa payment webhooks: events and signatures\n\nThe event envelope, the delivery headers, the HMAC signature scheme, and every event type paylod can send you.\n\nWebhooks are **optional**. Register a webhook endpoint on the application's **Endpoints & Webhooks** tab. paylod then sends a signed event as soon as a payment settles. If you do not want to run a webhook endpoint, poll [`GET /status/:id`](/docs/api/payments) instead. paylod records every payment in both cases.\n\n## Delivery headers\n\n| Header | Description |\n| --- | --- |\n| `x-webhook-event` | The event type, e.g. `payment.success`. |\n| `x-webhook-id` | Unique delivery id. Use it to dedupe retries. |\n| `x-webhook-signature` | HMAC signature, format `t=<unix>,v1=<hex>`. |\n\n## Event types\n\n| Event | Fired when |\n| --- | --- |\n| `payment.success` | A collection settled. The source is an STK Push or an offline C2B payment. |\n| `payment.failed` | A collection failed. The customer cancelled it, or it timed out, or the PIN was wrong, or the funds were not sufficient. |\n| `payout.result` | A [`/payout`](/docs/api/payouts) reached a final state. |\n| `refund.result` | A [`/reversal`](/docs/api/payouts) reached a final state. |\n| `admin.transaction_status` | A [`/transaction-status`](/docs/api/other) query returned an answer. |\n| `admin.account_balance` | An [`/account-balance`](/docs/api/other) query returned an answer. |\n\nEvery event uses the same envelope and the same signature scheme.\n\n## The envelope\n\n```http title=\"Example delivery\"\nPOST https://your-app.com/webhooks/mpesa\nx-webhook-event: payment.success\nx-webhook-id: 8f3c…\nx-webhook-signature: t=1751394302,v1=5a2f…c9\n\n{\n  \"type\": \"payment.success\",\n  \"created\": 1751394302,\n  \"data\": {\n    \"paymentId\": \"e69e5c00-…\",\n    \"applicationId\": \"…\",\n    \"env\": \"production\",\n    \"status\": \"success\",\n    \"amount\": 10,\n    \"phone\": \"254712345678\",\n    \"accountRef\": \"INV-2041\",\n    \"mpesaReceipt\": \"UG1F3A1U7J\",\n    \"checkoutRequestId\": \"ws_CO_…\",\n    \"resultCode\": 0,\n    \"resultDesc\": \"The service request is processed successfully.\",\n    \"decoded\": null\n  }\n}\n```\n\nThe `data` object carries exactly these fields. It has no `metadata` field. The webhook does **not** return the opaque `metadata` that you pass to `collect()`. Key your fulfilment on `paymentId` or on `accountRef`. On a `payment.failed` event, `decoded` is a `{ code, title, cause, fix, category, retryable, customerMessage }` object. paylod decodes that object from the Safaricom result code. On a `payment.success` event, `decoded` is `null`.\n\n## How to verify the signature\n\nThe signature is an HMAC-SHA256 over the string `t + \".\" + rawBody`. The key is the endpoint's signing secret. paylod shows that secret once, when you add the endpoint. Every official SDK verifies the signature for you and returns a typed event. Compute the HMAC yourself only if you use another runtime.\n\n```ts tab=\"node\" title=\"Verify a webhook\"\nconst event = paylod.verifyWebhook({\n  payload: rawBody,                              // the RAW request bytes\n  signature: headers[\"x-webhook-signature\"],     // \"t=...,v1=...\"\n  secret: process.env.PAYLOD_WEBHOOK_SECRET,\n});\n// event.type, event.data.paymentId, event.data.mpesaReceipt\n```\n```php tab=\"php\" title=\"Verify a webhook\"\n$event = $paylod->verifyWebhook(\n    payload: $rawBody,                               // the RAW request bytes\n    signature: $_SERVER['HTTP_X_WEBHOOK_SIGNATURE'], // \"t=...,v1=...\"\n    secret: getenv('PAYLOD_WEBHOOK_SECRET'),\n);\n// $event->type, $event->data->paymentId, $event->data->mpesaReceipt\n```\n```python tab=\"python\" title=\"Verify a webhook\"\nevent = paylod.verify_webhook(\n    payload=raw_body,                                  # the RAW request bytes\n    signature=request.headers[\"x-webhook-signature\"],  # \"t=...,v1=...\"\n    secret=os.environ[\"PAYLOD_WEBHOOK_SECRET\"],\n)\n# event.type, event.data.payment_id, event.data.mpesa_receipt\n```\n```java tab=\"java\" title=\"Verify a webhook\"\nWebhookEvent event = paylod.verifyWebhook(\n    rawBody,                                    // the RAW request bytes\n    request.getHeader(\"x-webhook-signature\"),   // \"t=...,v1=...\"\n    System.getenv(\"PAYLOD_WEBHOOK_SECRET\"));\n// event.getType(), event.getData().getPaymentId()\n```\n```kotlin tab=\"kotlin\" title=\"Verify a webhook\"\nval event = paylod.verifyWebhook(\n    payload = rawBody,                                   // the RAW request bytes\n    signature = request.header(\"x-webhook-signature\"),   // \"t=...,v1=...\"\n    secret = System.getenv(\"PAYLOD_WEBHOOK_SECRET\"),\n)\n// event.type, event.data.paymentId, event.data.mpesaReceipt\n```\n```bash tab=\"curl\" title=\"Verify by hand\"\n# No SDK? Recompute the HMAC yourself (see the three rules below):\n#   signed   = \"{t}.{rawBody}\"\n#   expected = HMAC_SHA256(secret, signed)   # constant-time compare against v1\n#   reject if (now - t) > 300 seconds        # replay window\n```\n\nThe verification has three rules. Apply the three rules in this order:\n\n1. Compute the HMAC over the **raw** request body. Do not use a re-serialised object.\n2. Compare the result in **constant time** against `v1`.\n3. Reject a delivery if its `t` is more than about five minutes old. This rule limits replay attacks.\n\n## How to respond\n\nRespond `2xx` to acknowledge the event. If you do not respond `2xx`, paylod retries the delivery with backoff. The dashboard shows every attempt. paylod delivers each event at least once. Dedupe on `x-webhook-id`.\n\n> [!warn] Verify the signature. Then compare `data.amount` against your own order total. Do both steps **before** you fulfil the order. The webhook always carries the true settled amount. See [Secure integration](/docs/guides/security).",
  },
  {
    id: "errors",
    title: "M-Pesa result codes and paylod HTTP errors",
    keywords: ["error","errors","resultcode","failed","failure","decode","1032","2001","1037","1001","4999","insufficient","cancelled","pin","debug"],
    aliases: [],
    content: "# M-Pesa and Daraja error codes, decoded\n\nEvery Safaricom M-Pesa result code and Daraja API error decoded — cause, fix, and a customer-facing message. Searchable.\n\nM-Pesa reports a problem as a number and gives almost no other information. `resultCode: 1037` is an example. That number means nothing until you decode it.\n\nThis page decodes every result code. For each code it gives the meaning, the cause, whether a retry can help, and the message for the customer.\n\n## How to read a failure\n\nA failed payment gives you two fields. The two fields come from two different places:\n\n| Field | Where it comes from |\n| --- | --- |\n| HTTP status | paylod. `401` wrong API key, `422` wrong body, `502` Daraja refused the request. See [API errors](/docs/api). |\n| `resultCode` / `resultDesc` | M-Pesa. The customer cancelled the prompt, or the PIN was wrong, or the handset was off. This page decodes these codes below. |\n\nA `202` and then `resultCode: 1032` is not a bug. The push worked, and the customer pressed Cancel.\n\n## You do not have to implement this table\n\nThe [Node SDK](/docs/sdk) decodes result codes offline. It makes no network call. It needs no API key at call time.\n\n```ts title=\"decode.ts\"\nimport { decodeError } from \"@paylod/node\";\n\nconst err = decodeError(1032);\n\nerr.title;            // \"Payment cancelled by the customer\"\nerr.category;         // \"customer\"\nerr.retryable;        // true\nerr.customerMessage;  // \"Payment cancelled — you can try again whenever you're ready.\"\n```\n\nOn a failed payment, `collectAndWait()` returns the same object as `outcome.detail`. A `payment.failed` webhook carries the same object as `event.data.decoded`. The two objects are byte-for-byte identical. The message that you show to a customer is therefore the same for every integration. The SDK also exports the whole catalogue as `ERROR_CATALOG`.\n\nMost integrations do not need the raw catalogue. `outcome.message` is the decoded sentence for the customer. `outcome.retryable` tells you whether a second charge is safe. Use the catalogue below for your records, for your dashboards, and for debug work during an incident.\n\nThe [CLI](/docs/cli) decodes the same catalogue in your terminal. Run `paylod errors 1032`. This command is the fastest way to read the meaning of a code during an incident.\n\n> [!warn] **Do not show a failure for the codes `4999` and `500.001.1001`. These two codes mean pending, not failed.** Daraja returns them while the customer still looks at the PIN prompt. They decode with `category: \"pending\"` and `retryable: false`. Here `retryable: false` does not mean that a retry is pointless. It means that the first prompt is still live, and a second push can **double-charge** the customer. Do not collect again on these two codes. Continue to poll the status. The SDK polls for you.\n\n## The most common codes\n\n| Code | Meaning | What to do |\n| --- | --- | --- |\n| `0` | Success. | Fulfil the order, but first check the settled `amount`. |\n| `1032` | The customer pressed **Cancel** on the prompt. | Offer the customer a retry. This code is not an error in your code. |\n| `1037` | Timeout. The prompt did not reach the handset, or the customer ignored it. | Retry. The handset was off, or out of coverage, or the customer did not answer. |\n| `2001` | Wrong M-Pesa PIN. | Ask the customer to try again. |\n| `1` | Insufficient balance. | Tell the customer that the M-Pesa balance is too low. |\n| `4999` | **Not a failure.** The customer did not enter the PIN yet. | Continue to poll. Never collect again: the prompt is still live, and a second push can double-charge the customer. |\n\nThe catalogue below contains every other code. It contains every Daraja API error, every C2B result code and every B2C result code. You can search it.\n\n## Full catalogue\n\nThe full catalogue is a **tool**, not a table to copy: call `decode_mpesa_error` with any result code and it returns `{ code, title, cause, fix, category, retryable, customerMessage }` offline. In application code, the SDK does it for you — `outcome.message` is already the customer-facing sentence. The browsable reference is at https://paylod.dev/docs/errors.",
  },
  {
    id: "security",
    title: "Secure integration — where the API key may and may not go",
    keywords: ["security","secure","safe","tamper","proxy","undercut","browser","client","frontend","leak","best","practice"],
    aliases: [],
    content: "# Secure your M-Pesa integration\n\nSix rules that make it impossible for a payer with an intercepting proxy to undercut you, and why the amount must stay on your server.\n\n## The question everyone asks\n\n*Can a customer open my checkout in an intercepting proxy, for example Burp or mitmproxy, and decrease the amount before the request reaches M-Pesa?*\n\nIn a correct integration, **no**.\n\nThe API key that calls `/collect` authenticates **you**, the merchant. The API key does not authenticate the payer. Only `amount` and `phone` come from the request body. paylod reads your shortcode, your passkey and your Daraja credentials from encrypted storage on the server. These values never go over the network. The settlement uses the amount that your server sent. paylod signs every webhook with HMAC, and every webhook reports the **true settled amount**.\n\nOnly one method lets a payer pay you less. That method gives the payer control of **both** the key and the amount. This occurs when you call `/collect` directly from a browser or a mobile app with an amount from the client. A payer can then change the request and pay less. paylod correctly reports the true, lower amount. But paylod cannot correct a merchant who gave control of the amount to the client.\n\nKeep the amount on your server, and this attack is not possible.\n\n## The short version\n\n| Do | Don't |\n| --- | --- |\n| Keep `mp_live_…` and `mp_test_…` on the server only | Put the API key in a browser, a mobile app, or another client |\n| Take `amount` from your own order record | Send an amount from the client directly to `/collect` |\n| Verify `x-webhook-signature` before you read the body | Trust an unverified webhook |\n| Confirm that `amount` matches the order **and** that `status === \"success\"` before you fulfil | Fulfil an order on `status` alone |\n| Send one `Idempotency-Key` for each **payment attempt** | Use an order id or a product id as the key, or generate a new key on each call |\n| Expect a `429` and back off | Continue to send requests at the rate limit |\n\n## 1. Keys are server-only secrets\n\nNever put `mp_live_…` or `mp_test_…` into a browser, a mobile app, or another client. Keys stay only on your server. A key in a client is fully compromised, and you must think of such a key as public. Each person who holds the key can use your account.\n\n## 2. Your server owns the amount\n\nYour server is the only authority on the amount. Take the amount from your own order record or price record. Never send an amount from the client directly to `/collect`. If the client holds the key and also sets the amount, a payer can decrease the amount. This is the only method that lets a payer pay you less.\n\n## 3. Verify, then check the amount, before you fulfil\n\nIn your webhook handler, verify the signature **first**. Then confirm the amount and the status **before** you fulfil the order. `paylod.webhookHandler()` verifies the signature for you. Thus your handler runs only on an authentic event.\n\n```ts title=\"fulfil.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n\nexport const POST = paylod.webhookHandler(async (event) => {\n  if (event.type !== \"payment.success\") return;\n\n  // Look the order up by paymentId — the id you stored when you called collect().\n  const order = await db.orders.findByPaymentId(event.data.paymentId);\n  if (!order) return;\n\n  // Never fulfil on status alone. Check the TRUE settled amount.\n  if (event.data.amount === order.amountKes) {\n    await fulfil(order);                    // paid in full\n  } else {\n    await flagForReview(order, event.data); // underpaid / mismatch\n  }\n});\n```\n\nThe webhook always carries the true settled amount. Thus you see a partial payment as soon as you check the amount.\n\nStore `ack.paymentId` with your order when you call `collect()`. Then use `ack.paymentId` for the lookup. The event carries `paymentId`, `accountRef` and the M-Pesa fields. The event does **not** return the `metadata` that you sent to `collect()`. Thus do not use the `metadata` in your fulfilment code.\n\nMake your handler **idempotent**. paylod sends a delivery again until you return `2xx`. A second copy of an event that you already handled is normal, and is not an error.\n\n## 4. Treat callback and mint tokens as secrets\n\nThe callback URL and the mint token are secrets. Do not write them into a log. Do not send them to a client. Keep them on your server with the API key.\n\n## 5. One idempotency key per payment attempt\n\nCreate one `Idempotency-Key` for each **payment attempt**. Do not create one key for each order, and never create one key for each product. A double-click and a job that runs again are duplicates of one attempt. Duplicates of one attempt then become a single charge, also under concurrency. If you use a key again with a **different** body, paylod returns `409`.\n\nA key is **spent** after you use it. Thus an order id or a product id is the wrong choice: a key that you use again replays the *original* payment, and does not make a new payment. A retry after a wrong M-Pesa PIN is a new charge, and needs a new key.\n\n> [!warn] A request can stop while the call to the provider is in flight. That key then returns `409` *indeterminate*, and paylod never sends the call again on its own. The outcome of such a payment is unknown, and a timeout is not proof that the money did not move. **Do not retry the same key.** First read the payment status. Then retry with a **new** key if no payment occurred. For money, at-most-once is better than at-least-once. See [Idempotency](/docs/sdk#idempotency).\n\n## 6. Expect rate limits\n\npaylod applies a rate limit to `/collect` for each API key and for each phone number. Expect `429 Too Many Requests` during a burst of requests, and back off. paylod refuses a `429` request before the request reaches Daraja. Thus a `429` does not spend the key, and a retry with the same `Idempotency-Key` after a short delay is safe.\n\n---\n\nObey these six rules. Then a payer with an intercepting proxy cannot pay you less, and cannot charge you more. That payer cannot send the money to a different account. That payer also cannot make you fulfil an order that the payer did not pay in full. The platform is safe by design. The platform only needs the amount to stay on your server.",
  },
  {
    id: "sandbox",
    title: "Test in the sandbox, including every failure path",
    keywords: ["test","testing","simulate","simulation","fake","sandbox","shortcode","174379"],
    aliases: [],
    content: "# Test M-Pesa payments in the Daraja sandbox\n\nBuild the whole flow against Safaricom's sandbox with test credentials — including the failure paths — before a shilling moves.\n\n## Test every failure path without a phone\n\nStart here. You do **not** need a handset to test a cancellation, a wrong M-Pesa PIN, insufficient funds or a timeout. Do not try to make these outcomes by hand. paylod supplies a simulator that uses the **real** settlement path. The simulator makes the same payment row, the same result codes, and the same signed webhook that your production code receives. Only the handset is simulated.\n\nTwo calls:\n\n1. **Create a simulated payment** — use `paylod.simulate.collect()` in the SDK, the `simulate_test_payment` MCP tool, or `POST /simulate/collect` over REST with your `mp_test_…` key. paylod sends no STK push. The call returns `{ paymentId, status: \"pending\", outcomes }`, and `outcomes` is an array of `{ id, label, status }`.\n2. **Force the result** — use `paylod.simulate.outcome()`, the `simulate_outcome` MCP tool, or `POST /simulate/outcome`. Give that `paymentId` and one `outcome` id.\n\n| `outcome` | Settles as | Result code |\n| --- | --- | --- |\n| `approve` | `success` | `0` |\n| `user_cancelled` | `failed` | `1032` |\n| `timeout` | `failed` | `1037` |\n| `wrong_pin` | `failed` | `2001` |\n| `insufficient_funds` | `failed` | `1` |\n\n### From the SDK\n\nOn Node, use this method. The method is one call. The method returns the same `PaymentOutcome` that your production code already displays. Thus you test the real code, and not special test code.\n\n```ts title=\"checkout.test.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_TEST_KEY!);   // mp_test_…\n\nfor (const outcome of paylod.simulate.outcomes) {\n  const result = await paylod.simulate.pay({ outcome, amount: 10 });\n  console.log(outcome, \"→\", result.status, result.retryable, result.message);\n}\n// approve            → succeeded false Payment received — thank you!\n// wrong_pin          → failed    true  That M-Pesa PIN was incorrect. …\n// insufficient_funds → failed    true  Your M-Pesa balance is too low. …\n// user_cancelled     → cancelled true  Payment cancelled — you can try again …\n// timeout            → failed    true  The M-Pesa prompt expired before it was answered. …\n```\n\nTo test *your* handler, and not the handler of the SDK, make the client with `{ simulate: true }`. `collect()` then creates a simulated payment, and sends no STK push. Your `/api/pay` route operates without a change. The SDK refuses an `mp_live_…` key locally, before it sends a request. The [SDK reference](/docs/sdk#test-your-checkout-without-a-phone) gives the full details.\n\n### Over REST\n\n```bash title=\"Simulate a cancelled payment over REST\"\n# 1. create the pending simulated payment\ncurl -sX POST https://paylod.dev/functions/v1/simulate/collect \\\n  -H \"Authorization: Bearer $PAYLOD_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"applicationId\":\"<your-app-id>\",\"phone\":\"254708374149\",\"amount\":10}'\n# → { \"paymentId\": \"…\", \"status\": \"pending\", \"outcomes\": [ { \"id\": \"approve\", … } ] }\n\n# 2. force the outcome — your webhook fires, your poller settles, exactly as in production\ncurl -sX POST https://paylod.dev/functions/v1/simulate/outcome \\\n  -H \"Authorization: Bearer $PAYLOD_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"paymentId\":\"<paymentId>\",\"outcome\":\"user_cancelled\"}'\n```\n\nAn AI agent with the `paylod:payments.simulate` scope can do the same with `simulate_test_payment` and `simulate_outcome`. Such an agent cannot touch real money. See the [MCP server](/docs/mcp).\n\n> [!note] The simulator operates only in sandbox. The simulator is the fastest method to prove three things: your code shows `outcome.message`, your code offers a retry only when `retryable` is true, and your code never fulfils an order on a `1032`.\n\n## Get sandbox credentials\n\nCreate an app on the [Safaricom Daraja portal](https://developer.safaricom.co.ke). Then open the **Sandbox** credentials of the app. You need four values:\n\n| Field | Sandbox value |\n| --- | --- |\n| Consumer key | From the Daraja portal |\n| Consumer secret | From the Daraja portal |\n| Shortcode | `174379` (Safaricom's shared test paybill) |\n| Passkey | From \"Lipa na M-Pesa Online\" in the portal |\n\nCopy the four values into the **Sandbox** environment of your paylod application. Then create an `mp_test_…` key.\n\n## What sandbox does\n\nSandbox is the environment of Safaricom, and not a paylod simulation. The STK push is real. The STK push goes to Daraja, Daraja answers, and the callback comes back to the hosted receiver of paylod. This is the same behaviour as production. Only the money is not real, because nothing settles.\n\nThus each item that you build is the same in production: the `202`, the `paymentId`, the webhook envelope, the signature and the result codes. You do not write a second integration.\n\n## Send a test payment\n\nSet `PAYLOD_API_KEY` to the `mp_test_…` key. Nothing else changes.\n\n```ts title=\"sandbox.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);   // mp_test_… key\n\nconst outcome = await paylod.collectAndWait({\n  amount: 10,\n  phone: \"254708374149\",     // Safaricom's published test MSISDN\n  accountReference: \"TEST-1\",\n});\n\nconsole.log(outcome.paid ? outcome.receipt : outcome.message);\n```\n\nUse a real number that you control, and you get a real prompt. No real money moves.\n\n## Test the failure paths\n\nMost production incidents occur on a failure path that nobody tested. Before you go live, make sure that your code gives a correct result for each failure path below. Use the **simulator** above. The simulator is deterministic, needs no handset, and runs in your test suite. The handset column is only for a person who wants to see a real failure one time.\n\n| Result code | Simulator `outcome` | On a real handset | What your code should do |\n| --- | --- | --- | --- |\n| `1032` | `user_cancelled` | Press **Cancel** on the prompt | Offer a retry. Do not fulfil the order |\n| `1037` | `timeout` | Ignore the prompt until it expires | Retry, or use the poll method |\n| `2001` | `wrong_pin` | Enter the wrong M-Pesa PIN | Ask the customer to try again |\n| `1` | `insufficient_funds` | Send an amount above the test balance | Tell the customer that the balance is low |\n\nThe [error reference](/docs/errors) gives every code with its cause and its correction.\n\n## Test your webhook locally\n\nYou need no tunnel. The [CLI](/docs/cli) sends your live webhook events to `localhost`, and keeps the signature:\n\n```bash title=\"Terminal\"\nnpx @paylod/cli listen --forward http://localhost:3000/webhook\n```\n\nYour handler then verifies a real signature against a real payload, on your own computer. See [Handle the result](/docs/guides/handle-the-result).\n\n## Next\n\n[Go live](/docs/guides/go-live) — replace the credentials, replace the key, and keep the code.",
  },
  {
    id: "go-live",
    title: "Go live — the callback URL, production credentials, the checklist",
    keywords: ["live","production","go-live","launch","ship","callback","callbackurl","resulturl","validationurl","confirmationurl","portal","safaricom"],
    aliases: ["callback-url","production"],
    content: "# Go live on production Daraja credentials\n\nMove from sandbox to production: production Daraja credentials, the callback URL you paste into the Daraja portal, and the go-live checklist.\n\n## What changes\n\nAlmost nothing changes. Your code is the same. Three items change.\n\n1. Put your *production* Daraja credentials and your real shortcode into the **Production** environment of your application.\n2. Copy the **production callback URL** into the Daraja portal.\n3. Change `mp_test_…` to `mp_live_…` in your backend.\n\nThe endpoints, the request bodies, the webhook envelope and the result codes are the same. This is the reason to build against [sandbox](/docs/guides/test-in-sandbox) first.\n\n## 1. Get production Daraja credentials\n\nSafaricom issues production credentials for your **real** till or paybill. Get the credentials on the [Daraja portal](https://developer.safaricom.co.ke), under \"Go Live\". You need these values:\n\n- Your consumer key and your consumer secret for the production app.\n- Your shortcode — the till number or the paybill number that customers already pay.\n- Your Lipa na M-Pesa Online passkey.\n- For payouts, reversals and the admin queries: an **Initiator Name** and an **Initiator Password**. Safaricom issues these two values separately. The Initiator Password is also called the security credential.\n\nCopy the values into the **Production** environment of the application. paylod encrypts the values at rest, and never reads the values back.\n\n## 2. Set the callback URL in the Daraja portal\n\npaylod hosts the callback receiver. Thus you copy into Daraja the URL that paylod shows on the application's **Endpoints & Webhooks** tab.\n\n> You must do this step yourself, because Safaricom has no API that registers a callback URL. You can automate all the other steps of a paylod integration.\n\nKeep the callback URL secret. The callback URL contains a token for one application.\n\n## 3. Replace the key\n\nCreate an `mp_live_…` key on the **API Keys** tab. Then put the key into the secret store of your server. Your code does not change:\n\n```bash title=\".env\"\nPAYLOD_API_KEY=mp_live_YOUR_API_KEY\n```\n\nThe key alone selects production. There is no environment flag that you can forget.\n\n## Go-live checklist\n\n- Keep the API key on your **server**. Never put the API key in a browser or a mobile app.\n- Take the `amount` from your own order record. Never take the `amount` from the client.\n- Verify the **HMAC signature** in your webhook handler before you parse the body. `paylod.webhookHandler()` does this for you.\n- Compare `data.amount` with the order total **before** you fulfil the order.\n- Use one `Idempotency-Key` for each **payment attempt**, and not for each order or product. A retry after a failure is a new attempt, and needs a new key.\n- After a `409` *indeterminate* reply, read the payment status. Do not retry the same key.\n- Handle a `429` with a backoff. Do not let your program stop.\n- Test your failure paths (`1032`, `1037`, `2001`) at least one time.\n- If you use payouts or admin queries, set the Initiator Name and the Initiator Password on the **Production** environment.\n\n[Secure integration](/docs/guides/security) explains the reason for each item above.\n\n## Then monitor it\n\nThe dashboard shows payments, webhook deliveries and delivery retries. If a delivery fails, the dashboard shows the attempts and the response of your endpoint.",
  },
  {
    id: "mcp",
    title: "The MCP server — the tools, and the order to call them in",
    keywords: ["mcp","tool","tools","agent","agents","scope","scopes","oauth","create_app","set_credentials","mint_key","consent"],
    aliases: [],
    content: "# MCP server: M-Pesa for AI agents\n\nLet an AI agent manage and use your M-Pesa integration — a remote, OAuth-authenticated MCP endpoint. Nothing to install.\n\n## Nothing to install\n\npaylod supplies a **remote** MCP server. You add no npm package. You run no process. You paste no API key into a config file. You give your AI client one URL. The client then finds the authorization server and opens your browser one time. You approve the exact capabilities that you want. The agent gets a scoped token. The agent never gets your Daraja credentials and never gets your paylod API key.\n\n```text title=\"Server URL\"\nhttps://mcp.paylod.dev/mcp\n```\n\nTransport: `streamable-http`. Auth: OAuth 2.1 (authorization code + PKCE, with Dynamic Client Registration) against `https://paylod.dev/oauth`. Any MCP client that supports remote HTTP works. A stdio-only client can use a bridge.\n\nThe server is open source: [github.com/mosesmrima/paylod-mcp](https://github.com/mosesmrima/paylod-mcp).\n\n## Add it to your client\n\nPick your client. Each client runs the OAuth flow for you on the first connection. A browser tab opens. You sign in to paylod. You consent to the scopes.\n\n### Claude Code\n\nRun one command. Use `--scope user` to make the server available in all of your projects. The default scope is `local`. The `project` scope writes a shared `.mcp.json`.\n\n```bash title=\"Terminal\"\nclaude mcp add --transport http --scope user paylod https://mcp.paylod.dev/mcp\n```\n\nThen run `/mcp` in Claude Code and pick **paylod** to authenticate. After that, `/mcp` shows the server as connected and shows its tools.\n\nYou can also commit the config. To do this, put the block below in `.mcp.json` at the repo root. Always include the `type` field. Claude Code treats an entry that has a `url` but no `type` as stdio, and skips that entry.\n\n```json title=\".mcp.json\"\n{\n  \"mcpServers\": {\n    \"paylod\": {\n      \"type\": \"http\",\n      \"url\": \"https://mcp.paylod.dev/mcp\"\n    }\n  }\n}\n```\n\n### Claude Desktop\n\nAdd remote servers in the UI, not in `claude_desktop_config.json`. That file is only for local stdio servers. Go to **Settings → Connectors → Add custom connector**. Paste the server URL. Click **Connect** to run the OAuth flow. Leave the advanced client-ID and secret fields blank. paylod supports Dynamic Client Registration, so your client registers itself.\n\n### Cursor\n\nAdd the block below to `~/.cursor/mcp.json` for all projects, or to `.cursor/mcp.json` for one project. Cursor identifies a remote server by the `url` field. Open **Settings → MCP**. Click the login prompt to authorize.\n\n```json title=\"~/.cursor/mcp.json\"\n{\n  \"mcpServers\": {\n    \"paylod\": {\n      \"url\": \"https://mcp.paylod.dev/mcp\"\n    }\n  }\n}\n```\n\n### VS Code\n\nAdd the block below to `.vscode/mcp.json`. Note that the top-level key is `servers`, not `mcpServers`. VS Code implements the MCP authorization spec, and it includes DCR. VS Code therefore prompts you to sign in the first time that you use the server.\n\n```json title=\".vscode/mcp.json\"\n{\n  \"servers\": {\n    \"paylod\": {\n      \"type\": \"http\",\n      \"url\": \"https://mcp.paylod.dev/mcp\"\n    }\n  }\n}\n```\n\n### Windsurf\n\nAdd the block below to `~/.codeium/windsurf/mcp_config.json`. Windsurf uses `serverUrl` for a remote server. Windsurf also supports OAuth on Streamable HTTP.\n\n```json title=\"~/.codeium/windsurf/mcp_config.json\"\n{\n  \"mcpServers\": {\n    \"paylod\": {\n      \"serverUrl\": \"https://mcp.paylod.dev/mcp\"\n    }\n  }\n}\n```\n\n### Codex CLI\n\nCodex supports remote Streamable-HTTP MCP servers with OAuth. Add the block below to `~/.codex/config.toml`, then run `codex mcp login paylod`.\n\n```toml title=\"~/.codex/config.toml\"\n[mcp_servers.paylod]\nurl = \"https://mcp.paylod.dev/mcp\"\n```\n\n### Fallback for stdio-only clients\n\nIf your client supports only stdio, use `mcp-remote` as a bridge. `mcp-remote` is the standard stdio-to-HTTP proxy. It runs the OAuth flow for you. It opens your browser and it caches the token in `~/.mcp-auth`. It needs no flags for paylod.\n\n```json title=\"mcp-remote bridge\"\n{\n  \"mcpServers\": {\n    \"paylod\": {\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"mcp-remote\", \"https://mcp.paylod.dev/mcp\"]\n    }\n  }\n}\n```\n\nMost users do not need this bridge. Claude Code, Claude Desktop, Cursor, VS Code, Windsurf and Codex all support remote HTTP directly.\n\n## First thing to try\n\n`decode_mpesa_error` is pure and offline. It needs no scopes and no credentials, and it uses no network. Use this tool to confirm that the server is set correctly before you grant any scope. Ask your agent:\n\n```text title=\"Prompt\"\nWhat does M-Pesa error 2001 mean?\n```\n\nA good answer gives the cause (wrong PIN), the fix, and the message to show the customer. The tool supplies that answer. The model does not guess it. A Daraja response gives you only the bare code `2001`, and this tool explains the code.\n\n## How the OAuth flow works\n\nYour client calls the server URL with no token. The server returns a `401` with a `WWW-Authenticate` header. That header points to the protected-resource metadata. From the metadata, the client finds the authorization server at `https://paylod.dev/oauth`. The client registers itself dynamically and opens your browser. You sign in to paylod. paylod then shows a consent screen. The consent screen lists each capability that the agent requests, as one scope per capability with its own checkbox.\n\n```http title=\"The 401 that starts it all\"\ncurl -i -X POST https://mcp.paylod.dev/mcp \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"tools/list\"}'\n\nHTTP/1.1 401 Unauthorized\nWWW-Authenticate: Bearer resource_metadata=\"https://mcp.paylod.dev/.well-known/oauth-protected-resource\"\n```\n\nThree scopes are high-risk: `payments.payout` (send money out), `credentials.write` (write your Daraja keys) and `keys.mint` (mint API keys). The consent screen puts these three scopes in their own flagged group, so you always see them. The consent screen shows every requested scope as **checked by default**. Untick each scope that you do not want to grant. paylod grants only the scopes that stay ticked.\n\nUntick the three high-risk scopes to stop the agent from calling `payout`, `reversal`, `set_credentials` or `mint_key`. The server rejects each such call on the scope. The server does not depend on the model to obey an instruction. If you approve only `payments.simulate`, the agent can build against the sandbox and cannot move real money.\n\npaylod binds the access token to this server as its audience. Your client holds the token. The token never appears in the conversation. Your Daraja consumer key and secret are write-only. No tool reads them back, so an agent can store them for you but cannot extract them.\n\n## Tools\n\nThe OAuth access token authorizes every call. Each tool also needs the scope that the table shows next to it. A token without that scope cannot call the tool.\n\n### Management — set the integration up\n\n| Tool | Scope | What it does |\n| --- | --- | --- |\n| `create_app` | `paylod:apps.write` | Use this tool on your first run. It creates your organization and its first application (paybill or till). It returns the applicationId, your callback URL and a one-time API key. |\n| `create_application` | `paylod:apps.write` | Add another application to an organization that you already have. |\n| `get_callback_url` | `paylod:apps.write` | Get the hosted M-Pesa callback URL for an application. You paste this URL into the Daraja portal. Treat the URL as a secret. |\n| `set_credentials` | `paylod:credentials.write` | Store or rotate the Daraja consumer key, secret, shortcode and passkey. These values are write-only. No tool reads them back. |\n| `mint_key` | `paylod:keys.mint` | Mint a paylod API key (`mp_test_…` / `mp_live_…`) for your own backend. The tool returns the key one time only. |\n| `list_keys` | `paylod:keys.mint` | List the API keys of an application. The tool returns the prefixes only. It never returns the secret. |\n| `revoke_key` | `paylod:keys.mint` | Revoke an API key by id. |\n| `configure_webhook` | `paylod:webhooks.write` | Create or update a webhook endpoint. paylod then sends signed results to your server. |\n| `list_webhooks` | `paylod:webhooks.write` | List the webhook endpoints that an application uses. |\n| `list_applications` | `paylod:team.read` | List the applications that you can access. The tool also gives their environments and their configuration state. |\n| `authenticate` | — (no scope) | Show the owner of the token. Show also the scopes that paylod granted. |\n\n### Runtime — move and read money\n\n| Tool | Scope | What it does |\n| --- | --- | --- |\n| `request_stk_push` | `paylod:payments.collect` | Send an STK Push to the handset of a customer. The tool accepts one idempotency key for each payment attempt. Duplicate deliveries of that attempt therefore become a single charge. If the call stops part way, the key is spent and the tool returns `409` *indeterminate*. In that case paylod cannot prove that no debit occurred. Read `get_payment_status` first, then retry with a **new** key. |\n| `get_payment_status` | `paylod:payments.read` | Read one payment by id. If the payment is still pending, the tool reads the live STK status and settles the payment immediately. |\n| `generate_qr` | `paylod:payments.collect` | Generate an M-Pesa QR code (base64 PNG). The tool is stateless and moves no money. |\n| `register_c2b` | `paylod:payments.collect` | Register the C2B validation URL and confirmation URL. Paybill payments made outside your app then reach you. |\n| `get_account_balance` | `paylod:payments.read` | Read the M-Pesa account balance. The tool is asynchronous. It returns a queryId, and the result arrives on your callback. |\n| `get_transaction_status` | `paylod:payments.read` | Read the status of an M-Pesa transaction by receipt. The tool is asynchronous and uses the same callback pattern. |\n| `payout` | `paylod:payments.payout` | Send money out (B2C). On a live application you cannot reverse this action. The tool needs the opt-in money-out scope. |\n| `reversal` | `paylod:payments.payout` | Reverse or refund a transaction. The tool needs the opt-in money-out scope. |\n\n### Sandbox & offline\n\n| Tool | Scope | What it does |\n| --- | --- | --- |\n| `simulate_test_payment` | `paylod:payments.simulate` | Create a simulated collection in the sandbox. The tool uses no handset and no real money. |\n| `simulate_outcome` | `paylod:payments.simulate` | Force a simulated payment to succeed or to fail. The agent can then test your failure paths. |\n| `decode_mpesa_error` | — (no scope) | Decode any Safaricom result code or Daraja error. The tool returns the cause, the fix, and a message for the customer. It is pure and offline. |\n| `get_docs` | — (no scope) | Get paylod documentation by topic. The tool is pure and local. It needs no scope, no token and no network. |\n\nThe [error reference](/docs/errors) lists the same codes that `decode_mpesa_error` returns.\n\n## Set an integration up with the tools, in order\n\nUse the tools to **set the integration up and to inspect it**. Your application does not call these tools at runtime. At runtime your application calls the [Node SDK](/docs/sdk). Call the tools in this order.\n\n1. **Check for an existing app** → `list_applications`. Any result means that you are onboarded.\n2. **Create the application.** A new user calls `create_app`, which creates the organization *and* its first application. A user who already has an organization calls `create_application` instead, because `create_app` returns a `409 already onboarded`. Do not pick the wrong tool here. This is the most common first-call mistake.\n3. **Store the Daraja credentials** → `set_credentials`. The tool needs `product`, which is the *kind of shortcode*, `paybill` or `till`. `product` is not the Daraja product name. For a **till**, also pass `partyB`, the till number that receives the funds. Make sure that `partyB` differs from `shortcode`, or Daraja rejects the push with `2002`. paylod encrypts the credentials at rest and keeps them write-only, and no tool reads them back.\n4. **Get the callback URL** → `get_callback_url`. Then **paste the URL into the Safaricom Daraja portal** as the CallbackURL / ResultURL / ValidationURL / ConfirmationURL. Do not skip this step, because no payment resolves without it. No agent can do this step for you, because Safaricom has no API for it. Treat the URL as a secret. The random `cbk_…` token in the path authenticates the callback from Safaricom.\n5. **Mint an API key** → `mint_key` (`mp_test_…` for sandbox, `mp_live_…` for production). paylod shows the key one time only. Your *server* puts this key in `PAYLOD_API_KEY`. The MCP server itself never uses an API key.\n6. **Register a webhook if you want one** → `configure_webhook`. You can skip this step and poll instead. paylod records every payment in both cases.\n7. **Write the application code with the SDK.** Run `npm install @paylod/node`. Then use `new Paylod(process.env.PAYLOD_API_KEY!)` and `await paylod.collectAndWait({ amount, phone })`. Show `outcome.message`, and offer a retry only when `outcome.retryable` is true. Do not write a fetch wrapper, a poll loop, or a callback handler. See [Quickstart](/docs/quickstart) and the [Node SDK](/docs/sdk).\n8. **Test the failure paths** → use `simulate_test_payment` with `simulate_outcome`. Together they drive success, cancellation, a wrong PIN or a timeout through the real settlement path. They need no handset.\n\n`request_stk_push` and `get_payment_status` let an agent *test* the loop from end to end. They also let a person say \"charge this number 10 bob\". Do not ship these two tools as your runtime integration. If you do, you rebuild the SDK badly.\n\n> [!warn] Do not treat result code `4999` as a failed payment. `4999` means that the payment is **pending**, because the customer must still enter the PIN. `4999` is not retryable. A second `request_stk_push` sends a second prompt and can charge the customer two times. Poll `get_payment_status` instead.\n\n## What an agent can actually do\n\nThe management tools let an agent build a complete live M-Pesa integration for you. You do not open a dashboard. Connect the server, then ask:\n\n```text title=\"Prompt\"\nSet up M-Pesa for my shop \"Acme Ltd\". Here are my Daraja sandbox\ncredentials: consumer key …, secret …, shortcode 174379, passkey ….\nCreate the app, store them, mint me a test API key, give me my\ncallback URL, then send a 10 KES STK push to 0712345678 and tell me\nwhen it settles.\n```\n\nThe agent calls `create_app` → `set_credentials` → `mint_key` → `get_callback_url` → `request_stk_push`. The agent then polls `get_payment_status` until the payment settles. If the payment fails, the agent calls `decode_mpesa_error`. The agent then tells you in simple words that the customer cancelled the prompt (`1032`) or that the handset timed out (`1037`). You give the agent a phone number. The agent gives you an API key and a receipt.\n\nThe agent cannot paste the callback URL into the Safaricom Daraja portal, because Safaricom has no API for it. This is the only step that you must do yourself. The agent can do every other step. This includes `configure_webhook`, which sets the webhook endpoint that receives results on your own server.\n\n> The agent uses the same platform over MCP that your backend uses over HTTP at `/functions/v1`. See the [API reference](/docs/api). MCP is a second front door. It is not a separate system.",
  },
  {
    id: "cli",
    title: "The paylod CLI (installs as @paylod/cli)",
    keywords: ["cli","terminal","command","shell","npx","forward","tunnel","ngrok"],
    aliases: [],
    content: "# paylod CLI: M-Pesa from your terminal\n\nSend a real M-Pesa STK Push from your terminal, decode any Daraja result code offline, and forward live webhooks to localhost without ngrok.\n\nThe `paylod` CLI sends a real M-Pesa STK Push from your terminal. The CLI also decodes Daraja result codes offline. The CLI also forwards live webhooks to `localhost` without ngrok. The CLI uses the same API as the [API reference](/docs/api).\n\n```bash title=\"Install\"\nnpm install -g @paylod/cli\n```\n\nThe package is `@paylod/cli`. The package installs the command `paylod`. Source: [github.com/mosesmrima/paylod-cli](https://github.com/mosesmrima/paylod-cli).\n\nYou can also run the CLI without installation:\n\n```bash title=\"npx\"\nnpx @paylod/cli errors 1032\n```\n\nThe CLI needs Node 20 or newer.\n\n## Set up\n\n```bash title=\"Terminal\"\npaylod init\n```\n\n`init` opens your browser and signs you in. Then `init` lets you pick an application. Then `init` mints an API key for this machine. The CLI puts the credentials in your OS keychain. If your machine has no keychain, the CLI uses a `0600` file under `~/.config/paylod`.\n\nTo only sign in, use these commands:\n\n```bash title=\"Terminal\"\npaylod login\npaylod whoami       # who am I, and where is the token stored?\n```\n\nIn CI, do not sign in. Set `PAYLOD_API_KEY` in the environment. Every data-plane command (`collect`, `status`, `listen`) uses that environment variable.\n\n## Take a payment\n\n```bash title=\"Terminal\"\npaylod collect --phone 254712345678 --amount 100\n```\n\nThe customer receives the STK Push prompt on the handset. The CLI polls until the payment settles. Then the CLI prints the outcome. On success, the outcome is an M-Pesa receipt. On failure, the outcome is a decoded reason.\n\n| Flag | What it does |\n| --- | --- |\n| `-p, --phone <msisdn>` | The customer's number. `0712…`, `254712…` and `+254712…` are all correct. |\n| `-a, --amount <kes>` | Whole shillings. M-Pesa rejects decimals. |\n| `-r, --ref <reference>` | Your own correlation id, such as an invoice number or an order number. The result contains this value. Maximum 12 characters. A paybill shows this value to the customer. A till never shows it. |\n| `-d, --description <text>` | The payment description on the STK Push prompt. Maximum 64 characters. |\n| `--idempotency-key <key>` | The name of one payment attempt. paylod collapses duplicates of that attempt into a single charge. A new charge needs a new key. |\n| `--no-wait` | Send the STK Push and exit immediately. The CLI does not poll. |\n| `--timeout <secs>` | The time to wait for settlement. Default 120. |\n| `--env <sandbox\\|production>` | The M-Pesa environment for the charge. |\n| `--api-key <key>` | Use this key in place of `PAYLOD_API_KEY` or your stored profile. |\n\nTo read a payment later, or to watch a payment settle, use these commands:\n\n```bash title=\"Terminal\"\npaylod status e69e5c00-8a01-44ed-b003-48e2e86a7c9e\npaylod status e69e5c00-8a01-44ed-b003-48e2e86a7c9e --watch\n```\n\n`status` decodes the failure reason for you. You do not have to read `2001` in a PDF.\n\n## Decode a result code\n\n> Do not show a failure to a customer for the codes `4999` and `500.001.1001`. These two codes decode as **pending**, not failed. They mean that the customer did not enter the PIN yet. The payment is still live and the customer can still pay it.\n\nThis command works offline. It needs no login, no network and no API key.\n\n```bash title=\"Terminal\"\npaylod errors 1032          # why did that payment fail?\npaylod errors --list        # the whole catalogue\npaylod errors 2001 --json   # machine-readable, for your error handler\n```\n\nUse this command to find the meaning of a code such as `2001` quickly during an incident. The [error reference](/docs/errors) shows the same catalogue.\n\n## Webhooks on localhost, without ngrok\n\n```bash title=\"Terminal\"\npaylod listen --forward http://localhost:3000/webhook\n```\n\n`listen` streams the live webhook events of your application. `listen` then sends each event again to your local server. The signature stays intact. Therefore your code verifies a real signature against a real payload. You need no tunnel, no public URL and no ngrok.\n\n| Flag | What it does |\n| --- | --- |\n| `-f, --forward <url>` | POST each event to this local URL. |\n| `-e, --events <types...>` | Forward only these types (`payment.success`, `payment.failed`). |\n| `--print-json` | Print the full JSON payload of every event. |\n| `--secret <whsec>` | The signing secret. If you omit this flag, the CLI reads the secret from your profile, or gets it from the API. |\n| `--tolerance <secs>` | The freshness window of the signature, in seconds. Default 300. `0` disables the check. |\n| `--skip-verify` | Do not verify the HMAC. Use this flag only to debug. |\n| `--port <port>` | The port of the local webhook endpoint. Default 4242. |\n\nTo get a new signing secret, or to read the current signing secret, use this command:\n\n```bash title=\"Terminal\"\npaylod webhooks secret\n```\n\n## Test without spending money\n\n```bash title=\"Terminal\"\npaylod simulate --outcome user_cancelled\npaylod simulate --interactive\n```\n\n`simulate` creates a payment in the paylod sandbox and forces an outcome. `simulate` makes no Daraja call, uses no handset and moves no money. Use `simulate` to test your failure paths in CI.\n\n| Outcome | Result code |\n| --- | --- |\n| `approve` | `0` |\n| `wrong_pin` | `2001` |\n| `insufficient_funds` | `1` |\n| `user_cancelled` | `1032` |\n| `timeout` | `1037` |\n\n## Manage the integration\n\nThese commands do everything the dashboard does.\n\n| Command | What it manages |\n| --- | --- |\n| `paylod apps list \\| create \\| use \\| rename \\| delete` | Applications. You need one application for each till or paybill. |\n| `paylod creds` | Your Daraja consumer key, secret, shortcode and passkey. Write-only. |\n| `paylod keys mint \\| list \\| revoke \\| use` | paylod API keys (`mp_test_…` / `mp_live_…`). |\n| `paylod webhooks list \\| add \\| toggle \\| delete \\| secret` | Webhook endpoints. |\n| `paylod payments` | Your M-Pesa payments. |\n| `paylod orgs` | Organizations. |\n\n## Scripting\n\nEvery command takes a global `--json` flag and prints machine-readable output.\n\n```bash title=\"Terminal\"\npaylod collect -p 254712345678 -a 100 --json | jq -r '.mpesaReceipt'\n```\n\n`paylod collect` exits `5` if the payment is still pending when the timeout expires. Exit code `5` is not a failure. It means that the customer did not answer the prompt yet. On a settled failure, the CLI exits with a non-zero code. The JSON output then contains the decoded reason.\n\n## Environment variables\n\n| Variable | What it does |\n| --- | --- |\n| `PAYLOD_API_KEY` | The merchant API key. The CLI does not read the config file. Set this variable to run paylod in CI. |\n| `PAYLOD_WEBHOOK_SECRET` | The signing secret for `paylod listen`. |\n| `PAYLOD_API_BASE` | Overrides the API base. Default `https://paylod.dev/functions/v1`. |\n| `PAYLOD_CONFIG_DIR` | The directory for the CLI config. Default `~/.config/paylod`. |\n| `PAYLOD_NO_KEYCHAIN=1` | Never use the OS keychain. Use the `0600` file in place of the keychain. Set this variable on headless Linux and in containers. |\n| `NO_COLOR` | Disables colour. |\n\n> [!warn] Keep `mp_live_…` keys out of your shell history and out of committed `.env` files. These keys move real money.\n\n## Next\n\n- To build an application and not a script, use the [Node SDK](/docs/sdk).\n- To set up an AI agent, use the [MCP server](/docs/mcp). The MCP server shows the same capabilities as tools.\n- To read about the HTTP endpoints below the CLI, see the [API reference](/docs/api).",
  },
  {
    id: "endpoints",
    title: "The full REST surface — payouts, reversals, QR, C2B, queries",
    keywords: ["endpoint","endpoints","rest","http","api","reference","payout","b2c","reversal","refund","qr","c2b","balance","transaction"],
    aliases: ["api","payouts"],
    content: "# M-Pesa API overview: base URL, auth, conventions\n\nThe base URL, the auth header, the request/response conventions, and an index of every paylod endpoint.\n\n## Base URL\n\n```text title=\"Base URL\"\nhttps://paylod.dev/functions/v1\n```\n\n## Authentication\n\nEvery endpoint needs a bearer API key. The API key identifies the application and the environment. No request carries a shortcode or a Daraja credential.\n\n```http title=\"Header\"\nAuthorization: Bearer mp_live_YOUR_API_KEY\n```\n\nSee [Authentication](/docs/authentication) for API keys, environments, idempotency and rate limits.\n\n## Endpoints\n\n| Method | Path | What it does |\n| --- | --- | --- |\n| `POST` | [`/collect`](/docs/api/payments) | Send an STK Push to a customer's handset |\n| `GET` | [`/status/:id`](/docs/api/payments) | Read a payment's current state |\n| `POST` | [`/payout`](/docs/api/payouts) | Send money out to a customer (B2C) |\n| `POST` | [`/reversal`](/docs/api/payouts) | Reverse or refund a completed transaction |\n| `POST` | [`/qr-generate`](/docs/api/other) | Generate a dynamic Lipa na M-Pesa QR code |\n| `POST` | [`/c2b-register`](/docs/api/other) | Register the offline C2B confirmation URL and validation URL |\n| `POST` | [`/transaction-status`](/docs/api/other) | Read a past transaction by its receipt |\n| `POST` | [`/account-balance`](/docs/api/other) | Read your shortcode's M-Pesa float |\n\n## Synchronous and asynchronous endpoints\n\nM-Pesa is an asynchronous network. The paylod API keeps that behaviour visible.\n\n- **`200 OK`** — the answer is in the response body. Only `GET /status/:id`, `POST /qr-generate` and `POST /c2b-register` work this way.\n- **`202 Accepted`** — paylod accepted the request and sent it to M-Pesa. The final outcome comes later on your [webhook](/docs/api/webhooks). For `/collect` you can also poll `GET /status/:id`. The response body carries an id for correlation.\n\nFour endpoints move money or read the ledger: `/payout`, `/reversal`, `/transaction-status` and `/account-balance`. For these four endpoints you must also set the application's **Initiator Name** and **Initiator Password**. See [Go live](/docs/guides/go-live).\n\n## Errors\n\n| Status | Meaning |\n| --- | --- |\n| `400` | The JSON is malformed, or you did not set Daraja credentials for this application and environment |\n| `401` | The API key is missing or invalid |\n| `404` | The resource does not exist, or it belongs to another application |\n| `409` | You reused the idempotency key with a different body. Or the first request is still in flight. Or an interrupted provider call spent the key |\n| `422` | The body failed validation |\n| `429` | You reached the rate limit |\n| `502` | Daraja rejected the request. paylod passes the upstream error through |\n\nThe [error reference](/docs/errors) decodes M-Pesa's own result codes, such as `1032`, `1037` and `2001`.\n\n---\n\n# Payouts API: M-Pesa B2C payouts and reversals\n\nPOST /payout to send money out (B2C) and POST /reversal to refund a completed transaction.\n\nBoth endpoints move money **out** of your shortcode. Both endpoints are asynchronous. The HTTP call acknowledges with `202`. paylod delivers the final result to your [webhook](/docs/api/webhooks). For both endpoints you must set the application's **Initiator Name** and **Initiator Password**.\n\n> [!warn] On a production application you cannot reverse these two operations. Put your own authorisation in front of them. Never expose them to a client.\n\n## POST /payout\n\nB2C payout — send money from your shortcode to a customer.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/payout\n```\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `amount` | integer | Required | Whole KES to send, 1–150000. |\n| `phone` | string | Required | Recipient MSISDN (`0712…`, `254712…`, `+254712…`). paylod normalises it for you. |\n| `commandId` | string | Optional | `SalaryPayment`, `BusinessPayment`, or `PromotionPayment`. Defaults to `BusinessPayment`. |\n| `remarks` | string | Optional | Free-text remarks. Up to 100 chars. |\n| `occasion` | string | Optional | Occasion label. Up to 100 chars. |\n\n### Example\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/payout \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"amount\": 2500,\n    \"phone\": \"254712345678\",\n    \"commandId\": \"BusinessPayment\",\n    \"remarks\": \"Payout for July\"\n  }'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"disbursementId\": \"d3f1a2b3-…\",\n  \"conversationId\": \"AG_20260707_0000c1d2e3f4\",\n  \"status\": \"pending\"\n}\n```\n\npaylod sends the final result to your webhook as an HMAC-signed `payout.result` event. Correlate that event with `disbursementId` or `conversationId`.\n\n## POST /reversal\n\nReverse or refund a completed transaction to the customer.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/reversal\n```\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `transactionId` | string | Required | The M-Pesa receipt to reverse. |\n| `amount` | integer | Required | Whole KES, 1–150000. Must match the original transaction amount. |\n| `remarks` | string | Optional | Free-text remarks. Up to 100 chars. |\n| `occasion` | string | Optional | Occasion label. Up to 100 chars. |\n\n### Example\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/reversal \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"transactionId\": \"UG1F3A1U7J\",\n    \"amount\": 2500,\n    \"remarks\": \"Customer refund\"\n  }'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"disbursementId\": \"f7e6d5c4-…\",\n  \"conversationId\": \"AG_20260707_0000a9b8c7d6\",\n  \"status\": \"pending\"\n}\n```\n\npaylod sends the result to your webhook as an HMAC-signed `refund.result` event.\n\n---\n\n# QR codes, C2B registration and M-Pesa queries\n\nGenerate an M-Pesa QR code, register offline C2B URLs, and query transaction status or account balance.\n\nThese endpoints cover the rest of the Daraja surface. They use the same hosted model. They use the same base URL and the same bearer token. No request carries a shortcode.\n\n## POST /qr-generate\n\nGenerate a dynamic Lipa na M-Pesa QR code for a given amount. This endpoint is **synchronous**. It returns a base64-encoded PNG. Show that PNG to the customer to scan. This endpoint is stateless and creates no ledger row. When the customer pays, the payment arrives later on your C2B confirmation URL with source `c2b`. A signed webhook also fires, like any other offline payment.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/qr-generate\n```\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `amount` | number | Required | Amount to encode in the QR. Must be greater than 0. |\n| `refNo` | string | Optional | Reference shown to the customer. Up to 64 chars. |\n| `merchantName` | string | Optional | Merchant / display name. Up to 64 chars. |\n| `trxCode` | string | Optional | `BG` Buy Goods (till), `PB` Pay Bill, `WA` Withdraw at Agent, `SB` Send to Business, `SM` Send Money. Defaults to `BG`. |\n| `cpi` | string | Optional | Credit Party Identifier (till / paybill). Defaults to the tenant shortcode. |\n| `size` | string | Optional | Requested QR image size in pixels. |\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/qr-generate \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"amount\": 500,\n    \"refNo\": \"INV-2041\",\n    \"merchantName\": \"Acme Ltd\",\n    \"trxCode\": \"BG\"\n  }'\n```\n\n```json title=\"Response · 200 OK\"\n{\n  \"qrBase64\": \"iVBORw0KGgoAAAANSUhEUgAAAP…\"\n}\n```\n\n## POST /c2b-register\n\nRegister the offline C2B confirmation URL and validation URL for your shortcode. C2B covers Lipa na M-Pesa paybill and till payments. This endpoint is **idempotent**, so you can run it again safely. It needs no body. paylod derives the shortcode and the callback URL from your API key on the server.\n\nAfter you register, M-Pesa delivers offline customer payments to paylod as C2B confirmations. paylod records each one as a payment with source `c2b`. A signed webhook then fires. That webhook uses the **same envelope** as STK payments.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/c2b-register\n```\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/c2b-register \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{}'\n```\n\n```json title=\"Response · 200 OK\"\n{\n  \"registered\": true,\n  \"shortcode\": \"600123\",\n  \"confirmationUrl\": \"https://paylod.dev/functions/v1/c2b-hook/8f3c…\",\n  \"validationUrl\": \"https://paylod.dev/functions/v1/c2b-hook/8f3c…\",\n  \"responseCode\": \"0\",\n  \"responseDescription\": \"Success\"\n}\n```\n\nBoth URLs point at the **same** endpoint, `/c2b-hook/<token>`. The path token is unguessable. Safaricom POSTs both validation and confirmation to that endpoint. `responseCode` and `responseDescription` echo the Daraja acknowledgement. Both fields can be `null` if the shortcode already had a registration.\n\n## POST /transaction-status\n\nRead the status of any past M-Pesa transaction by its receipt. This endpoint is **asynchronous**. The HTTP call acknowledges with `202`. The answer arrives on a signed webhook of type `admin.transaction_status`. You must set the application's Initiator Name and Initiator Password.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/transaction-status\n```\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `transactionId` | string | Required | The M-Pesa receipt or TransactionID to read. |\n| `identifierType` | string | Optional | `1` MSISDN, `2` till, `4` shortcode. Defaults to `4`. |\n| `remarks` | string | Optional | Free-text remarks for the query. |\n| `occasion` | string | Optional | Occasion label. |\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/transaction-status \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"transactionId\": \"UG1F3A1U7J\",\n    \"identifierType\": \"4\"\n  }'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"queryId\": \"b1e2c3d4-…\",\n  \"conversationId\": \"AG_20260707_00001a2b3c4d\"\n}\n```\n\nCorrelate the webhook with `queryId` or `conversationId`.\n\n## POST /account-balance\n\nRead your shortcode's M-Pesa float. This endpoint is **asynchronous**. The HTTP call acknowledges with `202`. The answer arrives on a signed webhook of type `admin.account_balance`. You must set the application's Initiator Name and Initiator Password.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/account-balance\n```\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `identifierType` | string | Optional | `1` MSISDN, `2` till, `4` shortcode. Defaults to `4`. |\n| `remarks` | string | Optional | Free-text remarks for the query. |\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/account-balance \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{}'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"queryId\": \"9a8b7c6d-…\",\n  \"conversationId\": \"AG_20260707_00005e6f7a8b\"\n}\n```",
  },
];

/** Every page, concatenated — the same document served at https://paylod.dev/llms-full.txt. */
export const DOCS_FULL: string = "# paylod — complete documentation for AI agents (llms-full.txt)\n\n> paylod is M-Pesa (Safaricom Daraja) without the backend. Your server calls one SDK method;\n> paylod holds the Daraja credentials, hosts the callback, decodes every result code and signs\n> your webhooks. Money settles straight to your own till or paybill — paylod never takes custody\n> and never takes a cut.\n\nThis file is the whole of https://paylod.dev/docs in one request. It is GENERATED from the docs\nmarkdown at build time, so it cannot disagree with the website. The index is at\nhttps://paylod.dev/llms.txt; each page is also served as raw markdown at `<page>.md`.\n\n## Read this first (the nine things agents get wrong)\n\n1. **Use the SDK. Do not hand-roll HTTP.** `npm install @paylod/node`, then\n   `new Paylod(process.env.PAYLOD_API_KEY!)` and `await paylod.collectAndWait({ amount, phone })`.\n   Do not write a fetch wrapper, a polling loop, a callback endpoint, a Daraja OAuth token fetch,\n   or a base64 `shortcode+passkey+timestamp` password. All of that already exists. Only reach for\n   the raw HTTP API if you are not on a JavaScript runtime.\n2. **Render `result.message`.** Every settling call returns a `PaymentOutcome`:\n   `{ status, message, retryable, paid, receipt, code, detail, payment }`. `message` is a\n   customer-facing sentence, already decoded. Show it. Offer a retry **only** when\n   `retryable` is true. An integrator should never branch on a Daraja result code to decide what\n   a human sees.\n3. **`4999` is PENDING, not failed.** It means the STK prompt is still on the handset and the\n   customer has not entered their PIN. It is `retryable: false` — re-collecting fires a SECOND STK\n   prompt and can DOUBLE-CHARGE a real customer. Keep polling. (`500.001.1001` is the same thing.)\n4. **A timeout throws.** `wait()` / `collectAndWait()` throw `PaylodTimeoutError` rather than\n   returning `status: \"failed\"`, because an indeterminate payment is not a failed payment — the\n   customer may still be typing their PIN. Leave the order open; reconcile with `check()`.\n5. **The API key is server-side only.** It can move money (`/payout`, `/reversal`) and the amount\n   is caller-supplied, so a key in client-side JavaScript lets a payer set their own price. Never\n   put it in a browser, a mobile app, or a `NEXT_PUBLIC_*` variable. The browser calls YOUR server;\n   your server calls paylod.\n6. **Webhooks are optional.** Polling is a first-class, complete integration. Add a signed webhook\n   when your *server* needs to hear about a payment it is not actively waiting on.\n7. **`1032` (cancelled), `1037` (timeout) and `2001` (wrong PIN) are normal outcomes, not bugs.**\n   `2001`'s raw Daraja text says \"The initiator information is invalid\", which is misleading: it is\n   a customer-entered wrong PIN, not a credentials problem.\n8. **`accountReference` is a correlation id, not a message to the customer.** paylod returns it\n   to you as `accountRef` on the status read and on the webhook, so use it to tie a payment to\n   your order. Safaricom shows it to the payer only on a **Paybill** (`CustomerPayBillOnline`),\n   where it is the account number; on a **Till / Buy Goods** shortcode it is not displayed at all —\n   the payer sees the business name and the amount. `description` is what reaches the STK prompt.\n9. **Packages:** the SDK is `@paylod/node` (v0.2.0). The CLI installs as **`@paylod/cli`** — npm\n   rejected the unscoped name as too similar to `payload` — while the command it installs is still\n   `paylod`. The MCP server is remote: `https://mcp.paylod.dev/mcp`, OAuth 2.1, nothing to install.\n\n---\n\n## paylod: M-Pesa payments without a backend\n\nSource: https://paylod.dev/docs\n\npaylod is M-Pesa (Safaricom Daraja) without the backend: one HTTP call sends the STK Push, we host the callback, sign your webhooks and decode every result code.\n\n## What paylod is\n\npaylod is a hosted layer over Safaricom's **Daraja** API. You keep your own till or paybill, and your own Daraja credentials. M-Pesa settles the money **straight to you**. paylod never holds the money and never takes a part of it.\n\npaylod does the difficult work for you. You do not host a public HTTPS callback. You do not refresh access tokens. You do not retry failed STK pushes. You do not decode `resultCode: 1037`. You do not build a webhook signature scheme.\n\n```ts title=\"The whole integration\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n\nconst outcome = await paylod.collectAndWait({ amount: 100, phone: \"0712345678\" });\n\nif (outcome.paid) console.log(\"Paid:\", outcome.receipt);\nelse console.log(outcome.message);   // already decoded, safe to show a customer\n```\n\nThat is the full integration. Install `@paylod/node` and use one API key. paylod does the other work. If you do not use Node, make the same call over plain HTTP. See the [API reference](/docs/api).\n\n## How it works\n\n1. Create an **application** for a till or a paybill. Then put your Daraja consumer key, consumer secret, shortcode and passkey into the application. paylod encrypts these four values at rest, and the values never leave the server.\n2. paylod gives you a **hosted callback URL**. Copy the callback URL into the Safaricom Daraja portal one time. That is the only Safaricom procedure that you do.\n3. Your backend calls `collect()` with an amount and a phone number, and with a paylod **API key**. The customer's handset shows an M-Pesa PIN prompt.\n4. M-Pesa sends the result to paylod's callback. paylod puts the result into a standard format, decodes the result code, and records the payment. Then paylod **sends you a signed webhook**, or you **poll** for the result. You select the method.\n\n> Your integration must never let the client set the amount. Your server owns the amount. See [Secure integration](/docs/guides/security).\n\n## Where to start\n\n| If you want to… | Go to |\n| --- | --- |\n| Send your first STK push now | [Quickstart](/docs/quickstart) |\n| Use the Node/TypeScript client | [Node SDK](/docs/sdk) |\n| Send a payment from your terminal | [CLI](/docs/cli) |\n| Understand keys, environments and idempotency | [Authentication](/docs/authentication) |\n| Charge a customer correctly, in production | [Accept a payment](/docs/guides/accept-a-payment) |\n| Know when the money is in your account | [Handle the result](/docs/guides/handle-the-result) |\n| Find the parameters of an endpoint | [API reference](/docs/api) |\n| Find what `1032` means | [Error codes](/docs/errors) |\n| Let an AI agent do all of the above | [MCP server](/docs/mcp) |\n\n## What you can build\n\n- **Checkout** — collect a payment for an order. Fulfil the order when the webhook confirms the settled amount.\n- **Payouts** — send money to customers, riders or staff (B2C). Reverse a transaction when you must refund a customer.\n- **Offline payments** — register C2B, and counter payments to your till then reach your systems. Or give the customer a dynamic QR code.\n- **Operations** — read a past transaction by receipt, or read the float of your shortcode.\n\nEach item above is one SDK call, or one HTTP request. Each item uses one API key. You host no callback.\n\n---\n\n## Quickstart: your first M-Pesa STK Push\n\nSource: https://paylod.dev/docs/quickstart\n\nYour first M-Pesa STK Push in five minutes — create an application, add your Daraja credentials, mint a key, and collect a payment.\n\nYou need a Safaricom Daraja account and approximately five minutes. The [sandbox](https://developer.safaricom.co.ke) account is free and immediate. Do one `npm install`, or make one cURL call from any other stack. You deploy nothing: no callback server and no tunnel.\n\n## 1. Create an application\n\nIn the paylod dashboard, create an **application**. Create one application for each till or paybill. Give the application a name. You can add a Sandbox environment and a Production environment to the same application.\n\n## 2. Add your Daraja credentials\n\nOn the application's **Credentials** tab, put in the four values that Daraja gives you:\n\n| Field | Where it comes from |\n| --- | --- |\n| Consumer key | Daraja portal → your app |\n| Consumer secret | Daraja portal → your app |\n| Shortcode | Your till / paybill number (`174379` in sandbox) |\n| Passkey | Daraja portal → Lipa na M-Pesa Online |\n\npaylod encrypts the four values at rest. The four values are **write-only**: no API, no tool and no dashboard screen reads them back.\n\n## 3. Copy the callback URL into Daraja\n\npaylod shows you a hosted callback URL on the application's **Endpoints & Webhooks** tab. Copy the callback URL into the Daraja portal as your Lipa na M-Pesa Online callback URL.\n\nThis step replaces the public HTTPS endpoint that you would otherwise host. You write no callback code.\n\n## 4. Create an API key\n\nOn the **API Keys** tab, create a key. Each key applies to one environment:\n\n- `mp_test_…` → Sandbox\n- `mp_live_…` → Production\n\npaylod shows the key **one time only**. The key tells paylod which application and which environment to use for a request. Thus you never send a shortcode or a Daraja credential in a request.\n\n> [!warn] The API key is a server-side secret. Never put the API key in a browser, in a mobile app, or in other software that a payer opens.\n\n## 5. Install the SDK\n\nOn Node, add the official client. For all other languages, call the HTTP API directly with cURL. The PHP, Python, Java and Kotlin clients are still in development. The samples below on this page show the interface that these clients will have. Use the language selector in the top-left corner to change all the samples on this page together.\n\n```bash tab=\"node\" title=\"npm\"\nnpm install @paylod/node\n```\n```text tab=\"php\" title=\"PHP — not released yet\"\nThe PHP SDK is not on Packagist yet. Follow this quickstart on the cURL tab.\n```\n```text tab=\"python\" title=\"Python — not released yet\"\nThe Python SDK is not on PyPI yet. Follow this quickstart on the cURL tab.\n```\n```text tab=\"java\" title=\"Java — not released yet\"\nThe Java SDK is not on Maven Central yet. Follow this quickstart on the cURL tab.\n```\n```text tab=\"kotlin\" title=\"Kotlin — not released yet\"\nThe Kotlin SDK is not on Maven Central yet. Follow this quickstart on the cURL tab.\n```\n```bash tab=\"curl\" title=\"No dependency\"\n# Nothing to install — any HTTP client works. Check you have cURL:\ncurl --version\n```\n\nPut the API key in your environment. Each client reads the API key automatically:\n\n```bash title=\".env\"\nPAYLOD_API_KEY=mp_test_YOUR_API_KEY\n```\n\n## 6. Send your first STK push\n\n> [!warn] **Always pass `idempotencyKey`. Create one idempotency key for each payment attempt.** A double-clicked Pay button, a refreshed tab and a retried request are duplicates of one attempt. Duplicates of one attempt then become **one** prompt and **one** charge, even when they arrive at the same moment. If you do not pass `idempotencyKey`, each call is a new charge: two clicks give two prompts and two debits. Do **not** use the order id or the product id as the idempotency key. A used key replays the *old* payment, and does not make a new payment. A retry after a wrong M-Pesa PIN is a new charge, and needs a **new** key. If you omit `idempotencyKey`, the SDK gives you a warning. [Full rules](/docs/sdk#idempotency).\n\n```ts tab=\"node\" title=\"collect.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n\nconst outcome = await paylod.collectAndWait({\n  amount: 10,\n  phone: \"0712345678\",\n  idempotencyKey: attempt.id,    // one key per payment attempt. A double-click cannot charge twice.\n  accountReference: \"INV-2041\",\n  description: \"Order #2041\",\n});\n\nif (outcome.paid) {\n  console.log(\"Paid:\", outcome.receipt);   // \"UG1F3A1U7J\"\n} else {\n  console.log(outcome.message);            // already decoded, safe to show a customer\n}\n```\n```php tab=\"php\" title=\"collect.php\"\nuse Paylod\\Paylod;\n\n$paylod = new Paylod(getenv('PAYLOD_API_KEY'));\n\n$outcome = $paylod->collectAndWait([\n    'amount' => 10,\n    'phone' => '0712345678',\n    'idempotencyKey' => $attempt->id,   // one key per payment attempt\n    'accountReference' => 'INV-2041',\n    'description' => 'Order #2041',\n]);\n\nif ($outcome->paid) {\n    echo \"Paid: {$outcome->receipt}\";\n} else {\n    echo $outcome->message;             // already decoded, safe to show a customer\n}\n```\n```python tab=\"python\" title=\"collect.py\"\nimport os\nfrom paylod import Paylod\n\npaylod = Paylod(os.environ[\"PAYLOD_API_KEY\"])\n\noutcome = paylod.collect_and_wait(\n    amount=10,\n    phone=\"0712345678\",\n    idempotency_key=attempt.id,   # one key per payment attempt\n    account_reference=\"INV-2041\",\n    description=\"Order #2041\",\n)\n\nif outcome.paid:\n    print(\"Paid:\", outcome.receipt)\nelse:\n    print(outcome.message)        # already decoded, safe to show a customer\n```\n```java tab=\"java\" title=\"Collect.java\"\nimport dev.paylod.Paylod;\nimport dev.paylod.CollectParams;\nimport dev.paylod.PaymentOutcome;\n\nPaylod paylod = new Paylod(System.getenv(\"PAYLOD_API_KEY\"));\n\nPaymentOutcome outcome = paylod.collectAndWait(\n    CollectParams.builder()\n        .amount(10)\n        .phone(\"0712345678\")\n        .idempotencyKey(attempt.id())   // one key per payment attempt\n        .accountReference(\"INV-2041\")\n        .description(\"Order #2041\")\n        .build());\n\nif (outcome.isPaid()) {\n    System.out.println(\"Paid: \" + outcome.getReceipt());\n} else {\n    System.out.println(outcome.getMessage());\n}\n```\n```kotlin tab=\"kotlin\" title=\"Collect.kt\"\nimport dev.paylod.Paylod\n\nval paylod = Paylod(System.getenv(\"PAYLOD_API_KEY\"))\n\nval outcome = paylod.collectAndWait(\n    amount = 10,\n    phone = \"0712345678\",\n    idempotencyKey = attempt.id,   // one key per payment attempt\n    accountReference = \"INV-2041\",\n    description = \"Order #2041\",\n)\n\nif (outcome.paid) {\n    println(\"Paid: ${outcome.receipt}\")\n} else {\n    println(outcome.message)\n}\n```\n```bash tab=\"curl\" title=\"Raw HTTP\"\n# collectAndWait is SDK sugar: POST /collect, then poll GET /status/:id.\ncurl -X POST https://paylod.dev/functions/v1/collect \\\n  -H \"Authorization: Bearer mp_test_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Idempotency-Key: att_9f2c1b40\" \\\n  -d '{ \"amount\": 10, \"phone\": \"254712345678\", \"accountReference\": \"INV-2041\", \"description\": \"Order #2041\" }'\n# → 202 { \"paymentId\": \"...\", \"status\": \"pending\" }\n# then poll GET /functions/v1/status/{paymentId} until status is success or failed\n```\n\nThat is the whole integration: one argument to the constructor, and one call.\n\n`collectAndWait()` sends the STK push. Then `collectAndWait()` polls until the payment settles. The customer's handset shows the prompt, the customer enters the M-Pesa PIN, and you get an answer. You write no callback server. You refresh no access token. You write no result code table.\n\n> [!warn] A **timeout throws** `PaylodTimeoutError`. A timeout does not report `status: \"failed\"`. An unanswered prompt is not a failed payment, because the customer can still be busy with the M-Pesa PIN. Keep the order open. See [Handle the result](/docs/guides/handle-the-result).\n\nThe answer is a `PaymentOutcome`. The `message` field is already decoded, and you can show the `message` field to a customer. The `retryable` flag tells you if a second charge is safe. A wrong M-Pesa PIN is a business outcome and not a program fault. Thus the SDK returns a wrong M-Pesa PIN as data, and does not throw an error.\n\npaylod puts phone numbers into a standard format for you. Thus `0712345678`, `+254712345678` and `254712345678` all work.\n\nYou do not need a webhook. A poll is a fully supported method, and `collectAndWait()` polls for you. Add a signed webhook when your server does not wait for a payment. paylod then tells your server about that payment.\n\n## If you do not use Node\n\nThe SDK is a wrapper around a plain HTTP API. Every language can call the HTTP API directly:\n\n```bash title=\"The same call, over HTTP\"\ncurl -X POST https://paylod.dev/functions/v1/collect \\\n  -H \"Authorization: Bearer mp_test_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Idempotency-Key: attempt_9f3c1a7e\" \\\n  -d '{ \"amount\": 10, \"phone\": \"254712345678\" }'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"paymentId\": \"e69e5c00-8a01-44ed-b003-48e2e86a7c9e\",\n  \"status\": \"pending\",\n  \"checkoutRequestId\": \"ws_CO_010720261905029287161380\"\n}\n```\n\nThen poll `GET /functions/v1/status/{paymentId}` until the payment settles. If you call the HTTP API directly, you must write the code that the SDK usually gives you: the poll loop, the idempotency key, and the result code decoder. The [API reference](/docs/api) shows the full interface.\n\n## Next\n\n- Every method, option and error class: [Node SDK](/docs/sdk).\n- Do you prefer the terminal? The [CLI](/docs/cli) sends the same STK push in one command.\n- Do you prefer a webhook instead of a poll? [Handle the result](/docs/guides/handle-the-result) describes signed webhooks.\n- Test the failure paths first: [Test in sandbox](/docs/guides/test-in-sandbox).\n- Are you ready for real money? [Go live](/docs/guides/go-live).\n\n---\n\n## Authenticating with the paylod M-Pesa API\n\nSource: https://paylod.dev/docs/authentication\n\nAPI keys, environments, the base URL, idempotency and rate limits — everything every paylod request has in common.\n\n## Base URL\n\nEvery endpoint is under `/functions/v1`.\n\n```text title=\"Base URL\"\nhttps://paylod.dev/functions/v1\n```\n\n## API keys\n\nAuthenticate every request with a paylod API key as a bearer token.\n\n```http title=\"Header\"\nAuthorization: Bearer mp_live_YOUR_API_KEY\n```\n\nCreate API keys on the application's **API Keys** tab. paylod shows an API key once. The API key encodes two things:\n\n- **The application** that the request runs against. You therefore never send a shortcode.\n- **The environment** that the request runs in. You therefore never send a `?sandbox=true` flag.\n\n| Prefix | Environment | Money |\n| --- | --- | --- |\n| `mp_test_…` | Sandbox | Simulated. Nothing settles. |\n| `mp_live_…` | Production | Real. The money settles to your till. |\n\nYour Daraja consumer key, secret and passkey stay in encrypted storage on the server. They never travel over the network. No endpoint reads them back.\n\n> [!warn] Keep every API key on the server. An API key in a browser or in a mobile app is fully compromised. Treat that API key as public and rotate it. See [Secure integration](/docs/guides/security).\n\n## Idempotency\n\n`POST /collect` accepts an `Idempotency-Key` header. It also accepts an `idempotencyKey` field in the body. The idempotency key names **one payment attempt**. Send the idempotency key, and duplicate deliveries of that attempt collapse into a single payment and a single STK Push. A double-click, a refreshed tab, a redelivered job and an internal network retry are all duplicate deliveries.\n\n```bash tab=\"curl\" title=\"Idempotent collect\"\ncurl -X POST https://paylod.dev/functions/v1/collect \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Idempotency-Key: att_9f2c1b40\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{ \"amount\": 10, \"phone\": \"254712345678\" }'\n```\n```ts tab=\"node\" title=\"Idempotent collect\"\n// The SDK sends an Idempotency-Key on every collect() — pass one per attempt.\nconst ack = await paylod.collect({\n  amount: 10,\n  phone: \"254712345678\",\n  idempotencyKey: attempt.id,\n});\n```\n```php tab=\"php\" title=\"Idempotent collect\"\n// The SDK sends an Idempotency-Key on every collect() — pass one per attempt.\n$ack = $paylod->collect([\n    'amount' => 10,\n    'phone' => '254712345678',\n    'idempotencyKey' => $attempt->id,\n]);\n```\n```python tab=\"python\" title=\"Idempotent collect\"\n# The SDK sends an Idempotency-Key on every collect() — pass one per attempt.\nack = paylod.collect(\n    amount=10,\n    phone=\"254712345678\",\n    idempotency_key=attempt.id,\n)\n```\n```java tab=\"java\" title=\"Idempotent collect\"\n// The SDK sends an Idempotency-Key on every collect() — pass one per attempt.\nCollectAck ack = paylod.collect(\n    CollectParams.builder()\n        .amount(10)\n        .phone(\"254712345678\")\n        .idempotencyKey(attempt.id())\n        .build());\n```\n```kotlin tab=\"kotlin\" title=\"Idempotent collect\"\n// The SDK sends an Idempotency-Key on every collect() — pass one per attempt.\nval ack = paylod.collect(\n    amount = 10,\n    phone = \"254712345678\",\n    idempotencyKey = attempt.id,\n)\n```\n\n**Make one idempotency key for each attempt, not for each order and not for each product.** paylod reserves the idempotency key *before* it calls Daraja. Ten simultaneous requests with the same key therefore produce exactly one payment and one push. All ten requests return the same `paymentId`. A key is also spent once you use it. If you key on an order id, a retry of that order replays the failed first attempt. If you key on a product id, every later customer replays the first payment for that product. A retry after a wrong PIN is a **new charge** and needs a **new key**.\n\npaylod rejects a reused idempotency key in three cases. All three cases return `409`:\n\n| Reuse | Response |\n| --- | --- |\n| Same key, **different** body | `409`. Two different charges collided on one idempotency key. This case is a bug in your code. |\n| Same key, first request still in flight | `409` with `Retry-After`. The first request is still in contact with Daraja. Wait, then read the result. |\n| Same key, a previous attempt was **interrupted mid-flight against Daraja** | `409` **indeterminate**. paylod cannot prove that no debit occurred, and paylod does not repeat the call. |\n\n> [!warn] **Do not retry the indeterminate `409` with the same idempotency key.** A timeout is not evidence that the money did not move. paylod therefore does not send the call again. For money, at-most-once is better than at-least-once. Read the payment status first, with `GET /status/:id` or with your webhook. Then decide. If the payment settled, you are done. If nothing happened, start a new attempt with a **new** key.\n\n## Rate limits\n\npaylod limits the rate of `POST /collect` for each API key and for each phone number. A burst returns `429 Too Many Requests`. Back off when you receive a `429`. paylod refuses a `429` *before* the request reaches Daraja. A rate-limited request does not spend the idempotency key. You can therefore retry with the same `Idempotency-Key` after a short delay.\n\n## Errors\n\npaylod returns conventional HTTP status codes.\n\n| Status | Meaning |\n| --- | --- |\n| `400` | The JSON is malformed, or you did not set Daraja credentials for this application and environment. |\n| `401` | The API key is missing or invalid. |\n| `404` | The resource does not exist, or it belongs to another application. |\n| `409` | You reused the idempotency key with a different body. Or the first request is still in flight. Or an interrupted provider call spent the key. The third case is indeterminate: read the status, then retry with a new key. |\n| `422` | The body failed validation. For example, `amount` ≤ 0, or paylod cannot parse the phone number. |\n| `429` | You reached the rate limit. Back off, then retry. |\n| `502` | Daraja rejected the request. paylod passes the upstream error through. |\n\nM-Pesa also has its own result codes: `1032` customer cancelled, `1037` timeout, `2001` wrong PIN. The [error reference](/docs/errors) decodes every one of these codes.\n\n---\n\n## Accept an M-Pesa payment with an STK Push\n\nSource: https://paylod.dev/docs/guides/accept-a-payment\n\nCollect money from a customer with an M-Pesa STK Push: what to send, what comes back, and what the customer sees.\n\nCharge a customer. Your server sets the amount. The customer's handset shows a prompt, the customer enters the M-Pesa PIN, and the money settles to your till.\n\n## Send the push\n\n> [!warn] **Always pass `idempotencyKey`. Create one idempotency key for each payment attempt.** A double-clicked Pay button, a refreshed tab and a retried job are duplicates of one attempt. Duplicates of one attempt then become **one** prompt and **one** charge. If you do not pass `idempotencyKey`, each call is a new charge: two clicks give two prompts and two debits. Do **not** use the order id or the product id as the idempotency key. A used key replays an old payment, and does not make a new payment. See [Idempotency](#idempotency-one-key-per-payment-attempt).\n\n```ts title=\"charge.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n\nconst outcome = await paylod.collectAndWait({\n  amount: 1500,                     // whole KES, from YOUR order record\n  phone: \"0712345678\",\n  idempotencyKey: attempt.id,       // one key per payment attempt. A double-click cannot charge twice.\n  accountReference: \"INV-2041\",     // YOUR correlation id — comes back on the result\n  description: \"Order #2041\",\n  metadata: { orderId: \"2041\" },    // opaque, stored with the payment\n});\n\nif (outcome.paid) {\n  await fulfil(\"2041\", outcome.receipt);\n} else {\n  await notify(outcome.message);\n}\n```\n\n`collectAndWait()` sends the STK push. Then `collectAndWait()` polls until the payment settles. You need only one condition: `outcome.paid`. If `outcome.paid` is false, `outcome.message` is a decoded sentence that you can show to the customer.\n\nDo you want the `paymentId` immediately? Use `collect()`. `collect()` returns when the prompt is on the handset. See [Handle the result](/docs/guides/handle-the-result).\n\n## What M-Pesa requires\n\n- **Whole shillings.** `amount` is an integer from 1 to 150,000. M-Pesa rejects decimals.\n- **A Kenyan number.** `0712…`, `254712…` and `+254712…` all work. The SDK puts them into a standard format.\n- **One prompt at a time.** You cannot send a second prompt to a customer who already has a prompt.\n\nThe maximum length of `accountReference` is 12 characters. The maximum length of `description` is 64 characters. The SDK checks both fields locally, before the request leaves your process.\n\n## What `accountReference` is\n\n`accountReference` is a **correlation id that you select**, for example an invoice number or an order number. paylod stores `accountReference` on the payment. paylod then returns the value as `accountRef` on the webhook. Thus you use `accountReference` to connect an M-Pesa payment to the order in your own database. The `GET /status/:id` read returns only `id`, `status`, `mpesaReceipt`, `resultCode` and `resultDesc`.\n\n> [!warn] Never put text that the customer must read into `accountReference`. The `description` field supplies the text that the prompt shows. Also, never put a **secret** into `accountReference`, because a payer on a paybill can see the value.\n\n`accountReference` is *not* a message to the customer. Safaricom shows `accountReference` only on a **paybill** (`CustomerPayBillOnline`). On a paybill, `accountReference` is the account number that the payer pays to. On a **till** shortcode (`CustomerBuyGoodsOnline`), Safaricom does not show `accountReference`. The payer sees only your business name and the amount. Most small merchants use a till. Thus you must assume that the customer never reads `accountReference`.\n\nIf you omit `accountReference`, paylod uses a short prefix of the `paymentId` as the default value. The default value is unique, and it connects the payment to your M-Pesa statement.\n\n`accountReference` is a **label, and not a lock**. If you put your order id into `accountReference`, a second charge can still occur. `accountReference` only marks the payment. [`idempotencyKey`](#idempotency-one-key-per-payment-attempt) is the field that collapses duplicates. The two fields have different functions: put the order id into `accountReference`, and put a key for each attempt into `idempotencyKey`.\n\n## Idempotency: one key per payment attempt\n\nA customer double-clicks **Pay**, and your handler runs two times. Without an idempotency key, the customer gets two STK prompts and two debits. Then the customer asks you for a refund and an explanation.\n\nCreate a key when the attempt starts, and two debits cannot occur:\n\n```ts title=\"charge.ts\"\nconst attempt = await db.attempts.create({ orderId: order.id });   // one row per press of Pay\n\nawait paylod.collectAndWait({\n  amount: order.amount,\n  phone,\n  idempotencyKey: attempt.id,   // ← the whole fix\n});\n```\n\nThe two requests carry the same key. paylod reserves the key *before* paylod calls Daraja. Thus only one request reaches Daraja, and the other request replays the first answer. Both requests get the same `paymentId` and the same `checkoutRequestId`. The handset shows only one prompt. This behaviour also applies when the two requests arrive in the same millisecond: ten concurrent calls with one key make one payment and one STK push.\n\n| What the customer does | With a per-attempt `idempotencyKey` | Without a key |\n| --- | --- | --- |\n| Double-clicks **Pay** | 1 prompt, 1 charge | **2 prompts, 2 charges** |\n| Refreshes the tab and re-submits | 1 prompt, 1 charge | **2 prompts, 2 charges** |\n| Your job queue retries the handler | 1 prompt, 1 charge | **2 prompts, 2 charges** |\n\nIf you omit the key, the SDK gives you a warning one time. A missing key is a money defect.\n\n### Per attempt — not per order, not per product\n\nThe key must be **the same for all duplicates of one attempt**. The key must also be **new for a new charge**. Two usual short methods do not obey these two rules:\n\n- **The order id** is the same for all duplicates, but the order id is never new. The customer enters a wrong M-Pesa PIN. You then retry the order with the same key. paylod replays the **failed** first attempt, and does not charge the customer. Thus the order is never paid.\n- **A product id**, or any other value that you use again for a different purchase, is worse. The second customer buys the same product and uses the key that the first customer already spent. paylod then replays the payment of the first customer. **paylod charges no customer after the first customer.**\n\nA retry after a wrong M-Pesa PIN, a cancelled prompt or a timeout is a **new attempt**. A new attempt needs a new row and a new key.\n\n> [!warn] Do **not** stop the SDK warning with `idempotencyKey: crypto.randomUUID()` **at the call site**. A key that you generate again on each call has the same effect as no key. Such a key only hides the warning that tells you the customer is exposed. A random UUID is a good key. But you must create the UUID **one time for each attempt** and store it. Do not generate a new UUID inside the call.\n\n### When the same key is *not* a safe retry\n\nIf you use a key again with a *different* body, for example a different amount, paylod returns `409`. A `409` of this type is always a defect in your code: two different charges use one key.\n\nOne condition looks like a network error, but you must not retry it. A request can stop after paylod sent the call to Daraja and before an answer came back. That key is then **spent**. If you retry the spent key, paylod returns `409` with an *indeterminate* message, and sends no second STK push. The outcome of such a payment is unknown, and a timeout is not proof that the money did not move. Thus paylod does not guess: **for money, at-most-once is better than at-least-once.**\n\n> [!warn] **After an indeterminate `409`, do not retry with the same key.** First read the payment status with `paylod.check(paymentId)` or `GET /status/:id`. Then decide. If the payment settled, fulfil the order. If no payment occurred, open a **new attempt with a new key**. A retry with the spent key returns the same `409`.\n\nYou cannot always supply a key at the start. Then store the generated key with the attempt before you retry:\n\n```ts title=\"retry.ts\"\nconst ack = await paylod.collect({ amount: 1500, phone: \"0712345678\" });\nawait db.attempts.update(attempt.id, { idempotencyKey: ack.idempotencyKey });\n\n// Retrying THAT attempt: same key, same body → the original payment, no second prompt.\nawait paylod.collect({\n  amount: 1500,\n  phone: \"0712345678\",\n  idempotencyKey: ack.idempotencyKey,\n});\n// A genuinely new attempt gets a NEW key. Reusing this one would replay the old result.\n```\n\n## Never take the amount from the client\n\n> [!warn] Always take the amount from your own order record, on your own server. A browser or a mobile app can hold both the API key and the amount. A payer can then change the request and pay less. This is the only method that lets a payer pay you less, and you can prevent it. See [Secure integration](/docs/guides/security).\n\n## Next\n\n[Handle the result](/docs/guides/handle-the-result) describes webhooks. It also tells you what to check before you fulfil an order.\n\n---\n\n## Handle the M-Pesa payment result\n\nSource: https://paylod.dev/docs/guides/handle-the-result\n\nTwo ways to learn a payment settled — poll GET /status, or receive a signed webhook. How to choose, and how to verify.\n\n`collect()` returns before the customer enters the M-Pesa PIN. Two methods tell you the result, and both methods read the same settled record.\n\n| | Poll | Webhook |\n| --- | --- | --- |\n| You host | Nothing | An HTTPS endpoint |\n| Good for | Scripts, a checkout with a spinner | Servers, high volume, back-office fulfilment |\n\nStart with the poll method. Change to a webhook when you have a server that must receive the result.\n\n## Option A: poll\n\n> A timeout **throws** `PaylodTimeoutError`. A timeout does not return `ok: false`. An unanswered prompt is not a failed payment: the customer can still enter the M-Pesa PIN, and the payment can still succeed. Do not mark the order as failed. Let the webhook tell you the result.\n\n```ts title=\"poll.ts\"\nconst outcome = await paylod.wait(ack.paymentId, { timeoutMs: 120_000 });\n\nif (outcome.paid) await fulfil(outcome.receipt);\nelse              await notify(outcome.message);\n```\n\n`wait()` polls with a random and increasing delay until the payment settles. If the payment is still pending, paylod makes a live M-Pesa STK Query and settles the payment immediately. Thus a poll never returns an old answer.\n\n`paylod.status(paymentId)` reads the record one time, and does not poll.\n\n## Option B: receive a signed webhook\n\nRegister an endpoint on the application's **Endpoints & Webhooks** tab. The SDK verifies the signature for you.\n\n```ts title=\"app/api/webhooks/paylod/route.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod();\n\nexport const POST = paylod.webhookHandler(async (event) => {\n  if (event.type === \"payment.success\") {\n    await fulfil(event.data.paymentId, event.data.mpesaReceipt);\n  } else if (event.data.decoded) {\n    await notify(event.data.decoded.customerMessage);\n  }\n});\n```\n\n`decoded` is `DecodedError | null`. paylod fills `decoded` on `payment.failed`, and sets `decoded` to `null` on `payment.success`. Test `decoded` before you read it.\n\nThis handler operates on each platform that supplies the Web `Request` and `Response` API: Next.js, Hono, Remix, Workers, Bun and Deno. On Express, use `paylod.webhook(handler)`.\n\n> [!warn] On Express, mount this route **before** each global `express.json()`. A JSON parser consumes the body, and the raw bytes are then not available. Without the raw bytes, no software can verify the signature. The middleware finds this condition and throws an error. The middleware does not skip the check.\n\npaylod checks the signature **before** your handler runs. An incorrect signature returns `400`, and your handler does not receive the event. If your handler throws an error, the route returns `500`. paylod then sends the event again. Thus you must make your handler idempotent.\n\n## Check the amount before you fulfil\n\nThe webhook carries the **true settled amount**. If you fulfil an order on the `status` field alone, a customer can pay you less than the order total.\n\n```ts title=\"fulfil.ts\"\nexport const POST = paylod.webhookHandler(async (event) => {\n  if (event.type !== \"payment.success\") return;\n\n  // Look the order up by paymentId — the id you stored when you called collect().\n  const order = await db.orders.findByPaymentId(event.data.paymentId);\n  if (!order) return;\n\n  if (event.data.amount === order.amountKes) {\n    await fulfil(order);\n  } else {\n    await flagForReview(order, event.data);   // underpaid\n  }\n});\n```\n\nStore `ack.paymentId` with your order when you send the STK push. Then use `ack.paymentId` for the lookup. The event does not return the `metadata` that you passed to `collect()`.\n\n## Failures are results, not outages\n\n`payment.failed` is a normal outcome. paylod decodes `event.data.decoded` for you:\n\n- `1032` — the customer pressed Cancel. Offer a retry.\n- `1037` — the prompt did not reach the handset, or the customer ignored the prompt.\n- `2001` — the customer entered a wrong M-Pesa PIN.\n- `1` — the customer has insufficient balance.\n\n`4999` is **not** a failure. `4999` means that the customer did not enter the M-Pesa PIN yet. Continue to poll. Never send a second `collect()` call. A second STK push makes a second prompt, and can charge the customer two times. The [error reference](/docs/errors) lists every code.\n\n## Retry a failed payment\n\nA payment can truly fail: the customer cancelled the prompt, entered a wrong M-Pesa PIN, or has insufficient balance. A second charge is then a **new payment attempt**, and the new attempt needs a **new idempotency key**. A retry with the key of the failed attempt does not charge the customer. Such a retry only replays the failure that you already have.\n\n> [!warn] **One `409` is not a retry signal: the *indeterminate* `409`.** An earlier request under that key can stop while the call to Daraja is in flight. paylod then refuses to send the call again, because the money can already have moved. A timeout is not proof that the money did not move. First read the payment status with `paylod.check(paymentId)` or `GET /status/:id`, or wait for the webhook. If the payment settled, fulfil the order. If no payment occurred, open a new attempt with a **new** key. Never retry the spent key. See [Idempotency](/docs/sdk#idempotency) for the full rules.\n\n```ts title=\"retry.ts\"\nif (!outcome.paid && outcome.retryable) {\n  const retry = await db.attempts.create({ orderId: order.id });   // new attempt → new key\n  await paylod.collectAndWait({ amount, phone, idempotencyKey: retry.id });\n}\n```\n\n## Verify it yourself\n\nIf you do not use the SDK, compute the signature yourself. The signature is an HMAC-SHA256 over `t + \".\" + rawBody`, with the signing secret of the endpoint as the key. The freshness window is 5 minutes. Compute the signature over the raw bytes. Never compute the signature over an object that you serialised again. The [webhooks reference](/docs/api/webhooks) specifies the scheme.\n\n## Next\n\n[Test in sandbox](/docs/guides/test-in-sandbox) makes each of these outcomes on demand.\n\n---\n\n## Test M-Pesa payments in the Daraja sandbox\n\nSource: https://paylod.dev/docs/guides/test-in-sandbox\n\nBuild the whole flow against Safaricom's sandbox with test credentials — including the failure paths — before a shilling moves.\n\n## Test every failure path without a phone\n\nStart here. You do **not** need a handset to test a cancellation, a wrong M-Pesa PIN, insufficient funds or a timeout. Do not try to make these outcomes by hand. paylod supplies a simulator that uses the **real** settlement path. The simulator makes the same payment row, the same result codes, and the same signed webhook that your production code receives. Only the handset is simulated.\n\nTwo calls:\n\n1. **Create a simulated payment** — use `paylod.simulate.collect()` in the SDK, the `simulate_test_payment` MCP tool, or `POST /simulate/collect` over REST with your `mp_test_…` key. paylod sends no STK push. The call returns `{ paymentId, status: \"pending\", outcomes }`, and `outcomes` is an array of `{ id, label, status }`.\n2. **Force the result** — use `paylod.simulate.outcome()`, the `simulate_outcome` MCP tool, or `POST /simulate/outcome`. Give that `paymentId` and one `outcome` id.\n\n| `outcome` | Settles as | Result code |\n| --- | --- | --- |\n| `approve` | `success` | `0` |\n| `user_cancelled` | `failed` | `1032` |\n| `timeout` | `failed` | `1037` |\n| `wrong_pin` | `failed` | `2001` |\n| `insufficient_funds` | `failed` | `1` |\n\n### From the SDK\n\nOn Node, use this method. The method is one call. The method returns the same `PaymentOutcome` that your production code already displays. Thus you test the real code, and not special test code.\n\n```ts title=\"checkout.test.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_TEST_KEY!);   // mp_test_…\n\nfor (const outcome of paylod.simulate.outcomes) {\n  const result = await paylod.simulate.pay({ outcome, amount: 10 });\n  console.log(outcome, \"→\", result.status, result.retryable, result.message);\n}\n// approve            → succeeded false Payment received — thank you!\n// wrong_pin          → failed    true  That M-Pesa PIN was incorrect. …\n// insufficient_funds → failed    true  Your M-Pesa balance is too low. …\n// user_cancelled     → cancelled true  Payment cancelled — you can try again …\n// timeout            → failed    true  The M-Pesa prompt expired before it was answered. …\n```\n\nTo test *your* handler, and not the handler of the SDK, make the client with `{ simulate: true }`. `collect()` then creates a simulated payment, and sends no STK push. Your `/api/pay` route operates without a change. The SDK refuses an `mp_live_…` key locally, before it sends a request. The [SDK reference](/docs/sdk#test-your-checkout-without-a-phone) gives the full details.\n\n### Over REST\n\n```bash title=\"Simulate a cancelled payment over REST\"\n# 1. create the pending simulated payment\ncurl -sX POST https://paylod.dev/functions/v1/simulate/collect \\\n  -H \"Authorization: Bearer $PAYLOD_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"applicationId\":\"<your-app-id>\",\"phone\":\"254708374149\",\"amount\":10}'\n# → { \"paymentId\": \"…\", \"status\": \"pending\", \"outcomes\": [ { \"id\": \"approve\", … } ] }\n\n# 2. force the outcome — your webhook fires, your poller settles, exactly as in production\ncurl -sX POST https://paylod.dev/functions/v1/simulate/outcome \\\n  -H \"Authorization: Bearer $PAYLOD_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"paymentId\":\"<paymentId>\",\"outcome\":\"user_cancelled\"}'\n```\n\nAn AI agent with the `paylod:payments.simulate` scope can do the same with `simulate_test_payment` and `simulate_outcome`. Such an agent cannot touch real money. See the [MCP server](/docs/mcp).\n\n> [!note] The simulator operates only in sandbox. The simulator is the fastest method to prove three things: your code shows `outcome.message`, your code offers a retry only when `retryable` is true, and your code never fulfils an order on a `1032`.\n\n## Get sandbox credentials\n\nCreate an app on the [Safaricom Daraja portal](https://developer.safaricom.co.ke). Then open the **Sandbox** credentials of the app. You need four values:\n\n| Field | Sandbox value |\n| --- | --- |\n| Consumer key | From the Daraja portal |\n| Consumer secret | From the Daraja portal |\n| Shortcode | `174379` (Safaricom's shared test paybill) |\n| Passkey | From \"Lipa na M-Pesa Online\" in the portal |\n\nCopy the four values into the **Sandbox** environment of your paylod application. Then create an `mp_test_…` key.\n\n## What sandbox does\n\nSandbox is the environment of Safaricom, and not a paylod simulation. The STK push is real. The STK push goes to Daraja, Daraja answers, and the callback comes back to the hosted receiver of paylod. This is the same behaviour as production. Only the money is not real, because nothing settles.\n\nThus each item that you build is the same in production: the `202`, the `paymentId`, the webhook envelope, the signature and the result codes. You do not write a second integration.\n\n## Send a test payment\n\nSet `PAYLOD_API_KEY` to the `mp_test_…` key. Nothing else changes.\n\n```ts title=\"sandbox.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);   // mp_test_… key\n\nconst outcome = await paylod.collectAndWait({\n  amount: 10,\n  phone: \"254708374149\",     // Safaricom's published test MSISDN\n  accountReference: \"TEST-1\",\n});\n\nconsole.log(outcome.paid ? outcome.receipt : outcome.message);\n```\n\nUse a real number that you control, and you get a real prompt. No real money moves.\n\n## Test the failure paths\n\nMost production incidents occur on a failure path that nobody tested. Before you go live, make sure that your code gives a correct result for each failure path below. Use the **simulator** above. The simulator is deterministic, needs no handset, and runs in your test suite. The handset column is only for a person who wants to see a real failure one time.\n\n| Result code | Simulator `outcome` | On a real handset | What your code should do |\n| --- | --- | --- | --- |\n| `1032` | `user_cancelled` | Press **Cancel** on the prompt | Offer a retry. Do not fulfil the order |\n| `1037` | `timeout` | Ignore the prompt until it expires | Retry, or use the poll method |\n| `2001` | `wrong_pin` | Enter the wrong M-Pesa PIN | Ask the customer to try again |\n| `1` | `insufficient_funds` | Send an amount above the test balance | Tell the customer that the balance is low |\n\nThe [error reference](/docs/errors) gives every code with its cause and its correction.\n\n## Test your webhook locally\n\nYou need no tunnel. The [CLI](/docs/cli) sends your live webhook events to `localhost`, and keeps the signature:\n\n```bash title=\"Terminal\"\nnpx @paylod/cli listen --forward http://localhost:3000/webhook\n```\n\nYour handler then verifies a real signature against a real payload, on your own computer. See [Handle the result](/docs/guides/handle-the-result).\n\n## Next\n\n[Go live](/docs/guides/go-live) — replace the credentials, replace the key, and keep the code.\n\n---\n\n## Go live on production Daraja credentials\n\nSource: https://paylod.dev/docs/guides/go-live\n\nMove from sandbox to production: production Daraja credentials, the callback URL you paste into the Daraja portal, and the go-live checklist.\n\n## What changes\n\nAlmost nothing changes. Your code is the same. Three items change.\n\n1. Put your *production* Daraja credentials and your real shortcode into the **Production** environment of your application.\n2. Copy the **production callback URL** into the Daraja portal.\n3. Change `mp_test_…` to `mp_live_…` in your backend.\n\nThe endpoints, the request bodies, the webhook envelope and the result codes are the same. This is the reason to build against [sandbox](/docs/guides/test-in-sandbox) first.\n\n## 1. Get production Daraja credentials\n\nSafaricom issues production credentials for your **real** till or paybill. Get the credentials on the [Daraja portal](https://developer.safaricom.co.ke), under \"Go Live\". You need these values:\n\n- Your consumer key and your consumer secret for the production app.\n- Your shortcode — the till number or the paybill number that customers already pay.\n- Your Lipa na M-Pesa Online passkey.\n- For payouts, reversals and the admin queries: an **Initiator Name** and an **Initiator Password**. Safaricom issues these two values separately. The Initiator Password is also called the security credential.\n\nCopy the values into the **Production** environment of the application. paylod encrypts the values at rest, and never reads the values back.\n\n## 2. Set the callback URL in the Daraja portal\n\npaylod hosts the callback receiver. Thus you copy into Daraja the URL that paylod shows on the application's **Endpoints & Webhooks** tab.\n\n> You must do this step yourself, because Safaricom has no API that registers a callback URL. You can automate all the other steps of a paylod integration.\n\nKeep the callback URL secret. The callback URL contains a token for one application.\n\n## 3. Replace the key\n\nCreate an `mp_live_…` key on the **API Keys** tab. Then put the key into the secret store of your server. Your code does not change:\n\n```bash title=\".env\"\nPAYLOD_API_KEY=mp_live_YOUR_API_KEY\n```\n\nThe key alone selects production. There is no environment flag that you can forget.\n\n## Go-live checklist\n\n- Keep the API key on your **server**. Never put the API key in a browser or a mobile app.\n- Take the `amount` from your own order record. Never take the `amount` from the client.\n- Verify the **HMAC signature** in your webhook handler before you parse the body. `paylod.webhookHandler()` does this for you.\n- Compare `data.amount` with the order total **before** you fulfil the order.\n- Use one `Idempotency-Key` for each **payment attempt**, and not for each order or product. A retry after a failure is a new attempt, and needs a new key.\n- After a `409` *indeterminate* reply, read the payment status. Do not retry the same key.\n- Handle a `429` with a backoff. Do not let your program stop.\n- Test your failure paths (`1032`, `1037`, `2001`) at least one time.\n- If you use payouts or admin queries, set the Initiator Name and the Initiator Password on the **Production** environment.\n\n[Secure integration](/docs/guides/security) explains the reason for each item above.\n\n## Then monitor it\n\nThe dashboard shows payments, webhook deliveries and delivery retries. If a delivery fails, the dashboard shows the attempts and the response of your endpoint.\n\n---\n\n## Secure your M-Pesa integration\n\nSource: https://paylod.dev/docs/guides/security\n\nSix rules that make it impossible for a payer with an intercepting proxy to undercut you, and why the amount must stay on your server.\n\n## The question everyone asks\n\n*Can a customer open my checkout in an intercepting proxy, for example Burp or mitmproxy, and decrease the amount before the request reaches M-Pesa?*\n\nIn a correct integration, **no**.\n\nThe API key that calls `/collect` authenticates **you**, the merchant. The API key does not authenticate the payer. Only `amount` and `phone` come from the request body. paylod reads your shortcode, your passkey and your Daraja credentials from encrypted storage on the server. These values never go over the network. The settlement uses the amount that your server sent. paylod signs every webhook with HMAC, and every webhook reports the **true settled amount**.\n\nOnly one method lets a payer pay you less. That method gives the payer control of **both** the key and the amount. This occurs when you call `/collect` directly from a browser or a mobile app with an amount from the client. A payer can then change the request and pay less. paylod correctly reports the true, lower amount. But paylod cannot correct a merchant who gave control of the amount to the client.\n\nKeep the amount on your server, and this attack is not possible.\n\n## The short version\n\n| Do | Don't |\n| --- | --- |\n| Keep `mp_live_…` and `mp_test_…` on the server only | Put the API key in a browser, a mobile app, or another client |\n| Take `amount` from your own order record | Send an amount from the client directly to `/collect` |\n| Verify `x-webhook-signature` before you read the body | Trust an unverified webhook |\n| Confirm that `amount` matches the order **and** that `status === \"success\"` before you fulfil | Fulfil an order on `status` alone |\n| Send one `Idempotency-Key` for each **payment attempt** | Use an order id or a product id as the key, or generate a new key on each call |\n| Expect a `429` and back off | Continue to send requests at the rate limit |\n\n## 1. Keys are server-only secrets\n\nNever put `mp_live_…` or `mp_test_…` into a browser, a mobile app, or another client. Keys stay only on your server. A key in a client is fully compromised, and you must think of such a key as public. Each person who holds the key can use your account.\n\n## 2. Your server owns the amount\n\nYour server is the only authority on the amount. Take the amount from your own order record or price record. Never send an amount from the client directly to `/collect`. If the client holds the key and also sets the amount, a payer can decrease the amount. This is the only method that lets a payer pay you less.\n\n## 3. Verify, then check the amount, before you fulfil\n\nIn your webhook handler, verify the signature **first**. Then confirm the amount and the status **before** you fulfil the order. `paylod.webhookHandler()` verifies the signature for you. Thus your handler runs only on an authentic event.\n\n```ts title=\"fulfil.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n\nexport const POST = paylod.webhookHandler(async (event) => {\n  if (event.type !== \"payment.success\") return;\n\n  // Look the order up by paymentId — the id you stored when you called collect().\n  const order = await db.orders.findByPaymentId(event.data.paymentId);\n  if (!order) return;\n\n  // Never fulfil on status alone. Check the TRUE settled amount.\n  if (event.data.amount === order.amountKes) {\n    await fulfil(order);                    // paid in full\n  } else {\n    await flagForReview(order, event.data); // underpaid / mismatch\n  }\n});\n```\n\nThe webhook always carries the true settled amount. Thus you see a partial payment as soon as you check the amount.\n\nStore `ack.paymentId` with your order when you call `collect()`. Then use `ack.paymentId` for the lookup. The event carries `paymentId`, `accountRef` and the M-Pesa fields. The event does **not** return the `metadata` that you sent to `collect()`. Thus do not use the `metadata` in your fulfilment code.\n\nMake your handler **idempotent**. paylod sends a delivery again until you return `2xx`. A second copy of an event that you already handled is normal, and is not an error.\n\n## 4. Treat callback and mint tokens as secrets\n\nThe callback URL and the mint token are secrets. Do not write them into a log. Do not send them to a client. Keep them on your server with the API key.\n\n## 5. One idempotency key per payment attempt\n\nCreate one `Idempotency-Key` for each **payment attempt**. Do not create one key for each order, and never create one key for each product. A double-click and a job that runs again are duplicates of one attempt. Duplicates of one attempt then become a single charge, also under concurrency. If you use a key again with a **different** body, paylod returns `409`.\n\nA key is **spent** after you use it. Thus an order id or a product id is the wrong choice: a key that you use again replays the *original* payment, and does not make a new payment. A retry after a wrong M-Pesa PIN is a new charge, and needs a new key.\n\n> [!warn] A request can stop while the call to the provider is in flight. That key then returns `409` *indeterminate*, and paylod never sends the call again on its own. The outcome of such a payment is unknown, and a timeout is not proof that the money did not move. **Do not retry the same key.** First read the payment status. Then retry with a **new** key if no payment occurred. For money, at-most-once is better than at-least-once. See [Idempotency](/docs/sdk#idempotency).\n\n## 6. Expect rate limits\n\npaylod applies a rate limit to `/collect` for each API key and for each phone number. Expect `429 Too Many Requests` during a burst of requests, and back off. paylod refuses a `429` request before the request reaches Daraja. Thus a `429` does not spend the key, and a retry with the same `Idempotency-Key` after a short delay is safe.\n\n---\n\nObey these six rules. Then a payer with an intercepting proxy cannot pay you less, and cannot charge you more. That payer cannot send the money to a different account. That payer also cannot make you fulfil an order that the payer did not pay in full. The platform is safe by design. The platform only needs the amount to stay on your server.\n\n---\n\n## M-Pesa API overview: base URL, auth, conventions\n\nSource: https://paylod.dev/docs/api\n\nThe base URL, the auth header, the request/response conventions, and an index of every paylod endpoint.\n\n## Base URL\n\n```text title=\"Base URL\"\nhttps://paylod.dev/functions/v1\n```\n\n## Authentication\n\nEvery endpoint needs a bearer API key. The API key identifies the application and the environment. No request carries a shortcode or a Daraja credential.\n\n```http title=\"Header\"\nAuthorization: Bearer mp_live_YOUR_API_KEY\n```\n\nSee [Authentication](/docs/authentication) for API keys, environments, idempotency and rate limits.\n\n## Endpoints\n\n| Method | Path | What it does |\n| --- | --- | --- |\n| `POST` | [`/collect`](/docs/api/payments) | Send an STK Push to a customer's handset |\n| `GET` | [`/status/:id`](/docs/api/payments) | Read a payment's current state |\n| `POST` | [`/payout`](/docs/api/payouts) | Send money out to a customer (B2C) |\n| `POST` | [`/reversal`](/docs/api/payouts) | Reverse or refund a completed transaction |\n| `POST` | [`/qr-generate`](/docs/api/other) | Generate a dynamic Lipa na M-Pesa QR code |\n| `POST` | [`/c2b-register`](/docs/api/other) | Register the offline C2B confirmation URL and validation URL |\n| `POST` | [`/transaction-status`](/docs/api/other) | Read a past transaction by its receipt |\n| `POST` | [`/account-balance`](/docs/api/other) | Read your shortcode's M-Pesa float |\n\n## Synchronous and asynchronous endpoints\n\nM-Pesa is an asynchronous network. The paylod API keeps that behaviour visible.\n\n- **`200 OK`** — the answer is in the response body. Only `GET /status/:id`, `POST /qr-generate` and `POST /c2b-register` work this way.\n- **`202 Accepted`** — paylod accepted the request and sent it to M-Pesa. The final outcome comes later on your [webhook](/docs/api/webhooks). For `/collect` you can also poll `GET /status/:id`. The response body carries an id for correlation.\n\nFour endpoints move money or read the ledger: `/payout`, `/reversal`, `/transaction-status` and `/account-balance`. For these four endpoints you must also set the application's **Initiator Name** and **Initiator Password**. See [Go live](/docs/guides/go-live).\n\n## Errors\n\n| Status | Meaning |\n| --- | --- |\n| `400` | The JSON is malformed, or you did not set Daraja credentials for this application and environment |\n| `401` | The API key is missing or invalid |\n| `404` | The resource does not exist, or it belongs to another application |\n| `409` | You reused the idempotency key with a different body. Or the first request is still in flight. Or an interrupted provider call spent the key |\n| `422` | The body failed validation |\n| `429` | You reached the rate limit |\n| `502` | Daraja rejected the request. paylod passes the upstream error through |\n\nThe [error reference](/docs/errors) decodes M-Pesa's own result codes, such as `1032`, `1037` and `2001`.\n\n---\n\n## Payments API: STK Push and payment status\n\nSource: https://paylod.dev/docs/api/payments\n\nPOST /collect to trigger an STK Push and GET /status/:id to read the outcome — the two endpoints that move and read money in.\n\n## POST /collect\n\nStart an STK Push. The customer receives an M-Pesa prompt on the handset. When the customer enters the PIN, the funds settle directly to your till. The endpoint returns a `paymentId` immediately. The final result arrives on the hosted callback. You read that result with `GET /status/:id` or with a [webhook](/docs/api/webhooks).\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/collect\n```\n\n### Headers\n\n| Name | Type | Required | Description |\n| --- | --- | --- | --- |\n| `Authorization` | string | Required | Bearer API key (`mp_live_…` / `mp_test_…`). |\n| `Content-Type` | string | Required | `application/json`. |\n| `Idempotency-Key` | string | **Send it** | The double-charge guard. The idempotency key names **one payment attempt**. Make a new key for each attempt, not for each order and not for each product. If you repeat the same key **and** the same body, paylod returns the **original payment**. paylod does not send a second STK prompt, even for concurrent duplicates. If you repeat the same key with a different body, paylod returns `409`. If an interrupted provider call spent the key, paylod returns `409` *indeterminate*. In that case, read the payment status, then retry with a **new** key. If you omit the idempotency key, every call is a new charge. A double-clicked Pay button then sends two prompts and makes two debits. |\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `amount` | integer | Required | Whole KES, 1–150000. M-Pesa rejects decimals. |\n| `phone` | string | Required | Kenyan MSISDN in any common form (`0712…`, `254712…`, `+254712…`). paylod normalises it for you. |\n| `accountReference` | string | Optional | Your correlation id, such as an invoice number or an order number. 1–12 chars. The webhook returns this value as `accountRef`. The `GET /status/:id` read carries only `id`, `status`, `mpesaReceipt`, `resultCode` and `resultDesc`. Safaricom shows this value to the customer only on a **Paybill** (`CustomerPayBillOnline`), where it is the account number. A **Till / Buy Goods** shortcode does not show it. The default is a short prefix of the `paymentId`, so an omitted reference stays unique and traceable. This value is a **label, not a lock**. It does not deduplicate anything. Only `Idempotency-Key` prevents a second charge. |\n| `description` | string | Optional | Free-text label, 1–64 chars. Defaults to `Payment`. |\n| `metadata` | object | Optional | Opaque keys and values that paylod stores with the payment. paylod never inspects them. `/status` and the webhook do not return them. Key your records on `paymentId`. |\n\n### Example\n\n```bash tab=\"curl\" title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/collect \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Idempotency-Key: att_9f2c1b40\" \\\n  -d '{\n    \"amount\": 10,\n    \"phone\": \"254712345678\",\n    \"accountReference\": \"INV-2041\",\n    \"description\": \"Order #2041\",\n    \"metadata\": { \"orderId\": \"2041\" }\n  }'\n```\n```ts tab=\"node\" title=\"collect\"\nconst ack = await paylod.collect({\n  amount: 10,\n  phone: \"254712345678\",\n  idempotencyKey: attempt.id,\n  accountReference: \"INV-2041\",\n  description: \"Order #2041\",\n  metadata: { orderId: \"2041\" },\n});\n// ack.paymentId, ack.status (\"pending\"), ack.checkoutRequestId\n```\n```php tab=\"php\" title=\"collect\"\n$ack = $paylod->collect([\n    'amount' => 10,\n    'phone' => '254712345678',\n    'idempotencyKey' => $attempt->id,\n    'accountReference' => 'INV-2041',\n    'description' => 'Order #2041',\n    'metadata' => ['orderId' => '2041'],\n]);\n// $ack->paymentId, $ack->status (\"pending\"), $ack->checkoutRequestId\n```\n```python tab=\"python\" title=\"collect\"\nack = paylod.collect(\n    amount=10,\n    phone=\"254712345678\",\n    idempotency_key=attempt.id,\n    account_reference=\"INV-2041\",\n    description=\"Order #2041\",\n    metadata={\"orderId\": \"2041\"},\n)\n# ack.payment_id, ack.status (\"pending\"), ack.checkout_request_id\n```\n```java tab=\"java\" title=\"collect\"\nCollectAck ack = paylod.collect(\n    CollectParams.builder()\n        .amount(10)\n        .phone(\"254712345678\")\n        .idempotencyKey(attempt.id())\n        .accountReference(\"INV-2041\")\n        .description(\"Order #2041\")\n        .metadata(Map.of(\"orderId\", \"2041\"))\n        .build());\n// ack.getPaymentId(), ack.getStatus() (\"pending\"), ack.getCheckoutRequestId()\n```\n```kotlin tab=\"kotlin\" title=\"collect\"\nval ack = paylod.collect(\n    amount = 10,\n    phone = \"254712345678\",\n    idempotencyKey = attempt.id,\n    accountReference = \"INV-2041\",\n    description = \"Order #2041\",\n    metadata = mapOf(\"orderId\" to \"2041\"),\n)\n// ack.paymentId, ack.status (\"pending\"), ack.checkoutRequestId\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"paymentId\": \"e69e5c00-8a01-44ed-b003-48e2e86a7c9e\",\n  \"status\": \"pending\",\n  \"checkoutRequestId\": \"ws_CO_010720261905029287161380\"\n}\n```\n\n> [!warn] **Send `Idempotency-Key`, and make one key for each payment attempt.** If you repeat the same key, you get the same `paymentId` and `checkoutRequestId` back. paylod does not send a second prompt to the handset, even for concurrent duplicates. If you omit the idempotency key, a double-clicked Pay button, a refreshed tab or a retried request each send a fresh STK prompt. Each one charges the customer again. Do **not** key on an order id or a product id. A key is spent once you use it, so a reused key replays an old payment. A retry after a wrong PIN is a **new charge** and needs a **new key**.\n\n> [!warn] **A `409` *indeterminate* means stop. It does not mean retry.** If an earlier request under this idempotency key stopped while the Daraja call was in flight, paylod does not repeat that call. The money may already have moved. A timeout is not evidence that the money did not move. For money, at-most-once is better than at-least-once. Read `GET /status/:id` first. If nothing happened, retry with a **new** key. The same key returns this `409` again.\n\n### Errors\n\n| Status | When |\n| --- | --- |\n| `401` | The API key is missing or invalid. |\n| `400` | The JSON is malformed, or you did not set Daraja credentials for this application and environment. |\n| `409` | You reused the idempotency key with a different body. Or the first request is still in flight (`Retry-After`). Or an interrupted provider call spent the key. The third case is *indeterminate*: read the status, then retry with a new key. |\n| `422` | The body failed validation. For example, `amount` ≤ 0, or paylod cannot parse the phone number. |\n| `429` | You reached the rate limit. paylod limits each API key and each phone number. |\n| `502` | Daraja rejected the STK Push. paylod passes the upstream error through. |\n\n## GET /status/:id\n\nRead the current state of a payment. This endpoint needs no webhook endpoint and no other infrastructure. If the payment is still `pending`, paylod runs a live M-Pesa STK Query. paylod settles the payment before it responds. The API key limits this read to its own application. A payment of another application returns `404`.\n\n```text title=\"Endpoint\"\nGET https://paylod.dev/functions/v1/status/:id\n```\n\n### Path\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `id` | string | Required | The `paymentId` returned by `POST /collect`. |\n\n### Example\n\n```bash tab=\"curl\" title=\"Request\"\ncurl https://paylod.dev/functions/v1/status/e69e5c00-8a01-44ed-b003-48e2e86a7c9e \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\"\n```\n```ts tab=\"node\" title=\"check\"\n// check() reads the payment and decodes it into a renderable PaymentOutcome.\nconst outcome = await paylod.check(\"e69e5c00-8a01-44ed-b003-48e2e86a7c9e\");\nif (outcome.paid) fulfil(outcome.receipt);\nelse console.log(outcome.message);\n```\n```php tab=\"php\" title=\"check\"\n// check() reads the payment and decodes it into a renderable PaymentOutcome.\n$outcome = $paylod->check('e69e5c00-8a01-44ed-b003-48e2e86a7c9e');\nif ($outcome->paid) {\n    fulfil($outcome->receipt);\n} else {\n    echo $outcome->message;\n}\n```\n```python tab=\"python\" title=\"check\"\n# check() reads the payment and decodes it into a renderable PaymentOutcome.\noutcome = paylod.check(\"e69e5c00-8a01-44ed-b003-48e2e86a7c9e\")\nif outcome.paid:\n    fulfil(outcome.receipt)\nelse:\n    print(outcome.message)\n```\n```java tab=\"java\" title=\"check\"\n// check() reads the payment and decodes it into a renderable PaymentOutcome.\nPaymentOutcome outcome = paylod.check(\"e69e5c00-8a01-44ed-b003-48e2e86a7c9e\");\nif (outcome.isPaid()) {\n    fulfil(outcome.getReceipt());\n} else {\n    System.out.println(outcome.getMessage());\n}\n```\n```kotlin tab=\"kotlin\" title=\"check\"\n// check() reads the payment and decodes it into a renderable PaymentOutcome.\nval outcome = paylod.check(\"e69e5c00-8a01-44ed-b003-48e2e86a7c9e\")\nif (outcome.paid) fulfil(outcome.receipt)\nelse println(outcome.message)\n```\n\n```json title=\"Response · 200 OK\"\n{\n  \"id\": \"e69e5c00-8a01-44ed-b003-48e2e86a7c9e\",\n  \"status\": \"success\",\n  \"mpesaReceipt\": \"UG1F3A1U7J\",\n  \"resultCode\": 0,\n  \"resultDesc\": \"The service request is processed successfully.\"\n}\n```\n\n### Status values\n\n| Value | Meaning |\n| --- | --- |\n| `pending` | paylod sent the prompt. The customer did not answer it yet. This state is not a failure. |\n| `success` | The payment settled. `mpesaReceipt` is the M-Pesa receipt number. |\n| `failed` | The payment did not happen. `resultCode` and `resultDesc` carry the M-Pesa reason. |\n\nOn a failure, `resultCode` is the raw Safaricom code: `1032` cancelled, `1037` timeout, `2001` wrong PIN. The [error reference](/docs/errors) decodes all of these codes.\n\n---\n\n## Payouts API: M-Pesa B2C payouts and reversals\n\nSource: https://paylod.dev/docs/api/payouts\n\nPOST /payout to send money out (B2C) and POST /reversal to refund a completed transaction.\n\nBoth endpoints move money **out** of your shortcode. Both endpoints are asynchronous. The HTTP call acknowledges with `202`. paylod delivers the final result to your [webhook](/docs/api/webhooks). For both endpoints you must set the application's **Initiator Name** and **Initiator Password**.\n\n> [!warn] On a production application you cannot reverse these two operations. Put your own authorisation in front of them. Never expose them to a client.\n\n## POST /payout\n\nB2C payout — send money from your shortcode to a customer.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/payout\n```\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `amount` | integer | Required | Whole KES to send, 1–150000. |\n| `phone` | string | Required | Recipient MSISDN (`0712…`, `254712…`, `+254712…`). paylod normalises it for you. |\n| `commandId` | string | Optional | `SalaryPayment`, `BusinessPayment`, or `PromotionPayment`. Defaults to `BusinessPayment`. |\n| `remarks` | string | Optional | Free-text remarks. Up to 100 chars. |\n| `occasion` | string | Optional | Occasion label. Up to 100 chars. |\n\n### Example\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/payout \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"amount\": 2500,\n    \"phone\": \"254712345678\",\n    \"commandId\": \"BusinessPayment\",\n    \"remarks\": \"Payout for July\"\n  }'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"disbursementId\": \"d3f1a2b3-…\",\n  \"conversationId\": \"AG_20260707_0000c1d2e3f4\",\n  \"status\": \"pending\"\n}\n```\n\npaylod sends the final result to your webhook as an HMAC-signed `payout.result` event. Correlate that event with `disbursementId` or `conversationId`.\n\n## POST /reversal\n\nReverse or refund a completed transaction to the customer.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/reversal\n```\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `transactionId` | string | Required | The M-Pesa receipt to reverse. |\n| `amount` | integer | Required | Whole KES, 1–150000. Must match the original transaction amount. |\n| `remarks` | string | Optional | Free-text remarks. Up to 100 chars. |\n| `occasion` | string | Optional | Occasion label. Up to 100 chars. |\n\n### Example\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/reversal \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"transactionId\": \"UG1F3A1U7J\",\n    \"amount\": 2500,\n    \"remarks\": \"Customer refund\"\n  }'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"disbursementId\": \"f7e6d5c4-…\",\n  \"conversationId\": \"AG_20260707_0000a9b8c7d6\",\n  \"status\": \"pending\"\n}\n```\n\npaylod sends the result to your webhook as an HMAC-signed `refund.result` event.\n\n---\n\n## QR codes, C2B registration and M-Pesa queries\n\nSource: https://paylod.dev/docs/api/other\n\nGenerate an M-Pesa QR code, register offline C2B URLs, and query transaction status or account balance.\n\nThese endpoints cover the rest of the Daraja surface. They use the same hosted model. They use the same base URL and the same bearer token. No request carries a shortcode.\n\n## POST /qr-generate\n\nGenerate a dynamic Lipa na M-Pesa QR code for a given amount. This endpoint is **synchronous**. It returns a base64-encoded PNG. Show that PNG to the customer to scan. This endpoint is stateless and creates no ledger row. When the customer pays, the payment arrives later on your C2B confirmation URL with source `c2b`. A signed webhook also fires, like any other offline payment.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/qr-generate\n```\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `amount` | number | Required | Amount to encode in the QR. Must be greater than 0. |\n| `refNo` | string | Optional | Reference shown to the customer. Up to 64 chars. |\n| `merchantName` | string | Optional | Merchant / display name. Up to 64 chars. |\n| `trxCode` | string | Optional | `BG` Buy Goods (till), `PB` Pay Bill, `WA` Withdraw at Agent, `SB` Send to Business, `SM` Send Money. Defaults to `BG`. |\n| `cpi` | string | Optional | Credit Party Identifier (till / paybill). Defaults to the tenant shortcode. |\n| `size` | string | Optional | Requested QR image size in pixels. |\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/qr-generate \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"amount\": 500,\n    \"refNo\": \"INV-2041\",\n    \"merchantName\": \"Acme Ltd\",\n    \"trxCode\": \"BG\"\n  }'\n```\n\n```json title=\"Response · 200 OK\"\n{\n  \"qrBase64\": \"iVBORw0KGgoAAAANSUhEUgAAAP…\"\n}\n```\n\n## POST /c2b-register\n\nRegister the offline C2B confirmation URL and validation URL for your shortcode. C2B covers Lipa na M-Pesa paybill and till payments. This endpoint is **idempotent**, so you can run it again safely. It needs no body. paylod derives the shortcode and the callback URL from your API key on the server.\n\nAfter you register, M-Pesa delivers offline customer payments to paylod as C2B confirmations. paylod records each one as a payment with source `c2b`. A signed webhook then fires. That webhook uses the **same envelope** as STK payments.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/c2b-register\n```\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/c2b-register \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{}'\n```\n\n```json title=\"Response · 200 OK\"\n{\n  \"registered\": true,\n  \"shortcode\": \"600123\",\n  \"confirmationUrl\": \"https://paylod.dev/functions/v1/c2b-hook/8f3c…\",\n  \"validationUrl\": \"https://paylod.dev/functions/v1/c2b-hook/8f3c…\",\n  \"responseCode\": \"0\",\n  \"responseDescription\": \"Success\"\n}\n```\n\nBoth URLs point at the **same** endpoint, `/c2b-hook/<token>`. The path token is unguessable. Safaricom POSTs both validation and confirmation to that endpoint. `responseCode` and `responseDescription` echo the Daraja acknowledgement. Both fields can be `null` if the shortcode already had a registration.\n\n## POST /transaction-status\n\nRead the status of any past M-Pesa transaction by its receipt. This endpoint is **asynchronous**. The HTTP call acknowledges with `202`. The answer arrives on a signed webhook of type `admin.transaction_status`. You must set the application's Initiator Name and Initiator Password.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/transaction-status\n```\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `transactionId` | string | Required | The M-Pesa receipt or TransactionID to read. |\n| `identifierType` | string | Optional | `1` MSISDN, `2` till, `4` shortcode. Defaults to `4`. |\n| `remarks` | string | Optional | Free-text remarks for the query. |\n| `occasion` | string | Optional | Occasion label. |\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/transaction-status \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"transactionId\": \"UG1F3A1U7J\",\n    \"identifierType\": \"4\"\n  }'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"queryId\": \"b1e2c3d4-…\",\n  \"conversationId\": \"AG_20260707_00001a2b3c4d\"\n}\n```\n\nCorrelate the webhook with `queryId` or `conversationId`.\n\n## POST /account-balance\n\nRead your shortcode's M-Pesa float. This endpoint is **asynchronous**. The HTTP call acknowledges with `202`. The answer arrives on a signed webhook of type `admin.account_balance`. You must set the application's Initiator Name and Initiator Password.\n\n```text title=\"Endpoint\"\nPOST https://paylod.dev/functions/v1/account-balance\n```\n\n### Body\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `identifierType` | string | Optional | `1` MSISDN, `2` till, `4` shortcode. Defaults to `4`. |\n| `remarks` | string | Optional | Free-text remarks for the query. |\n\n```bash title=\"Request\"\ncurl -X POST https://paylod.dev/functions/v1/account-balance \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{}'\n```\n\n```json title=\"Response · 202 Accepted\"\n{\n  \"queryId\": \"9a8b7c6d-…\",\n  \"conversationId\": \"AG_20260707_00005e6f7a8b\"\n}\n```\n\n---\n\n## M-Pesa payment webhooks: events and signatures\n\nSource: https://paylod.dev/docs/api/webhooks\n\nThe event envelope, the delivery headers, the HMAC signature scheme, and every event type paylod can send you.\n\nWebhooks are **optional**. Register a webhook endpoint on the application's **Endpoints & Webhooks** tab. paylod then sends a signed event as soon as a payment settles. If you do not want to run a webhook endpoint, poll [`GET /status/:id`](/docs/api/payments) instead. paylod records every payment in both cases.\n\n## Delivery headers\n\n| Header | Description |\n| --- | --- |\n| `x-webhook-event` | The event type, e.g. `payment.success`. |\n| `x-webhook-id` | Unique delivery id. Use it to dedupe retries. |\n| `x-webhook-signature` | HMAC signature, format `t=<unix>,v1=<hex>`. |\n\n## Event types\n\n| Event | Fired when |\n| --- | --- |\n| `payment.success` | A collection settled. The source is an STK Push or an offline C2B payment. |\n| `payment.failed` | A collection failed. The customer cancelled it, or it timed out, or the PIN was wrong, or the funds were not sufficient. |\n| `payout.result` | A [`/payout`](/docs/api/payouts) reached a final state. |\n| `refund.result` | A [`/reversal`](/docs/api/payouts) reached a final state. |\n| `admin.transaction_status` | A [`/transaction-status`](/docs/api/other) query returned an answer. |\n| `admin.account_balance` | An [`/account-balance`](/docs/api/other) query returned an answer. |\n\nEvery event uses the same envelope and the same signature scheme.\n\n## The envelope\n\n```http title=\"Example delivery\"\nPOST https://your-app.com/webhooks/mpesa\nx-webhook-event: payment.success\nx-webhook-id: 8f3c…\nx-webhook-signature: t=1751394302,v1=5a2f…c9\n\n{\n  \"type\": \"payment.success\",\n  \"created\": 1751394302,\n  \"data\": {\n    \"paymentId\": \"e69e5c00-…\",\n    \"applicationId\": \"…\",\n    \"env\": \"production\",\n    \"status\": \"success\",\n    \"amount\": 10,\n    \"phone\": \"254712345678\",\n    \"accountRef\": \"INV-2041\",\n    \"mpesaReceipt\": \"UG1F3A1U7J\",\n    \"checkoutRequestId\": \"ws_CO_…\",\n    \"resultCode\": 0,\n    \"resultDesc\": \"The service request is processed successfully.\",\n    \"decoded\": null\n  }\n}\n```\n\nThe `data` object carries exactly these fields. It has no `metadata` field. The webhook does **not** return the opaque `metadata` that you pass to `collect()`. Key your fulfilment on `paymentId` or on `accountRef`. On a `payment.failed` event, `decoded` is a `{ code, title, cause, fix, category, retryable, customerMessage }` object. paylod decodes that object from the Safaricom result code. On a `payment.success` event, `decoded` is `null`.\n\n## How to verify the signature\n\nThe signature is an HMAC-SHA256 over the string `t + \".\" + rawBody`. The key is the endpoint's signing secret. paylod shows that secret once, when you add the endpoint. Every official SDK verifies the signature for you and returns a typed event. Compute the HMAC yourself only if you use another runtime.\n\n```ts tab=\"node\" title=\"Verify a webhook\"\nconst event = paylod.verifyWebhook({\n  payload: rawBody,                              // the RAW request bytes\n  signature: headers[\"x-webhook-signature\"],     // \"t=...,v1=...\"\n  secret: process.env.PAYLOD_WEBHOOK_SECRET,\n});\n// event.type, event.data.paymentId, event.data.mpesaReceipt\n```\n```php tab=\"php\" title=\"Verify a webhook\"\n$event = $paylod->verifyWebhook(\n    payload: $rawBody,                               // the RAW request bytes\n    signature: $_SERVER['HTTP_X_WEBHOOK_SIGNATURE'], // \"t=...,v1=...\"\n    secret: getenv('PAYLOD_WEBHOOK_SECRET'),\n);\n// $event->type, $event->data->paymentId, $event->data->mpesaReceipt\n```\n```python tab=\"python\" title=\"Verify a webhook\"\nevent = paylod.verify_webhook(\n    payload=raw_body,                                  # the RAW request bytes\n    signature=request.headers[\"x-webhook-signature\"],  # \"t=...,v1=...\"\n    secret=os.environ[\"PAYLOD_WEBHOOK_SECRET\"],\n)\n# event.type, event.data.payment_id, event.data.mpesa_receipt\n```\n```java tab=\"java\" title=\"Verify a webhook\"\nWebhookEvent event = paylod.verifyWebhook(\n    rawBody,                                    // the RAW request bytes\n    request.getHeader(\"x-webhook-signature\"),   // \"t=...,v1=...\"\n    System.getenv(\"PAYLOD_WEBHOOK_SECRET\"));\n// event.getType(), event.getData().getPaymentId()\n```\n```kotlin tab=\"kotlin\" title=\"Verify a webhook\"\nval event = paylod.verifyWebhook(\n    payload = rawBody,                                   // the RAW request bytes\n    signature = request.header(\"x-webhook-signature\"),   // \"t=...,v1=...\"\n    secret = System.getenv(\"PAYLOD_WEBHOOK_SECRET\"),\n)\n// event.type, event.data.paymentId, event.data.mpesaReceipt\n```\n```bash tab=\"curl\" title=\"Verify by hand\"\n# No SDK? Recompute the HMAC yourself (see the three rules below):\n#   signed   = \"{t}.{rawBody}\"\n#   expected = HMAC_SHA256(secret, signed)   # constant-time compare against v1\n#   reject if (now - t) > 300 seconds        # replay window\n```\n\nThe verification has three rules. Apply the three rules in this order:\n\n1. Compute the HMAC over the **raw** request body. Do not use a re-serialised object.\n2. Compare the result in **constant time** against `v1`.\n3. Reject a delivery if its `t` is more than about five minutes old. This rule limits replay attacks.\n\n## How to respond\n\nRespond `2xx` to acknowledge the event. If you do not respond `2xx`, paylod retries the delivery with backoff. The dashboard shows every attempt. paylod delivers each event at least once. Dedupe on `x-webhook-id`.\n\n> [!warn] Verify the signature. Then compare `data.amount` against your own order total. Do both steps **before** you fulfil the order. The webhook always carries the true settled amount. See [Secure integration](/docs/guides/security).\n\n---\n\n## M-Pesa and Daraja error codes, decoded\n\nSource: https://paylod.dev/docs/errors\n\nEvery Safaricom M-Pesa result code and Daraja API error decoded — cause, fix, and a customer-facing message. Searchable.\n\nM-Pesa reports a problem as a number and gives almost no other information. `resultCode: 1037` is an example. That number means nothing until you decode it.\n\nThis page decodes every result code. For each code it gives the meaning, the cause, whether a retry can help, and the message for the customer.\n\n## How to read a failure\n\nA failed payment gives you two fields. The two fields come from two different places:\n\n| Field | Where it comes from |\n| --- | --- |\n| HTTP status | paylod. `401` wrong API key, `422` wrong body, `502` Daraja refused the request. See [API errors](/docs/api). |\n| `resultCode` / `resultDesc` | M-Pesa. The customer cancelled the prompt, or the PIN was wrong, or the handset was off. This page decodes these codes below. |\n\nA `202` and then `resultCode: 1032` is not a bug. The push worked, and the customer pressed Cancel.\n\n## You do not have to implement this table\n\nThe [Node SDK](/docs/sdk) decodes result codes offline. It makes no network call. It needs no API key at call time.\n\n```ts title=\"decode.ts\"\nimport { decodeError } from \"@paylod/node\";\n\nconst err = decodeError(1032);\n\nerr.title;            // \"Payment cancelled by the customer\"\nerr.category;         // \"customer\"\nerr.retryable;        // true\nerr.customerMessage;  // \"Payment cancelled — you can try again whenever you're ready.\"\n```\n\nOn a failed payment, `collectAndWait()` returns the same object as `outcome.detail`. A `payment.failed` webhook carries the same object as `event.data.decoded`. The two objects are byte-for-byte identical. The message that you show to a customer is therefore the same for every integration. The SDK also exports the whole catalogue as `ERROR_CATALOG`.\n\nMost integrations do not need the raw catalogue. `outcome.message` is the decoded sentence for the customer. `outcome.retryable` tells you whether a second charge is safe. Use the catalogue below for your records, for your dashboards, and for debug work during an incident.\n\nThe [CLI](/docs/cli) decodes the same catalogue in your terminal. Run `paylod errors 1032`. This command is the fastest way to read the meaning of a code during an incident.\n\n> [!warn] **Do not show a failure for the codes `4999` and `500.001.1001`. These two codes mean pending, not failed.** Daraja returns them while the customer still looks at the PIN prompt. They decode with `category: \"pending\"` and `retryable: false`. Here `retryable: false` does not mean that a retry is pointless. It means that the first prompt is still live, and a second push can **double-charge** the customer. Do not collect again on these two codes. Continue to poll the status. The SDK polls for you.\n\n## The most common codes\n\n| Code | Meaning | What to do |\n| --- | --- | --- |\n| `0` | Success. | Fulfil the order, but first check the settled `amount`. |\n| `1032` | The customer pressed **Cancel** on the prompt. | Offer the customer a retry. This code is not an error in your code. |\n| `1037` | Timeout. The prompt did not reach the handset, or the customer ignored it. | Retry. The handset was off, or out of coverage, or the customer did not answer. |\n| `2001` | Wrong M-Pesa PIN. | Ask the customer to try again. |\n| `1` | Insufficient balance. | Tell the customer that the M-Pesa balance is too low. |\n| `4999` | **Not a failure.** The customer did not enter the PIN yet. | Continue to poll. Never collect again: the prompt is still live, and a second push can double-charge the customer. |\n\nThe catalogue below contains every other code. It contains every Daraja API error, every C2B result code and every B2C result code. You can search it.\n\n## Full catalogue\n\nThe full catalogue is a **tool**, not a table to copy: call `decode_mpesa_error` with any result code and it returns `{ code, title, cause, fix, category, retryable, customerMessage }` offline. In application code, the SDK does it for you — `outcome.message` is already the customer-facing sentence. The browsable reference is at https://paylod.dev/docs/errors.\n\n---\n\n## M-Pesa SDKs and libraries for paylod\n\nSource: https://paylod.dev/docs/sdks\n\nEvery official paylod client — Node, PHP (Laravel-ready), Python, Java and Kotlin — plus the WooCommerce plugin. All mirror the same surface: collect, collectAndWait, check, verifyWebhook, decodeError.\n\npaylod makes an official client for each common runtime of a Kenyan checkout. Each client is a thin wrapper over the HTTP API in the [API reference](/docs/api). Each client has the same methods: `collect`, `collectAndWait`, `check`, `verifyWebhook` and `decodeError`. Therefore the code below has the same shape in each language. Use the language picker in the top-left corner to switch all the samples on this page and in the docs at the same time.\n\npaylod publishes only the Node client today. For each other language, the samples on this page show a client that is still in development. The HTTP API below those samples works now. The cURL tab shows that HTTP API.\n\nYour language may not be on this list. The API is plain HTTP with a bearer token, so you can call the API directly. The [quickstart](/docs/quickstart) and the [API reference](/docs/api) both show the raw cURL.\n\n## Official SDKs\n\n| Language | Status | Install | Repository |\n| --- | --- | --- | --- |\n| Node / TypeScript | Released | `npm install @paylod/node` | [github.com/mosesmrima/paylod-sdk](https://github.com/mosesmrima/paylod-sdk) |\n| PHP (Laravel-ready) | In development | Not published yet — use the [HTTP API](/docs/api) | [github.com/mosesmrima/paylod-php](https://github.com/mosesmrima/paylod-php) |\n| Python | In development | Not published yet — use the [HTTP API](/docs/api) | [github.com/mosesmrima/paylod-python](https://github.com/mosesmrima/paylod-python) |\n| Java | In development | Not published yet — use the [HTTP API](/docs/api) | [github.com/mosesmrima/paylod-jvm](https://github.com/mosesmrima/paylod-jvm) |\n| Kotlin | In development | Not published yet — use the [HTTP API](/docs/api) | [github.com/mosesmrima/paylod-jvm](https://github.com/mosesmrima/paylod-jvm) |\n\n**Node is the only client that paylod publishes today.** The PHP, Python, Java and Kotlin clients are complete and in test. But none of them is on Packagist, PyPI or Maven Central yet. Therefore no install command for them resolves. The samples below show the methods that those clients will have. Until paylod releases those clients, call the HTTP API directly. The API needs only a bearer token and a JSON body. The cURL tab on each sample shows the exact request.\n\nThe Node client has the most complete reference: the [Node SDK](/docs/sdk) page. paylod builds the other clients to match the Node client method for method. Each language keeps its own idiom. Python has the methods `collect_and_wait`, `verify_webhook` and `decode_error`. The other languages keep the `collectAndWait`, `verifyWebhook` and `decodeError` spelling.\n\n> [!note] The PHP client works with any framework. The PHP client also includes a Laravel service provider and facade. On Laravel you will resolve `Paylod` from the container and read the config from `config/paylod.php`. You will not construct the client by hand.\n\nEach SDK repository is public. You can read the source, file an issue, or open a pull request. Java and Kotlin share one repository, [paylod-jvm](https://github.com/mosesmrima/paylod-jvm). A public repository is not a release. The unreleased clients still have no install command that resolves. Read the repository, but do not depend on it.\n\n## Install\n\n```bash tab=\"node\" title=\"npm\"\nnpm install @paylod/node\n```\n```text tab=\"php\" title=\"PHP — not released yet\"\nThe PHP SDK is not on Packagist yet, so there is no composer command to run.\nCall the HTTP API directly in the meantime — switch to the cURL tab.\n```\n```text tab=\"python\" title=\"Python — not released yet\"\nThe Python SDK is not on PyPI yet, so there is no pip command to run.\nCall the HTTP API directly in the meantime — switch to the cURL tab.\n```\n```text tab=\"java\" title=\"Java — not released yet\"\nThe Java SDK is not on Maven Central yet, so there is no coordinate to depend on.\nCall the HTTP API directly in the meantime — switch to the cURL tab.\n```\n```text tab=\"kotlin\" title=\"Kotlin — not released yet\"\nThe Kotlin SDK is not on Maven Central yet, so there is no coordinate to depend on.\nCall the HTTP API directly in the meantime — switch to the cURL tab.\n```\n```bash tab=\"curl\" title=\"No dependency\"\n# Nothing to install — any HTTP client works.\nexport PAYLOD_API_KEY=mp_test_YOUR_API_KEY\n```\n\n## Initialize the client\n\n> [!warn] Never put the API key in a browser, a mobile app, or any other program that a customer can open. The API key can move money. Therefore the API key is a server-side secret. See [Secure integration](/docs/guides/security).\n\nThe client takes one argument: your API key. You set no base URL, and you refresh no OAuth token.\n\n```ts tab=\"node\" title=\"paylod.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nexport const paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n```\n```php tab=\"php\" title=\"paylod.php\"\nuse Paylod\\Paylod;\n\n$paylod = new Paylod(getenv('PAYLOD_API_KEY'));\n```\n```python tab=\"python\" title=\"paylod.py\"\nimport os\nfrom paylod import Paylod\n\npaylod = Paylod(os.environ[\"PAYLOD_API_KEY\"])\n```\n```java tab=\"java\" title=\"Paylod setup\"\nimport dev.paylod.Paylod;\n\nPaylod paylod = new Paylod(System.getenv(\"PAYLOD_API_KEY\"));\n```\n```kotlin tab=\"kotlin\" title=\"Paylod setup\"\nimport dev.paylod.Paylod\n\nval paylod = Paylod(System.getenv(\"PAYLOD_API_KEY\"))\n```\n```bash tab=\"curl\" title=\"Just an env var\"\n# The \"client\" is a bearer token on every request.\nexport PAYLOD_API_KEY=mp_test_YOUR_API_KEY\n```\n\n## Collect a payment\n\n`collectAndWait` sends the STK Push. Then `collectAndWait` polls until the payment settles. Then `collectAndWait` returns a `PaymentOutcome`. The `PaymentOutcome` contains a decoded `message` for the customer and a `retryable` flag. `retryable=true` means that you can safely charge the customer again. `retryable=false` means that paylod cannot prove that no debit occurred, or that the first prompt is still live.\n\n```ts tab=\"node\" title=\"collect.ts\"\nconst outcome = await paylod.collectAndWait({\n  amount: 100,\n  phone: \"0712345678\",\n  idempotencyKey: attempt.id,   // one key per payment attempt — a double-click cannot charge twice\n});\n\nif (outcome.paid) fulfil(outcome.receipt);\nelse console.log(outcome.message);\n```\n```php tab=\"php\" title=\"collect.php\"\n$outcome = $paylod->collectAndWait([\n    'amount' => 100,\n    'phone' => '0712345678',\n    'idempotencyKey' => $attempt->id,   // one key per payment attempt\n]);\n\nif ($outcome->paid) {\n    fulfil($outcome->receipt);\n} else {\n    echo $outcome->message;\n}\n```\n```python tab=\"python\" title=\"collect.py\"\noutcome = paylod.collect_and_wait(\n    amount=100,\n    phone=\"0712345678\",\n    idempotency_key=attempt.id,   # one key per payment attempt\n)\n\nif outcome.paid:\n    fulfil(outcome.receipt)\nelse:\n    print(outcome.message)\n```\n```java tab=\"java\" title=\"Collect.java\"\nPaymentOutcome outcome = paylod.collectAndWait(\n    CollectParams.builder()\n        .amount(100)\n        .phone(\"0712345678\")\n        .idempotencyKey(attempt.id())   // one key per payment attempt\n        .build());\n\nif (outcome.isPaid()) {\n    fulfil(outcome.getReceipt());\n} else {\n    System.out.println(outcome.getMessage());\n}\n```\n```kotlin tab=\"kotlin\" title=\"Collect.kt\"\nval outcome = paylod.collectAndWait(\n    amount = 100,\n    phone = \"0712345678\",\n    idempotencyKey = attempt.id,   // one key per payment attempt\n)\n\nif (outcome.paid) fulfil(outcome.receipt)\nelse println(outcome.message)\n```\n```bash tab=\"curl\" title=\"Raw HTTP\"\ncurl -X POST https://paylod.dev/functions/v1/collect \\\n  -H \"Authorization: Bearer mp_test_YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Idempotency-Key: att_9f2c1b40\" \\\n  -d '{ \"amount\": 100, \"phone\": \"254712345678\" }'\n# → 202 { \"paymentId\": \"...\", \"status\": \"pending\" } — then poll GET /status/:id\n```\n\n## Check a payment\n\n`check` reads a payment. `check` then decodes the payment into the same `PaymentOutcome` that you can show to the customer. A polling endpoint in your own application must return this `PaymentOutcome` to its frontend.\n\n```ts tab=\"node\" title=\"check\"\nconst outcome = await paylod.check(paymentId);\noutcome.status;   // \"pending\" | \"succeeded\" | \"cancelled\" | \"failed\"\noutcome.message;  // renderable, already decoded\n```\n```php tab=\"php\" title=\"check\"\n$outcome = $paylod->check($paymentId);\n$outcome->status;   // \"pending\" | \"succeeded\" | \"cancelled\" | \"failed\"\n$outcome->message;  // renderable, already decoded\n```\n```python tab=\"python\" title=\"check\"\noutcome = paylod.check(payment_id)\noutcome.status   # \"pending\" | \"succeeded\" | \"cancelled\" | \"failed\"\noutcome.message  # renderable, already decoded\n```\n```java tab=\"java\" title=\"check\"\nPaymentOutcome outcome = paylod.check(paymentId);\noutcome.getStatus();   // \"pending\" | \"succeeded\" | \"cancelled\" | \"failed\"\noutcome.getMessage();  // renderable, already decoded\n```\n```kotlin tab=\"kotlin\" title=\"check\"\nval outcome = paylod.check(paymentId)\noutcome.status    // \"pending\" | \"succeeded\" | \"cancelled\" | \"failed\"\noutcome.message   // renderable, already decoded\n```\n```bash tab=\"curl\" title=\"GET /status/:id\"\ncurl https://paylod.dev/functions/v1/status/e69e5c00-8a01-44ed-b003-48e2e86a7c9e \\\n  -H \"Authorization: Bearer mp_live_YOUR_API_KEY\"\n# → 200 { \"id\": \"...\", \"status\": \"success\", \"mpesaReceipt\": \"UG1F3A1U7J\", ... }\n```\n\n## Verify a webhook\n\nPass the **raw** request bytes. Do not pass an object that you serialised again. Each SDK verifies the `x-webhook-signature` header and returns a typed event. The header is an HMAC-SHA256 over `t + \".\" + rawBody`. The [Webhooks](/docs/api/webhooks) page documents this scheme.\n\n```ts tab=\"node\" title=\"verifyWebhook\"\nconst event = paylod.verifyWebhook({\n  payload: rawBody,\n  signature: headers[\"x-webhook-signature\"],\n  secret: process.env.PAYLOD_WEBHOOK_SECRET,\n});\n```\n```php tab=\"php\" title=\"verifyWebhook\"\n$event = $paylod->verifyWebhook(\n    payload: $rawBody,\n    signature: $_SERVER['HTTP_X_WEBHOOK_SIGNATURE'],\n    secret: getenv('PAYLOD_WEBHOOK_SECRET'),\n);\n```\n```python tab=\"python\" title=\"verify_webhook\"\nevent = paylod.verify_webhook(\n    payload=raw_body,\n    signature=request.headers[\"x-webhook-signature\"],\n    secret=os.environ[\"PAYLOD_WEBHOOK_SECRET\"],\n)\n```\n```java tab=\"java\" title=\"verifyWebhook\"\nWebhookEvent event = paylod.verifyWebhook(\n    rawBody,\n    request.getHeader(\"x-webhook-signature\"),\n    System.getenv(\"PAYLOD_WEBHOOK_SECRET\"));\n```\n```kotlin tab=\"kotlin\" title=\"verifyWebhook\"\nval event = paylod.verifyWebhook(\n    payload = rawBody,\n    signature = request.header(\"x-webhook-signature\"),\n    secret = System.getenv(\"PAYLOD_WEBHOOK_SECRET\"),\n)\n```\n\n## Decode an error\n\n`decodeError` turns a raw M-Pesa result code into a decoded error that you can show. `decodeError` works offline and makes no network call. You rarely need `decodeError` directly, because `check` and `collectAndWait` already return a decoded `message`. Use `decodeError` for records, dashboards and support tools. The [error reference](/docs/errors) shows the full catalogue.\n\n```ts tab=\"node\" title=\"decodeError\"\nconst err = paylod.decodeError(1032);\n// err.title, err.cause, err.fix, err.category, err.retryable, err.customerMessage\n```\n```php tab=\"php\" title=\"decodeError\"\n$err = $paylod->decodeError(1032);\n// $err->title, $err->cause, $err->fix, $err->category, $err->retryable, $err->customerMessage\n```\n```python tab=\"python\" title=\"decode_error\"\nerr = paylod.decode_error(1032)\n# err.title, err.cause, err.fix, err.category, err.retryable, err.customer_message\n```\n```java tab=\"java\" title=\"decodeError\"\nDecodedError err = paylod.decodeError(1032);\n// err.getTitle(), err.getCause(), err.getFix(), err.getCategory(), err.isRetryable(), err.getCustomerMessage()\n```\n```kotlin tab=\"kotlin\" title=\"decodeError\"\nval err = paylod.decodeError(1032)\n// err.title, err.cause, err.fix, err.category, err.retryable, err.customerMessage\n```\n\n## Plugins\n\n| Plugin | Platform | Status | Repository |\n| --- | --- | --- | --- |\n| paylod for WooCommerce | WordPress / WooCommerce | In development — not yet downloadable | [github.com/mosesmrima/paylod-woocommerce](https://github.com/mosesmrima/paylod-woocommerce) |\n\nDo not install the WooCommerce plugin on a live store. paylod did not release the plugin yet. The repository above is public to read, but you cannot download the plugin.\n\nThe WooCommerce plugin adds M-Pesa as a checkout method. You install the plugin and paste an `mp_live_…` key. The plugin then handles the STK Push, the hosted callback and the order-status update. You write no code. Until paylod releases the plugin, a WordPress store can call the [HTTP API](/docs/api). The store can make that call from a small snippet or from any server-side hook.\n\n## Next\n\n- For the most complete per-method reference, see the [Node SDK](/docs/sdk).\n- For the HTTP endpoints below each SDK, see the [API reference](/docs/api).\n- To work in the terminal, use the [CLI](/docs/cli). The CLI sends the same STK Push in one command.\n- For each decoded result code, see the [error reference](/docs/errors).\n\n---\n\n## Node SDK for M-Pesa: @paylod/node\n\nSource: https://paylod.dev/docs/sdk\n\n@paylod/node is the official Node/TypeScript client: STK Push, status polling with backoff, signed webhook verification and offline error decoding, with zero runtime dependencies.\n\n`@paylod/node` is the official Node/TypeScript client. It wraps the same HTTP API that the [API reference](/docs/api) documents. The client also adds the parts that almost every integration must write:\n\n- phone-number normalisation\n- idempotency keys\n- a polling loop with backoff\n- webhook signature verification\n- an offline result-code decoder\n\nThe client has **zero runtime dependencies**. It works on Node 18+, Bun, Deno, Vercel, Cloudflare Workers and any other runtime with a global `fetch`.\n\nThis page is the Node reference. Node is the only client that paylod publishes today. The PHP, Python, Java and Kotlin clients are still in development, and so is the WooCommerce plugin. These clients will mirror this same surface; see [SDKs & libraries](/docs/sdks). Until paylod releases them, call the [HTTP API](/docs/api) directly from those languages. Source: [github.com/mosesmrima/paylod-sdk](https://github.com/mosesmrima/paylod-sdk).\n\n```bash tab=\"node\" title=\"npm\"\nnpm install @paylod/node\n```\n```text tab=\"php\" title=\"PHP — not released yet\"\nThe PHP SDK is not on Packagist yet. Call the HTTP API directly — see the cURL tab.\n```\n```text tab=\"python\" title=\"Python — not released yet\"\nThe Python SDK is not on PyPI yet. Call the HTTP API directly — see the cURL tab.\n```\n```text tab=\"java\" title=\"Java — not released yet\"\nThe Java SDK is not on Maven Central yet. Call the HTTP API directly — see the cURL tab.\n```\n```text tab=\"kotlin\" title=\"Kotlin — not released yet\"\nThe Kotlin SDK is not on Maven Central yet. Call the HTTP API directly — see the cURL tab.\n```\n```bash tab=\"curl\" title=\"No dependency\"\n# Nothing to install — any HTTP client works.\nexport PAYLOD_API_KEY=mp_test_YOUR_API_KEY\n```\n\n## Server-side only\n\nThe API key can move money. `@paylod/node` is a server library. Call it from your backend, a serverless function, or an edge worker. Never call it from a browser or a mobile app.\n\n> [!warn] Never put an API key in client-side JavaScript. Every visitor downloads that key, and the key is then fully compromised. If you need a browser checkout, make the browser call *your* server, and let your server call paylod. See [Secure integration](/docs/guides/security).\n\n## Set up the client\n\nOne argument: your API key.\n\n```ts title=\"paylod.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nexport const paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n```\n\nThat is the whole configuration. There is no base URL to pass, no OAuth token to get and refresh, and no callback URL to host. If you omit the argument, the client reads `PAYLOD_API_KEY` from the environment:\n\n```ts title=\"Also fine\"\nconst paylod = new Paylod();\n```\n\nIn both cases the client throws a `PaylodConfigError` immediately when it finds no key. It does not give you a client that fails on its first call.\n\nTimeouts, retries and the other options have sensible defaults. You can override them, but you rarely need to. See [Options](#options) at the end of this page.\n\n## Collect a payment\n\n```ts title=\"collect.ts\"\nconst outcome = await paylod.collectAndWait({\n  amount: 100,\n  phone: \"0712345678\",\n  idempotencyKey: attempt.id,      // one key per payment attempt — a double-click cannot charge twice\n});\n\nif (outcome.paid) {\n  await fulfil(outcome.receipt);   // \"UG1F3A1U7J\"\n} else {\n  console.log(outcome.message);    // \"That M-Pesa PIN was incorrect. Please try again …\"\n}\n```\n\n`collectAndWait()` sends the STK Push and polls until the payment settles. You get back a `PaymentOutcome`. This flat object holds a customer-facing `message` in decoded form, and a `retryable` flag for your retry button.\n\n> [!warn] **Pass `idempotencyKey`, and make one key for each payment attempt.** Duplicates of one attempt collapse into **one** prompt and **one** charge: a double-clicked Pay button, a refreshed tab, or a redelivered job. If you omit the key, every call makes a new charge. Do **not** key on the order id or the product id. Such a key replays an old payment instead of a new one. A retry after a wrong PIN is a new charge and needs a **new** key. See [Idempotency](#idempotency).\n\nA wrong PIN is a business outcome, not a crash. The client returns it as data, and does not throw an error.\n\nThe client normalises phone numbers for you. `0712345678`, `+254712345678`, `254712345678` and `712345678` all work.\n\n## The outcome\n\nEvery method that settles a payment returns the same shape.\n\n```ts title=\"PaymentOutcome\"\n{\n  status: \"succeeded\" | \"pending\" | \"cancelled\" | \"failed\";\n  message: string;        // customer-facing, already decoded. Render this.\n  retryable: boolean;     // SAFE TO CHARGE AGAIN. Gate your retry button on this.\n  paid: boolean;          // the one branch a backend needs\n  receipt: string | null; // M-Pesa confirmation code; non-null exactly when `paid`\n  code: string | null;    // raw result code, for your logs\n  detail: DecodedError | null;\n  payment: Payment;\n}\n```\n\nYour whole UI:\n\n```tsx title=\"Checkout.tsx\"\n<p>{outcome.message}</p>\n{outcome.retryable && <button onClick={retry}>Try again</button>}\n```\n\nDo not `switch` on result codes. Your app needs no error table. To decide what to *show* a person, use `message` and not `code`. The `code` and `detail` fields are for your records and for support tools.\n\n> [!warn] Gate the retry button on `retryable`. `retryable` means **safe to charge again**. It does not mean that the customer is allowed to press a button. A `pending` payment is never retryable, because the STK prompt is still live on the handset. A second collect call sends a second prompt, which can charge the customer twice.\n\n## Methods\n\n### `collect(params)`\n\nThe client sends the STK Push and returns as soon as the prompt reaches the handset. The payment is `pending`.\n\n```ts title=\"collect\"\nconst ack = await paylod.collect({\n  amount: 100,                      // whole KES, 1–150000. M-Pesa rejects decimals.\n  phone: \"0712345678\",\n  idempotencyKey: attempt.id,       // PASS THIS — one per payment attempt (see below)\n  accountReference: \"INV-2041\",     // optional, ≤ 12 chars — YOUR correlation id (see below)\n  description: \"Order #2041\",       // optional, ≤ 64 chars — shown on the STK prompt\n  metadata: { orderId: \"2041\" },    // optional, opaque; stored with the payment\n});\n\nack.paymentId;          // \"e69e5c00-…\"\nack.checkoutRequestId;  // \"ws_CO_…\"\nack.idempotencyKey;     // the key that was sent — yours, or a random one if you passed none\n```\n\n`idempotencyKey` collapses duplicate deliveries of one payment attempt into a single charge. Make one key for each attempt and store it. See [Idempotency](#idempotency) for what the key guarantees, what it does **not** guarantee, and what happens if you omit it.\n\nThe client validates `amount`, `phone`, `accountReference` and `description` locally, before the request leaves your process. A wrong amount fails immediately in your own stack trace, and not as a `422` one round trip later.\n\n`accountReference` is a **correlation id that you choose**. It is not a message to the customer. It comes back as `accountRef` on the webhook, so you use it to tie a payment to your order. The `status()` read returns only `id`, `status`, `mpesaReceipt`, `resultCode` and `resultDesc`, and not `accountRef`. Safaricom shows `accountReference` to the customer only on a **Paybill** (`CustomerPayBillOnline`), where it is the account number. On a **Till / Buy Goods** shortcode Safaricom does not show it at all: the customer sees your business name and the amount.\n\n`description` is the field that reaches the STK prompt. If you omit `accountReference`, it defaults to a short prefix of the `paymentId`. That default is still unique, and you can still trace it back to the payment.\n\n`accountReference` is only a **label**. Your order id in that field does not remove any duplicate. `idempotencyKey` is the field that collapses duplicates of one attempt. Put your order id in `accountReference`, and a per-attempt key in `idempotencyKey`. The two fields do different jobs.\n\n### `status(paymentId)`\n\nThis method reads a payment. If the payment is still pending, paylod runs a live M-Pesa STK Query and settles the payment before it answers.\n\n```ts title=\"status\"\nconst payment = await paylod.status(ack.paymentId);\n\npayment.status;        // \"pending\" | \"success\" | \"failed\"\npayment.mpesaReceipt;  // \"UG1F3A1U7J\" on success, otherwise null\npayment.resultCode;    // 0 | 1032 | 2001 | …\npayment.resultDesc;\n```\n\n### `check(paymentId)`\n\nThis method reads a payment and decodes it. It returns a `PaymentOutcome` that you can show.\n\n```ts title=\"check\"\nconst outcome = await paylod.check(ack.paymentId);\n\noutcome.status;     // \"pending\" | \"succeeded\" | \"cancelled\" | \"failed\"\noutcome.message;    // \"Check your phone and enter your M-Pesa PIN to complete this payment.\"\noutcome.retryable;  // false while pending\n```\n\nA polling endpoint in your own app should return this shape to your frontend.\n\n### `wait(paymentId, options)`\n\nThis method polls an existing payment until the payment settles. It uses a jittered backoff ramp (1s → 5s). It returns a `PaymentOutcome`.\n\n```ts title=\"wait\"\nconst outcome = await paylod.wait(ack.paymentId, {\n  timeoutMs: 120_000,   // default. STK prompts expire around 60s; PIN entry takes 20–30s more.\n  onPoll: (p) => console.log(\"still\", p.status),\n  signal: controller.signal,\n});\n```\n\n`wait()` decides that a payment settles from the result **code**, and not from the raw status field. Daraja reports `4999` on a payment that it also marks `failed`. That code means the prompt is still live. `wait()` continues to poll, and does not report a failure for a payment that can still succeed.\n\nIf the payment is **still pending** at the deadline, `wait()` throws a `PaylodTimeoutError`. That error is not a failure, and paylod deliberately does not fold it into `status: \"failed\"`. The customer can still type the PIN. Leave the order open.\n\n### `collectAndWait(params, options)`\n\nThis method calls `collect()` and then `wait()`. Most integrations want this one call. It takes the same `params` as `collect()`, and this includes `idempotencyKey`. Pass one `idempotencyKey` for each payment attempt.\n\n### `pendingOutcome(paymentId)`\n\nThis function returns the form that your UI can show for a prompt that you just sent. `collect()` gives you an ack, but a prompt on a handset is a pending payment. Your UI shows that payment like any other state.\n\n```ts title=\"pendingOutcome\"\nimport { pendingOutcome } from \"@paylod/node\";\n\nconst ack = await paylod.collect({ amount: 100, phone });\nreturn pendingOutcome(ack.paymentId);   // same shape as check() and wait()\n```\n\n### `decodeError(resultCode, rawDesc?)`\n\nThis method decodes an M-Pesa result code offline. It makes no network call, and it needs no API key at call time.\n\n```ts title=\"decodeError\"\nconst err = paylod.decodeError(1032);\n\nerr.title;            // \"Payment cancelled by the customer\"\nerr.cause;            // \"The customer received the STK prompt but pressed Cancel …\"\nerr.fix;              // \"Nothing is wrong with your setup — offer a clear retry …\"\nerr.category;         // \"customer\"\nerr.retryable;        // true\nerr.customerMessage;  // \"Payment cancelled — you can try again whenever you're ready.\"\n```\n\nThese strings are byte-for-byte the strings that paylod puts in the `decoded` object on a `payment.failed` webhook. What you show a customer is therefore the same in both cases. You can browse the whole catalogue in the [error reference](/docs/errors).\n\npaylod also exports `decodeError` as a standalone function, together with the raw `ERROR_CATALOG`:\n\n```ts title=\"Standalone\"\nimport { decodeError, ERROR_CATALOG } from \"@paylod/node\";\n```\n\n### `4999` and `500.001.1001` are pending, not failed\n\nDaraja's STK Query returns `4999` (\"the transaction is still under processing\") while the customer still looks at the PIN prompt. paylod classifies `4999` and `500.001.1001` as **pending**. Therefore `status()` continues to report `pending`, and `wait()` continues to poll.\n\nIf you decode one of these two codes directly, `category` is `\"pending\"` and `retryable` is `false`. `retryable` is `false` because the first prompt is still live, and a second push can charge the customer twice. It is not `false` because a retry is pointless. Do not show a customer a failure on these codes, and do not collect again. Continue to poll.\n\n## Test your checkout without a phone\n\nPayment bugs live in your failure paths, and until now a test of those paths needed a real handset and a wrong PIN. `paylod.simulate` removes the handset. It removes nothing else. You get a real payment row, the real Daraja result codes, and the real settlement path. paylod also sends a real signed webhook to your webhook endpoint. Only the handset is not real.\n\n`paylod.simulate` requires an `mp_test_…` key. The client refuses a `mp_live_…` key **locally**, before it sends a request. A simulator must never touch production.\n\n```ts title=\"checkout.test.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_TEST_KEY!);   // mp_test_…\n\nconst outcome = await paylod.simulate.pay({ outcome: \"wrong_pin\" });\n\noutcome.status;     // \"failed\"\noutcome.message;    // \"That M-Pesa PIN was incorrect. Please try again and enter the right PIN.\"\noutcome.retryable;  // true — no money moved, so a fresh charge is safe\n```\n\nThat is an ordinary `PaymentOutcome`. It is the same object that `check()` and `wait()` return. There is no \"simulated\" type and no special branch. The code that you test is therefore the code that runs in production.\n\n### The five outcomes\n\n`outcome` is a typed union. A typo is therefore a compile error, and not a `422` that you find in CI.\n\n| `outcome` | `status` | Result code | `retryable` |\n| --- | --- | --- | --- |\n| `approve` | `succeeded` | `0` | `false` (it worked) |\n| `wrong_pin` | `failed` | `2001` | `true` |\n| `insufficient_funds` | `failed` | `1` | `true` |\n| `user_cancelled` | `cancelled` | `1032` | `true` |\n| `timeout` | `failed` | `1037` | `true` |\n\n`SIM_OUTCOMES` (or `paylod.simulate.outcomes`) is the whole list. You can loop over it:\n\n```ts title=\"Drive every path\"\nfor (const outcome of paylod.simulate.outcomes) {\n  const result = await paylod.simulate.pay({ outcome });\n  expect(render(result)).toMatchSnapshot();\n}\n```\n\n> [!note] `timeout` here is Daraja's `1037`, which means that M-Pesa could not reach the handset. That result is a **settled** failure. It is not `PaylodTimeoutError`. `wait()` throws `PaylodTimeoutError` when a payment is still `pending` at your deadline. An indeterminate payment is not a failed payment.\n\n### `simulate.collect()` and `simulate.outcome()`\n\nSplit the simulation in two parts, and put *your* code in the middle. The payment id is a real id, so your poller, your webhook route and your UI all run unchanged:\n\n```ts title=\"Testing your own code\"\nconst sim = await paylod.simulate.collect({ amount: 250 });\n\nawait readCheckout(sim.paymentId);   // your code — sees a genuinely pending payment\nawait paylod.simulate.outcome(sim.paymentId, \"insufficient_funds\");\nconst view = await readCheckout(sim.paymentId);   // your code — sees the settled failure\n\nexpect(view.message).toMatch(/balance is too low/);\n```\n\nA simulated payment stays `pending` until you force an outcome. A live prompt behaves the same way while a customer looks at it. You can settle a simulated payment only once. A second call gets a `409`, the same as a real handset.\n\n### How to test your charge path: `{ simulate: true }`\n\nThe steps above still do not exercise your own `collect()` call. Construct the client with `simulate: true`. Then `collect()` and `collectAndWait()` create a simulated payment instead of a real STK Push to a handset. Your `/api/pay` handler therefore runs **completely unchanged**:\n\n```ts title=\"Your handler, under test\"\nconst paylod = new Paylod(process.env.PAYLOD_TEST_KEY!, { simulate: true });\n\nconst view = await startCheckout(order.id, \"0712345678\", attemptId);   // your handler, verbatim\nawait paylod.simulate.outcome(view.paymentId!, \"user_cancelled\");      // no handset involved\nexpect((await readCheckout(view.paymentId!)).status).toBe(\"cancelled\");\n```\n\nThe constructor throws `PaylodSandboxOnlyError` if the key is not `mp_test_…`. This flag can therefore never point at production, even by accident.\n\n> [!note] The simulator obeys `Idempotency-Key` with the **same semantics as production**. The same key returns the same `paymentId` and creates no second payment. Concurrent duplicates collapse into one payment. A different key creates a new payment. A test that asserts \"a double-click cannot charge twice\" therefore tests the real behaviour, and not a stub of it.\n\n## Idempotency\n\nAn idempotency key names **one payment attempt**. Duplicate deliveries of that one attempt collapse into a single charge: a double-click, a refreshed tab, a job-queue redelivery, or an internal network retry. That is the guarantee. Understand it precisely, because it is narrower than \"a reused key is always a safe retry\".\n\n```ts title=\"One key per payment attempt\"\nconst attempt = await db.attempts.create({ orderId: order.id });   // a row per press of Pay\nawait paylod.collectAndWait({ amount, phone, idempotencyKey: attempt.id });\n```\n\n### Pass a key per attempt — not per order, and not per product\n\nThe key must be **stable across duplicates of one attempt** and **fresh for a genuinely new charge**. An order id is stable, but it is not fresh. A product id is not fresh either, and many customers share it:\n\n| Key you pass | What happens |\n| --- | --- |\n| An id minted per **payment attempt** | Correct. Duplicates of that attempt collapse into one charge. A new attempt is a new charge. |\n| Your **order id** | The customer types a wrong PIN, and you retry the same order. paylod then replays the **failed** first attempt and charges nobody. The order never gets paid. |\n| A **product id** (or any value reused across purchases) | Catastrophic. Every customer after the first one replays the **first-ever** payment for that product. paylod charges nobody after customer one. |\n| `crypto.randomUUID()` **per call** | The same as no key: a double-click makes two keys, two prompts, and two charges. |\n\nOne rule resolves all four rows. **Make the key when a payment attempt starts. Store the key on that attempt. Never reuse it for a different charge.** A retry after a wrong PIN, a cancelled prompt or a timeout is a *new attempt*, with a new row and a new key.\n\nA missing key is a money bug. The SDK therefore writes a one-time `console.warn` when you call `collect()` or `collectAndWait()` without a key.\n\n> [!warn] Do **not** silence that warning with `idempotencyKey: crypto.randomUUID()` or `Date.now()` **at the call site**. A key that changes on every call is exactly the same as no key at all. Such a key only hides the warning that tells you the customer is at risk. A random UUID is a good key, but you must make it **once for each attempt** and store it. Do not generate the key inside the call.\n\n### A concurrent double-click cannot double-charge\n\nThis part is unconditional. Send ten simultaneous requests with the same `Idempotency-Key`, and you get **one** payment and **one** STK push. paylod reserves the key before it calls the provider, so exactly one request wins. The other requests wait for the answer of the winner and then replay that answer. All ten calls come back with the same `paymentId`. The handset receives only one prompt.\n\n### The one case where the same key is *not* a safe retry\n\nA request can die mid-flight against Daraja: after paylod hands the call to the provider, and before an answer comes back. That key is then **spent**. A retry under that key does not silently send the request again. It returns `409` with an **indeterminate** message:\n\n```text\nA previous request with this Idempotency-Key was interrupted while the provider call was\nin flight, so it may or may not have completed. We will not repeat it — that could charge\nor pay twice. Check the payment/disbursement status; if nothing happened, retry with a NEW key.\n```\n\nA timeout is not evidence that the money did not move. It is the absence of evidence. paylod therefore refuses to guess. **For money, at-most-once is better than at-least-once.** paylod makes you check the status, rather than charge someone twice.\n\n> [!warn] **If you get the indeterminate `409`, do not retry with the same key.** Read the payment status first, with `paylod.check(paymentId)`, `GET /status/:id`, or your webhook endpoint. Then decide. If the payment did settle, you are done. If nothing happened, start a **new attempt with a new key**. A retry under the spent key returns the same `409` every time.\n\n### The rules\n\n- **Same key + same body, already settled** → paylod replays the original payment. You get the same `paymentId` and the same `checkoutRequestId`. There is no second prompt and no second debit.\n- **Same key + same body, first request still in flight** → `409` with a `Retry-After`. For a plain double-click, the SDK waits for the winner and gives you its response.\n- **Same key + different body** → `409`, which the SDK shows as `PaylodApiError` with `.isIdempotencyConflict === true`. This is always a bug on your side: two different charges collided on one key. For example, you changed the amount but kept the key.\n- **Same key, previous attempt interrupted against the provider** → `409` **indeterminate**. Read the status, then retry with a **new** key. paylod never sends that request again.\n- **Internal retries** (network blip, `5xx`, `429`) reuse the same key automatically. That reuse is what makes a retry of a `POST` safe. If the interruption happened against Daraja, that retry returns the indeterminate `409` instead of a second charge.\n\nIf you did not pass a key, and you want to retry the *same attempt* later, store the key that you got back:\n\n```ts title=\"Persist the key with the attempt\"\nconst ack = await paylod.collect({ amount, phone });\nawait db.attempts.update(attempt.id, { idempotencyKey: ack.idempotencyKey });\n// Retrying THAT attempt with THAT key collapses into the original payment.\n// A genuinely new attempt gets a new key — reusing this one would replay the old result.\n```\n\nDo you call the HTTP API directly? Use the `Idempotency-Key` request header. It behaves in the same way.\n\n## Webhooks (optional)\n\nYou do not need a webhook to take a payment. `collectAndWait()` polls for you, and `check()` lets your own frontend poll. That is a complete, supported integration, and it needs nothing but `PAYLOD_API_KEY`.\n\nAdd a webhook when you want paylod to tell your **server** about a payment that your server does not wait on. For example, the customer closed the tab right after PIN entry, your request timed out but the money landed, or your process restarted. Polling tells the browser, and a webhook tells your backend. If you have an order to fulfil, you will want both.\n\nSet the signing secret when you add a webhook:\n\n```bash title=\".env\"\nPAYLOD_WEBHOOK_SECRET=whsec_...   # only if you consume webhooks\n```\n\n### Web `Request`/`Response` — Next.js, Hono, Remix, Workers, Bun, Deno\n\n```ts title=\"app/api/webhooks/paylod/route.ts\"\nimport { Paylod } from \"@paylod/node\";\n\nconst paylod = new Paylod();\n\nexport const POST = paylod.webhookHandler(async (event) => {\n  if (event.type === \"payment.success\") {\n    await fulfil(event.data.paymentId, event.data.mpesaReceipt);\n  } else if (event.data.decoded) {\n    await notify(event.data.decoded.customerMessage);\n  }\n});\n```\n\n`event.data.decoded` is `DecodedError | null`. paylod populates it on `payment.failed`, and sets it to `null` on `payment.success`. A check on `event.type` does not narrow that field, so check the field before you read it.\n\npaylod verifies the signature before your handler runs. A wrong signature returns `400`, and paylod never calls your handler. If your handler throws, the route returns `500` and paylod retries the delivery. A duplicate is better than a lost payment, so make your handler idempotent.\n\n### Express\n\n```ts title=\"server.ts\"\napp.post(\"/webhooks/paylod\", paylod.webhook(async (event) => {\n  if (event.type === \"payment.success\") await fulfil(event.data.paymentId);\n}));\n```\n\n> [!warn] Mount this route **before** any global `express.json()`, or give it `express.raw({ type: \"application/json\" })`. A JSON parser turns the body into an object and destroys the raw bytes. paylod cannot verify the signature without the raw bytes. The middleware detects this problem and returns a `400`, rather than skip the verification.\n\n### Verify it yourself\n\n```ts title=\"verifyWebhook\"\nconst event = paylod.verifyWebhook({\n  payload: rawBody,                            // string | Buffer | Uint8Array — the RAW bytes\n  signature: headers[\"x-webhook-signature\"],\n  toleranceSec: 300,                           // default\n});\n```\n\n`verifyWebhook()` throws `PaylodSignatureVerificationError` if the signature does not verify. [Webhooks](/docs/api/webhooks) documents the signature scheme.\n\n## Errors\n\nEvery error that the client throws extends `PaylodError`.\n\n| Class | When |\n| --- | --- |\n| `PaylodConfigError` | No API key, or no global `fetch`. The constructor throws it. |\n| `PaylodSandboxOnlyError` | A `simulate` call (or `{ simulate: true }`) with a key that is not `mp_test_…`. The client throws it locally and sends no request. It extends `PaylodConfigError`. |\n| `PaylodInvalidRequestError` | Local validation failed: a wrong amount, or a phone number that the client cannot parse. The client sent no request. |\n| `PaylodApiError` | paylod returned a non-2xx status. It carries `.status` and the parsed body. |\n| `PaylodConnectionError` | The request never got an answer (DNS, TLS, socket). The client retries it automatically. |\n| `PaylodTimeoutError` | `wait()` hit its deadline with the payment still `pending`. This is not a failure. |\n| `PaylodSignatureVerificationError` | A webhook signature did not verify. |\n\nThe client retries transient failures automatically, with backoff: network blips, `429` and `5xx`. `400`, `401`, `404`, `409` and `422` are real answers, and the client throws them immediately.\n\n## A complete example\n\nThis is a safe browser checkout. The page calls your server, and your server calls paylod. The key never leaves the server.\n\n```ts title=\"server.ts\"\nimport { Paylod, pendingOutcome } from \"@paylod/node\";\n\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!);\n\n// POST /api/pay → ring the phone.\n// Takes an orderId, never an amount: the price is looked up here, so a payer\n// with a proxy cannot discount their own order.\nasync function startPayment(orderId: string, phone: string) {\n  const order = await getOrder(orderId);\n\n  // One row per press of Pay — that row's id IS the idempotency key.\n  // Double-click Pay and the second call replays the first payment instead of\n  // sending a second STK prompt. Retry after a wrong PIN and you get a NEW\n  // attempt, a NEW key, and a real second charge — which is what a retry means.\n  // Keying on `order.id` would replay the FAILED attempt forever; keying on a\n  // product id would replay the first customer's payment to everyone after them.\n  const attempt = await db.attempts.create({ orderId: order.id });\n\n  const ack = await paylod.collect({\n    amount: order.amount,\n    phone,\n    idempotencyKey: attempt.id,\n  });\n  return pendingOutcome(ack.paymentId);\n}\n\n// GET /api/pay/:id → has it settled? Already decoded, ready to render.\nasync function readPayment(paymentId: string) {\n  return paylod.check(paymentId);\n}\n```\n\nBoth functions return the same `PaymentOutcome`. Your frontend therefore has one shape to show, and no result codes to interpret.\n\nThat is the whole integration. The [paylod demo](https://github.com/paylod/demo) is a working version of exactly this integration. It includes the retry path and a verified webhook.\n\n## Options\n\nYou do not normally need any of these options. The defaults are the right answer.\n\n```ts title=\"Escape hatches\"\nconst paylod = new Paylod(process.env.PAYLOD_API_KEY!, {\n  timeoutMs: 30_000,\n  maxRetries: 2,\n});\n```\n\n| Option | Default | What it is for |\n| --- | --- | --- |\n| `apiKey` | `process.env.PAYLOD_API_KEY` | Authentication. The only required value. |\n| `timeoutMs` | `30_000` | Per HTTP request. |\n| `maxRetries` | `2` | Transient failures only (network, `429`, `5xx`). |\n| `webhookSecret` | `process.env.PAYLOD_WEBHOOK_SECRET` | Only if you consume webhooks. |\n| `baseUrl` | `https://paylod.dev/functions/v1` | paylod sets this value. Override it only when you self-host, or when you point the client at a stub in tests. |\n| `fetch` | global `fetch` | Inject an instrumented or proxied fetch. |\n| `simulate` | `false` | Tests only. `collect()` creates a simulated payment instead of a real STK Push to a handset. It requires an `mp_test_…` key; otherwise the constructor throws. See [Test your checkout without a phone](#test-your-checkout-without-a-phone). |\n\n| Environment variable | Used for |\n| --- | --- |\n| `PAYLOD_API_KEY` | Authentication. Required. |\n| `PAYLOD_WEBHOOK_SECRET` | `verifyWebhook()`, `webhook()`, `webhookHandler()`. Optional. |\n| `PAYLOD_BASE_URL` | An override of the API base URL. Rarely needed. |\n\n## Next\n\n- Do you prefer the terminal? The [CLI](/docs/cli) sends the same STK Push in one command.\n- The HTTP endpoints underneath: [API reference](/docs/api).\n- Every result code, decoded: [error reference](/docs/errors).\n\n---\n\n## paylod CLI: M-Pesa from your terminal\n\nSource: https://paylod.dev/docs/cli\n\nSend a real M-Pesa STK Push from your terminal, decode any Daraja result code offline, and forward live webhooks to localhost without ngrok.\n\nThe `paylod` CLI sends a real M-Pesa STK Push from your terminal. The CLI also decodes Daraja result codes offline. The CLI also forwards live webhooks to `localhost` without ngrok. The CLI uses the same API as the [API reference](/docs/api).\n\n```bash title=\"Install\"\nnpm install -g @paylod/cli\n```\n\nThe package is `@paylod/cli`. The package installs the command `paylod`. Source: [github.com/mosesmrima/paylod-cli](https://github.com/mosesmrima/paylod-cli).\n\nYou can also run the CLI without installation:\n\n```bash title=\"npx\"\nnpx @paylod/cli errors 1032\n```\n\nThe CLI needs Node 20 or newer.\n\n## Set up\n\n```bash title=\"Terminal\"\npaylod init\n```\n\n`init` opens your browser and signs you in. Then `init` lets you pick an application. Then `init` mints an API key for this machine. The CLI puts the credentials in your OS keychain. If your machine has no keychain, the CLI uses a `0600` file under `~/.config/paylod`.\n\nTo only sign in, use these commands:\n\n```bash title=\"Terminal\"\npaylod login\npaylod whoami       # who am I, and where is the token stored?\n```\n\nIn CI, do not sign in. Set `PAYLOD_API_KEY` in the environment. Every data-plane command (`collect`, `status`, `listen`) uses that environment variable.\n\n## Take a payment\n\n```bash title=\"Terminal\"\npaylod collect --phone 254712345678 --amount 100\n```\n\nThe customer receives the STK Push prompt on the handset. The CLI polls until the payment settles. Then the CLI prints the outcome. On success, the outcome is an M-Pesa receipt. On failure, the outcome is a decoded reason.\n\n| Flag | What it does |\n| --- | --- |\n| `-p, --phone <msisdn>` | The customer's number. `0712…`, `254712…` and `+254712…` are all correct. |\n| `-a, --amount <kes>` | Whole shillings. M-Pesa rejects decimals. |\n| `-r, --ref <reference>` | Your own correlation id, such as an invoice number or an order number. The result contains this value. Maximum 12 characters. A paybill shows this value to the customer. A till never shows it. |\n| `-d, --description <text>` | The payment description on the STK Push prompt. Maximum 64 characters. |\n| `--idempotency-key <key>` | The name of one payment attempt. paylod collapses duplicates of that attempt into a single charge. A new charge needs a new key. |\n| `--no-wait` | Send the STK Push and exit immediately. The CLI does not poll. |\n| `--timeout <secs>` | The time to wait for settlement. Default 120. |\n| `--env <sandbox\\|production>` | The M-Pesa environment for the charge. |\n| `--api-key <key>` | Use this key in place of `PAYLOD_API_KEY` or your stored profile. |\n\nTo read a payment later, or to watch a payment settle, use these commands:\n\n```bash title=\"Terminal\"\npaylod status e69e5c00-8a01-44ed-b003-48e2e86a7c9e\npaylod status e69e5c00-8a01-44ed-b003-48e2e86a7c9e --watch\n```\n\n`status` decodes the failure reason for you. You do not have to read `2001` in a PDF.\n\n## Decode a result code\n\n> Do not show a failure to a customer for the codes `4999` and `500.001.1001`. These two codes decode as **pending**, not failed. They mean that the customer did not enter the PIN yet. The payment is still live and the customer can still pay it.\n\nThis command works offline. It needs no login, no network and no API key.\n\n```bash title=\"Terminal\"\npaylod errors 1032          # why did that payment fail?\npaylod errors --list        # the whole catalogue\npaylod errors 2001 --json   # machine-readable, for your error handler\n```\n\nUse this command to find the meaning of a code such as `2001` quickly during an incident. The [error reference](/docs/errors) shows the same catalogue.\n\n## Webhooks on localhost, without ngrok\n\n```bash title=\"Terminal\"\npaylod listen --forward http://localhost:3000/webhook\n```\n\n`listen` streams the live webhook events of your application. `listen` then sends each event again to your local server. The signature stays intact. Therefore your code verifies a real signature against a real payload. You need no tunnel, no public URL and no ngrok.\n\n| Flag | What it does |\n| --- | --- |\n| `-f, --forward <url>` | POST each event to this local URL. |\n| `-e, --events <types...>` | Forward only these types (`payment.success`, `payment.failed`). |\n| `--print-json` | Print the full JSON payload of every event. |\n| `--secret <whsec>` | The signing secret. If you omit this flag, the CLI reads the secret from your profile, or gets it from the API. |\n| `--tolerance <secs>` | The freshness window of the signature, in seconds. Default 300. `0` disables the check. |\n| `--skip-verify` | Do not verify the HMAC. Use this flag only to debug. |\n| `--port <port>` | The port of the local webhook endpoint. Default 4242. |\n\nTo get a new signing secret, or to read the current signing secret, use this command:\n\n```bash title=\"Terminal\"\npaylod webhooks secret\n```\n\n## Test without spending money\n\n```bash title=\"Terminal\"\npaylod simulate --outcome user_cancelled\npaylod simulate --interactive\n```\n\n`simulate` creates a payment in the paylod sandbox and forces an outcome. `simulate` makes no Daraja call, uses no handset and moves no money. Use `simulate` to test your failure paths in CI.\n\n| Outcome | Result code |\n| --- | --- |\n| `approve` | `0` |\n| `wrong_pin` | `2001` |\n| `insufficient_funds` | `1` |\n| `user_cancelled` | `1032` |\n| `timeout` | `1037` |\n\n## Manage the integration\n\nThese commands do everything the dashboard does.\n\n| Command | What it manages |\n| --- | --- |\n| `paylod apps list \\| create \\| use \\| rename \\| delete` | Applications. You need one application for each till or paybill. |\n| `paylod creds` | Your Daraja consumer key, secret, shortcode and passkey. Write-only. |\n| `paylod keys mint \\| list \\| revoke \\| use` | paylod API keys (`mp_test_…` / `mp_live_…`). |\n| `paylod webhooks list \\| add \\| toggle \\| delete \\| secret` | Webhook endpoints. |\n| `paylod payments` | Your M-Pesa payments. |\n| `paylod orgs` | Organizations. |\n\n## Scripting\n\nEvery command takes a global `--json` flag and prints machine-readable output.\n\n```bash title=\"Terminal\"\npaylod collect -p 254712345678 -a 100 --json | jq -r '.mpesaReceipt'\n```\n\n`paylod collect` exits `5` if the payment is still pending when the timeout expires. Exit code `5` is not a failure. It means that the customer did not answer the prompt yet. On a settled failure, the CLI exits with a non-zero code. The JSON output then contains the decoded reason.\n\n## Environment variables\n\n| Variable | What it does |\n| --- | --- |\n| `PAYLOD_API_KEY` | The merchant API key. The CLI does not read the config file. Set this variable to run paylod in CI. |\n| `PAYLOD_WEBHOOK_SECRET` | The signing secret for `paylod listen`. |\n| `PAYLOD_API_BASE` | Overrides the API base. Default `https://paylod.dev/functions/v1`. |\n| `PAYLOD_CONFIG_DIR` | The directory for the CLI config. Default `~/.config/paylod`. |\n| `PAYLOD_NO_KEYCHAIN=1` | Never use the OS keychain. Use the `0600` file in place of the keychain. Set this variable on headless Linux and in containers. |\n| `NO_COLOR` | Disables colour. |\n\n> [!warn] Keep `mp_live_…` keys out of your shell history and out of committed `.env` files. These keys move real money.\n\n## Next\n\n- To build an application and not a script, use the [Node SDK](/docs/sdk).\n- To set up an AI agent, use the [MCP server](/docs/mcp). The MCP server shows the same capabilities as tools.\n- To read about the HTTP endpoints below the CLI, see the [API reference](/docs/api).\n\n---\n\n## MCP server: M-Pesa for AI agents\n\nSource: https://paylod.dev/docs/mcp\n\nLet an AI agent manage and use your M-Pesa integration — a remote, OAuth-authenticated MCP endpoint. Nothing to install.\n\n## Nothing to install\n\npaylod supplies a **remote** MCP server. You add no npm package. You run no process. You paste no API key into a config file. You give your AI client one URL. The client then finds the authorization server and opens your browser one time. You approve the exact capabilities that you want. The agent gets a scoped token. The agent never gets your Daraja credentials and never gets your paylod API key.\n\n```text title=\"Server URL\"\nhttps://mcp.paylod.dev/mcp\n```\n\nTransport: `streamable-http`. Auth: OAuth 2.1 (authorization code + PKCE, with Dynamic Client Registration) against `https://paylod.dev/oauth`. Any MCP client that supports remote HTTP works. A stdio-only client can use a bridge.\n\nThe server is open source: [github.com/mosesmrima/paylod-mcp](https://github.com/mosesmrima/paylod-mcp).\n\n## Add it to your client\n\nPick your client. Each client runs the OAuth flow for you on the first connection. A browser tab opens. You sign in to paylod. You consent to the scopes.\n\n### Claude Code\n\nRun one command. Use `--scope user` to make the server available in all of your projects. The default scope is `local`. The `project` scope writes a shared `.mcp.json`.\n\n```bash title=\"Terminal\"\nclaude mcp add --transport http --scope user paylod https://mcp.paylod.dev/mcp\n```\n\nThen run `/mcp` in Claude Code and pick **paylod** to authenticate. After that, `/mcp` shows the server as connected and shows its tools.\n\nYou can also commit the config. To do this, put the block below in `.mcp.json` at the repo root. Always include the `type` field. Claude Code treats an entry that has a `url` but no `type` as stdio, and skips that entry.\n\n```json title=\".mcp.json\"\n{\n  \"mcpServers\": {\n    \"paylod\": {\n      \"type\": \"http\",\n      \"url\": \"https://mcp.paylod.dev/mcp\"\n    }\n  }\n}\n```\n\n### Claude Desktop\n\nAdd remote servers in the UI, not in `claude_desktop_config.json`. That file is only for local stdio servers. Go to **Settings → Connectors → Add custom connector**. Paste the server URL. Click **Connect** to run the OAuth flow. Leave the advanced client-ID and secret fields blank. paylod supports Dynamic Client Registration, so your client registers itself.\n\n### Cursor\n\nAdd the block below to `~/.cursor/mcp.json` for all projects, or to `.cursor/mcp.json` for one project. Cursor identifies a remote server by the `url` field. Open **Settings → MCP**. Click the login prompt to authorize.\n\n```json title=\"~/.cursor/mcp.json\"\n{\n  \"mcpServers\": {\n    \"paylod\": {\n      \"url\": \"https://mcp.paylod.dev/mcp\"\n    }\n  }\n}\n```\n\n### VS Code\n\nAdd the block below to `.vscode/mcp.json`. Note that the top-level key is `servers`, not `mcpServers`. VS Code implements the MCP authorization spec, and it includes DCR. VS Code therefore prompts you to sign in the first time that you use the server.\n\n```json title=\".vscode/mcp.json\"\n{\n  \"servers\": {\n    \"paylod\": {\n      \"type\": \"http\",\n      \"url\": \"https://mcp.paylod.dev/mcp\"\n    }\n  }\n}\n```\n\n### Windsurf\n\nAdd the block below to `~/.codeium/windsurf/mcp_config.json`. Windsurf uses `serverUrl` for a remote server. Windsurf also supports OAuth on Streamable HTTP.\n\n```json title=\"~/.codeium/windsurf/mcp_config.json\"\n{\n  \"mcpServers\": {\n    \"paylod\": {\n      \"serverUrl\": \"https://mcp.paylod.dev/mcp\"\n    }\n  }\n}\n```\n\n### Codex CLI\n\nCodex supports remote Streamable-HTTP MCP servers with OAuth. Add the block below to `~/.codex/config.toml`, then run `codex mcp login paylod`.\n\n```toml title=\"~/.codex/config.toml\"\n[mcp_servers.paylod]\nurl = \"https://mcp.paylod.dev/mcp\"\n```\n\n### Fallback for stdio-only clients\n\nIf your client supports only stdio, use `mcp-remote` as a bridge. `mcp-remote` is the standard stdio-to-HTTP proxy. It runs the OAuth flow for you. It opens your browser and it caches the token in `~/.mcp-auth`. It needs no flags for paylod.\n\n```json title=\"mcp-remote bridge\"\n{\n  \"mcpServers\": {\n    \"paylod\": {\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"mcp-remote\", \"https://mcp.paylod.dev/mcp\"]\n    }\n  }\n}\n```\n\nMost users do not need this bridge. Claude Code, Claude Desktop, Cursor, VS Code, Windsurf and Codex all support remote HTTP directly.\n\n## First thing to try\n\n`decode_mpesa_error` is pure and offline. It needs no scopes and no credentials, and it uses no network. Use this tool to confirm that the server is set correctly before you grant any scope. Ask your agent:\n\n```text title=\"Prompt\"\nWhat does M-Pesa error 2001 mean?\n```\n\nA good answer gives the cause (wrong PIN), the fix, and the message to show the customer. The tool supplies that answer. The model does not guess it. A Daraja response gives you only the bare code `2001`, and this tool explains the code.\n\n## How the OAuth flow works\n\nYour client calls the server URL with no token. The server returns a `401` with a `WWW-Authenticate` header. That header points to the protected-resource metadata. From the metadata, the client finds the authorization server at `https://paylod.dev/oauth`. The client registers itself dynamically and opens your browser. You sign in to paylod. paylod then shows a consent screen. The consent screen lists each capability that the agent requests, as one scope per capability with its own checkbox.\n\n```http title=\"The 401 that starts it all\"\ncurl -i -X POST https://mcp.paylod.dev/mcp \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"tools/list\"}'\n\nHTTP/1.1 401 Unauthorized\nWWW-Authenticate: Bearer resource_metadata=\"https://mcp.paylod.dev/.well-known/oauth-protected-resource\"\n```\n\nThree scopes are high-risk: `payments.payout` (send money out), `credentials.write` (write your Daraja keys) and `keys.mint` (mint API keys). The consent screen puts these three scopes in their own flagged group, so you always see them. The consent screen shows every requested scope as **checked by default**. Untick each scope that you do not want to grant. paylod grants only the scopes that stay ticked.\n\nUntick the three high-risk scopes to stop the agent from calling `payout`, `reversal`, `set_credentials` or `mint_key`. The server rejects each such call on the scope. The server does not depend on the model to obey an instruction. If you approve only `payments.simulate`, the agent can build against the sandbox and cannot move real money.\n\npaylod binds the access token to this server as its audience. Your client holds the token. The token never appears in the conversation. Your Daraja consumer key and secret are write-only. No tool reads them back, so an agent can store them for you but cannot extract them.\n\n## Tools\n\nThe OAuth access token authorizes every call. Each tool also needs the scope that the table shows next to it. A token without that scope cannot call the tool.\n\n### Management — set the integration up\n\n| Tool | Scope | What it does |\n| --- | --- | --- |\n| `create_app` | `paylod:apps.write` | Use this tool on your first run. It creates your organization and its first application (paybill or till). It returns the applicationId, your callback URL and a one-time API key. |\n| `create_application` | `paylod:apps.write` | Add another application to an organization that you already have. |\n| `get_callback_url` | `paylod:apps.write` | Get the hosted M-Pesa callback URL for an application. You paste this URL into the Daraja portal. Treat the URL as a secret. |\n| `set_credentials` | `paylod:credentials.write` | Store or rotate the Daraja consumer key, secret, shortcode and passkey. These values are write-only. No tool reads them back. |\n| `mint_key` | `paylod:keys.mint` | Mint a paylod API key (`mp_test_…` / `mp_live_…`) for your own backend. The tool returns the key one time only. |\n| `list_keys` | `paylod:keys.mint` | List the API keys of an application. The tool returns the prefixes only. It never returns the secret. |\n| `revoke_key` | `paylod:keys.mint` | Revoke an API key by id. |\n| `configure_webhook` | `paylod:webhooks.write` | Create or update a webhook endpoint. paylod then sends signed results to your server. |\n| `list_webhooks` | `paylod:webhooks.write` | List the webhook endpoints that an application uses. |\n| `list_applications` | `paylod:team.read` | List the applications that you can access. The tool also gives their environments and their configuration state. |\n| `authenticate` | — (no scope) | Show the owner of the token. Show also the scopes that paylod granted. |\n\n### Runtime — move and read money\n\n| Tool | Scope | What it does |\n| --- | --- | --- |\n| `request_stk_push` | `paylod:payments.collect` | Send an STK Push to the handset of a customer. The tool accepts one idempotency key for each payment attempt. Duplicate deliveries of that attempt therefore become a single charge. If the call stops part way, the key is spent and the tool returns `409` *indeterminate*. In that case paylod cannot prove that no debit occurred. Read `get_payment_status` first, then retry with a **new** key. |\n| `get_payment_status` | `paylod:payments.read` | Read one payment by id. If the payment is still pending, the tool reads the live STK status and settles the payment immediately. |\n| `generate_qr` | `paylod:payments.collect` | Generate an M-Pesa QR code (base64 PNG). The tool is stateless and moves no money. |\n| `register_c2b` | `paylod:payments.collect` | Register the C2B validation URL and confirmation URL. Paybill payments made outside your app then reach you. |\n| `get_account_balance` | `paylod:payments.read` | Read the M-Pesa account balance. The tool is asynchronous. It returns a queryId, and the result arrives on your callback. |\n| `get_transaction_status` | `paylod:payments.read` | Read the status of an M-Pesa transaction by receipt. The tool is asynchronous and uses the same callback pattern. |\n| `payout` | `paylod:payments.payout` | Send money out (B2C). On a live application you cannot reverse this action. The tool needs the opt-in money-out scope. |\n| `reversal` | `paylod:payments.payout` | Reverse or refund a transaction. The tool needs the opt-in money-out scope. |\n\n### Sandbox & offline\n\n| Tool | Scope | What it does |\n| --- | --- | --- |\n| `simulate_test_payment` | `paylod:payments.simulate` | Create a simulated collection in the sandbox. The tool uses no handset and no real money. |\n| `simulate_outcome` | `paylod:payments.simulate` | Force a simulated payment to succeed or to fail. The agent can then test your failure paths. |\n| `decode_mpesa_error` | — (no scope) | Decode any Safaricom result code or Daraja error. The tool returns the cause, the fix, and a message for the customer. It is pure and offline. |\n| `get_docs` | — (no scope) | Get paylod documentation by topic. The tool is pure and local. It needs no scope, no token and no network. |\n\nThe [error reference](/docs/errors) lists the same codes that `decode_mpesa_error` returns.\n\n## Set an integration up with the tools, in order\n\nUse the tools to **set the integration up and to inspect it**. Your application does not call these tools at runtime. At runtime your application calls the [Node SDK](/docs/sdk). Call the tools in this order.\n\n1. **Check for an existing app** → `list_applications`. Any result means that you are onboarded.\n2. **Create the application.** A new user calls `create_app`, which creates the organization *and* its first application. A user who already has an organization calls `create_application` instead, because `create_app` returns a `409 already onboarded`. Do not pick the wrong tool here. This is the most common first-call mistake.\n3. **Store the Daraja credentials** → `set_credentials`. The tool needs `product`, which is the *kind of shortcode*, `paybill` or `till`. `product` is not the Daraja product name. For a **till**, also pass `partyB`, the till number that receives the funds. Make sure that `partyB` differs from `shortcode`, or Daraja rejects the push with `2002`. paylod encrypts the credentials at rest and keeps them write-only, and no tool reads them back.\n4. **Get the callback URL** → `get_callback_url`. Then **paste the URL into the Safaricom Daraja portal** as the CallbackURL / ResultURL / ValidationURL / ConfirmationURL. Do not skip this step, because no payment resolves without it. No agent can do this step for you, because Safaricom has no API for it. Treat the URL as a secret. The random `cbk_…` token in the path authenticates the callback from Safaricom.\n5. **Mint an API key** → `mint_key` (`mp_test_…` for sandbox, `mp_live_…` for production). paylod shows the key one time only. Your *server* puts this key in `PAYLOD_API_KEY`. The MCP server itself never uses an API key.\n6. **Register a webhook if you want one** → `configure_webhook`. You can skip this step and poll instead. paylod records every payment in both cases.\n7. **Write the application code with the SDK.** Run `npm install @paylod/node`. Then use `new Paylod(process.env.PAYLOD_API_KEY!)` and `await paylod.collectAndWait({ amount, phone })`. Show `outcome.message`, and offer a retry only when `outcome.retryable` is true. Do not write a fetch wrapper, a poll loop, or a callback handler. See [Quickstart](/docs/quickstart) and the [Node SDK](/docs/sdk).\n8. **Test the failure paths** → use `simulate_test_payment` with `simulate_outcome`. Together they drive success, cancellation, a wrong PIN or a timeout through the real settlement path. They need no handset.\n\n`request_stk_push` and `get_payment_status` let an agent *test* the loop from end to end. They also let a person say \"charge this number 10 bob\". Do not ship these two tools as your runtime integration. If you do, you rebuild the SDK badly.\n\n> [!warn] Do not treat result code `4999` as a failed payment. `4999` means that the payment is **pending**, because the customer must still enter the PIN. `4999` is not retryable. A second `request_stk_push` sends a second prompt and can charge the customer two times. Poll `get_payment_status` instead.\n\n## What an agent can actually do\n\nThe management tools let an agent build a complete live M-Pesa integration for you. You do not open a dashboard. Connect the server, then ask:\n\n```text title=\"Prompt\"\nSet up M-Pesa for my shop \"Acme Ltd\". Here are my Daraja sandbox\ncredentials: consumer key …, secret …, shortcode 174379, passkey ….\nCreate the app, store them, mint me a test API key, give me my\ncallback URL, then send a 10 KES STK push to 0712345678 and tell me\nwhen it settles.\n```\n\nThe agent calls `create_app` → `set_credentials` → `mint_key` → `get_callback_url` → `request_stk_push`. The agent then polls `get_payment_status` until the payment settles. If the payment fails, the agent calls `decode_mpesa_error`. The agent then tells you in simple words that the customer cancelled the prompt (`1032`) or that the handset timed out (`1037`). You give the agent a phone number. The agent gives you an API key and a receipt.\n\nThe agent cannot paste the callback URL into the Safaricom Daraja portal, because Safaricom has no API for it. This is the only step that you must do yourself. The agent can do every other step. This includes `configure_webhook`, which sets the webhook endpoint that receives results on your own server.\n\n> The agent uses the same platform over MCP that your backend uses over HTTP at `/functions/v1`. See the [API reference](/docs/api). MCP is a second front door. It is not a separate system.\n";
